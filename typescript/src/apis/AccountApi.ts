/* tslint:disable */
/* eslint-disable */
/**
 * Mencoro API
 * Public API for Mencoro customers. Authenticate with an API key: `Authorization: Bearer mencoro_sk_...`.
 *
 * The version of the OpenAPI document: 1.0.0
 * Contact: support@mencoro.com
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */

import * as runtime from '../runtime';
import {
    type GetMeStats200Response,
    GetMeStats200ResponseFromJSON,
    GetMeStats200ResponseToJSON,
} from '../models/GetMeStats200Response';
import {
    type MeResource,
    MeResourceFromJSON,
    MeResourceToJSON,
} from '../models/MeResource';

/**
 * AccountApi - interface
 * 
 * @export
 * @interface AccountApiInterface
 */
export interface AccountApiInterface {
    /**
     * Creates request options for getMe without sending the request
     * @throws {RequiredError}
     * @memberof AccountApiInterface
     */
    getMeRequestOpts(): Promise<runtime.RequestOpts>;

    /**
     * Returns the user the API key belongs to, plus the key\'s capabilities and scope. Use it to confirm which credential a call runs under.
     * @summary Get the authenticated identity
     * @param {*} [options] Override http request option.
     * @throws {RequiredError}
     * @memberof AccountApiInterface
     */
    getMeRaw(initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<MeResource>>;

    /**
     * Returns the user the API key belongs to, plus the key\'s capabilities and scope. Use it to confirm which credential a call runs under.
     * Get the authenticated identity
     */
    getMe(initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<MeResource>;

    /**
     * Creates request options for getMeStats without sending the request
     * @throws {RequiredError}
     * @memberof AccountApiInterface
     */
    getMeStatsRequestOpts(): Promise<runtime.RequestOpts>;

    /**
     * Aggregate counts over every organization the key\'s owner is an active member of, narrowed to the key\'s scope — the same set /api/v1/organizations pages through. `organizations.total` is that set\'s size; `projects.total` and `projects.active` count the projects inside it, archived ones included in the total and excluded from the active figure. Every value is an exact count: zero means zero, and no value here is ever null or unknown. Per-organization billing and usage figures are not part of this response; read them from the subscription and entitlements operations instead.
     * @summary Counts across everything the key can reach
     * @param {*} [options] Override http request option.
     * @throws {RequiredError}
     * @memberof AccountApiInterface
     */
    getMeStatsRaw(initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<GetMeStats200Response>>;

    /**
     * Aggregate counts over every organization the key\'s owner is an active member of, narrowed to the key\'s scope — the same set /api/v1/organizations pages through. `organizations.total` is that set\'s size; `projects.total` and `projects.active` count the projects inside it, archived ones included in the total and excluded from the active figure. Every value is an exact count: zero means zero, and no value here is ever null or unknown. Per-organization billing and usage figures are not part of this response; read them from the subscription and entitlements operations instead.
     * Counts across everything the key can reach
     */
    getMeStats(initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<GetMeStats200Response>;

}

/**
 * 
 */
export class AccountApi extends runtime.BaseAPI implements AccountApiInterface {

    /**
     * Creates request options for getMe without sending the request
     */
    async getMeRequestOpts(): Promise<runtime.RequestOpts> {
        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("ApiKey", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/api/v1/me`;

        return {
            path: urlPath,
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        };
    }

    /**
     * Returns the user the API key belongs to, plus the key\'s capabilities and scope. Use it to confirm which credential a call runs under.
     * Get the authenticated identity
     */
    async getMeRaw(initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<MeResource>> {
        const requestOptions = await this.getMeRequestOpts();
        const response = await this.request(requestOptions, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => MeResourceFromJSON(jsonValue));
    }

    /**
     * Returns the user the API key belongs to, plus the key\'s capabilities and scope. Use it to confirm which credential a call runs under.
     * Get the authenticated identity
     */
    async getMe(initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<MeResource> {
        const response = await this.getMeRaw(initOverrides);
        return await response.value();
    }

    /**
     * Creates request options for getMeStats without sending the request
     */
    async getMeStatsRequestOpts(): Promise<runtime.RequestOpts> {
        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("ApiKey", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/api/v1/me/stats`;

        return {
            path: urlPath,
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        };
    }

    /**
     * Aggregate counts over every organization the key\'s owner is an active member of, narrowed to the key\'s scope — the same set /api/v1/organizations pages through. `organizations.total` is that set\'s size; `projects.total` and `projects.active` count the projects inside it, archived ones included in the total and excluded from the active figure. Every value is an exact count: zero means zero, and no value here is ever null or unknown. Per-organization billing and usage figures are not part of this response; read them from the subscription and entitlements operations instead.
     * Counts across everything the key can reach
     */
    async getMeStatsRaw(initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<GetMeStats200Response>> {
        const requestOptions = await this.getMeStatsRequestOpts();
        const response = await this.request(requestOptions, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => GetMeStats200ResponseFromJSON(jsonValue));
    }

    /**
     * Aggregate counts over every organization the key\'s owner is an active member of, narrowed to the key\'s scope — the same set /api/v1/organizations pages through. `organizations.total` is that set\'s size; `projects.total` and `projects.active` count the projects inside it, archived ones included in the total and excluded from the active figure. Every value is an exact count: zero means zero, and no value here is ever null or unknown. Per-organization billing and usage figures are not part of this response; read them from the subscription and entitlements operations instead.
     * Counts across everything the key can reach
     */
    async getMeStats(initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<GetMeStats200Response> {
        const response = await this.getMeStatsRaw(initOverrides);
        return await response.value();
    }

}

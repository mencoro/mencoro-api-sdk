/* tslint:disable */
/* eslint-disable */
/**
 * Mencoro API
 * Public API for Mencoro customers. Authenticate with an API key: `Authorization: Bearer mencoro_sk_...`.
 *
 * The version of the OpenAPI document: 1.0.0
 * Contact: support@mencoro.com
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */

import * as runtime from '../runtime';
import {
    type ListAiResponses200Response,
    ListAiResponses200ResponseFromJSON,
    ListAiResponses200ResponseToJSON,
} from '../models/ListAiResponses200Response';
import {
    type ListSearchSnapshots200Response,
    ListSearchSnapshots200ResponseFromJSON,
    ListSearchSnapshots200ResponseToJSON,
} from '../models/ListSearchSnapshots200Response';
import {
    type ListShoppingSnapshots200Response,
    ListShoppingSnapshots200ResponseFromJSON,
    ListShoppingSnapshots200ResponseToJSON,
} from '../models/ListShoppingSnapshots200Response';

export interface ListAiResponsesRequest {
    organizationId: string;
    projectId: string;
    dateFrom?: Date;
    dateTo?: Date;
    trackedQueryId?: string;
    engines?: Array<ListAiResponsesEnginesEnum>;
    limit?: number;
    offset?: number;
    sortOrder?: ListAiResponsesSortOrderEnum;
}

export interface ListSearchSnapshotsRequest {
    organizationId: string;
    projectId: string;
    dateFrom?: Date;
    dateTo?: Date;
    trackedQueryId?: string;
    limit?: number;
    offset?: number;
    sortOrder?: ListSearchSnapshotsSortOrderEnum;
}

export interface ListShoppingSnapshotsRequest {
    organizationId: string;
    projectId: string;
    dateFrom?: Date;
    dateTo?: Date;
    trackedQueryId?: string;
    limit?: number;
    offset?: number;
    sortOrder?: ListShoppingSnapshotsSortOrderEnum;
}

/**
 * CapturesApi - interface
 * 
 * @export
 * @interface CapturesApiInterface
 */
export interface CapturesApiInterface {
    /**
     * Creates request options for listAiResponses without sending the request
     * @param {string} organizationId 
     * @param {string} projectId 
     * @param {Date} [dateFrom] Inclusive lower bound, widened to 00:00:00 UTC of the named day. Must be inside the retention window.
     * @param {Date} [dateTo] Inclusive upper bound, widened to 23:59:59 UTC of the named day.
     * @param {string} [trackedQueryId] Only captures of this tracked query. Must belong to the project in the path.
     * @param {Array<'chatgpt' | 'perplexity' | 'google_ai_overview' | 'google_ai_mode'>} [engines] Only captures from these AI engines. Repeat the parameter or pass a comma-separated list. An empty filter means every engine.
     * @param {number} [limit] Page size. A larger value is rejected, never silently reduced.
     * @param {number} [offset] Number of captures to skip before the page starts.
     * @param {'asc' | 'desc'} [sortOrder] Direction of the capture-time ordering. An unknown value is rejected, not replaced by the default.
     * @throws {RequiredError}
     * @memberof CapturesApiInterface
     */
    listAiResponsesRequestOpts(requestParameters: ListAiResponsesRequest): Promise<runtime.RequestOpts>;

    /**
     * Minimum role: viewer. Every AI answer captured for a project, newest first, one page at a time. This is the evidence under the analytics operations: the full answer text and the sources the engine cited, exactly as captured. Read `unresolved` on a citation before attributing its domain — a true value means the URL is still a redirector and the domain belongs to that redirector, not to the publisher; count the citation, do not credit the site. `passIndex` and `passCount` describe multi-pass sampling: rows sharing a `trackedQueryId` and `capturedAt` are passes of one run, not duplicates, so aggregating across them without dividing by `passCount` double-counts that run. `responseText` is the whole answer, so a page of 100 is a large response — lower `limit` rather than paging blind. Captures are kept for 16 months and purged after that; a `dateFrom` before the window is rejected rather than answered with an empty page. `total` counts every capture the filter matches, not the size of this page.
     * @summary List captured AI answers
     * @param {string} organizationId 
     * @param {string} projectId 
     * @param {Date} [dateFrom] Inclusive lower bound, widened to 00:00:00 UTC of the named day. Must be inside the retention window.
     * @param {Date} [dateTo] Inclusive upper bound, widened to 23:59:59 UTC of the named day.
     * @param {string} [trackedQueryId] Only captures of this tracked query. Must belong to the project in the path.
     * @param {Array<'chatgpt' | 'perplexity' | 'google_ai_overview' | 'google_ai_mode'>} [engines] Only captures from these AI engines. Repeat the parameter or pass a comma-separated list. An empty filter means every engine.
     * @param {number} [limit] Page size. A larger value is rejected, never silently reduced.
     * @param {number} [offset] Number of captures to skip before the page starts.
     * @param {'asc' | 'desc'} [sortOrder] Direction of the capture-time ordering. An unknown value is rejected, not replaced by the default.
     * @param {*} [options] Override http request option.
     * @throws {RequiredError}
     * @memberof CapturesApiInterface
     */
    listAiResponsesRaw(requestParameters: ListAiResponsesRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<ListAiResponses200Response>>;

    /**
     * Minimum role: viewer. Every AI answer captured for a project, newest first, one page at a time. This is the evidence under the analytics operations: the full answer text and the sources the engine cited, exactly as captured. Read `unresolved` on a citation before attributing its domain — a true value means the URL is still a redirector and the domain belongs to that redirector, not to the publisher; count the citation, do not credit the site. `passIndex` and `passCount` describe multi-pass sampling: rows sharing a `trackedQueryId` and `capturedAt` are passes of one run, not duplicates, so aggregating across them without dividing by `passCount` double-counts that run. `responseText` is the whole answer, so a page of 100 is a large response — lower `limit` rather than paging blind. Captures are kept for 16 months and purged after that; a `dateFrom` before the window is rejected rather than answered with an empty page. `total` counts every capture the filter matches, not the size of this page.
     * List captured AI answers
     */
    listAiResponses(requestParameters: ListAiResponsesRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<ListAiResponses200Response>;

    /**
     * Creates request options for listSearchSnapshots without sending the request
     * @param {string} organizationId 
     * @param {string} projectId 
     * @param {Date} [dateFrom] Inclusive lower bound, widened to 00:00:00 UTC of the named day. Must be inside the retention window.
     * @param {Date} [dateTo] Inclusive upper bound, widened to 23:59:59 UTC of the named day.
     * @param {string} [trackedQueryId] Only captures of this tracked query. Must belong to the project in the path.
     * @param {number} [limit] Page size. A larger value is rejected, never silently reduced.
     * @param {number} [offset] Number of captures to skip before the page starts.
     * @param {'asc' | 'desc'} [sortOrder] Direction of the capture-time ordering. An unknown value is rejected, not replaced by the default.
     * @throws {RequiredError}
     * @memberof CapturesApiInterface
     */
    listSearchSnapshotsRequestOpts(requestParameters: ListSearchSnapshotsRequest): Promise<runtime.RequestOpts>;

    /**
     * Minimum role: viewer. Every organic search page captured for a project, newest first, one page at a time. This is what the rank figures in the analytics operations were computed from: the ranked results as they stood at `capturedAt`, not as they stand now. Read `unresolved` on a result before attributing its domain — a true value means the URL is still a redirector and the domain belongs to that redirector; count the result, do not credit the site. `rating` and `ratingVotes` come from a rich-result star rating when Google showed one and are absent far more often than present; their absence says nothing about the page. There is no `engines` filter because every capture here comes from Google SERP. Captures are kept for 16 months and purged after that; a `dateFrom` before the window is rejected rather than answered with an empty page. `total` counts every capture the filter matches, not the size of this page.
     * @summary List captured search-results pages
     * @param {string} organizationId 
     * @param {string} projectId 
     * @param {Date} [dateFrom] Inclusive lower bound, widened to 00:00:00 UTC of the named day. Must be inside the retention window.
     * @param {Date} [dateTo] Inclusive upper bound, widened to 23:59:59 UTC of the named day.
     * @param {string} [trackedQueryId] Only captures of this tracked query. Must belong to the project in the path.
     * @param {number} [limit] Page size. A larger value is rejected, never silently reduced.
     * @param {number} [offset] Number of captures to skip before the page starts.
     * @param {'asc' | 'desc'} [sortOrder] Direction of the capture-time ordering. An unknown value is rejected, not replaced by the default.
     * @param {*} [options] Override http request option.
     * @throws {RequiredError}
     * @memberof CapturesApiInterface
     */
    listSearchSnapshotsRaw(requestParameters: ListSearchSnapshotsRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<ListSearchSnapshots200Response>>;

    /**
     * Minimum role: viewer. Every organic search page captured for a project, newest first, one page at a time. This is what the rank figures in the analytics operations were computed from: the ranked results as they stood at `capturedAt`, not as they stand now. Read `unresolved` on a result before attributing its domain — a true value means the URL is still a redirector and the domain belongs to that redirector; count the result, do not credit the site. `rating` and `ratingVotes` come from a rich-result star rating when Google showed one and are absent far more often than present; their absence says nothing about the page. There is no `engines` filter because every capture here comes from Google SERP. Captures are kept for 16 months and purged after that; a `dateFrom` before the window is rejected rather than answered with an empty page. `total` counts every capture the filter matches, not the size of this page.
     * List captured search-results pages
     */
    listSearchSnapshots(requestParameters: ListSearchSnapshotsRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<ListSearchSnapshots200Response>;

    /**
     * Creates request options for listShoppingSnapshots without sending the request
     * @param {string} organizationId 
     * @param {string} projectId 
     * @param {Date} [dateFrom] Inclusive lower bound, widened to 00:00:00 UTC of the named day. A date before the retention floor is refused rather than answered with an empty page: shopping captures are aged out by the raw-data purge like every other capture.
     * @param {Date} [dateTo] Inclusive upper bound, widened to 23:59:59 UTC of the named day.
     * @param {string} [trackedQueryId] Only captures of this tracked query. Must belong to the project in the path.
     * @param {number} [limit] Page size. A larger value is rejected, never silently reduced.
     * @param {number} [offset] Number of captures to skip before the page starts.
     * @param {'asc' | 'desc'} [sortOrder] Direction of the capture-time ordering. An unknown value is rejected, not replaced by the default.
     * @throws {RequiredError}
     * @memberof CapturesApiInterface
     */
    listShoppingSnapshotsRequestOpts(requestParameters: ListShoppingSnapshotsRequest): Promise<runtime.RequestOpts>;

    /**
     * Minimum role: viewer. Every shopping page captured for a project, newest first, one page at a time: the ranked offers as they stood at `capturedAt`. `price` and `currency` are what the seller showed at that moment — no tax normalisation, no shipping, no conversion — and two offers in one snapshot may carry different currencies, so convert before comparing. `productId` is the marketplace\'s own identifier for the listing, stable enough to follow one offer across snapshots, and it is not a Mencoro id. Offers turn over far faster than organic results, so two snapshots days apart routinely share none; that is the marketplace behaving normally, not a gap in the capture. Unlike organic results, a shopping offer carries no `unresolved` flag: the capture pipeline does not record one for this surface. Shopping captures are trimmed by the same raw-data purge as AI answers and search pages, so the same retention floor applies: a `dateFrom` before it is refused rather than answered with an empty page. `total` counts every capture the filter matches, not the size of this page.
     * @summary List captured shopping-results pages
     * @param {string} organizationId 
     * @param {string} projectId 
     * @param {Date} [dateFrom] Inclusive lower bound, widened to 00:00:00 UTC of the named day. A date before the retention floor is refused rather than answered with an empty page: shopping captures are aged out by the raw-data purge like every other capture.
     * @param {Date} [dateTo] Inclusive upper bound, widened to 23:59:59 UTC of the named day.
     * @param {string} [trackedQueryId] Only captures of this tracked query. Must belong to the project in the path.
     * @param {number} [limit] Page size. A larger value is rejected, never silently reduced.
     * @param {number} [offset] Number of captures to skip before the page starts.
     * @param {'asc' | 'desc'} [sortOrder] Direction of the capture-time ordering. An unknown value is rejected, not replaced by the default.
     * @param {*} [options] Override http request option.
     * @throws {RequiredError}
     * @memberof CapturesApiInterface
     */
    listShoppingSnapshotsRaw(requestParameters: ListShoppingSnapshotsRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<ListShoppingSnapshots200Response>>;

    /**
     * Minimum role: viewer. Every shopping page captured for a project, newest first, one page at a time: the ranked offers as they stood at `capturedAt`. `price` and `currency` are what the seller showed at that moment — no tax normalisation, no shipping, no conversion — and two offers in one snapshot may carry different currencies, so convert before comparing. `productId` is the marketplace\'s own identifier for the listing, stable enough to follow one offer across snapshots, and it is not a Mencoro id. Offers turn over far faster than organic results, so two snapshots days apart routinely share none; that is the marketplace behaving normally, not a gap in the capture. Unlike organic results, a shopping offer carries no `unresolved` flag: the capture pipeline does not record one for this surface. Shopping captures are trimmed by the same raw-data purge as AI answers and search pages, so the same retention floor applies: a `dateFrom` before it is refused rather than answered with an empty page. `total` counts every capture the filter matches, not the size of this page.
     * List captured shopping-results pages
     */
    listShoppingSnapshots(requestParameters: ListShoppingSnapshotsRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<ListShoppingSnapshots200Response>;

}

/**
 * 
 */
export class CapturesApi extends runtime.BaseAPI implements CapturesApiInterface {

    /**
     * Creates request options for listAiResponses without sending the request
     */
    async listAiResponsesRequestOpts(requestParameters: ListAiResponsesRequest): Promise<runtime.RequestOpts> {
        if (requestParameters['organizationId'] == null) {
            throw new runtime.RequiredError(
                'organizationId',
                'Required parameter "organizationId" was null or undefined when calling listAiResponses().'
            );
        }

        if (requestParameters['projectId'] == null) {
            throw new runtime.RequiredError(
                'projectId',
                'Required parameter "projectId" was null or undefined when calling listAiResponses().'
            );
        }

        const queryParameters: any = {};

        if (requestParameters['dateFrom'] != null) {
            queryParameters['dateFrom'] = (requestParameters['dateFrom'] as any).toISOString().substring(0,10);
        }

        if (requestParameters['dateTo'] != null) {
            queryParameters['dateTo'] = (requestParameters['dateTo'] as any).toISOString().substring(0,10);
        }

        if (requestParameters['trackedQueryId'] != null) {
            queryParameters['trackedQueryId'] = requestParameters['trackedQueryId'];
        }

        if (requestParameters['engines'] != null) {
            queryParameters['engines'] = requestParameters['engines'];
        }

        if (requestParameters['limit'] != null) {
            queryParameters['limit'] = requestParameters['limit'];
        }

        if (requestParameters['offset'] != null) {
            queryParameters['offset'] = requestParameters['offset'];
        }

        if (requestParameters['sortOrder'] != null) {
            queryParameters['sortOrder'] = requestParameters['sortOrder'];
        }

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("ApiKey", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/api/v1/organizations/{organizationId}/projects/{projectId}/ai-responses`;
        urlPath = urlPath.replace('{organizationId}', encodeURIComponent(String(requestParameters['organizationId'])));
        urlPath = urlPath.replace('{projectId}', encodeURIComponent(String(requestParameters['projectId'])));

        return {
            path: urlPath,
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        };
    }

    /**
     * Minimum role: viewer. Every AI answer captured for a project, newest first, one page at a time. This is the evidence under the analytics operations: the full answer text and the sources the engine cited, exactly as captured. Read `unresolved` on a citation before attributing its domain — a true value means the URL is still a redirector and the domain belongs to that redirector, not to the publisher; count the citation, do not credit the site. `passIndex` and `passCount` describe multi-pass sampling: rows sharing a `trackedQueryId` and `capturedAt` are passes of one run, not duplicates, so aggregating across them without dividing by `passCount` double-counts that run. `responseText` is the whole answer, so a page of 100 is a large response — lower `limit` rather than paging blind. Captures are kept for 16 months and purged after that; a `dateFrom` before the window is rejected rather than answered with an empty page. `total` counts every capture the filter matches, not the size of this page.
     * List captured AI answers
     */
    async listAiResponsesRaw(requestParameters: ListAiResponsesRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<ListAiResponses200Response>> {
        const requestOptions = await this.listAiResponsesRequestOpts(requestParameters);
        const response = await this.request(requestOptions, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => ListAiResponses200ResponseFromJSON(jsonValue));
    }

    /**
     * Minimum role: viewer. Every AI answer captured for a project, newest first, one page at a time. This is the evidence under the analytics operations: the full answer text and the sources the engine cited, exactly as captured. Read `unresolved` on a citation before attributing its domain — a true value means the URL is still a redirector and the domain belongs to that redirector, not to the publisher; count the citation, do not credit the site. `passIndex` and `passCount` describe multi-pass sampling: rows sharing a `trackedQueryId` and `capturedAt` are passes of one run, not duplicates, so aggregating across them without dividing by `passCount` double-counts that run. `responseText` is the whole answer, so a page of 100 is a large response — lower `limit` rather than paging blind. Captures are kept for 16 months and purged after that; a `dateFrom` before the window is rejected rather than answered with an empty page. `total` counts every capture the filter matches, not the size of this page.
     * List captured AI answers
     */
    async listAiResponses(requestParameters: ListAiResponsesRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<ListAiResponses200Response> {
        const response = await this.listAiResponsesRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Creates request options for listSearchSnapshots without sending the request
     */
    async listSearchSnapshotsRequestOpts(requestParameters: ListSearchSnapshotsRequest): Promise<runtime.RequestOpts> {
        if (requestParameters['organizationId'] == null) {
            throw new runtime.RequiredError(
                'organizationId',
                'Required parameter "organizationId" was null or undefined when calling listSearchSnapshots().'
            );
        }

        if (requestParameters['projectId'] == null) {
            throw new runtime.RequiredError(
                'projectId',
                'Required parameter "projectId" was null or undefined when calling listSearchSnapshots().'
            );
        }

        const queryParameters: any = {};

        if (requestParameters['dateFrom'] != null) {
            queryParameters['dateFrom'] = (requestParameters['dateFrom'] as any).toISOString().substring(0,10);
        }

        if (requestParameters['dateTo'] != null) {
            queryParameters['dateTo'] = (requestParameters['dateTo'] as any).toISOString().substring(0,10);
        }

        if (requestParameters['trackedQueryId'] != null) {
            queryParameters['trackedQueryId'] = requestParameters['trackedQueryId'];
        }

        if (requestParameters['limit'] != null) {
            queryParameters['limit'] = requestParameters['limit'];
        }

        if (requestParameters['offset'] != null) {
            queryParameters['offset'] = requestParameters['offset'];
        }

        if (requestParameters['sortOrder'] != null) {
            queryParameters['sortOrder'] = requestParameters['sortOrder'];
        }

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("ApiKey", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/api/v1/organizations/{organizationId}/projects/{projectId}/search-snapshots`;
        urlPath = urlPath.replace('{organizationId}', encodeURIComponent(String(requestParameters['organizationId'])));
        urlPath = urlPath.replace('{projectId}', encodeURIComponent(String(requestParameters['projectId'])));

        return {
            path: urlPath,
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        };
    }

    /**
     * Minimum role: viewer. Every organic search page captured for a project, newest first, one page at a time. This is what the rank figures in the analytics operations were computed from: the ranked results as they stood at `capturedAt`, not as they stand now. Read `unresolved` on a result before attributing its domain — a true value means the URL is still a redirector and the domain belongs to that redirector; count the result, do not credit the site. `rating` and `ratingVotes` come from a rich-result star rating when Google showed one and are absent far more often than present; their absence says nothing about the page. There is no `engines` filter because every capture here comes from Google SERP. Captures are kept for 16 months and purged after that; a `dateFrom` before the window is rejected rather than answered with an empty page. `total` counts every capture the filter matches, not the size of this page.
     * List captured search-results pages
     */
    async listSearchSnapshotsRaw(requestParameters: ListSearchSnapshotsRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<ListSearchSnapshots200Response>> {
        const requestOptions = await this.listSearchSnapshotsRequestOpts(requestParameters);
        const response = await this.request(requestOptions, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => ListSearchSnapshots200ResponseFromJSON(jsonValue));
    }

    /**
     * Minimum role: viewer. Every organic search page captured for a project, newest first, one page at a time. This is what the rank figures in the analytics operations were computed from: the ranked results as they stood at `capturedAt`, not as they stand now. Read `unresolved` on a result before attributing its domain — a true value means the URL is still a redirector and the domain belongs to that redirector; count the result, do not credit the site. `rating` and `ratingVotes` come from a rich-result star rating when Google showed one and are absent far more often than present; their absence says nothing about the page. There is no `engines` filter because every capture here comes from Google SERP. Captures are kept for 16 months and purged after that; a `dateFrom` before the window is rejected rather than answered with an empty page. `total` counts every capture the filter matches, not the size of this page.
     * List captured search-results pages
     */
    async listSearchSnapshots(requestParameters: ListSearchSnapshotsRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<ListSearchSnapshots200Response> {
        const response = await this.listSearchSnapshotsRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Creates request options for listShoppingSnapshots without sending the request
     */
    async listShoppingSnapshotsRequestOpts(requestParameters: ListShoppingSnapshotsRequest): Promise<runtime.RequestOpts> {
        if (requestParameters['organizationId'] == null) {
            throw new runtime.RequiredError(
                'organizationId',
                'Required parameter "organizationId" was null or undefined when calling listShoppingSnapshots().'
            );
        }

        if (requestParameters['projectId'] == null) {
            throw new runtime.RequiredError(
                'projectId',
                'Required parameter "projectId" was null or undefined when calling listShoppingSnapshots().'
            );
        }

        const queryParameters: any = {};

        if (requestParameters['dateFrom'] != null) {
            queryParameters['dateFrom'] = (requestParameters['dateFrom'] as any).toISOString().substring(0,10);
        }

        if (requestParameters['dateTo'] != null) {
            queryParameters['dateTo'] = (requestParameters['dateTo'] as any).toISOString().substring(0,10);
        }

        if (requestParameters['trackedQueryId'] != null) {
            queryParameters['trackedQueryId'] = requestParameters['trackedQueryId'];
        }

        if (requestParameters['limit'] != null) {
            queryParameters['limit'] = requestParameters['limit'];
        }

        if (requestParameters['offset'] != null) {
            queryParameters['offset'] = requestParameters['offset'];
        }

        if (requestParameters['sortOrder'] != null) {
            queryParameters['sortOrder'] = requestParameters['sortOrder'];
        }

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("ApiKey", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/api/v1/organizations/{organizationId}/projects/{projectId}/shopping-snapshots`;
        urlPath = urlPath.replace('{organizationId}', encodeURIComponent(String(requestParameters['organizationId'])));
        urlPath = urlPath.replace('{projectId}', encodeURIComponent(String(requestParameters['projectId'])));

        return {
            path: urlPath,
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        };
    }

    /**
     * Minimum role: viewer. Every shopping page captured for a project, newest first, one page at a time: the ranked offers as they stood at `capturedAt`. `price` and `currency` are what the seller showed at that moment — no tax normalisation, no shipping, no conversion — and two offers in one snapshot may carry different currencies, so convert before comparing. `productId` is the marketplace\'s own identifier for the listing, stable enough to follow one offer across snapshots, and it is not a Mencoro id. Offers turn over far faster than organic results, so two snapshots days apart routinely share none; that is the marketplace behaving normally, not a gap in the capture. Unlike organic results, a shopping offer carries no `unresolved` flag: the capture pipeline does not record one for this surface. Shopping captures are trimmed by the same raw-data purge as AI answers and search pages, so the same retention floor applies: a `dateFrom` before it is refused rather than answered with an empty page. `total` counts every capture the filter matches, not the size of this page.
     * List captured shopping-results pages
     */
    async listShoppingSnapshotsRaw(requestParameters: ListShoppingSnapshotsRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<ListShoppingSnapshots200Response>> {
        const requestOptions = await this.listShoppingSnapshotsRequestOpts(requestParameters);
        const response = await this.request(requestOptions, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => ListShoppingSnapshots200ResponseFromJSON(jsonValue));
    }

    /**
     * Minimum role: viewer. Every shopping page captured for a project, newest first, one page at a time: the ranked offers as they stood at `capturedAt`. `price` and `currency` are what the seller showed at that moment — no tax normalisation, no shipping, no conversion — and two offers in one snapshot may carry different currencies, so convert before comparing. `productId` is the marketplace\'s own identifier for the listing, stable enough to follow one offer across snapshots, and it is not a Mencoro id. Offers turn over far faster than organic results, so two snapshots days apart routinely share none; that is the marketplace behaving normally, not a gap in the capture. Unlike organic results, a shopping offer carries no `unresolved` flag: the capture pipeline does not record one for this surface. Shopping captures are trimmed by the same raw-data purge as AI answers and search pages, so the same retention floor applies: a `dateFrom` before it is refused rather than answered with an empty page. `total` counts every capture the filter matches, not the size of this page.
     * List captured shopping-results pages
     */
    async listShoppingSnapshots(requestParameters: ListShoppingSnapshotsRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<ListShoppingSnapshots200Response> {
        const response = await this.listShoppingSnapshotsRaw(requestParameters, initOverrides);
        return await response.value();
    }

}

/**
 * @export
 */
export const ListAiResponsesEnginesEnum = {
    Chatgpt: 'chatgpt',
    Perplexity: 'perplexity',
    GoogleAiOverview: 'google_ai_overview',
    GoogleAiMode: 'google_ai_mode'
} as const;
export type ListAiResponsesEnginesEnum = typeof ListAiResponsesEnginesEnum[keyof typeof ListAiResponsesEnginesEnum];
/**
 * @export
 */
export const ListAiResponsesSortOrderEnum = {
    Asc: 'asc',
    Desc: 'desc'
} as const;
export type ListAiResponsesSortOrderEnum = typeof ListAiResponsesSortOrderEnum[keyof typeof ListAiResponsesSortOrderEnum];
/**
 * @export
 */
export const ListSearchSnapshotsSortOrderEnum = {
    Asc: 'asc',
    Desc: 'desc'
} as const;
export type ListSearchSnapshotsSortOrderEnum = typeof ListSearchSnapshotsSortOrderEnum[keyof typeof ListSearchSnapshotsSortOrderEnum];
/**
 * @export
 */
export const ListShoppingSnapshotsSortOrderEnum = {
    Asc: 'asc',
    Desc: 'desc'
} as const;
export type ListShoppingSnapshotsSortOrderEnum = typeof ListShoppingSnapshotsSortOrderEnum[keyof typeof ListShoppingSnapshotsSortOrderEnum];

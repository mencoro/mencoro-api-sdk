/* tslint:disable */
/* eslint-disable */
/**
 * Mencoro API
 * Public API for Mencoro customers. Authenticate with an API key: `Authorization: Bearer mencoro_sk_...`.
 *
 * The version of the OpenAPI document: 1.0.0
 * Contact: support@mencoro.com
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */

import * as runtime from '../runtime';
import {
    type AcceptedJobResource,
    AcceptedJobResourceFromJSON,
    AcceptedJobResourceToJSON,
} from '../models/AcceptedJobResource';
import {
    type StartBrandDiscoveryJobRequest,
    StartBrandDiscoveryJobRequestFromJSON,
    StartBrandDiscoveryJobRequestToJSON,
} from '../models/StartBrandDiscoveryJobRequest';
import {
    type StartBrandNameSuggestionJobRequest,
    StartBrandNameSuggestionJobRequestFromJSON,
    StartBrandNameSuggestionJobRequestToJSON,
} from '../models/StartBrandNameSuggestionJobRequest';
import {
    type StartKeywordDiscoveryJobRequest,
    StartKeywordDiscoveryJobRequestFromJSON,
    StartKeywordDiscoveryJobRequestToJSON,
} from '../models/StartKeywordDiscoveryJobRequest';
import {
    type StartPromptDiscoveryJobRequest,
    StartPromptDiscoveryJobRequestFromJSON,
    StartPromptDiscoveryJobRequestToJSON,
} from '../models/StartPromptDiscoveryJobRequest';

export interface StartBrandDiscoveryJobOperationRequest {
    organizationId: string;
    projectId: string;
    idempotencyKey: string;
    startBrandDiscoveryJobRequest?: StartBrandDiscoveryJobRequest;
}

export interface StartBrandNameSuggestionJobOperationRequest {
    organizationId: string;
    idempotencyKey: string;
    startBrandNameSuggestionJobRequest: StartBrandNameSuggestionJobRequest;
}

export interface StartKeywordDiscoveryJobOperationRequest {
    organizationId: string;
    projectId: string;
    idempotencyKey: string;
    startKeywordDiscoveryJobRequest: StartKeywordDiscoveryJobRequest;
}

export interface StartPromptDiscoveryJobOperationRequest {
    organizationId: string;
    projectId: string;
    idempotencyKey: string;
    startPromptDiscoveryJobRequest: StartPromptDiscoveryJobRequest;
}

/**
 * DiscoveryApi - interface
 * 
 * @export
 * @interface DiscoveryApiInterface
 */
export interface DiscoveryApiInterface {
    /**
     * Creates request options for startBrandDiscoveryJob without sending the request
     * @param {string} organizationId 
     * @param {string} projectId Must belong to the organization in the path.
     * @param {string} idempotencyKey Repeat it to retry a start whose response was lost; the same key returns the same job instead of starting a second one.
     * @param {StartBrandDiscoveryJobRequest} [startBrandDiscoveryJobRequest] 
     * @throws {RequiredError}
     * @memberof DiscoveryApiInterface
     */
    startBrandDiscoveryJobRequestOpts(requestParameters: StartBrandDiscoveryJobOperationRequest): Promise<runtime.RequestOpts>;

    /**
     * Minimum role: manager. Starts background discovery of the brands and competitors around a project and answers 202 with the job to poll; it never returns brands inline. The job PROPOSES names: it creates and updates nothing, so neither the brand profile nor the competitor list changes because this endpoint was called. The organization must be active. Unlike keyword and prompt discovery this operation does not require an entitled subscription, matching the application behaviour it mirrors. Ten starts per minute per organization, counted across every key of every member and shared with the same operation in the Mencoro app; a retry carrying an Idempotency-Key already answered is served from the record and does not count. A project whose brand monitoring profile has not been created yet is still accepted and still billed: the job runs grounded on the project name alone, with no website domains, brand names or competitors to work from, and completes normally. Configure the brand profile first if you want the discovery grounded.
     * @summary Start a brand discovery job for a project
     * @param {string} organizationId 
     * @param {string} projectId Must belong to the organization in the path.
     * @param {string} idempotencyKey Repeat it to retry a start whose response was lost; the same key returns the same job instead of starting a second one.
     * @param {StartBrandDiscoveryJobRequest} [startBrandDiscoveryJobRequest] 
     * @param {*} [options] Override http request option.
     * @throws {RequiredError}
     * @memberof DiscoveryApiInterface
     */
    startBrandDiscoveryJobRaw(requestParameters: StartBrandDiscoveryJobOperationRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<AcceptedJobResource>>;

    /**
     * Minimum role: manager. Starts background discovery of the brands and competitors around a project and answers 202 with the job to poll; it never returns brands inline. The job PROPOSES names: it creates and updates nothing, so neither the brand profile nor the competitor list changes because this endpoint was called. The organization must be active. Unlike keyword and prompt discovery this operation does not require an entitled subscription, matching the application behaviour it mirrors. Ten starts per minute per organization, counted across every key of every member and shared with the same operation in the Mencoro app; a retry carrying an Idempotency-Key already answered is served from the record and does not count. A project whose brand monitoring profile has not been created yet is still accepted and still billed: the job runs grounded on the project name alone, with no website domains, brand names or competitors to work from, and completes normally. Configure the brand profile first if you want the discovery grounded.
     * Start a brand discovery job for a project
     */
    startBrandDiscoveryJob(requestParameters: StartBrandDiscoveryJobOperationRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<AcceptedJobResource>;

    /**
     * Creates request options for startBrandNameSuggestionJob without sending the request
     * @param {string} organizationId 
     * @param {string} idempotencyKey Repeat it to retry a start whose response was lost; the same key returns the same job instead of starting a second one.
     * @param {StartBrandNameSuggestionJobRequest} startBrandNameSuggestionJobRequest 
     * @throws {RequiredError}
     * @memberof DiscoveryApiInterface
     */
    startBrandNameSuggestionJobRequestOpts(requestParameters: StartBrandNameSuggestionJobOperationRequest): Promise<runtime.RequestOpts>;

    /**
     * Minimum role: manager. Starts a background, web-search-grounded job that suggests alias spellings for one entity - a brand, a project or a competitor - described entirely by the body, and answers 202 with the job to poll; it never returns suggestions inline. The entity need not exist: nothing is looked up, nothing is attached and nothing is saved, so the suggestions are the caller\'s to apply. Names sent in `enteredBrandNames` are excluded from the result. The organization must be active; no subscription is required, matching the application behaviour this mirrors. Ten starts per minute per organization, counted across every key of every member; a retry carrying an Idempotency-Key already answered is served from the record and does not count.
     * @summary Start a brand-name alias suggestion job
     * @param {string} organizationId 
     * @param {string} idempotencyKey Repeat it to retry a start whose response was lost; the same key returns the same job instead of starting a second one.
     * @param {StartBrandNameSuggestionJobRequest} startBrandNameSuggestionJobRequest 
     * @param {*} [options] Override http request option.
     * @throws {RequiredError}
     * @memberof DiscoveryApiInterface
     */
    startBrandNameSuggestionJobRaw(requestParameters: StartBrandNameSuggestionJobOperationRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<AcceptedJobResource>>;

    /**
     * Minimum role: manager. Starts a background, web-search-grounded job that suggests alias spellings for one entity - a brand, a project or a competitor - described entirely by the body, and answers 202 with the job to poll; it never returns suggestions inline. The entity need not exist: nothing is looked up, nothing is attached and nothing is saved, so the suggestions are the caller\'s to apply. Names sent in `enteredBrandNames` are excluded from the result. The organization must be active; no subscription is required, matching the application behaviour this mirrors. Ten starts per minute per organization, counted across every key of every member; a retry carrying an Idempotency-Key already answered is served from the record and does not count.
     * Start a brand-name alias suggestion job
     */
    startBrandNameSuggestionJob(requestParameters: StartBrandNameSuggestionJobOperationRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<AcceptedJobResource>;

    /**
     * Creates request options for startKeywordDiscoveryJob without sending the request
     * @param {string} organizationId 
     * @param {string} projectId Must belong to the organization in the path.
     * @param {string} idempotencyKey Repeat it to retry a start whose response was lost; the same key returns the same job instead of starting a second one.
     * @param {StartKeywordDiscoveryJobRequest} startKeywordDiscoveryJobRequest 
     * @throws {RequiredError}
     * @memberof DiscoveryApiInterface
     */
    startKeywordDiscoveryJobRequestOpts(requestParameters: StartKeywordDiscoveryJobOperationRequest): Promise<runtime.RequestOpts>;

    /**
     * Minimum role: manager. Starts background keyword discovery from free-text seed input and answers 202 with the job to poll; it never returns keywords inline. The job SUGGESTS keywords - it creates no tracked queries and changes nothing in the project, so a completed job is a list to choose from, not work that has been applied. Queries the project already tracks are excluded from the suggestions automatically; `excludeQueries` adds more on top for this run only. The organization must be active and hold an entitled subscription, because the run spends provider budget. Ten starts per minute per organization, counted across every key of every member; a retry carrying an Idempotency-Key already answered is served from the record and does not count. Run tuning is not exposed: the discovery fan-out uses provider defaults.
     * @summary Start a keyword discovery job for a project
     * @param {string} organizationId 
     * @param {string} projectId Must belong to the organization in the path.
     * @param {string} idempotencyKey Repeat it to retry a start whose response was lost; the same key returns the same job instead of starting a second one.
     * @param {StartKeywordDiscoveryJobRequest} startKeywordDiscoveryJobRequest 
     * @param {*} [options] Override http request option.
     * @throws {RequiredError}
     * @memberof DiscoveryApiInterface
     */
    startKeywordDiscoveryJobRaw(requestParameters: StartKeywordDiscoveryJobOperationRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<AcceptedJobResource>>;

    /**
     * Minimum role: manager. Starts background keyword discovery from free-text seed input and answers 202 with the job to poll; it never returns keywords inline. The job SUGGESTS keywords - it creates no tracked queries and changes nothing in the project, so a completed job is a list to choose from, not work that has been applied. Queries the project already tracks are excluded from the suggestions automatically; `excludeQueries` adds more on top for this run only. The organization must be active and hold an entitled subscription, because the run spends provider budget. Ten starts per minute per organization, counted across every key of every member; a retry carrying an Idempotency-Key already answered is served from the record and does not count. Run tuning is not exposed: the discovery fan-out uses provider defaults.
     * Start a keyword discovery job for a project
     */
    startKeywordDiscoveryJob(requestParameters: StartKeywordDiscoveryJobOperationRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<AcceptedJobResource>;

    /**
     * Creates request options for startPromptDiscoveryJob without sending the request
     * @param {string} organizationId 
     * @param {string} projectId Must belong to the organization in the path.
     * @param {string} idempotencyKey Repeat it to retry a start whose response was lost; the same key returns the same job instead of starting a second one.
     * @param {StartPromptDiscoveryJobRequest} startPromptDiscoveryJobRequest 
     * @throws {RequiredError}
     * @memberof DiscoveryApiInterface
     */
    startPromptDiscoveryJobRequestOpts(requestParameters: StartPromptDiscoveryJobOperationRequest): Promise<runtime.RequestOpts>;

    /**
     * Minimum role: manager. Starts background discovery of natural-language prompts - the questions an answer engine gets asked - from free-text seed input, and answers 202 with the job to poll; it never returns prompts inline. The job SUGGESTS prompts: it creates no tracked queries and changes nothing in the project. `country` is required here, unlike keyword discovery, because a prompt is a question asked from somewhere and the provider request has no default for it. Queries the project already tracks are excluded automatically; `excludeQueries` adds more for this run only. The organization must be active and hold an entitled subscription, because the run spends provider budget. Ten starts per minute per organization, counted across every key of every member; a retry carrying an Idempotency-Key already answered is served from the record and does not count. Run tuning is not exposed: the discovery fan-out uses provider defaults.
     * @summary Start a geo prompt discovery job for a project
     * @param {string} organizationId 
     * @param {string} projectId Must belong to the organization in the path.
     * @param {string} idempotencyKey Repeat it to retry a start whose response was lost; the same key returns the same job instead of starting a second one.
     * @param {StartPromptDiscoveryJobRequest} startPromptDiscoveryJobRequest 
     * @param {*} [options] Override http request option.
     * @throws {RequiredError}
     * @memberof DiscoveryApiInterface
     */
    startPromptDiscoveryJobRaw(requestParameters: StartPromptDiscoveryJobOperationRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<AcceptedJobResource>>;

    /**
     * Minimum role: manager. Starts background discovery of natural-language prompts - the questions an answer engine gets asked - from free-text seed input, and answers 202 with the job to poll; it never returns prompts inline. The job SUGGESTS prompts: it creates no tracked queries and changes nothing in the project. `country` is required here, unlike keyword discovery, because a prompt is a question asked from somewhere and the provider request has no default for it. Queries the project already tracks are excluded automatically; `excludeQueries` adds more for this run only. The organization must be active and hold an entitled subscription, because the run spends provider budget. Ten starts per minute per organization, counted across every key of every member; a retry carrying an Idempotency-Key already answered is served from the record and does not count. Run tuning is not exposed: the discovery fan-out uses provider defaults.
     * Start a geo prompt discovery job for a project
     */
    startPromptDiscoveryJob(requestParameters: StartPromptDiscoveryJobOperationRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<AcceptedJobResource>;

}

/**
 * 
 */
export class DiscoveryApi extends runtime.BaseAPI implements DiscoveryApiInterface {

    /**
     * Creates request options for startBrandDiscoveryJob without sending the request
     */
    async startBrandDiscoveryJobRequestOpts(requestParameters: StartBrandDiscoveryJobOperationRequest): Promise<runtime.RequestOpts> {
        if (requestParameters['organizationId'] == null) {
            throw new runtime.RequiredError(
                'organizationId',
                'Required parameter "organizationId" was null or undefined when calling startBrandDiscoveryJob().'
            );
        }

        if (requestParameters['projectId'] == null) {
            throw new runtime.RequiredError(
                'projectId',
                'Required parameter "projectId" was null or undefined when calling startBrandDiscoveryJob().'
            );
        }

        if (requestParameters['idempotencyKey'] == null) {
            throw new runtime.RequiredError(
                'idempotencyKey',
                'Required parameter "idempotencyKey" was null or undefined when calling startBrandDiscoveryJob().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        headerParameters['Content-Type'] = 'application/json';

        if (requestParameters['idempotencyKey'] != null) {
            headerParameters['Idempotency-Key'] = String(requestParameters['idempotencyKey']);
        }

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("ApiKey", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/api/v1/organizations/{organizationId}/projects/{projectId}/discovery/brands`;
        urlPath = urlPath.replace('{organizationId}', encodeURIComponent(String(requestParameters['organizationId'])));
        urlPath = urlPath.replace('{projectId}', encodeURIComponent(String(requestParameters['projectId'])));

        return {
            path: urlPath,
            method: 'POST',
            headers: headerParameters,
            query: queryParameters,
            body: StartBrandDiscoveryJobRequestToJSON(requestParameters['startBrandDiscoveryJobRequest']),
        };
    }

    /**
     * Minimum role: manager. Starts background discovery of the brands and competitors around a project and answers 202 with the job to poll; it never returns brands inline. The job PROPOSES names: it creates and updates nothing, so neither the brand profile nor the competitor list changes because this endpoint was called. The organization must be active. Unlike keyword and prompt discovery this operation does not require an entitled subscription, matching the application behaviour it mirrors. Ten starts per minute per organization, counted across every key of every member and shared with the same operation in the Mencoro app; a retry carrying an Idempotency-Key already answered is served from the record and does not count. A project whose brand monitoring profile has not been created yet is still accepted and still billed: the job runs grounded on the project name alone, with no website domains, brand names or competitors to work from, and completes normally. Configure the brand profile first if you want the discovery grounded.
     * Start a brand discovery job for a project
     */
    async startBrandDiscoveryJobRaw(requestParameters: StartBrandDiscoveryJobOperationRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<AcceptedJobResource>> {
        const requestOptions = await this.startBrandDiscoveryJobRequestOpts(requestParameters);
        const response = await this.request(requestOptions, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => AcceptedJobResourceFromJSON(jsonValue));
    }

    /**
     * Minimum role: manager. Starts background discovery of the brands and competitors around a project and answers 202 with the job to poll; it never returns brands inline. The job PROPOSES names: it creates and updates nothing, so neither the brand profile nor the competitor list changes because this endpoint was called. The organization must be active. Unlike keyword and prompt discovery this operation does not require an entitled subscription, matching the application behaviour it mirrors. Ten starts per minute per organization, counted across every key of every member and shared with the same operation in the Mencoro app; a retry carrying an Idempotency-Key already answered is served from the record and does not count. A project whose brand monitoring profile has not been created yet is still accepted and still billed: the job runs grounded on the project name alone, with no website domains, brand names or competitors to work from, and completes normally. Configure the brand profile first if you want the discovery grounded.
     * Start a brand discovery job for a project
     */
    async startBrandDiscoveryJob(requestParameters: StartBrandDiscoveryJobOperationRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<AcceptedJobResource> {
        const response = await this.startBrandDiscoveryJobRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Creates request options for startBrandNameSuggestionJob without sending the request
     */
    async startBrandNameSuggestionJobRequestOpts(requestParameters: StartBrandNameSuggestionJobOperationRequest): Promise<runtime.RequestOpts> {
        if (requestParameters['organizationId'] == null) {
            throw new runtime.RequiredError(
                'organizationId',
                'Required parameter "organizationId" was null or undefined when calling startBrandNameSuggestionJob().'
            );
        }

        if (requestParameters['idempotencyKey'] == null) {
            throw new runtime.RequiredError(
                'idempotencyKey',
                'Required parameter "idempotencyKey" was null or undefined when calling startBrandNameSuggestionJob().'
            );
        }

        if (requestParameters['startBrandNameSuggestionJobRequest'] == null) {
            throw new runtime.RequiredError(
                'startBrandNameSuggestionJobRequest',
                'Required parameter "startBrandNameSuggestionJobRequest" was null or undefined when calling startBrandNameSuggestionJob().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        headerParameters['Content-Type'] = 'application/json';

        if (requestParameters['idempotencyKey'] != null) {
            headerParameters['Idempotency-Key'] = String(requestParameters['idempotencyKey']);
        }

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("ApiKey", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/api/v1/organizations/{organizationId}/brand-name-suggestions`;
        urlPath = urlPath.replace('{organizationId}', encodeURIComponent(String(requestParameters['organizationId'])));

        return {
            path: urlPath,
            method: 'POST',
            headers: headerParameters,
            query: queryParameters,
            body: StartBrandNameSuggestionJobRequestToJSON(requestParameters['startBrandNameSuggestionJobRequest']),
        };
    }

    /**
     * Minimum role: manager. Starts a background, web-search-grounded job that suggests alias spellings for one entity - a brand, a project or a competitor - described entirely by the body, and answers 202 with the job to poll; it never returns suggestions inline. The entity need not exist: nothing is looked up, nothing is attached and nothing is saved, so the suggestions are the caller\'s to apply. Names sent in `enteredBrandNames` are excluded from the result. The organization must be active; no subscription is required, matching the application behaviour this mirrors. Ten starts per minute per organization, counted across every key of every member; a retry carrying an Idempotency-Key already answered is served from the record and does not count.
     * Start a brand-name alias suggestion job
     */
    async startBrandNameSuggestionJobRaw(requestParameters: StartBrandNameSuggestionJobOperationRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<AcceptedJobResource>> {
        const requestOptions = await this.startBrandNameSuggestionJobRequestOpts(requestParameters);
        const response = await this.request(requestOptions, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => AcceptedJobResourceFromJSON(jsonValue));
    }

    /**
     * Minimum role: manager. Starts a background, web-search-grounded job that suggests alias spellings for one entity - a brand, a project or a competitor - described entirely by the body, and answers 202 with the job to poll; it never returns suggestions inline. The entity need not exist: nothing is looked up, nothing is attached and nothing is saved, so the suggestions are the caller\'s to apply. Names sent in `enteredBrandNames` are excluded from the result. The organization must be active; no subscription is required, matching the application behaviour this mirrors. Ten starts per minute per organization, counted across every key of every member; a retry carrying an Idempotency-Key already answered is served from the record and does not count.
     * Start a brand-name alias suggestion job
     */
    async startBrandNameSuggestionJob(requestParameters: StartBrandNameSuggestionJobOperationRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<AcceptedJobResource> {
        const response = await this.startBrandNameSuggestionJobRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Creates request options for startKeywordDiscoveryJob without sending the request
     */
    async startKeywordDiscoveryJobRequestOpts(requestParameters: StartKeywordDiscoveryJobOperationRequest): Promise<runtime.RequestOpts> {
        if (requestParameters['organizationId'] == null) {
            throw new runtime.RequiredError(
                'organizationId',
                'Required parameter "organizationId" was null or undefined when calling startKeywordDiscoveryJob().'
            );
        }

        if (requestParameters['projectId'] == null) {
            throw new runtime.RequiredError(
                'projectId',
                'Required parameter "projectId" was null or undefined when calling startKeywordDiscoveryJob().'
            );
        }

        if (requestParameters['idempotencyKey'] == null) {
            throw new runtime.RequiredError(
                'idempotencyKey',
                'Required parameter "idempotencyKey" was null or undefined when calling startKeywordDiscoveryJob().'
            );
        }

        if (requestParameters['startKeywordDiscoveryJobRequest'] == null) {
            throw new runtime.RequiredError(
                'startKeywordDiscoveryJobRequest',
                'Required parameter "startKeywordDiscoveryJobRequest" was null or undefined when calling startKeywordDiscoveryJob().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        headerParameters['Content-Type'] = 'application/json';

        if (requestParameters['idempotencyKey'] != null) {
            headerParameters['Idempotency-Key'] = String(requestParameters['idempotencyKey']);
        }

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("ApiKey", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/api/v1/organizations/{organizationId}/projects/{projectId}/discovery/keywords`;
        urlPath = urlPath.replace('{organizationId}', encodeURIComponent(String(requestParameters['organizationId'])));
        urlPath = urlPath.replace('{projectId}', encodeURIComponent(String(requestParameters['projectId'])));

        return {
            path: urlPath,
            method: 'POST',
            headers: headerParameters,
            query: queryParameters,
            body: StartKeywordDiscoveryJobRequestToJSON(requestParameters['startKeywordDiscoveryJobRequest']),
        };
    }

    /**
     * Minimum role: manager. Starts background keyword discovery from free-text seed input and answers 202 with the job to poll; it never returns keywords inline. The job SUGGESTS keywords - it creates no tracked queries and changes nothing in the project, so a completed job is a list to choose from, not work that has been applied. Queries the project already tracks are excluded from the suggestions automatically; `excludeQueries` adds more on top for this run only. The organization must be active and hold an entitled subscription, because the run spends provider budget. Ten starts per minute per organization, counted across every key of every member; a retry carrying an Idempotency-Key already answered is served from the record and does not count. Run tuning is not exposed: the discovery fan-out uses provider defaults.
     * Start a keyword discovery job for a project
     */
    async startKeywordDiscoveryJobRaw(requestParameters: StartKeywordDiscoveryJobOperationRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<AcceptedJobResource>> {
        const requestOptions = await this.startKeywordDiscoveryJobRequestOpts(requestParameters);
        const response = await this.request(requestOptions, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => AcceptedJobResourceFromJSON(jsonValue));
    }

    /**
     * Minimum role: manager. Starts background keyword discovery from free-text seed input and answers 202 with the job to poll; it never returns keywords inline. The job SUGGESTS keywords - it creates no tracked queries and changes nothing in the project, so a completed job is a list to choose from, not work that has been applied. Queries the project already tracks are excluded from the suggestions automatically; `excludeQueries` adds more on top for this run only. The organization must be active and hold an entitled subscription, because the run spends provider budget. Ten starts per minute per organization, counted across every key of every member; a retry carrying an Idempotency-Key already answered is served from the record and does not count. Run tuning is not exposed: the discovery fan-out uses provider defaults.
     * Start a keyword discovery job for a project
     */
    async startKeywordDiscoveryJob(requestParameters: StartKeywordDiscoveryJobOperationRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<AcceptedJobResource> {
        const response = await this.startKeywordDiscoveryJobRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Creates request options for startPromptDiscoveryJob without sending the request
     */
    async startPromptDiscoveryJobRequestOpts(requestParameters: StartPromptDiscoveryJobOperationRequest): Promise<runtime.RequestOpts> {
        if (requestParameters['organizationId'] == null) {
            throw new runtime.RequiredError(
                'organizationId',
                'Required parameter "organizationId" was null or undefined when calling startPromptDiscoveryJob().'
            );
        }

        if (requestParameters['projectId'] == null) {
            throw new runtime.RequiredError(
                'projectId',
                'Required parameter "projectId" was null or undefined when calling startPromptDiscoveryJob().'
            );
        }

        if (requestParameters['idempotencyKey'] == null) {
            throw new runtime.RequiredError(
                'idempotencyKey',
                'Required parameter "idempotencyKey" was null or undefined when calling startPromptDiscoveryJob().'
            );
        }

        if (requestParameters['startPromptDiscoveryJobRequest'] == null) {
            throw new runtime.RequiredError(
                'startPromptDiscoveryJobRequest',
                'Required parameter "startPromptDiscoveryJobRequest" was null or undefined when calling startPromptDiscoveryJob().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        headerParameters['Content-Type'] = 'application/json';

        if (requestParameters['idempotencyKey'] != null) {
            headerParameters['Idempotency-Key'] = String(requestParameters['idempotencyKey']);
        }

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("ApiKey", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/api/v1/organizations/{organizationId}/projects/{projectId}/discovery/prompts`;
        urlPath = urlPath.replace('{organizationId}', encodeURIComponent(String(requestParameters['organizationId'])));
        urlPath = urlPath.replace('{projectId}', encodeURIComponent(String(requestParameters['projectId'])));

        return {
            path: urlPath,
            method: 'POST',
            headers: headerParameters,
            query: queryParameters,
            body: StartPromptDiscoveryJobRequestToJSON(requestParameters['startPromptDiscoveryJobRequest']),
        };
    }

    /**
     * Minimum role: manager. Starts background discovery of natural-language prompts - the questions an answer engine gets asked - from free-text seed input, and answers 202 with the job to poll; it never returns prompts inline. The job SUGGESTS prompts: it creates no tracked queries and changes nothing in the project. `country` is required here, unlike keyword discovery, because a prompt is a question asked from somewhere and the provider request has no default for it. Queries the project already tracks are excluded automatically; `excludeQueries` adds more for this run only. The organization must be active and hold an entitled subscription, because the run spends provider budget. Ten starts per minute per organization, counted across every key of every member; a retry carrying an Idempotency-Key already answered is served from the record and does not count. Run tuning is not exposed: the discovery fan-out uses provider defaults.
     * Start a geo prompt discovery job for a project
     */
    async startPromptDiscoveryJobRaw(requestParameters: StartPromptDiscoveryJobOperationRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<AcceptedJobResource>> {
        const requestOptions = await this.startPromptDiscoveryJobRequestOpts(requestParameters);
        const response = await this.request(requestOptions, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => AcceptedJobResourceFromJSON(jsonValue));
    }

    /**
     * Minimum role: manager. Starts background discovery of natural-language prompts - the questions an answer engine gets asked - from free-text seed input, and answers 202 with the job to poll; it never returns prompts inline. The job SUGGESTS prompts: it creates no tracked queries and changes nothing in the project. `country` is required here, unlike keyword discovery, because a prompt is a question asked from somewhere and the provider request has no default for it. Queries the project already tracks are excluded automatically; `excludeQueries` adds more for this run only. The organization must be active and hold an entitled subscription, because the run spends provider budget. Ten starts per minute per organization, counted across every key of every member; a retry carrying an Idempotency-Key already answered is served from the record and does not count. Run tuning is not exposed: the discovery fan-out uses provider defaults.
     * Start a geo prompt discovery job for a project
     */
    async startPromptDiscoveryJob(requestParameters: StartPromptDiscoveryJobOperationRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<AcceptedJobResource> {
        const response = await this.startPromptDiscoveryJobRaw(requestParameters, initOverrides);
        return await response.value();
    }

}

/* tslint:disable */
/* eslint-disable */
/**
 * Mencoro API
 * Public API for Mencoro customers. Authenticate with an API key: `Authorization: Bearer mencoro_sk_...`.
 *
 * The version of the OpenAPI document: 1.0.0
 * Contact: support@mencoro.com
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */

import * as runtime from '../runtime';
import {
    type ChangeMemberRoleRequest,
    ChangeMemberRoleRequestFromJSON,
    ChangeMemberRoleRequestToJSON,
} from '../models/ChangeMemberRoleRequest';
import {
    type ListMembers200Response,
    ListMembers200ResponseFromJSON,
    ListMembers200ResponseToJSON,
} from '../models/ListMembers200Response';
import {
    type MemberResource,
    MemberResourceFromJSON,
    MemberResourceToJSON,
} from '../models/MemberResource';

export interface ChangeMemberRoleOperationRequest {
    organizationId: string;
    memberId: string;
    xMencoroConfirmation: string;
    idempotencyKey: string;
    changeMemberRoleRequest: ChangeMemberRoleRequest;
}

export interface GetMemberRequest {
    organizationId: string;
    memberId: string;
}

export interface ListMembersRequest {
    organizationId: string;
    limit?: number;
    offset?: number;
    status?: ListMembersStatusEnum;
    sortBy?: ListMembersSortByEnum;
    sortOrder?: ListMembersSortOrderEnum;
}

export interface ReactivateMemberRequest {
    organizationId: string;
    memberId: string;
    xMencoroConfirmation: string;
    idempotencyKey: string;
}

export interface SuspendMemberRequest {
    organizationId: string;
    memberId: string;
    xMencoroConfirmation: string;
    idempotencyKey: string;
}

/**
 * MembersApi - interface
 * 
 * @export
 * @interface MembersApiInterface
 */
export interface MembersApiInterface {
    /**
     * Creates request options for changeMemberRole without sending the request
     * @param {string} organizationId 
     * @param {string} memberId 
     * @param {string} xMencoroConfirmation 
     * @param {string} idempotencyKey 
     * @param {ChangeMemberRoleRequest} changeMemberRoleRequest 
     * @throws {RequiredError}
     * @memberof MembersApiInterface
     */
    changeMemberRoleRequestOpts(requestParameters: ChangeMemberRoleOperationRequest): Promise<runtime.RequestOpts>;

    /**
     * Minimum role: owner. The role of a suspended member cannot be changed, and the last active owner cannot be demoted. Preview it first: the confirmation is bound to the number of active owners, so another owner being suspended in between invalidates it rather than stranding the organization.
     * @summary Change a member role
     * @param {string} organizationId 
     * @param {string} memberId 
     * @param {string} xMencoroConfirmation 
     * @param {string} idempotencyKey 
     * @param {ChangeMemberRoleRequest} changeMemberRoleRequest 
     * @param {*} [options] Override http request option.
     * @throws {RequiredError}
     * @memberof MembersApiInterface
     */
    changeMemberRoleRaw(requestParameters: ChangeMemberRoleOperationRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<MemberResource>>;

    /**
     * Minimum role: owner. The role of a suspended member cannot be changed, and the last active owner cannot be demoted. Preview it first: the confirmation is bound to the number of active owners, so another owner being suspended in between invalidates it rather than stranding the organization.
     * Change a member role
     */
    changeMemberRole(requestParameters: ChangeMemberRoleOperationRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<MemberResource>;

    /**
     * Creates request options for getMember without sending the request
     * @param {string} organizationId 
     * @param {string} memberId The membership id, not the user id. Must belong to the organization in the path.
     * @throws {RequiredError}
     * @memberof MembersApiInterface
     */
    getMemberRequestOpts(requestParameters: GetMemberRequest): Promise<runtime.RequestOpts>;

    /**
     * Minimum role: owner — the same floor the members listing enforces, because a caller who can page the roster has already seen this record. Returns the facts of one membership: its role, whether it is active or suspended, and when it was joined. It does NOT describe the person behind it: no name, no email address, no phone number and no profile image, so a membership id can never be turned into a contact lookup. The membership is read from PostgreSQL, the same row at the same freshness the listing publishes. A membership belonging to another organization answers 404, exactly as an unknown or malformed id does, so the API never confirms that an inaccessible membership exists; the one 403 is a key without the read capability. The response carries no organizationId — it is the one in the path — and no creation timestamp; joinedAt is the membership fact. No query parameters are accepted.
     * @summary Get one organization membership
     * @param {string} organizationId 
     * @param {string} memberId The membership id, not the user id. Must belong to the organization in the path.
     * @param {*} [options] Override http request option.
     * @throws {RequiredError}
     * @memberof MembersApiInterface
     */
    getMemberRaw(requestParameters: GetMemberRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<MemberResource>>;

    /**
     * Minimum role: owner — the same floor the members listing enforces, because a caller who can page the roster has already seen this record. Returns the facts of one membership: its role, whether it is active or suspended, and when it was joined. It does NOT describe the person behind it: no name, no email address, no phone number and no profile image, so a membership id can never be turned into a contact lookup. The membership is read from PostgreSQL, the same row at the same freshness the listing publishes. A membership belonging to another organization answers 404, exactly as an unknown or malformed id does, so the API never confirms that an inaccessible membership exists; the one 403 is a key without the read capability. The response carries no organizationId — it is the one in the path — and no creation timestamp; joinedAt is the membership fact. No query parameters are accepted.
     * Get one organization membership
     */
    getMember(requestParameters: GetMemberRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<MemberResource>;

    /**
     * Creates request options for listMembers without sending the request
     * @param {string} organizationId 
     * @param {number} [limit] 
     * @param {number} [offset] 
     * @param {'active' | 'suspended'} [status] Absent means both states.
     * @param {'joinedAt' | 'role'} [sortBy] 
     * @param {'asc' | 'desc'} [sortOrder] 
     * @throws {RequiredError}
     * @memberof MembersApiInterface
     */
    listMembersRequestOpts(requestParameters: ListMembersRequest): Promise<runtime.RequestOpts>;

    /**
     * Minimum role: owner. The application exposes this roster twice and the two disagree — its member screen shows it to any viewer, while its non-BFF endpoint requires an owner — so the published API takes the stricter of the two and requires an owner. A membership describes the membership, not the person: names and email addresses are never returned here, and there is no search parameter, because both would turn the roster into a contact export. Omitting \"status\" returns active and suspended memberships alike. Sorting by \"role\" is alphabetical on the role name, not by seniority.Send `Accept: text/csv` to receive the same page as a CSV download instead of JSON: same filters, same authorization, same page window and the same maximum of 100 rows — it is this page in another format, not a bulk export, so a whole collection is still read by paging. The CSV carries no `total`, because a table whose every row is a record has nowhere to put one; read it from the JSON representation of the same request. Cells beginning with `=`, `+`, `-` or `@` are prefixed with an apostrophe so a spreadsheet treats them as text rather than running them as formulas.
     * @summary List an organization\'s members
     * @param {string} organizationId 
     * @param {number} [limit] 
     * @param {number} [offset] 
     * @param {'active' | 'suspended'} [status] Absent means both states.
     * @param {'joinedAt' | 'role'} [sortBy] 
     * @param {'asc' | 'desc'} [sortOrder] 
     * @param {*} [options] Override http request option.
     * @throws {RequiredError}
     * @memberof MembersApiInterface
     */
    listMembersRaw(requestParameters: ListMembersRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<ListMembers200Response>>;

    /**
     * Minimum role: owner. The application exposes this roster twice and the two disagree — its member screen shows it to any viewer, while its non-BFF endpoint requires an owner — so the published API takes the stricter of the two and requires an owner. A membership describes the membership, not the person: names and email addresses are never returned here, and there is no search parameter, because both would turn the roster into a contact export. Omitting \"status\" returns active and suspended memberships alike. Sorting by \"role\" is alphabetical on the role name, not by seniority.Send `Accept: text/csv` to receive the same page as a CSV download instead of JSON: same filters, same authorization, same page window and the same maximum of 100 rows — it is this page in another format, not a bulk export, so a whole collection is still read by paging. The CSV carries no `total`, because a table whose every row is a record has nowhere to put one; read it from the JSON representation of the same request. Cells beginning with `=`, `+`, `-` or `@` are prefixed with an apostrophe so a spreadsheet treats them as text rather than running them as formulas.
     * List an organization\'s members
     */
    listMembers(requestParameters: ListMembersRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<ListMembers200Response>;

    /**
     * Creates request options for reactivateMember without sending the request
     * @param {string} organizationId 
     * @param {string} memberId 
     * @param {string} xMencoroConfirmation 
     * @param {string} idempotencyKey 
     * @throws {RequiredError}
     * @memberof MembersApiInterface
     */
    reactivateMemberRequestOpts(requestParameters: ReactivateMemberRequest): Promise<runtime.RequestOpts>;

    /**
     * Minimum role: owner. The member keeps the role they had and regains access on their very next request. A member who is already active cannot be reactivated.
     * @summary Reactivate a suspended member
     * @param {string} organizationId 
     * @param {string} memberId 
     * @param {string} xMencoroConfirmation 
     * @param {string} idempotencyKey 
     * @param {*} [options] Override http request option.
     * @throws {RequiredError}
     * @memberof MembersApiInterface
     */
    reactivateMemberRaw(requestParameters: ReactivateMemberRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<MemberResource>>;

    /**
     * Minimum role: owner. The member keeps the role they had and regains access on their very next request. A member who is already active cannot be reactivated.
     * Reactivate a suspended member
     */
    reactivateMember(requestParameters: ReactivateMemberRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<MemberResource>;

    /**
     * Creates request options for suspendMember without sending the request
     * @param {string} organizationId 
     * @param {string} memberId 
     * @param {string} xMencoroConfirmation 
     * @param {string} idempotencyKey 
     * @throws {RequiredError}
     * @memberof MembersApiInterface
     */
    suspendMemberRequestOpts(requestParameters: SuspendMemberRequest): Promise<runtime.RequestOpts>;

    /**
     * Minimum role: owner. The member loses access on their very next request, including through any API key they own that is scoped to this organization. The last active owner cannot be suspended.
     * @summary Suspend a member
     * @param {string} organizationId 
     * @param {string} memberId 
     * @param {string} xMencoroConfirmation 
     * @param {string} idempotencyKey 
     * @param {*} [options] Override http request option.
     * @throws {RequiredError}
     * @memberof MembersApiInterface
     */
    suspendMemberRaw(requestParameters: SuspendMemberRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<MemberResource>>;

    /**
     * Minimum role: owner. The member loses access on their very next request, including through any API key they own that is scoped to this organization. The last active owner cannot be suspended.
     * Suspend a member
     */
    suspendMember(requestParameters: SuspendMemberRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<MemberResource>;

}

/**
 * 
 */
export class MembersApi extends runtime.BaseAPI implements MembersApiInterface {

    /**
     * Creates request options for changeMemberRole without sending the request
     */
    async changeMemberRoleRequestOpts(requestParameters: ChangeMemberRoleOperationRequest): Promise<runtime.RequestOpts> {
        if (requestParameters['organizationId'] == null) {
            throw new runtime.RequiredError(
                'organizationId',
                'Required parameter "organizationId" was null or undefined when calling changeMemberRole().'
            );
        }

        if (requestParameters['memberId'] == null) {
            throw new runtime.RequiredError(
                'memberId',
                'Required parameter "memberId" was null or undefined when calling changeMemberRole().'
            );
        }

        if (requestParameters['xMencoroConfirmation'] == null) {
            throw new runtime.RequiredError(
                'xMencoroConfirmation',
                'Required parameter "xMencoroConfirmation" was null or undefined when calling changeMemberRole().'
            );
        }

        if (requestParameters['idempotencyKey'] == null) {
            throw new runtime.RequiredError(
                'idempotencyKey',
                'Required parameter "idempotencyKey" was null or undefined when calling changeMemberRole().'
            );
        }

        if (requestParameters['changeMemberRoleRequest'] == null) {
            throw new runtime.RequiredError(
                'changeMemberRoleRequest',
                'Required parameter "changeMemberRoleRequest" was null or undefined when calling changeMemberRole().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        headerParameters['Content-Type'] = 'application/json';

        if (requestParameters['xMencoroConfirmation'] != null) {
            headerParameters['X-Mencoro-Confirmation'] = String(requestParameters['xMencoroConfirmation']);
        }

        if (requestParameters['idempotencyKey'] != null) {
            headerParameters['Idempotency-Key'] = String(requestParameters['idempotencyKey']);
        }

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("ApiKey", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/api/v1/organizations/{organizationId}/members/{memberId}`;
        urlPath = urlPath.replace('{organizationId}', encodeURIComponent(String(requestParameters['organizationId'])));
        urlPath = urlPath.replace('{memberId}', encodeURIComponent(String(requestParameters['memberId'])));

        return {
            path: urlPath,
            method: 'PATCH',
            headers: headerParameters,
            query: queryParameters,
            body: ChangeMemberRoleRequestToJSON(requestParameters['changeMemberRoleRequest']),
        };
    }

    /**
     * Minimum role: owner. The role of a suspended member cannot be changed, and the last active owner cannot be demoted. Preview it first: the confirmation is bound to the number of active owners, so another owner being suspended in between invalidates it rather than stranding the organization.
     * Change a member role
     */
    async changeMemberRoleRaw(requestParameters: ChangeMemberRoleOperationRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<MemberResource>> {
        const requestOptions = await this.changeMemberRoleRequestOpts(requestParameters);
        const response = await this.request(requestOptions, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => MemberResourceFromJSON(jsonValue));
    }

    /**
     * Minimum role: owner. The role of a suspended member cannot be changed, and the last active owner cannot be demoted. Preview it first: the confirmation is bound to the number of active owners, so another owner being suspended in between invalidates it rather than stranding the organization.
     * Change a member role
     */
    async changeMemberRole(requestParameters: ChangeMemberRoleOperationRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<MemberResource> {
        const response = await this.changeMemberRoleRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Creates request options for getMember without sending the request
     */
    async getMemberRequestOpts(requestParameters: GetMemberRequest): Promise<runtime.RequestOpts> {
        if (requestParameters['organizationId'] == null) {
            throw new runtime.RequiredError(
                'organizationId',
                'Required parameter "organizationId" was null or undefined when calling getMember().'
            );
        }

        if (requestParameters['memberId'] == null) {
            throw new runtime.RequiredError(
                'memberId',
                'Required parameter "memberId" was null or undefined when calling getMember().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("ApiKey", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/api/v1/organizations/{organizationId}/members/{memberId}`;
        urlPath = urlPath.replace('{organizationId}', encodeURIComponent(String(requestParameters['organizationId'])));
        urlPath = urlPath.replace('{memberId}', encodeURIComponent(String(requestParameters['memberId'])));

        return {
            path: urlPath,
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        };
    }

    /**
     * Minimum role: owner — the same floor the members listing enforces, because a caller who can page the roster has already seen this record. Returns the facts of one membership: its role, whether it is active or suspended, and when it was joined. It does NOT describe the person behind it: no name, no email address, no phone number and no profile image, so a membership id can never be turned into a contact lookup. The membership is read from PostgreSQL, the same row at the same freshness the listing publishes. A membership belonging to another organization answers 404, exactly as an unknown or malformed id does, so the API never confirms that an inaccessible membership exists; the one 403 is a key without the read capability. The response carries no organizationId — it is the one in the path — and no creation timestamp; joinedAt is the membership fact. No query parameters are accepted.
     * Get one organization membership
     */
    async getMemberRaw(requestParameters: GetMemberRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<MemberResource>> {
        const requestOptions = await this.getMemberRequestOpts(requestParameters);
        const response = await this.request(requestOptions, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => MemberResourceFromJSON(jsonValue));
    }

    /**
     * Minimum role: owner — the same floor the members listing enforces, because a caller who can page the roster has already seen this record. Returns the facts of one membership: its role, whether it is active or suspended, and when it was joined. It does NOT describe the person behind it: no name, no email address, no phone number and no profile image, so a membership id can never be turned into a contact lookup. The membership is read from PostgreSQL, the same row at the same freshness the listing publishes. A membership belonging to another organization answers 404, exactly as an unknown or malformed id does, so the API never confirms that an inaccessible membership exists; the one 403 is a key without the read capability. The response carries no organizationId — it is the one in the path — and no creation timestamp; joinedAt is the membership fact. No query parameters are accepted.
     * Get one organization membership
     */
    async getMember(requestParameters: GetMemberRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<MemberResource> {
        const response = await this.getMemberRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Creates request options for listMembers without sending the request
     */
    async listMembersRequestOpts(requestParameters: ListMembersRequest): Promise<runtime.RequestOpts> {
        if (requestParameters['organizationId'] == null) {
            throw new runtime.RequiredError(
                'organizationId',
                'Required parameter "organizationId" was null or undefined when calling listMembers().'
            );
        }

        const queryParameters: any = {};

        if (requestParameters['limit'] != null) {
            queryParameters['limit'] = requestParameters['limit'];
        }

        if (requestParameters['offset'] != null) {
            queryParameters['offset'] = requestParameters['offset'];
        }

        if (requestParameters['status'] != null) {
            queryParameters['status'] = requestParameters['status'];
        }

        if (requestParameters['sortBy'] != null) {
            queryParameters['sortBy'] = requestParameters['sortBy'];
        }

        if (requestParameters['sortOrder'] != null) {
            queryParameters['sortOrder'] = requestParameters['sortOrder'];
        }

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("ApiKey", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/api/v1/organizations/{organizationId}/members`;
        urlPath = urlPath.replace('{organizationId}', encodeURIComponent(String(requestParameters['organizationId'])));

        return {
            path: urlPath,
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        };
    }

    /**
     * Minimum role: owner. The application exposes this roster twice and the two disagree — its member screen shows it to any viewer, while its non-BFF endpoint requires an owner — so the published API takes the stricter of the two and requires an owner. A membership describes the membership, not the person: names and email addresses are never returned here, and there is no search parameter, because both would turn the roster into a contact export. Omitting \"status\" returns active and suspended memberships alike. Sorting by \"role\" is alphabetical on the role name, not by seniority.Send `Accept: text/csv` to receive the same page as a CSV download instead of JSON: same filters, same authorization, same page window and the same maximum of 100 rows — it is this page in another format, not a bulk export, so a whole collection is still read by paging. The CSV carries no `total`, because a table whose every row is a record has nowhere to put one; read it from the JSON representation of the same request. Cells beginning with `=`, `+`, `-` or `@` are prefixed with an apostrophe so a spreadsheet treats them as text rather than running them as formulas.
     * List an organization\'s members
     */
    async listMembersRaw(requestParameters: ListMembersRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<ListMembers200Response>> {
        const requestOptions = await this.listMembersRequestOpts(requestParameters);
        const response = await this.request(requestOptions, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => ListMembers200ResponseFromJSON(jsonValue));
    }

    /**
     * Minimum role: owner. The application exposes this roster twice and the two disagree — its member screen shows it to any viewer, while its non-BFF endpoint requires an owner — so the published API takes the stricter of the two and requires an owner. A membership describes the membership, not the person: names and email addresses are never returned here, and there is no search parameter, because both would turn the roster into a contact export. Omitting \"status\" returns active and suspended memberships alike. Sorting by \"role\" is alphabetical on the role name, not by seniority.Send `Accept: text/csv` to receive the same page as a CSV download instead of JSON: same filters, same authorization, same page window and the same maximum of 100 rows — it is this page in another format, not a bulk export, so a whole collection is still read by paging. The CSV carries no `total`, because a table whose every row is a record has nowhere to put one; read it from the JSON representation of the same request. Cells beginning with `=`, `+`, `-` or `@` are prefixed with an apostrophe so a spreadsheet treats them as text rather than running them as formulas.
     * List an organization\'s members
     */
    async listMembers(requestParameters: ListMembersRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<ListMembers200Response> {
        const response = await this.listMembersRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Creates request options for reactivateMember without sending the request
     */
    async reactivateMemberRequestOpts(requestParameters: ReactivateMemberRequest): Promise<runtime.RequestOpts> {
        if (requestParameters['organizationId'] == null) {
            throw new runtime.RequiredError(
                'organizationId',
                'Required parameter "organizationId" was null or undefined when calling reactivateMember().'
            );
        }

        if (requestParameters['memberId'] == null) {
            throw new runtime.RequiredError(
                'memberId',
                'Required parameter "memberId" was null or undefined when calling reactivateMember().'
            );
        }

        if (requestParameters['xMencoroConfirmation'] == null) {
            throw new runtime.RequiredError(
                'xMencoroConfirmation',
                'Required parameter "xMencoroConfirmation" was null or undefined when calling reactivateMember().'
            );
        }

        if (requestParameters['idempotencyKey'] == null) {
            throw new runtime.RequiredError(
                'idempotencyKey',
                'Required parameter "idempotencyKey" was null or undefined when calling reactivateMember().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (requestParameters['xMencoroConfirmation'] != null) {
            headerParameters['X-Mencoro-Confirmation'] = String(requestParameters['xMencoroConfirmation']);
        }

        if (requestParameters['idempotencyKey'] != null) {
            headerParameters['Idempotency-Key'] = String(requestParameters['idempotencyKey']);
        }

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("ApiKey", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/api/v1/organizations/{organizationId}/members/{memberId}/reactivate`;
        urlPath = urlPath.replace('{organizationId}', encodeURIComponent(String(requestParameters['organizationId'])));
        urlPath = urlPath.replace('{memberId}', encodeURIComponent(String(requestParameters['memberId'])));

        return {
            path: urlPath,
            method: 'POST',
            headers: headerParameters,
            query: queryParameters,
        };
    }

    /**
     * Minimum role: owner. The member keeps the role they had and regains access on their very next request. A member who is already active cannot be reactivated.
     * Reactivate a suspended member
     */
    async reactivateMemberRaw(requestParameters: ReactivateMemberRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<MemberResource>> {
        const requestOptions = await this.reactivateMemberRequestOpts(requestParameters);
        const response = await this.request(requestOptions, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => MemberResourceFromJSON(jsonValue));
    }

    /**
     * Minimum role: owner. The member keeps the role they had and regains access on their very next request. A member who is already active cannot be reactivated.
     * Reactivate a suspended member
     */
    async reactivateMember(requestParameters: ReactivateMemberRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<MemberResource> {
        const response = await this.reactivateMemberRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Creates request options for suspendMember without sending the request
     */
    async suspendMemberRequestOpts(requestParameters: SuspendMemberRequest): Promise<runtime.RequestOpts> {
        if (requestParameters['organizationId'] == null) {
            throw new runtime.RequiredError(
                'organizationId',
                'Required parameter "organizationId" was null or undefined when calling suspendMember().'
            );
        }

        if (requestParameters['memberId'] == null) {
            throw new runtime.RequiredError(
                'memberId',
                'Required parameter "memberId" was null or undefined when calling suspendMember().'
            );
        }

        if (requestParameters['xMencoroConfirmation'] == null) {
            throw new runtime.RequiredError(
                'xMencoroConfirmation',
                'Required parameter "xMencoroConfirmation" was null or undefined when calling suspendMember().'
            );
        }

        if (requestParameters['idempotencyKey'] == null) {
            throw new runtime.RequiredError(
                'idempotencyKey',
                'Required parameter "idempotencyKey" was null or undefined when calling suspendMember().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (requestParameters['xMencoroConfirmation'] != null) {
            headerParameters['X-Mencoro-Confirmation'] = String(requestParameters['xMencoroConfirmation']);
        }

        if (requestParameters['idempotencyKey'] != null) {
            headerParameters['Idempotency-Key'] = String(requestParameters['idempotencyKey']);
        }

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("ApiKey", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/api/v1/organizations/{organizationId}/members/{memberId}/suspend`;
        urlPath = urlPath.replace('{organizationId}', encodeURIComponent(String(requestParameters['organizationId'])));
        urlPath = urlPath.replace('{memberId}', encodeURIComponent(String(requestParameters['memberId'])));

        return {
            path: urlPath,
            method: 'POST',
            headers: headerParameters,
            query: queryParameters,
        };
    }

    /**
     * Minimum role: owner. The member loses access on their very next request, including through any API key they own that is scoped to this organization. The last active owner cannot be suspended.
     * Suspend a member
     */
    async suspendMemberRaw(requestParameters: SuspendMemberRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<MemberResource>> {
        const requestOptions = await this.suspendMemberRequestOpts(requestParameters);
        const response = await this.request(requestOptions, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => MemberResourceFromJSON(jsonValue));
    }

    /**
     * Minimum role: owner. The member loses access on their very next request, including through any API key they own that is scoped to this organization. The last active owner cannot be suspended.
     * Suspend a member
     */
    async suspendMember(requestParameters: SuspendMemberRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<MemberResource> {
        const response = await this.suspendMemberRaw(requestParameters, initOverrides);
        return await response.value();
    }

}

/**
 * @export
 */
export const ListMembersStatusEnum = {
    Active: 'active',
    Suspended: 'suspended'
} as const;
export type ListMembersStatusEnum = typeof ListMembersStatusEnum[keyof typeof ListMembersStatusEnum];
/**
 * @export
 */
export const ListMembersSortByEnum = {
    JoinedAt: 'joinedAt',
    Role: 'role'
} as const;
export type ListMembersSortByEnum = typeof ListMembersSortByEnum[keyof typeof ListMembersSortByEnum];
/**
 * @export
 */
export const ListMembersSortOrderEnum = {
    Asc: 'asc',
    Desc: 'desc'
} as const;
export type ListMembersSortOrderEnum = typeof ListMembersSortOrderEnum[keyof typeof ListMembersSortOrderEnum];

/* tslint:disable */
/* eslint-disable */
/**
 * Mencoro API
 * Public API for Mencoro customers. Authenticate with an API key: `Authorization: Bearer mencoro_sk_...`.
 *
 * The version of the OpenAPI document: 1.0.0
 * Contact: support@mencoro.com
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */

import * as runtime from '../runtime';
import {
    type CreateInvitation200Response,
    CreateInvitation200ResponseFromJSON,
    CreateInvitation200ResponseToJSON,
} from '../models/CreateInvitation200Response';
import {
    type CreateInvitation201Response,
    CreateInvitation201ResponseFromJSON,
    CreateInvitation201ResponseToJSON,
} from '../models/CreateInvitation201Response';
import {
    type CreateInvitationRequest,
    CreateInvitationRequestFromJSON,
    CreateInvitationRequestToJSON,
} from '../models/CreateInvitationRequest';
import {
    type InvitationResource,
    InvitationResourceFromJSON,
    InvitationResourceToJSON,
} from '../models/InvitationResource';
import {
    type ListInvitations200Response,
    ListInvitations200ResponseFromJSON,
    ListInvitations200ResponseToJSON,
} from '../models/ListInvitations200Response';

export interface CancelInvitationRequest {
    organizationId: string;
    invitationId: string;
    xMencoroConfirmation: string;
    idempotencyKey: string;
}

export interface CreateInvitationOperationRequest {
    organizationId: string;
    xMencoroConfirmation: string;
    idempotencyKey: string;
    createInvitationRequest: CreateInvitationRequest;
}

export interface ListInvitationsRequest {
    organizationId: string;
    limit?: number;
    offset?: number;
    search?: string;
    status?: ListInvitationsStatusEnum;
    sortBy?: ListInvitationsSortByEnum;
    sortOrder?: ListInvitationsSortOrderEnum;
}

/**
 * InvitationsApi - interface
 * 
 * @export
 * @interface InvitationsApiInterface
 */
export interface InvitationsApiInterface {
    /**
     * Creates request options for cancelInvitation without sending the request
     * @param {string} organizationId 
     * @param {string} invitationId 
     * @param {string} xMencoroConfirmation 
     * @param {string} idempotencyKey 
     * @throws {RequiredError}
     * @memberof InvitationsApiInterface
     */
    cancelInvitationRequestOpts(requestParameters: CancelInvitationRequest): Promise<runtime.RequestOpts>;

    /**
     * Minimum role: owner. The link already emailed to the invitee stops working. Only a pending invitation can be cancelled; one that was accepted, rejected or has expired is refused.
     * @summary Cancel a pending invitation
     * @param {string} organizationId 
     * @param {string} invitationId 
     * @param {string} xMencoroConfirmation 
     * @param {string} idempotencyKey 
     * @param {*} [options] Override http request option.
     * @throws {RequiredError}
     * @memberof InvitationsApiInterface
     */
    cancelInvitationRaw(requestParameters: CancelInvitationRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<InvitationResource>>;

    /**
     * Minimum role: owner. The link already emailed to the invitee stops working. Only a pending invitation can be cancelled; one that was accepted, rejected or has expired is refused.
     * Cancel a pending invitation
     */
    cancelInvitation(requestParameters: CancelInvitationRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<InvitationResource>;

    /**
     * Creates request options for createInvitation without sending the request
     * @param {string} organizationId 
     * @param {string} xMencoroConfirmation 
     * @param {string} idempotencyKey 
     * @param {CreateInvitationRequest} createInvitationRequest 
     * @throws {RequiredError}
     * @memberof InvitationsApiInterface
     */
    createInvitationRequestOpts(requestParameters: CreateInvitationOperationRequest): Promise<runtime.RequestOpts>;

    /**
     * Minimum role: owner. Sends an invitation email that expires after 14 days. If the address already belongs to an active member the call succeeds and creates nothing - answered as 200 with created=false rather than 201, so a client can tell the two apart. Preview it first; the preview says which of the two will happen.
     * @summary Invite somebody to an organization
     * @param {string} organizationId 
     * @param {string} xMencoroConfirmation 
     * @param {string} idempotencyKey 
     * @param {CreateInvitationRequest} createInvitationRequest 
     * @param {*} [options] Override http request option.
     * @throws {RequiredError}
     * @memberof InvitationsApiInterface
     */
    createInvitationRaw(requestParameters: CreateInvitationOperationRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<CreateInvitation200Response>>;

    /**
     * Minimum role: owner. Sends an invitation email that expires after 14 days. If the address already belongs to an active member the call succeeds and creates nothing - answered as 200 with created=false rather than 201, so a client can tell the two apart. Preview it first; the preview says which of the two will happen.
     * Invite somebody to an organization
     */
    createInvitation(requestParameters: CreateInvitationOperationRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<CreateInvitation200Response>;

    /**
     * Creates request options for listInvitations without sending the request
     * @param {string} organizationId 
     * @param {number} [limit] 
     * @param {number} [offset] 
     * @param {string} [search] Matches part of the invited email address.
     * @param {'pending' | 'accepted' | 'rejected' | 'expired' | 'cancelled'} [status] Absent means every state.
     * @param {'createdAt' | 'expiresAt'} [sortBy] 
     * @param {'asc' | 'desc'} [sortOrder] 
     * @throws {RequiredError}
     * @memberof InvitationsApiInterface
     */
    listInvitationsRequestOpts(requestParameters: ListInvitationsRequest): Promise<runtime.RequestOpts>;

    /**
     * Minimum role: owner. Returns the invitations issued for this organization, in every state. The invitation token is never returned. `status` matches the stored state, so an invitation that has passed its `expiresAt` is still listed as pending until it is transitioned; compare `expiresAt` to decide. Rows are ordered by the chosen sort field, and invitations sharing the same value keep no guaranteed relative order across pages.Send `Accept: text/csv` to receive the same page as a CSV download instead of JSON: same filters, same authorization, same page window and the same maximum of 100 rows — it is this page in another format, not a bulk export, so a whole collection is still read by paging. The CSV carries no `total`, because a table whose every row is a record has nowhere to put one; read it from the JSON representation of the same request. Cells beginning with `=`, `+`, `-` or `@` are prefixed with an apostrophe so a spreadsheet treats them as text rather than running them as formulas.
     * @summary List an organization\'s invitations
     * @param {string} organizationId 
     * @param {number} [limit] 
     * @param {number} [offset] 
     * @param {string} [search] Matches part of the invited email address.
     * @param {'pending' | 'accepted' | 'rejected' | 'expired' | 'cancelled'} [status] Absent means every state.
     * @param {'createdAt' | 'expiresAt'} [sortBy] 
     * @param {'asc' | 'desc'} [sortOrder] 
     * @param {*} [options] Override http request option.
     * @throws {RequiredError}
     * @memberof InvitationsApiInterface
     */
    listInvitationsRaw(requestParameters: ListInvitationsRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<ListInvitations200Response>>;

    /**
     * Minimum role: owner. Returns the invitations issued for this organization, in every state. The invitation token is never returned. `status` matches the stored state, so an invitation that has passed its `expiresAt` is still listed as pending until it is transitioned; compare `expiresAt` to decide. Rows are ordered by the chosen sort field, and invitations sharing the same value keep no guaranteed relative order across pages.Send `Accept: text/csv` to receive the same page as a CSV download instead of JSON: same filters, same authorization, same page window and the same maximum of 100 rows — it is this page in another format, not a bulk export, so a whole collection is still read by paging. The CSV carries no `total`, because a table whose every row is a record has nowhere to put one; read it from the JSON representation of the same request. Cells beginning with `=`, `+`, `-` or `@` are prefixed with an apostrophe so a spreadsheet treats them as text rather than running them as formulas.
     * List an organization\'s invitations
     */
    listInvitations(requestParameters: ListInvitationsRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<ListInvitations200Response>;

}

/**
 * 
 */
export class InvitationsApi extends runtime.BaseAPI implements InvitationsApiInterface {

    /**
     * Creates request options for cancelInvitation without sending the request
     */
    async cancelInvitationRequestOpts(requestParameters: CancelInvitationRequest): Promise<runtime.RequestOpts> {
        if (requestParameters['organizationId'] == null) {
            throw new runtime.RequiredError(
                'organizationId',
                'Required parameter "organizationId" was null or undefined when calling cancelInvitation().'
            );
        }

        if (requestParameters['invitationId'] == null) {
            throw new runtime.RequiredError(
                'invitationId',
                'Required parameter "invitationId" was null or undefined when calling cancelInvitation().'
            );
        }

        if (requestParameters['xMencoroConfirmation'] == null) {
            throw new runtime.RequiredError(
                'xMencoroConfirmation',
                'Required parameter "xMencoroConfirmation" was null or undefined when calling cancelInvitation().'
            );
        }

        if (requestParameters['idempotencyKey'] == null) {
            throw new runtime.RequiredError(
                'idempotencyKey',
                'Required parameter "idempotencyKey" was null or undefined when calling cancelInvitation().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (requestParameters['xMencoroConfirmation'] != null) {
            headerParameters['X-Mencoro-Confirmation'] = String(requestParameters['xMencoroConfirmation']);
        }

        if (requestParameters['idempotencyKey'] != null) {
            headerParameters['Idempotency-Key'] = String(requestParameters['idempotencyKey']);
        }

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("ApiKey", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/api/v1/organizations/{organizationId}/invitations/{invitationId}/cancel`;
        urlPath = urlPath.replace('{organizationId}', encodeURIComponent(String(requestParameters['organizationId'])));
        urlPath = urlPath.replace('{invitationId}', encodeURIComponent(String(requestParameters['invitationId'])));

        return {
            path: urlPath,
            method: 'POST',
            headers: headerParameters,
            query: queryParameters,
        };
    }

    /**
     * Minimum role: owner. The link already emailed to the invitee stops working. Only a pending invitation can be cancelled; one that was accepted, rejected or has expired is refused.
     * Cancel a pending invitation
     */
    async cancelInvitationRaw(requestParameters: CancelInvitationRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<InvitationResource>> {
        const requestOptions = await this.cancelInvitationRequestOpts(requestParameters);
        const response = await this.request(requestOptions, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => InvitationResourceFromJSON(jsonValue));
    }

    /**
     * Minimum role: owner. The link already emailed to the invitee stops working. Only a pending invitation can be cancelled; one that was accepted, rejected or has expired is refused.
     * Cancel a pending invitation
     */
    async cancelInvitation(requestParameters: CancelInvitationRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<InvitationResource> {
        const response = await this.cancelInvitationRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Creates request options for createInvitation without sending the request
     */
    async createInvitationRequestOpts(requestParameters: CreateInvitationOperationRequest): Promise<runtime.RequestOpts> {
        if (requestParameters['organizationId'] == null) {
            throw new runtime.RequiredError(
                'organizationId',
                'Required parameter "organizationId" was null or undefined when calling createInvitation().'
            );
        }

        if (requestParameters['xMencoroConfirmation'] == null) {
            throw new runtime.RequiredError(
                'xMencoroConfirmation',
                'Required parameter "xMencoroConfirmation" was null or undefined when calling createInvitation().'
            );
        }

        if (requestParameters['idempotencyKey'] == null) {
            throw new runtime.RequiredError(
                'idempotencyKey',
                'Required parameter "idempotencyKey" was null or undefined when calling createInvitation().'
            );
        }

        if (requestParameters['createInvitationRequest'] == null) {
            throw new runtime.RequiredError(
                'createInvitationRequest',
                'Required parameter "createInvitationRequest" was null or undefined when calling createInvitation().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        headerParameters['Content-Type'] = 'application/json';

        if (requestParameters['xMencoroConfirmation'] != null) {
            headerParameters['X-Mencoro-Confirmation'] = String(requestParameters['xMencoroConfirmation']);
        }

        if (requestParameters['idempotencyKey'] != null) {
            headerParameters['Idempotency-Key'] = String(requestParameters['idempotencyKey']);
        }

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("ApiKey", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/api/v1/organizations/{organizationId}/invitations`;
        urlPath = urlPath.replace('{organizationId}', encodeURIComponent(String(requestParameters['organizationId'])));

        return {
            path: urlPath,
            method: 'POST',
            headers: headerParameters,
            query: queryParameters,
            body: CreateInvitationRequestToJSON(requestParameters['createInvitationRequest']),
        };
    }

    /**
     * Minimum role: owner. Sends an invitation email that expires after 14 days. If the address already belongs to an active member the call succeeds and creates nothing - answered as 200 with created=false rather than 201, so a client can tell the two apart. Preview it first; the preview says which of the two will happen.
     * Invite somebody to an organization
     */
    async createInvitationRaw(requestParameters: CreateInvitationOperationRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<CreateInvitation200Response>> {
        const requestOptions = await this.createInvitationRequestOpts(requestParameters);
        const response = await this.request(requestOptions, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => CreateInvitation200ResponseFromJSON(jsonValue));
    }

    /**
     * Minimum role: owner. Sends an invitation email that expires after 14 days. If the address already belongs to an active member the call succeeds and creates nothing - answered as 200 with created=false rather than 201, so a client can tell the two apart. Preview it first; the preview says which of the two will happen.
     * Invite somebody to an organization
     */
    async createInvitation(requestParameters: CreateInvitationOperationRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<CreateInvitation200Response> {
        const response = await this.createInvitationRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Creates request options for listInvitations without sending the request
     */
    async listInvitationsRequestOpts(requestParameters: ListInvitationsRequest): Promise<runtime.RequestOpts> {
        if (requestParameters['organizationId'] == null) {
            throw new runtime.RequiredError(
                'organizationId',
                'Required parameter "organizationId" was null or undefined when calling listInvitations().'
            );
        }

        const queryParameters: any = {};

        if (requestParameters['limit'] != null) {
            queryParameters['limit'] = requestParameters['limit'];
        }

        if (requestParameters['offset'] != null) {
            queryParameters['offset'] = requestParameters['offset'];
        }

        if (requestParameters['search'] != null) {
            queryParameters['search'] = requestParameters['search'];
        }

        if (requestParameters['status'] != null) {
            queryParameters['status'] = requestParameters['status'];
        }

        if (requestParameters['sortBy'] != null) {
            queryParameters['sortBy'] = requestParameters['sortBy'];
        }

        if (requestParameters['sortOrder'] != null) {
            queryParameters['sortOrder'] = requestParameters['sortOrder'];
        }

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("ApiKey", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/api/v1/organizations/{organizationId}/invitations`;
        urlPath = urlPath.replace('{organizationId}', encodeURIComponent(String(requestParameters['organizationId'])));

        return {
            path: urlPath,
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        };
    }

    /**
     * Minimum role: owner. Returns the invitations issued for this organization, in every state. The invitation token is never returned. `status` matches the stored state, so an invitation that has passed its `expiresAt` is still listed as pending until it is transitioned; compare `expiresAt` to decide. Rows are ordered by the chosen sort field, and invitations sharing the same value keep no guaranteed relative order across pages.Send `Accept: text/csv` to receive the same page as a CSV download instead of JSON: same filters, same authorization, same page window and the same maximum of 100 rows — it is this page in another format, not a bulk export, so a whole collection is still read by paging. The CSV carries no `total`, because a table whose every row is a record has nowhere to put one; read it from the JSON representation of the same request. Cells beginning with `=`, `+`, `-` or `@` are prefixed with an apostrophe so a spreadsheet treats them as text rather than running them as formulas.
     * List an organization\'s invitations
     */
    async listInvitationsRaw(requestParameters: ListInvitationsRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<ListInvitations200Response>> {
        const requestOptions = await this.listInvitationsRequestOpts(requestParameters);
        const response = await this.request(requestOptions, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => ListInvitations200ResponseFromJSON(jsonValue));
    }

    /**
     * Minimum role: owner. Returns the invitations issued for this organization, in every state. The invitation token is never returned. `status` matches the stored state, so an invitation that has passed its `expiresAt` is still listed as pending until it is transitioned; compare `expiresAt` to decide. Rows are ordered by the chosen sort field, and invitations sharing the same value keep no guaranteed relative order across pages.Send `Accept: text/csv` to receive the same page as a CSV download instead of JSON: same filters, same authorization, same page window and the same maximum of 100 rows — it is this page in another format, not a bulk export, so a whole collection is still read by paging. The CSV carries no `total`, because a table whose every row is a record has nowhere to put one; read it from the JSON representation of the same request. Cells beginning with `=`, `+`, `-` or `@` are prefixed with an apostrophe so a spreadsheet treats them as text rather than running them as formulas.
     * List an organization\'s invitations
     */
    async listInvitations(requestParameters: ListInvitationsRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<ListInvitations200Response> {
        const response = await this.listInvitationsRaw(requestParameters, initOverrides);
        return await response.value();
    }

}

/**
 * @export
 */
export const ListInvitationsStatusEnum = {
    Pending: 'pending',
    Accepted: 'accepted',
    Rejected: 'rejected',
    Expired: 'expired',
    Cancelled: 'cancelled'
} as const;
export type ListInvitationsStatusEnum = typeof ListInvitationsStatusEnum[keyof typeof ListInvitationsStatusEnum];
/**
 * @export
 */
export const ListInvitationsSortByEnum = {
    CreatedAt: 'createdAt',
    ExpiresAt: 'expiresAt'
} as const;
export type ListInvitationsSortByEnum = typeof ListInvitationsSortByEnum[keyof typeof ListInvitationsSortByEnum];
/**
 * @export
 */
export const ListInvitationsSortOrderEnum = {
    Asc: 'asc',
    Desc: 'desc'
} as const;
export type ListInvitationsSortOrderEnum = typeof ListInvitationsSortOrderEnum[keyof typeof ListInvitationsSortOrderEnum];

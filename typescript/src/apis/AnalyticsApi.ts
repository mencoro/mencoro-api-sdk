/* tslint:disable */
/* eslint-disable */
/**
 * Mencoro API
 * Public API for Mencoro customers. Authenticate with an API key: `Authorization: Bearer mencoro_sk_...`.
 *
 * The version of the OpenAPI document: 1.0.0
 * Contact: support@mencoro.com
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */

import * as runtime from '../runtime';
import {
    type CitedSourcesResponse,
    CitedSourcesResponseFromJSON,
    CitedSourcesResponseToJSON,
} from '../models/CitedSourcesResponse';
import {
    type CompetitorCoOccurrenceResponse,
    CompetitorCoOccurrenceResponseFromJSON,
    CompetitorCoOccurrenceResponseToJSON,
} from '../models/CompetitorCoOccurrenceResponse';
import {
    type GetAvailableFilters200Response,
    GetAvailableFilters200ResponseFromJSON,
    GetAvailableFilters200ResponseToJSON,
} from '../models/GetAvailableFilters200Response';
import {
    type GetMentionSamples400Response,
    GetMentionSamples400ResponseFromJSON,
    GetMentionSamples400ResponseToJSON,
} from '../models/GetMentionSamples400Response';
import {
    type GetMetricGlossary200Response,
    GetMetricGlossary200ResponseFromJSON,
    GetMetricGlossary200ResponseToJSON,
} from '../models/GetMetricGlossary200Response';
import {
    type GetOrganizationOverview200Response,
    GetOrganizationOverview200ResponseFromJSON,
    GetOrganizationOverview200ResponseToJSON,
} from '../models/GetOrganizationOverview200Response';
import {
    type GetShareOfVoiceFormula200Response,
    GetShareOfVoiceFormula200ResponseFromJSON,
    GetShareOfVoiceFormula200ResponseToJSON,
} from '../models/GetShareOfVoiceFormula200Response';
import {
    type GetTrackingCoverage200Response,
    GetTrackingCoverage200ResponseFromJSON,
    GetTrackingCoverage200ResponseToJSON,
} from '../models/GetTrackingCoverage200Response';
import {
    type ListKeywordListings200Response,
    ListKeywordListings200ResponseFromJSON,
    ListKeywordListings200ResponseToJSON,
} from '../models/ListKeywordListings200Response';
import {
    type ProjectMentionMixResponse,
    ProjectMentionMixResponseFromJSON,
    ProjectMentionMixResponseToJSON,
} from '../models/ProjectMentionMixResponse';
import {
    type ProjectMentionSamplesResponse,
    ProjectMentionSamplesResponseFromJSON,
    ProjectMentionSamplesResponseToJSON,
} from '../models/ProjectMentionSamplesResponse';
import {
    type ProjectRankTrackingClusterBreakdown,
    ProjectRankTrackingClusterBreakdownFromJSON,
    ProjectRankTrackingClusterBreakdownToJSON,
} from '../models/ProjectRankTrackingClusterBreakdown';
import {
    type ProjectRankTrackingStats,
    ProjectRankTrackingStatsFromJSON,
    ProjectRankTrackingStatsToJSON,
} from '../models/ProjectRankTrackingStats';
import {
    type ProjectRankTrackingTimeSeries,
    ProjectRankTrackingTimeSeriesFromJSON,
    ProjectRankTrackingTimeSeriesToJSON,
} from '../models/ProjectRankTrackingTimeSeries';
import {
    type ProjectSentimentBreakdown,
    ProjectSentimentBreakdownFromJSON,
    ProjectSentimentBreakdownToJSON,
} from '../models/ProjectSentimentBreakdown';
import {
    type TrackedQueryMoversResponse,
    TrackedQueryMoversResponseFromJSON,
    TrackedQueryMoversResponseToJSON,
} from '../models/TrackedQueryMoversResponse';
import {
    type TrackedQueryRankTrackingTimeSeries,
    TrackedQueryRankTrackingTimeSeriesFromJSON,
    TrackedQueryRankTrackingTimeSeriesToJSON,
} from '../models/TrackedQueryRankTrackingTimeSeries';

export interface GetAvailableFiltersRequest {
    organizationId: string;
    projectId: string;
}

export interface GetCitedSourcesRequest {
    organizationId: string;
    projectId: string;
    dateFrom: Date;
    dateTo: Date;
    engines?: Array<GetCitedSourcesEnginesEnum>;
    groupBy?: GetCitedSourcesGroupByEnum;
    limit?: number;
    offset?: number;
}

export interface GetClusterBreakdownRequest {
    organizationId: string;
    projectId: string;
    dateFrom: Date;
    dateTo: Date;
    engines?: Array<GetClusterBreakdownEnginesEnum>;
    countries?: Array<string>;
    queryClusterIds?: Array<string>;
    includeUngroupedQueries?: boolean;
}

export interface GetCompetitorCoOccurrenceRequest {
    organizationId: string;
    projectId: string;
    dateFrom: Date;
    dateTo: Date;
    engines?: Array<GetCompetitorCoOccurrenceEnginesEnum>;
    countries?: Array<string>;
    competitorId?: string;
}

export interface GetMentionMixRequest {
    organizationId: string;
    projectId: string;
    dateFrom: Date;
    dateTo: Date;
    engines?: Array<GetMentionMixEnginesEnum>;
    countries?: Array<string>;
}

export interface GetMentionSamplesRequest {
    organizationId: string;
    projectId: string;
    dateFrom: Date;
    dateTo: Date;
    engines?: Array<GetMentionSamplesEnginesEnum>;
    countries?: Array<string>;
    sentiment?: GetMentionSamplesSentimentEnum;
    mentionType?: GetMentionSamplesMentionTypeEnum;
    competitorId?: string;
    sortBy?: GetMentionSamplesSortByEnum;
    limit?: number;
    offset?: number;
}

export interface GetOrganizationOverviewRequest {
    organizationId: string;
}

export interface GetProjectMetricsRequest {
    organizationId: string;
    projectId: string;
    dateFrom: Date;
    dateTo: Date;
    engines?: Array<GetProjectMetricsEnginesEnum>;
    countries?: Array<string>;
    queryClusterIds?: Array<string>;
    includeUngroupedQueries?: boolean;
}

export interface GetProjectSentimentRequest {
    organizationId: string;
    projectId: string;
    dateFrom: Date;
    dateTo: Date;
    engines?: Array<GetProjectSentimentEnginesEnum>;
    countries?: Array<string>;
    queryClusterIds?: Array<string>;
    includeUngroupedQueries?: boolean;
}

export interface GetProjectTimeSeriesRequest {
    organizationId: string;
    projectId: string;
    dateFrom: Date;
    dateTo: Date;
    granularity?: GetProjectTimeSeriesGranularityEnum;
    engines?: Array<GetProjectTimeSeriesEnginesEnum>;
    countries?: Array<string>;
    queryClusterIds?: Array<string>;
    includeUngroupedQueries?: boolean;
    competitorIds?: Array<string>;
}

export interface GetQueryMoversRequest {
    organizationId: string;
    projectId: string;
    dateFrom: Date;
    dateTo: Date;
    engines?: Array<GetQueryMoversEnginesEnum>;
    countries?: Array<string>;
    sortBy?: GetQueryMoversSortByEnum;
    sortOrder?: GetQueryMoversSortOrderEnum;
    limit?: number;
    offset?: number;
}

export interface GetShareOfVoiceFormulaRequest {
    organizationId: string;
    projectId: string;
}

export interface GetTrackedQueryTimeSeriesRequest {
    organizationId: string;
    projectId: string;
    trackedQueryId: string;
    dateFrom: Date;
    dateTo: Date;
    granularity?: GetTrackedQueryTimeSeriesGranularityEnum;
    competitorIds?: Array<string>;
}

export interface GetTrackingCoverageRequest {
    organizationId: string;
    projectId: string;
}

export interface ListKeywordListingsRequest {
    organizationId: string;
    projectId: string;
    dateFrom: Date;
    dateTo: Date;
    engines?: Array<ListKeywordListingsEnginesEnum>;
    countries?: Array<string>;
    queryClusterIds?: Array<string>;
    includeUngroupedQueries?: boolean;
    status?: ListKeywordListingsStatusEnum;
    checkFrequencies?: Array<ListKeywordListingsCheckFrequenciesEnum>;
    nPasses?: Array<number>;
    search?: string;
    sortBy?: ListKeywordListingsSortByEnum;
    sortOrder?: ListKeywordListingsSortOrderEnum;
    limit?: number;
    offset?: number;
}

/**
 * AnalyticsApi - interface
 * 
 * @export
 * @interface AnalyticsApiInterface
 */
export interface AnalyticsApiInterface {
    /**
     * Creates request options for getAvailableFilters without sending the request
     * @param {string} organizationId 
     * @param {string} projectId 
     * @throws {RequiredError}
     * @memberof AnalyticsApiInterface
     */
    getAvailableFiltersRequestOpts(requestParameters: GetAvailableFiltersRequest): Promise<runtime.RequestOpts>;

    /**
     * Minimum role: viewer. Call this first: it is where every other analytics operation sends you for the valid engines, countries and keyword clusters of a project, and the values it returns are the exact strings the engines, countries and queryClusterIds parameters accept — anything else is rejected as a 400. Engines are engine codes, countries are ISO-3166 alpha-2 codes, and clusters are {id, name} objects whose id goes in queryClusterIds. Takes no date window: it describes how the project is configured right now, so a value is listed as soon as a tracked query uses it, even when no response has been captured for it yet. An empty list therefore means nothing is configured for it, not that no data was collected. Competitor ids are not part of this response.
     * @summary Filter values a project is configured for
     * @param {string} organizationId 
     * @param {string} projectId 
     * @param {*} [options] Override http request option.
     * @throws {RequiredError}
     * @memberof AnalyticsApiInterface
     */
    getAvailableFiltersRaw(requestParameters: GetAvailableFiltersRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<GetAvailableFilters200Response>>;

    /**
     * Minimum role: viewer. Call this first: it is where every other analytics operation sends you for the valid engines, countries and keyword clusters of a project, and the values it returns are the exact strings the engines, countries and queryClusterIds parameters accept — anything else is rejected as a 400. Engines are engine codes, countries are ISO-3166 alpha-2 codes, and clusters are {id, name} objects whose id goes in queryClusterIds. Takes no date window: it describes how the project is configured right now, so a value is listed as soon as a tracked query uses it, even when no response has been captured for it yet. An empty list therefore means nothing is configured for it, not that no data was collected. Competitor ids are not part of this response.
     * Filter values a project is configured for
     */
    getAvailableFilters(requestParameters: GetAvailableFiltersRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<GetAvailableFilters200Response>;

    /**
     * Creates request options for getCitedSources without sending the request
     * @param {string} organizationId 
     * @param {string} projectId 
     * @param {Date} dateFrom Inclusive start of the window, Y-m-d. Must fall inside the data retention window.
     * @param {Date} dateTo Inclusive end of the window, Y-m-d.
     * @param {Array<'chatgpt' | 'perplexity' | 'google_ai_overview' | 'google_ai_mode' | 'google_serp' | 'google_shopping'>} [engines] Repeatable, or comma-separated. Only the AI engines carry citations.
     * @param {'domain' | 'page'} [groupBy] Grain of the roll-up: \&quot;domain\&quot; by host, \&quot;page\&quot; by exact URL.
     * @param {number} [limit] Page size. A larger value is rejected, never silently reduced.
     * @param {number} [offset] Number of sources to skip.
     * @throws {RequiredError}
     * @memberof AnalyticsApiInterface
     */
    getCitedSourcesRequestOpts(requestParameters: GetCitedSourcesRequest): Promise<runtime.RequestOpts>;

    /**
     * Minimum role: viewer. The sources the answer engines drew on across a project\'s AI answers over a date window, ranked by how often they were cited. Per source: citationCount, the total number of citations; distinctResponseCount and distinctQueryCount, how many captured answers and tracked queries it appeared in; avgPosition, its average 1-based rank inside the answers\' citation lists, where LOWER is better. A null avgPosition means no citation in the window carried a position, not a rank of zero; a null domain or sampleTitle means the citation never carried one. `total` counts the distinct sources matching the window, before paging. The list is UNFILTERED by ownership: the brand\'s, competitors\' and third-party sources sit in the same ranking. Only the AI answer engines (chatgpt, perplexity, google_ai_overview, google_ai_mode) produce citations, so filtering by google_serp or google_shopping is accepted and returns nothing. Citations still behind an answer engine\'s redirect (a google.com/goto link, whose URL names the engine rather than the source) are excluded, so a source cited only through such links is absent from this list rather than counted as zero.
     * @summary Domains and pages the AI answers cited
     * @param {string} organizationId 
     * @param {string} projectId 
     * @param {Date} dateFrom Inclusive start of the window, Y-m-d. Must fall inside the data retention window.
     * @param {Date} dateTo Inclusive end of the window, Y-m-d.
     * @param {Array<'chatgpt' | 'perplexity' | 'google_ai_overview' | 'google_ai_mode' | 'google_serp' | 'google_shopping'>} [engines] Repeatable, or comma-separated. Only the AI engines carry citations.
     * @param {'domain' | 'page'} [groupBy] Grain of the roll-up: \&quot;domain\&quot; by host, \&quot;page\&quot; by exact URL.
     * @param {number} [limit] Page size. A larger value is rejected, never silently reduced.
     * @param {number} [offset] Number of sources to skip.
     * @param {*} [options] Override http request option.
     * @throws {RequiredError}
     * @memberof AnalyticsApiInterface
     */
    getCitedSourcesRaw(requestParameters: GetCitedSourcesRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<CitedSourcesResponse>>;

    /**
     * Minimum role: viewer. The sources the answer engines drew on across a project\'s AI answers over a date window, ranked by how often they were cited. Per source: citationCount, the total number of citations; distinctResponseCount and distinctQueryCount, how many captured answers and tracked queries it appeared in; avgPosition, its average 1-based rank inside the answers\' citation lists, where LOWER is better. A null avgPosition means no citation in the window carried a position, not a rank of zero; a null domain or sampleTitle means the citation never carried one. `total` counts the distinct sources matching the window, before paging. The list is UNFILTERED by ownership: the brand\'s, competitors\' and third-party sources sit in the same ranking. Only the AI answer engines (chatgpt, perplexity, google_ai_overview, google_ai_mode) produce citations, so filtering by google_serp or google_shopping is accepted and returns nothing. Citations still behind an answer engine\'s redirect (a google.com/goto link, whose URL names the engine rather than the source) are excluded, so a source cited only through such links is absent from this list rather than counted as zero.
     * Domains and pages the AI answers cited
     */
    getCitedSources(requestParameters: GetCitedSourcesRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<CitedSourcesResponse>;

    /**
     * Creates request options for getClusterBreakdown without sending the request
     * @param {string} organizationId 
     * @param {string} projectId 
     * @param {Date} dateFrom Inclusive start of the window, Y-m-d. Must fall inside the data retention window.
     * @param {Date} dateTo Inclusive end of the window, Y-m-d.
     * @param {Array<'chatgpt' | 'perplexity' | 'google_ai_overview' | 'google_ai_mode' | 'google_serp' | 'google_shopping'>} [engines] Repeatable, or comma-separated.
     * @param {Array<string>} [countries] ISO-3166 alpha-2 codes or English names. Must be configured on the project.
     * @param {Array<string>} [queryClusterIds] Restrict to these clusters. Each must belong to the project.
     * @param {boolean} [includeUngroupedQueries] Sent alone, returns only the ungrouped bucket rather than adding it to every cluster.
     * @throws {RequiredError}
     * @memberof AnalyticsApiInterface
     */
    getClusterBreakdownRequestOpts(requestParameters: GetClusterBreakdownRequest): Promise<runtime.RequestOpts>;

    /**
     * Minimum role: viewer. One row per keyword cluster over a date window, with its tracked query and keyword counts, average positions, rates, share of voice and sentiment split. A row whose clusterId is null is the ungrouped bucket: the tracked queries belonging to no cluster. Position metrics are 1-based and LOWER is better; rates, positivityIndex and shareOfVoice are 0-100 percentages where higher is better. A null metric means nothing was captured for that cluster in the window — it is not a zero, and averaging or charting it as one would misstate the period. dataDirtySince is non-null while a recalculation is pending, meaning the numbers predate the latest configuration change.
     * @summary Rank-tracking metrics per keyword cluster
     * @param {string} organizationId 
     * @param {string} projectId 
     * @param {Date} dateFrom Inclusive start of the window, Y-m-d. Must fall inside the data retention window.
     * @param {Date} dateTo Inclusive end of the window, Y-m-d.
     * @param {Array<'chatgpt' | 'perplexity' | 'google_ai_overview' | 'google_ai_mode' | 'google_serp' | 'google_shopping'>} [engines] Repeatable, or comma-separated.
     * @param {Array<string>} [countries] ISO-3166 alpha-2 codes or English names. Must be configured on the project.
     * @param {Array<string>} [queryClusterIds] Restrict to these clusters. Each must belong to the project.
     * @param {boolean} [includeUngroupedQueries] Sent alone, returns only the ungrouped bucket rather than adding it to every cluster.
     * @param {*} [options] Override http request option.
     * @throws {RequiredError}
     * @memberof AnalyticsApiInterface
     */
    getClusterBreakdownRaw(requestParameters: GetClusterBreakdownRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<ProjectRankTrackingClusterBreakdown>>;

    /**
     * Minimum role: viewer. One row per keyword cluster over a date window, with its tracked query and keyword counts, average positions, rates, share of voice and sentiment split. A row whose clusterId is null is the ungrouped bucket: the tracked queries belonging to no cluster. Position metrics are 1-based and LOWER is better; rates, positivityIndex and shareOfVoice are 0-100 percentages where higher is better. A null metric means nothing was captured for that cluster in the window — it is not a zero, and averaging or charting it as one would misstate the period. dataDirtySince is non-null while a recalculation is pending, meaning the numbers predate the latest configuration change.
     * Rank-tracking metrics per keyword cluster
     */
    getClusterBreakdown(requestParameters: GetClusterBreakdownRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<ProjectRankTrackingClusterBreakdown>;

    /**
     * Creates request options for getCompetitorCoOccurrence without sending the request
     * @param {string} organizationId 
     * @param {string} projectId 
     * @param {Date} dateFrom Inclusive start of the window, Y-m-d. Must fall inside the data retention window.
     * @param {Date} dateTo Inclusive end of the window, Y-m-d.
     * @param {Array<'chatgpt' | 'perplexity' | 'google_ai_overview' | 'google_ai_mode' | 'google_serp' | 'google_shopping'>} [engines] Repeatable, or comma-separated. SERP and Shopping carry no AI answer text, so they contribute no co-occurrence.
     * @param {Array<string>} [countries] ISO-3166 alpha-2 codes or English names. Must be configured on the project.
     * @param {string} [competitorId] Restricts the answer to a single tracked competitor. Omit it for every tracked competitor. The available-filters endpoint lists the valid ids.
     * @throws {RequiredError}
     * @memberof AnalyticsApiInterface
     */
    getCompetitorCoOccurrenceRequestOpts(requestParameters: GetCompetitorCoOccurrenceRequest): Promise<runtime.RequestOpts>;

    /**
     * Minimum role: viewer. Restricted to the AI answers where the brand and a competitor are BOTH mentioned, one row per tracked competitor: sharedResponseCount is how many such answers there are, and brandWins / competitorWins / ties split them by who holds the better (lower) best mention position. winRate is the percentage 0-100 of those answers the brand wins; avgOwnPosition and avgCompetitorPosition are the average best mention position each side held, 1-based, so LOWER is better. exampleQueryText and exampleAiResponseId point at one representative shared answer. Every nullable field means \"not known yet\" rather than zero: a null winRate or average position is the absence of a shared answer in the window, not a record of losing. Tracked competitors only.
     * @summary Head-to-head record of the brand against each tracked competitor
     * @param {string} organizationId 
     * @param {string} projectId 
     * @param {Date} dateFrom Inclusive start of the window, Y-m-d. Must fall inside the data retention window.
     * @param {Date} dateTo Inclusive end of the window, Y-m-d.
     * @param {Array<'chatgpt' | 'perplexity' | 'google_ai_overview' | 'google_ai_mode' | 'google_serp' | 'google_shopping'>} [engines] Repeatable, or comma-separated. SERP and Shopping carry no AI answer text, so they contribute no co-occurrence.
     * @param {Array<string>} [countries] ISO-3166 alpha-2 codes or English names. Must be configured on the project.
     * @param {string} [competitorId] Restricts the answer to a single tracked competitor. Omit it for every tracked competitor. The available-filters endpoint lists the valid ids.
     * @param {*} [options] Override http request option.
     * @throws {RequiredError}
     * @memberof AnalyticsApiInterface
     */
    getCompetitorCoOccurrenceRaw(requestParameters: GetCompetitorCoOccurrenceRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<CompetitorCoOccurrenceResponse>>;

    /**
     * Minimum role: viewer. Restricted to the AI answers where the brand and a competitor are BOTH mentioned, one row per tracked competitor: sharedResponseCount is how many such answers there are, and brandWins / competitorWins / ties split them by who holds the better (lower) best mention position. winRate is the percentage 0-100 of those answers the brand wins; avgOwnPosition and avgCompetitorPosition are the average best mention position each side held, 1-based, so LOWER is better. exampleQueryText and exampleAiResponseId point at one representative shared answer. Every nullable field means \"not known yet\" rather than zero: a null winRate or average position is the absence of a shared answer in the window, not a record of losing. Tracked competitors only.
     * Head-to-head record of the brand against each tracked competitor
     */
    getCompetitorCoOccurrence(requestParameters: GetCompetitorCoOccurrenceRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<CompetitorCoOccurrenceResponse>;

    /**
     * Creates request options for getMentionMix without sending the request
     * @param {string} organizationId 
     * @param {string} projectId 
     * @param {Date} dateFrom Inclusive start of the window, Y-m-d. Must fall inside the data retention window.
     * @param {Date} dateTo Inclusive end of the window, Y-m-d.
     * @param {Array<'chatgpt' | 'perplexity' | 'google_ai_overview' | 'google_ai_mode' | 'google_serp' | 'google_shopping'>} [engines] Repeatable, or comma-separated. Non-AI engines contribute no mentions.
     * @param {Array<string>} [countries] ISO-3166 alpha-2 codes or English names. Must be configured on the project.
     * @throws {RequiredError}
     * @memberof AnalyticsApiInterface
     */
    getMentionMixRequestOpts(requestParameters: GetMentionMixRequest): Promise<runtime.RequestOpts>;

    /**
     * Minimum role: viewer. Counts of the project brand\'s own text mentions in AI answers over a date window, grouped three ways: byType (recommendation, comparison, listing, example, reference), byTone (positive, neutral, negative) and byQualifier (direct, conditional — a conditional mention is one the answer hedged with a condition). These are the inputs behind the Share of Voice weighted score. Every bucket is always present and is a plain count, never null: a zero means no mention of that kind was found in the window. The three groupings count the same mentions, so each one sums to the same total. Only the project brand is counted, never a competitor, and only mentions inside the answer text — a citation of the brand\'s URL is not a mention here. Only AI answer engines produce mentions, so restricting engines to google_serp or google_shopping alone returns all zeros. For the positive/neutral/negative split per engine and per competitor use the sentiment endpoint.
     * @summary Composition of a project brand mentions in AI answers
     * @param {string} organizationId 
     * @param {string} projectId 
     * @param {Date} dateFrom Inclusive start of the window, Y-m-d. Must fall inside the data retention window.
     * @param {Date} dateTo Inclusive end of the window, Y-m-d.
     * @param {Array<'chatgpt' | 'perplexity' | 'google_ai_overview' | 'google_ai_mode' | 'google_serp' | 'google_shopping'>} [engines] Repeatable, or comma-separated. Non-AI engines contribute no mentions.
     * @param {Array<string>} [countries] ISO-3166 alpha-2 codes or English names. Must be configured on the project.
     * @param {*} [options] Override http request option.
     * @throws {RequiredError}
     * @memberof AnalyticsApiInterface
     */
    getMentionMixRaw(requestParameters: GetMentionMixRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<ProjectMentionMixResponse>>;

    /**
     * Minimum role: viewer. Counts of the project brand\'s own text mentions in AI answers over a date window, grouped three ways: byType (recommendation, comparison, listing, example, reference), byTone (positive, neutral, negative) and byQualifier (direct, conditional — a conditional mention is one the answer hedged with a condition). These are the inputs behind the Share of Voice weighted score. Every bucket is always present and is a plain count, never null: a zero means no mention of that kind was found in the window. The three groupings count the same mentions, so each one sums to the same total. Only the project brand is counted, never a competitor, and only mentions inside the answer text — a citation of the brand\'s URL is not a mention here. Only AI answer engines produce mentions, so restricting engines to google_serp or google_shopping alone returns all zeros. For the positive/neutral/negative split per engine and per competitor use the sentiment endpoint.
     * Composition of a project brand mentions in AI answers
     */
    getMentionMix(requestParameters: GetMentionMixRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<ProjectMentionMixResponse>;

    /**
     * Creates request options for getMentionSamples without sending the request
     * @param {string} organizationId 
     * @param {string} projectId 
     * @param {Date} dateFrom Inclusive start of the window, Y-m-d. Must fall inside the data retention window.
     * @param {Date} dateTo Inclusive end of the window, Y-m-d.
     * @param {Array<'chatgpt' | 'perplexity' | 'google_ai_overview' | 'google_ai_mode' | 'google_serp' | 'google_shopping'>} [engines] Repeatable, or comma-separated. Non-AI engines contribute no mentions.
     * @param {Array<string>} [countries] ISO-3166 alpha-2 codes or English names. Must be configured on the project.
     * @param {'positive' | 'neutral' | 'negative'} [sentiment] Restrict to one sentiment label. Omit for every sentiment.
     * @param {'recommendation' | 'comparison' | 'listing' | 'example' | 'reference'} [mentionType] Restrict to one mention type. Omit for every type.
     * @param {string} [competitorId] UUID of a single competitor, as listed by the available-filters endpoint (competitors[].id). Omitted, the page is restricted to mentions storing no competitor id, which is own-brand and untracked-competitor mentions together — see the operation description and read mentionRelation to tell them apart.
     * @param {'recent' | 'negative' | 'engine' | 'country'} [sortBy] recent: newest first. negative: negative sentiment first, then neutral, then positive, newest first inside each. engine and country: grouped alphabetically, newest first inside each group. An unknown value is rejected, not replaced by the default.
     * @param {number} [limit] Page size. A larger value is rejected, never silently reduced.
     * @param {number} [offset] Number of matching mentions to skip before the page starts.
     * @throws {RequiredError}
     * @memberof AnalyticsApiInterface
     */
    getMentionSamplesRequestOpts(requestParameters: GetMentionSamplesRequest): Promise<runtime.RequestOpts>;

    /**
     * Minimum role: viewer. A paginated page of the individual mention texts behind the aggregate numbers, for qualitative review and for checking sentiment labels by eye. Each sample carries the mention text, the engine and country it was seen in, the tracked query that produced it, its sentiment and mention type, and mentionPosition — a 1-based rank inside the answer where LOWER is better, always present. `total` counts every mention matching the filters, not the size of the page returned. Only mentions inside the answer text are returned: a citation of the brand URL is not a mention here, and only AI answer engines produce mentions, so restricting engines to google_serp or google_shopping alone returns an empty page rather than an error. Two fields carry a \"not known\" rather than a zero: country is null and queryText is empty when the tracked query behind the mention has since been deleted, and competitorId is null when the mention row stores no competitor id — which is NOT an assertion that the mention is about your own brand, since an untracked competitor also stores none. Read mentionRelation instead: `own` and `tracked-competitor` are what the scraper resolved to a configured entity, `untracked-competitor` is a rival the project does not track, and null means the row predates the field. brandName carries the mentioned brand, and is the only way to name an untracked competitor, which has no competitor id to resolve one from. Omitting the competitorId filter returns exactly the rows with no competitor id stored — that is, own-brand AND untracked-competitor mentions together, not every competitor; pass a competitor UUID to restrict the page to that competitor, and use mentionRelation to separate the rest. For the aggregate positive/neutral/negative split use the sentiment endpoint, and for weighted mention-type counts the mention mix endpoint.
     * @summary Sample of the raw AI mention texts of a project
     * @param {string} organizationId 
     * @param {string} projectId 
     * @param {Date} dateFrom Inclusive start of the window, Y-m-d. Must fall inside the data retention window.
     * @param {Date} dateTo Inclusive end of the window, Y-m-d.
     * @param {Array<'chatgpt' | 'perplexity' | 'google_ai_overview' | 'google_ai_mode' | 'google_serp' | 'google_shopping'>} [engines] Repeatable, or comma-separated. Non-AI engines contribute no mentions.
     * @param {Array<string>} [countries] ISO-3166 alpha-2 codes or English names. Must be configured on the project.
     * @param {'positive' | 'neutral' | 'negative'} [sentiment] Restrict to one sentiment label. Omit for every sentiment.
     * @param {'recommendation' | 'comparison' | 'listing' | 'example' | 'reference'} [mentionType] Restrict to one mention type. Omit for every type.
     * @param {string} [competitorId] UUID of a single competitor, as listed by the available-filters endpoint (competitors[].id). Omitted, the page is restricted to mentions storing no competitor id, which is own-brand and untracked-competitor mentions together — see the operation description and read mentionRelation to tell them apart.
     * @param {'recent' | 'negative' | 'engine' | 'country'} [sortBy] recent: newest first. negative: negative sentiment first, then neutral, then positive, newest first inside each. engine and country: grouped alphabetically, newest first inside each group. An unknown value is rejected, not replaced by the default.
     * @param {number} [limit] Page size. A larger value is rejected, never silently reduced.
     * @param {number} [offset] Number of matching mentions to skip before the page starts.
     * @param {*} [options] Override http request option.
     * @throws {RequiredError}
     * @memberof AnalyticsApiInterface
     */
    getMentionSamplesRaw(requestParameters: GetMentionSamplesRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<ProjectMentionSamplesResponse>>;

    /**
     * Minimum role: viewer. A paginated page of the individual mention texts behind the aggregate numbers, for qualitative review and for checking sentiment labels by eye. Each sample carries the mention text, the engine and country it was seen in, the tracked query that produced it, its sentiment and mention type, and mentionPosition — a 1-based rank inside the answer where LOWER is better, always present. `total` counts every mention matching the filters, not the size of the page returned. Only mentions inside the answer text are returned: a citation of the brand URL is not a mention here, and only AI answer engines produce mentions, so restricting engines to google_serp or google_shopping alone returns an empty page rather than an error. Two fields carry a \"not known\" rather than a zero: country is null and queryText is empty when the tracked query behind the mention has since been deleted, and competitorId is null when the mention row stores no competitor id — which is NOT an assertion that the mention is about your own brand, since an untracked competitor also stores none. Read mentionRelation instead: `own` and `tracked-competitor` are what the scraper resolved to a configured entity, `untracked-competitor` is a rival the project does not track, and null means the row predates the field. brandName carries the mentioned brand, and is the only way to name an untracked competitor, which has no competitor id to resolve one from. Omitting the competitorId filter returns exactly the rows with no competitor id stored — that is, own-brand AND untracked-competitor mentions together, not every competitor; pass a competitor UUID to restrict the page to that competitor, and use mentionRelation to separate the rest. For the aggregate positive/neutral/negative split use the sentiment endpoint, and for weighted mention-type counts the mention mix endpoint.
     * Sample of the raw AI mention texts of a project
     */
    getMentionSamples(requestParameters: GetMentionSamplesRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<ProjectMentionSamplesResponse>;

    /**
     * Creates request options for getMetricGlossary without sending the request
     * @throws {RequiredError}
     * @memberof AnalyticsApiInterface
     */
    getMetricGlossaryRequestOpts(): Promise<runtime.RequestOpts>;

    /**
     * Static reference, no project data. Each entry gives a metric, the everyday words people use for it, its unit and range, whether higher or lower is better, the operation that returns it, and example questions. Useful when turning a vague or non-technical request into the right call.
     * @summary Map everyday wording to a metric and the operation that serves it
     * @param {*} [options] Override http request option.
     * @throws {RequiredError}
     * @memberof AnalyticsApiInterface
     */
    getMetricGlossaryRaw(initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<GetMetricGlossary200Response>>;

    /**
     * Static reference, no project data. Each entry gives a metric, the everyday words people use for it, its unit and range, whether higher or lower is better, the operation that returns it, and example questions. Useful when turning a vague or non-technical request into the right call.
     * Map everyday wording to a metric and the operation that serves it
     */
    getMetricGlossary(initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<GetMetricGlossary200Response>;

    /**
     * Creates request options for getOrganizationOverview without sending the request
     * @param {string} organizationId 
     * @throws {RequiredError}
     * @memberof AnalyticsApiInterface
     */
    getOrganizationOverviewRequestOpts(requestParameters: GetOrganizationOverviewRequest): Promise<runtime.RequestOpts>;

    /**
     * Minimum role: viewer. One row per active project — share of voice, mention rate, average mention position, positivity index and tracked-query count — ordered by share of voice, plus an organization-level aggregate of the same metrics. Archived projects are excluded. This is a current-state snapshot and takes no date window; for a date-ranged comparison call the per-project operations (getProjectMetrics, getProjectTimeSeries) for each projectId returned here. Every metric is nullable, and a null means the project has no rank data yet — not a score of zero. Higher is better for shareOfVoice, mentionRate and positivityIndex; avgMentionPosition is a 1-based rank, so LOWER is better.
     * @summary Snapshot rank-health board across an organization active projects
     * @param {string} organizationId 
     * @param {*} [options] Override http request option.
     * @throws {RequiredError}
     * @memberof AnalyticsApiInterface
     */
    getOrganizationOverviewRaw(requestParameters: GetOrganizationOverviewRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<GetOrganizationOverview200Response>>;

    /**
     * Minimum role: viewer. One row per active project — share of voice, mention rate, average mention position, positivity index and tracked-query count — ordered by share of voice, plus an organization-level aggregate of the same metrics. Archived projects are excluded. This is a current-state snapshot and takes no date window; for a date-ranged comparison call the per-project operations (getProjectMetrics, getProjectTimeSeries) for each projectId returned here. Every metric is nullable, and a null means the project has no rank data yet — not a score of zero. Higher is better for shareOfVoice, mentionRate and positivityIndex; avgMentionPosition is a 1-based rank, so LOWER is better.
     * Snapshot rank-health board across an organization active projects
     */
    getOrganizationOverview(requestParameters: GetOrganizationOverviewRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<GetOrganizationOverview200Response>;

    /**
     * Creates request options for getProjectMetrics without sending the request
     * @param {string} organizationId 
     * @param {string} projectId 
     * @param {Date} dateFrom Inclusive start of the window, Y-m-d. Must fall inside the data retention window.
     * @param {Date} dateTo Inclusive end of the window, Y-m-d.
     * @param {Array<'chatgpt' | 'perplexity' | 'google_ai_overview' | 'google_ai_mode' | 'google_serp' | 'google_shopping'>} [engines] Repeatable, or comma-separated.
     * @param {Array<string>} [countries] ISO-3166 alpha-2 codes or English names. Must be configured on the project.
     * @param {Array<string>} [queryClusterIds] Restrict to these keyword clusters. Each must belong to this project.
     * @param {boolean} [includeUngroupedQueries] Only meaningful together with queryClusterIds: also counts the tracked queries that belong to no cluster.
     * @throws {RequiredError}
     * @memberof AnalyticsApiInterface
     */
    getProjectMetricsRequestOpts(requestParameters: GetProjectMetricsRequest): Promise<runtime.RequestOpts>;

    /**
     * Minimum role: viewer. The project overview over a date window: share of voice (own and per competitor), mention / SERP / shopping rates, average and best positions, position stability, the sentiment split and the position-distribution buckets. Positions are 1-based, so a LOWER number is better; rates, the positivity index and share of voice are percentages from 0 to 100, where HIGHER is better. Every `trend*` field is the signed change against the immediately preceding window of the same length: negative means an improved position, positive means an improved rate or score. A null metric means \"not known yet\", never zero: a scalar is null when the window holds no checks at all, and a `trend*` field is null when there is no earlier window to compare against. The counters (`mentionCount`, `*TrackedQueryCount`, `*QueriesWithResult`, `sentiment*` and `mentionTypeCounts`) are genuine zeros instead, so an empty window reads as zero counts with null rates. `dataDirtySince` is non-null while a recalculation is pending, meaning the figures may still move for dates from then on.
     * @summary Headline visibility metrics of a project
     * @param {string} organizationId 
     * @param {string} projectId 
     * @param {Date} dateFrom Inclusive start of the window, Y-m-d. Must fall inside the data retention window.
     * @param {Date} dateTo Inclusive end of the window, Y-m-d.
     * @param {Array<'chatgpt' | 'perplexity' | 'google_ai_overview' | 'google_ai_mode' | 'google_serp' | 'google_shopping'>} [engines] Repeatable, or comma-separated.
     * @param {Array<string>} [countries] ISO-3166 alpha-2 codes or English names. Must be configured on the project.
     * @param {Array<string>} [queryClusterIds] Restrict to these keyword clusters. Each must belong to this project.
     * @param {boolean} [includeUngroupedQueries] Only meaningful together with queryClusterIds: also counts the tracked queries that belong to no cluster.
     * @param {*} [options] Override http request option.
     * @throws {RequiredError}
     * @memberof AnalyticsApiInterface
     */
    getProjectMetricsRaw(requestParameters: GetProjectMetricsRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<ProjectRankTrackingStats>>;

    /**
     * Minimum role: viewer. The project overview over a date window: share of voice (own and per competitor), mention / SERP / shopping rates, average and best positions, position stability, the sentiment split and the position-distribution buckets. Positions are 1-based, so a LOWER number is better; rates, the positivity index and share of voice are percentages from 0 to 100, where HIGHER is better. Every `trend*` field is the signed change against the immediately preceding window of the same length: negative means an improved position, positive means an improved rate or score. A null metric means \"not known yet\", never zero: a scalar is null when the window holds no checks at all, and a `trend*` field is null when there is no earlier window to compare against. The counters (`mentionCount`, `*TrackedQueryCount`, `*QueriesWithResult`, `sentiment*` and `mentionTypeCounts`) are genuine zeros instead, so an empty window reads as zero counts with null rates. `dataDirtySince` is non-null while a recalculation is pending, meaning the figures may still move for dates from then on.
     * Headline visibility metrics of a project
     */
    getProjectMetrics(requestParameters: GetProjectMetricsRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<ProjectRankTrackingStats>;

    /**
     * Creates request options for getProjectSentiment without sending the request
     * @param {string} organizationId 
     * @param {string} projectId 
     * @param {Date} dateFrom Inclusive start of the window, Y-m-d. Must fall inside the data retention window.
     * @param {Date} dateTo Inclusive end of the window, Y-m-d.
     * @param {Array<'chatgpt' | 'perplexity' | 'google_ai_overview' | 'google_ai_mode' | 'google_serp' | 'google_shopping'>} [engines] Repeatable, or comma-separated.
     * @param {Array<string>} [countries] ISO-3166 alpha-2 codes or English names. Must be configured on the project.
     * @param {Array<string>} [queryClusterIds] 
     * @param {boolean} [includeUngroupedQueries] 
     * @throws {RequiredError}
     * @memberof AnalyticsApiInterface
     */
    getProjectSentimentRequestOpts(requestParameters: GetProjectSentimentRequest): Promise<runtime.RequestOpts>;

    /**
     * Minimum role: viewer. Positive, neutral and negative split of the brand mentions in AI answers over a date window, per engine and per competitor. A null positivityIndex means no mentions were found in the window, which is not the same as a score of zero.
     * @summary Sentiment breakdown of a project brand mentions
     * @param {string} organizationId 
     * @param {string} projectId 
     * @param {Date} dateFrom Inclusive start of the window, Y-m-d. Must fall inside the data retention window.
     * @param {Date} dateTo Inclusive end of the window, Y-m-d.
     * @param {Array<'chatgpt' | 'perplexity' | 'google_ai_overview' | 'google_ai_mode' | 'google_serp' | 'google_shopping'>} [engines] Repeatable, or comma-separated.
     * @param {Array<string>} [countries] ISO-3166 alpha-2 codes or English names. Must be configured on the project.
     * @param {Array<string>} [queryClusterIds] 
     * @param {boolean} [includeUngroupedQueries] 
     * @param {*} [options] Override http request option.
     * @throws {RequiredError}
     * @memberof AnalyticsApiInterface
     */
    getProjectSentimentRaw(requestParameters: GetProjectSentimentRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<ProjectSentimentBreakdown>>;

    /**
     * Minimum role: viewer. Positive, neutral and negative split of the brand mentions in AI answers over a date window, per engine and per competitor. A null positivityIndex means no mentions were found in the window, which is not the same as a score of zero.
     * Sentiment breakdown of a project brand mentions
     */
    getProjectSentiment(requestParameters: GetProjectSentimentRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<ProjectSentimentBreakdown>;

    /**
     * Creates request options for getProjectTimeSeries without sending the request
     * @param {string} organizationId 
     * @param {string} projectId 
     * @param {Date} dateFrom Inclusive start of the window, Y-m-d. Must fall inside the data retention window.
     * @param {Date} dateTo Inclusive end of the window, Y-m-d.
     * @param {'daily' | 'weekly' | 'monthly'} [granularity] Bucket size of each point.
     * @param {Array<'chatgpt' | 'perplexity' | 'google_ai_overview' | 'google_ai_mode' | 'google_serp' | 'google_shopping'>} [engines] Repeatable, or comma-separated.
     * @param {Array<string>} [countries] ISO-3166 alpha-2 codes or English names. Must be configured on the project.
     * @param {Array<string>} [queryClusterIds] 
     * @param {boolean} [includeUngroupedQueries] On its own this NARROWS the series to tracked queries that belong to no cluster; combined with queryClusterIds it widens those clusters to also cover them.
     * @param {Array<string>} [competitorIds] Repeatable, or comma-separated. Each id adds one series under the competitors map of every point.
     * @throws {RequiredError}
     * @memberof AnalyticsApiInterface
     */
    getProjectTimeSeriesRequestOpts(requestParameters: GetProjectTimeSeriesRequest): Promise<runtime.RequestOpts>;

    /**
     * Minimum role: viewer. One point per bucket over the date window, each carrying the brand metrics and one same-shaped entry per requested competitor. Rank metrics (serp, shopping, mention, link) are 1-based averages where LOWER is better; positivity (0-100), shareOfVoice (0-100), mentionRate (0-100) and serpRate (0-100) are scores where higher is better. Every metric is nullable, and a null means no data was collected for that bucket, which is not the same as a value of zero. A project with no tracked queries returns an empty points list. Prefer weekly or monthly granularity over a long window to keep the response compact.
     * @summary Rank-tracking metrics of a project over time
     * @param {string} organizationId 
     * @param {string} projectId 
     * @param {Date} dateFrom Inclusive start of the window, Y-m-d. Must fall inside the data retention window.
     * @param {Date} dateTo Inclusive end of the window, Y-m-d.
     * @param {'daily' | 'weekly' | 'monthly'} [granularity] Bucket size of each point.
     * @param {Array<'chatgpt' | 'perplexity' | 'google_ai_overview' | 'google_ai_mode' | 'google_serp' | 'google_shopping'>} [engines] Repeatable, or comma-separated.
     * @param {Array<string>} [countries] ISO-3166 alpha-2 codes or English names. Must be configured on the project.
     * @param {Array<string>} [queryClusterIds] 
     * @param {boolean} [includeUngroupedQueries] On its own this NARROWS the series to tracked queries that belong to no cluster; combined with queryClusterIds it widens those clusters to also cover them.
     * @param {Array<string>} [competitorIds] Repeatable, or comma-separated. Each id adds one series under the competitors map of every point.
     * @param {*} [options] Override http request option.
     * @throws {RequiredError}
     * @memberof AnalyticsApiInterface
     */
    getProjectTimeSeriesRaw(requestParameters: GetProjectTimeSeriesRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<ProjectRankTrackingTimeSeries>>;

    /**
     * Minimum role: viewer. One point per bucket over the date window, each carrying the brand metrics and one same-shaped entry per requested competitor. Rank metrics (serp, shopping, mention, link) are 1-based averages where LOWER is better; positivity (0-100), shareOfVoice (0-100), mentionRate (0-100) and serpRate (0-100) are scores where higher is better. Every metric is nullable, and a null means no data was collected for that bucket, which is not the same as a value of zero. A project with no tracked queries returns an empty points list. Prefer weekly or monthly granularity over a long window to keep the response compact.
     * Rank-tracking metrics of a project over time
     */
    getProjectTimeSeries(requestParameters: GetProjectTimeSeriesRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<ProjectRankTrackingTimeSeries>;

    /**
     * Creates request options for getQueryMovers without sending the request
     * @param {string} organizationId 
     * @param {string} projectId 
     * @param {Date} dateFrom Inclusive start of the window, Y-m-d. Must fall inside the data retention window. The comparison window is the equally long stretch immediately before it.
     * @param {Date} dateTo Inclusive end of the window, Y-m-d.
     * @param {Array<'chatgpt' | 'perplexity' | 'google_ai_overview' | 'google_ai_mode' | 'google_serp' | 'google_shopping'>} [engines] Repeatable, or comma-separated.
     * @param {Array<string>} [countries] ISO-3166 alpha-2 codes or English names. Must be configured on the project.
     * @param {'trend_serp' | 'trend_shopping' | 'trend_mention' | 'trend_link' | 'trend_share_of_voice'} [sortBy] Which trend delta ranks the rows. Positions improve as they fall, so a positive delta is always an improvement whichever key you pick. An unknown value is rejected, not replaced by the default.
     * @param {'asc' | 'desc'} [sortOrder] desc for the top gainers, asc for the top losers.
     * @param {number} [limit] Page size. A value above the maximum is rejected, never clamped.
     * @param {number} [offset] 
     * @throws {RequiredError}
     * @memberof AnalyticsApiInterface
     */
    getQueryMoversRequestOpts(requestParameters: GetQueryMoversRequest): Promise<runtime.RequestOpts>;

    /**
     * Minimum role: viewer. One row per tracked query — a single engine plus country — carrying its current metric envelope and the signed change against the immediately preceding window of equal length: a 7-day window is compared with the 7 days before it. Every trend delta is signed so that POSITIVE means improved, including the position trends, where the underlying avgSerpPosition / avgShoppingPosition / avgMentionPosition / avgLinkPosition are 1-based ranks and therefore LOWER is better. shareOfVoice and positivityIndex are percentages from 0 to 100, where HIGHER is better. Sorting applies to the trend keys only: sortOrder=desc gives the top gainers, asc the top losers. Every nullable field means \"not known yet\" rather than zero — a null position or shareOfVoice is a query with no data in the window, and a null positivityIndex or trend is a period with no mentions to score, neither of which is a record of losing ground. total counts the tracked queries the filters match, not the rows on this page. dataDirtySince is a date from which the rank data is being recomputed, or null when nothing is pending; while it is non-null the deltas may still move.
     * @summary Tracked queries ranked by how much a metric moved
     * @param {string} organizationId 
     * @param {string} projectId 
     * @param {Date} dateFrom Inclusive start of the window, Y-m-d. Must fall inside the data retention window. The comparison window is the equally long stretch immediately before it.
     * @param {Date} dateTo Inclusive end of the window, Y-m-d.
     * @param {Array<'chatgpt' | 'perplexity' | 'google_ai_overview' | 'google_ai_mode' | 'google_serp' | 'google_shopping'>} [engines] Repeatable, or comma-separated.
     * @param {Array<string>} [countries] ISO-3166 alpha-2 codes or English names. Must be configured on the project.
     * @param {'trend_serp' | 'trend_shopping' | 'trend_mention' | 'trend_link' | 'trend_share_of_voice'} [sortBy] Which trend delta ranks the rows. Positions improve as they fall, so a positive delta is always an improvement whichever key you pick. An unknown value is rejected, not replaced by the default.
     * @param {'asc' | 'desc'} [sortOrder] desc for the top gainers, asc for the top losers.
     * @param {number} [limit] Page size. A value above the maximum is rejected, never clamped.
     * @param {number} [offset] 
     * @param {*} [options] Override http request option.
     * @throws {RequiredError}
     * @memberof AnalyticsApiInterface
     */
    getQueryMoversRaw(requestParameters: GetQueryMoversRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<TrackedQueryMoversResponse>>;

    /**
     * Minimum role: viewer. One row per tracked query — a single engine plus country — carrying its current metric envelope and the signed change against the immediately preceding window of equal length: a 7-day window is compared with the 7 days before it. Every trend delta is signed so that POSITIVE means improved, including the position trends, where the underlying avgSerpPosition / avgShoppingPosition / avgMentionPosition / avgLinkPosition are 1-based ranks and therefore LOWER is better. shareOfVoice and positivityIndex are percentages from 0 to 100, where HIGHER is better. Sorting applies to the trend keys only: sortOrder=desc gives the top gainers, asc the top losers. Every nullable field means \"not known yet\" rather than zero — a null position or shareOfVoice is a query with no data in the window, and a null positivityIndex or trend is a period with no mentions to score, neither of which is a record of losing ground. total counts the tracked queries the filters match, not the rows on this page. dataDirtySince is a date from which the rank data is being recomputed, or null when nothing is pending; while it is non-null the deltas may still move.
     * Tracked queries ranked by how much a metric moved
     */
    getQueryMovers(requestParameters: GetQueryMoversRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<TrackedQueryMoversResponse>;

    /**
     * Creates request options for getShareOfVoiceFormula without sending the request
     * @param {string} organizationId 
     * @param {string} projectId 
     * @throws {RequiredError}
     * @memberof AnalyticsApiInterface
     */
    getShareOfVoiceFormulaRequestOpts(requestParameters: GetShareOfVoiceFormulaRequest): Promise<runtime.RequestOpts>;

    /**
     * Minimum role: viewer. Static reference data, the same for every project: the weights and multipliers that turn individual brand mentions into a Share of Voice score. Each mention is worth `mentionTypeWeights[type] * sentimentMultipliers[tone] * (conditional ? conditionalMultiplier : directMultiplier)`, and a competitor\'s Share of Voice is its share of the summed weights of every brand in the window, as a percentage. mentionTypeWeights is keyed by mention type (recommendation, comparison, listing, example, reference) and sentimentMultipliers by tone (positive, neutral, negative); a negative mention is discounted, not discarded, because it still evidences presence. A conditional mention is one the answer hedged with a condition (\"if you need X\"). Use this to explain a score, not to recompute one: the counts it applies to come from the mention mix endpoint. Every value is always present and is never null.
     * @summary The constants behind the Share of Voice score
     * @param {string} organizationId 
     * @param {string} projectId 
     * @param {*} [options] Override http request option.
     * @throws {RequiredError}
     * @memberof AnalyticsApiInterface
     */
    getShareOfVoiceFormulaRaw(requestParameters: GetShareOfVoiceFormulaRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<GetShareOfVoiceFormula200Response>>;

    /**
     * Minimum role: viewer. Static reference data, the same for every project: the weights and multipliers that turn individual brand mentions into a Share of Voice score. Each mention is worth `mentionTypeWeights[type] * sentimentMultipliers[tone] * (conditional ? conditionalMultiplier : directMultiplier)`, and a competitor\'s Share of Voice is its share of the summed weights of every brand in the window, as a percentage. mentionTypeWeights is keyed by mention type (recommendation, comparison, listing, example, reference) and sentimentMultipliers by tone (positive, neutral, negative); a negative mention is discounted, not discarded, because it still evidences presence. A conditional mention is one the answer hedged with a condition (\"if you need X\"). Use this to explain a score, not to recompute one: the counts it applies to come from the mention mix endpoint. Every value is always present and is never null.
     * The constants behind the Share of Voice score
     */
    getShareOfVoiceFormula(requestParameters: GetShareOfVoiceFormulaRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<GetShareOfVoiceFormula200Response>;

    /**
     * Creates request options for getTrackedQueryTimeSeries without sending the request
     * @param {string} organizationId 
     * @param {string} projectId 
     * @param {string} trackedQueryId Must belong to the project in the path.
     * @param {Date} dateFrom Inclusive start of the window, Y-m-d. Must fall inside the data retention window.
     * @param {Date} dateTo Inclusive end of the window, Y-m-d.
     * @param {'daily' | 'weekly' | 'monthly'} [granularity] Bucket size. Prefer weekly or monthly for long windows.
     * @param {Array<string>} [competitorIds] Competitors to add as extra series, repeatable or comma-separated. Valid ids come from the available-filters endpoint.
     * @throws {RequiredError}
     * @memberof AnalyticsApiInterface
     */
    getTrackedQueryTimeSeriesRequestOpts(requestParameters: GetTrackedQueryTimeSeriesRequest): Promise<runtime.RequestOpts>;

    /**
     * Minimum role: viewer. One point per bucket over the date window, each carrying the brand metrics and one same-shaped entry per requested competitor. Positions (serp, shopping, mention, link) are 1-based, so a LOWER number is better; positivity, shareOfVoice, mentionRate and serpRate are percentages from 0 to 100, where higher is better. Every metric is nullable, and a null means nothing was captured for that entity in that bucket — it is not a zero: a null shareOfVoice means no measurement, a shareOfVoice of 0 means measured and never mentioned. The tracked query fixes its own engine and country, so no engine or country filter is accepted. A non-null dataDirtySince is a timestamp warning that tracked queries were deleted from the project and historical buckets may still include their contributions until the nightly refresh rebuilds them.
     * @summary Rank-tracking time series of a single tracked query
     * @param {string} organizationId 
     * @param {string} projectId 
     * @param {string} trackedQueryId Must belong to the project in the path.
     * @param {Date} dateFrom Inclusive start of the window, Y-m-d. Must fall inside the data retention window.
     * @param {Date} dateTo Inclusive end of the window, Y-m-d.
     * @param {'daily' | 'weekly' | 'monthly'} [granularity] Bucket size. Prefer weekly or monthly for long windows.
     * @param {Array<string>} [competitorIds] Competitors to add as extra series, repeatable or comma-separated. Valid ids come from the available-filters endpoint.
     * @param {*} [options] Override http request option.
     * @throws {RequiredError}
     * @memberof AnalyticsApiInterface
     */
    getTrackedQueryTimeSeriesRaw(requestParameters: GetTrackedQueryTimeSeriesRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<TrackedQueryRankTrackingTimeSeries>>;

    /**
     * Minimum role: viewer. One point per bucket over the date window, each carrying the brand metrics and one same-shaped entry per requested competitor. Positions (serp, shopping, mention, link) are 1-based, so a LOWER number is better; positivity, shareOfVoice, mentionRate and serpRate are percentages from 0 to 100, where higher is better. Every metric is nullable, and a null means nothing was captured for that entity in that bucket — it is not a zero: a null shareOfVoice means no measurement, a shareOfVoice of 0 means measured and never mentioned. The tracked query fixes its own engine and country, so no engine or country filter is accepted. A non-null dataDirtySince is a timestamp warning that tracked queries were deleted from the project and historical buckets may still include their contributions until the nightly refresh rebuilds them.
     * Rank-tracking time series of a single tracked query
     */
    getTrackedQueryTimeSeries(requestParameters: GetTrackedQueryTimeSeriesRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<TrackedQueryRankTrackingTimeSeries>;

    /**
     * Creates request options for getTrackingCoverage without sending the request
     * @param {string} organizationId 
     * @param {string} projectId 
     * @throws {RequiredError}
     * @memberof AnalyticsApiInterface
     */
    getTrackingCoverageRequestOpts(requestParameters: GetTrackingCoverageRequest): Promise<runtime.RequestOpts>;

    /**
     * Minimum role: viewer. A current-state snapshot answering \"what is stale or not being tracked\": total is every tracked query on the project, active and paused split it by status, neverChecked counts the active queries that have never run, and overdue counts the active queries whose last check is older than their own check-frequency interval (daily, weekly or monthly). neverChecked and overdue are disjoint — a query that has never run is never also counted as overdue — and both ignore paused queries, which are not expected to be checked at all. sample lists up to 20 of the overdue queries, most stale first; it is a sample of overdue only, so it never contains a never-checked query and is empty when overdue is 0. Takes no date window: every count describes the project as it stands right now.
     * @summary Coverage and staleness of a project tracked queries
     * @param {string} organizationId 
     * @param {string} projectId 
     * @param {*} [options] Override http request option.
     * @throws {RequiredError}
     * @memberof AnalyticsApiInterface
     */
    getTrackingCoverageRaw(requestParameters: GetTrackingCoverageRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<GetTrackingCoverage200Response>>;

    /**
     * Minimum role: viewer. A current-state snapshot answering \"what is stale or not being tracked\": total is every tracked query on the project, active and paused split it by status, neverChecked counts the active queries that have never run, and overdue counts the active queries whose last check is older than their own check-frequency interval (daily, weekly or monthly). neverChecked and overdue are disjoint — a query that has never run is never also counted as overdue — and both ignore paused queries, which are not expected to be checked at all. sample lists up to 20 of the overdue queries, most stale first; it is a sample of overdue only, so it never contains a never-checked query and is empty when overdue is 0. Takes no date window: every count describes the project as it stands right now.
     * Coverage and staleness of a project tracked queries
     */
    getTrackingCoverage(requestParameters: GetTrackingCoverageRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<GetTrackingCoverage200Response>;

    /**
     * Creates request options for listKeywordListings without sending the request
     * @param {string} organizationId 
     * @param {string} projectId 
     * @param {Date} dateFrom Inclusive start of the window, Y-m-d. Must fall inside the data retention window.
     * @param {Date} dateTo Inclusive end of the window, Y-m-d. The trend compares against the equally long window ending the day before dateFrom.
     * @param {Array<'chatgpt' | 'perplexity' | 'google_ai_overview' | 'google_ai_mode' | 'google_serp' | 'google_shopping'>} [engines] Repeatable, or comma-separated. Narrows which variants count towards each row.
     * @param {Array<string>} [countries] ISO-3166 alpha-2 codes or English names. Must be configured on the project.
     * @param {Array<string>} [queryClusterIds] Restrict to keywords with a variant in these clusters. Each must belong to the project.
     * @param {boolean} [includeUngroupedQueries] Sent alone, restricts the listing to keywords whose variants belong to no cluster; sent with queryClusterIds, adds them to that selection.
     * @param {'active' | 'paused'} [status] Restrict to variants with this status. A keyword whose variants disagree still reports statusSummary \&quot;mixed\&quot;.
     * @param {Array<'daily' | 'weekly' | 'monthly'>} [checkFrequencies] Repeatable, or comma-separated.
     * @param {Array<number>} [nPasses] Repeatable, or comma-separated. Restrict to variants configured with these pass counts.
     * @param {string} [search] Case-insensitive substring match on the keyword text.
     * @param {'keyword' | 'variantCount' | 'lastCheckedAt' | 'statusSummary' | 'positivityIndex' | 'shareOfVoice' | 'avgMentionPosition' | 'avgLinkPosition' | 'mentionPositionStability' | 'avgSerpPosition' | 'avgShoppingPosition'} [sortBy] Named after the field it orders by. Rows with no value for the chosen key sort last in either direction. An unknown key is rejected, not replaced by the default.
     * @param {'asc' | 'desc'} [sortOrder] 
     * @param {number} [limit] Page size. A larger value is rejected, never silently reduced.
     * @param {number} [offset] Number of matching keywords to skip before the page starts.
     * @throws {RequiredError}
     * @memberof AnalyticsApiInterface
     */
    listKeywordListingsRequestOpts(requestParameters: ListKeywordListingsRequest): Promise<runtime.RequestOpts>;

    /**
     * Minimum role: viewer. One row per distinct keyword text of the project — every tracked query asking that text, on any engine in any country, collapsed into a single row whose variantIds name the tracked queries behind it. Each row carries the metrics of the requested window and a signed trend against the window of equal length immediately before it, where POSITIVE ALWAYS MEANS BETTER whichever direction the metric itself runs. Positions are 1-based and lower is better; rates, positivityIndex and shareOfVoice are 0-100 and higher is better; mentionPositionStability is a day-to-day spread, so lower is steadier. A null metric means nothing was captured for that keyword in the window — it is not a zero. `total` counts the KEYWORDS matching the filters, not the rows on this page and not tracked queries; `totalVariantCount` counts the tracked queries behind the keywords matching every filter EXCEPT `search` — with a text search applied it still counts the project\'s variants, so do not size a force-check budget from it on a searched page. NEITHER IS THE PROJECT\'S TRACKED-QUERY COUNT: the count operation reads the write model, while this listing reads projections refreshed in the background from it, so the numbers legitimately differ while those projections catch up. A project whose cached tracked-query count has not been refreshed yet answers an empty page with total 0 — the same answer as a project with no tracked queries at all — so treat an unexpected empty page right after creating queries as \"not projected yet\", not as \"no data\". dataDirtySince is non-null while a recalculation is pending, meaning the metrics predate the latest configuration change. Filters narrow which VARIANTS count towards a row, so engines, countries, statuses, checkFrequencies and nPassesValues each report what the filters selected rather than everything the keyword has. A filter or sort key this endpoint cannot honour is rejected by name, never ignored, and a limit above the maximum is rejected rather than quietly reduced. This operation answers JSON only: a keyword row carries a nested mentionTypeCounts object, so it is not part of the CSV family.
     * @summary List a project\'s keywords with their windowed metrics
     * @param {string} organizationId 
     * @param {string} projectId 
     * @param {Date} dateFrom Inclusive start of the window, Y-m-d. Must fall inside the data retention window.
     * @param {Date} dateTo Inclusive end of the window, Y-m-d. The trend compares against the equally long window ending the day before dateFrom.
     * @param {Array<'chatgpt' | 'perplexity' | 'google_ai_overview' | 'google_ai_mode' | 'google_serp' | 'google_shopping'>} [engines] Repeatable, or comma-separated. Narrows which variants count towards each row.
     * @param {Array<string>} [countries] ISO-3166 alpha-2 codes or English names. Must be configured on the project.
     * @param {Array<string>} [queryClusterIds] Restrict to keywords with a variant in these clusters. Each must belong to the project.
     * @param {boolean} [includeUngroupedQueries] Sent alone, restricts the listing to keywords whose variants belong to no cluster; sent with queryClusterIds, adds them to that selection.
     * @param {'active' | 'paused'} [status] Restrict to variants with this status. A keyword whose variants disagree still reports statusSummary \&quot;mixed\&quot;.
     * @param {Array<'daily' | 'weekly' | 'monthly'>} [checkFrequencies] Repeatable, or comma-separated.
     * @param {Array<number>} [nPasses] Repeatable, or comma-separated. Restrict to variants configured with these pass counts.
     * @param {string} [search] Case-insensitive substring match on the keyword text.
     * @param {'keyword' | 'variantCount' | 'lastCheckedAt' | 'statusSummary' | 'positivityIndex' | 'shareOfVoice' | 'avgMentionPosition' | 'avgLinkPosition' | 'mentionPositionStability' | 'avgSerpPosition' | 'avgShoppingPosition'} [sortBy] Named after the field it orders by. Rows with no value for the chosen key sort last in either direction. An unknown key is rejected, not replaced by the default.
     * @param {'asc' | 'desc'} [sortOrder] 
     * @param {number} [limit] Page size. A larger value is rejected, never silently reduced.
     * @param {number} [offset] Number of matching keywords to skip before the page starts.
     * @param {*} [options] Override http request option.
     * @throws {RequiredError}
     * @memberof AnalyticsApiInterface
     */
    listKeywordListingsRaw(requestParameters: ListKeywordListingsRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<ListKeywordListings200Response>>;

    /**
     * Minimum role: viewer. One row per distinct keyword text of the project — every tracked query asking that text, on any engine in any country, collapsed into a single row whose variantIds name the tracked queries behind it. Each row carries the metrics of the requested window and a signed trend against the window of equal length immediately before it, where POSITIVE ALWAYS MEANS BETTER whichever direction the metric itself runs. Positions are 1-based and lower is better; rates, positivityIndex and shareOfVoice are 0-100 and higher is better; mentionPositionStability is a day-to-day spread, so lower is steadier. A null metric means nothing was captured for that keyword in the window — it is not a zero. `total` counts the KEYWORDS matching the filters, not the rows on this page and not tracked queries; `totalVariantCount` counts the tracked queries behind the keywords matching every filter EXCEPT `search` — with a text search applied it still counts the project\'s variants, so do not size a force-check budget from it on a searched page. NEITHER IS THE PROJECT\'S TRACKED-QUERY COUNT: the count operation reads the write model, while this listing reads projections refreshed in the background from it, so the numbers legitimately differ while those projections catch up. A project whose cached tracked-query count has not been refreshed yet answers an empty page with total 0 — the same answer as a project with no tracked queries at all — so treat an unexpected empty page right after creating queries as \"not projected yet\", not as \"no data\". dataDirtySince is non-null while a recalculation is pending, meaning the metrics predate the latest configuration change. Filters narrow which VARIANTS count towards a row, so engines, countries, statuses, checkFrequencies and nPassesValues each report what the filters selected rather than everything the keyword has. A filter or sort key this endpoint cannot honour is rejected by name, never ignored, and a limit above the maximum is rejected rather than quietly reduced. This operation answers JSON only: a keyword row carries a nested mentionTypeCounts object, so it is not part of the CSV family.
     * List a project\'s keywords with their windowed metrics
     */
    listKeywordListings(requestParameters: ListKeywordListingsRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<ListKeywordListings200Response>;

}

/**
 * 
 */
export class AnalyticsApi extends runtime.BaseAPI implements AnalyticsApiInterface {

    /**
     * Creates request options for getAvailableFilters without sending the request
     */
    async getAvailableFiltersRequestOpts(requestParameters: GetAvailableFiltersRequest): Promise<runtime.RequestOpts> {
        if (requestParameters['organizationId'] == null) {
            throw new runtime.RequiredError(
                'organizationId',
                'Required parameter "organizationId" was null or undefined when calling getAvailableFilters().'
            );
        }

        if (requestParameters['projectId'] == null) {
            throw new runtime.RequiredError(
                'projectId',
                'Required parameter "projectId" was null or undefined when calling getAvailableFilters().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("ApiKey", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/api/v1/organizations/{organizationId}/projects/{projectId}/available-filters`;
        urlPath = urlPath.replace('{organizationId}', encodeURIComponent(String(requestParameters['organizationId'])));
        urlPath = urlPath.replace('{projectId}', encodeURIComponent(String(requestParameters['projectId'])));

        return {
            path: urlPath,
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        };
    }

    /**
     * Minimum role: viewer. Call this first: it is where every other analytics operation sends you for the valid engines, countries and keyword clusters of a project, and the values it returns are the exact strings the engines, countries and queryClusterIds parameters accept — anything else is rejected as a 400. Engines are engine codes, countries are ISO-3166 alpha-2 codes, and clusters are {id, name} objects whose id goes in queryClusterIds. Takes no date window: it describes how the project is configured right now, so a value is listed as soon as a tracked query uses it, even when no response has been captured for it yet. An empty list therefore means nothing is configured for it, not that no data was collected. Competitor ids are not part of this response.
     * Filter values a project is configured for
     */
    async getAvailableFiltersRaw(requestParameters: GetAvailableFiltersRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<GetAvailableFilters200Response>> {
        const requestOptions = await this.getAvailableFiltersRequestOpts(requestParameters);
        const response = await this.request(requestOptions, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => GetAvailableFilters200ResponseFromJSON(jsonValue));
    }

    /**
     * Minimum role: viewer. Call this first: it is where every other analytics operation sends you for the valid engines, countries and keyword clusters of a project, and the values it returns are the exact strings the engines, countries and queryClusterIds parameters accept — anything else is rejected as a 400. Engines are engine codes, countries are ISO-3166 alpha-2 codes, and clusters are {id, name} objects whose id goes in queryClusterIds. Takes no date window: it describes how the project is configured right now, so a value is listed as soon as a tracked query uses it, even when no response has been captured for it yet. An empty list therefore means nothing is configured for it, not that no data was collected. Competitor ids are not part of this response.
     * Filter values a project is configured for
     */
    async getAvailableFilters(requestParameters: GetAvailableFiltersRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<GetAvailableFilters200Response> {
        const response = await this.getAvailableFiltersRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Creates request options for getCitedSources without sending the request
     */
    async getCitedSourcesRequestOpts(requestParameters: GetCitedSourcesRequest): Promise<runtime.RequestOpts> {
        if (requestParameters['organizationId'] == null) {
            throw new runtime.RequiredError(
                'organizationId',
                'Required parameter "organizationId" was null or undefined when calling getCitedSources().'
            );
        }

        if (requestParameters['projectId'] == null) {
            throw new runtime.RequiredError(
                'projectId',
                'Required parameter "projectId" was null or undefined when calling getCitedSources().'
            );
        }

        if (requestParameters['dateFrom'] == null) {
            throw new runtime.RequiredError(
                'dateFrom',
                'Required parameter "dateFrom" was null or undefined when calling getCitedSources().'
            );
        }

        if (requestParameters['dateTo'] == null) {
            throw new runtime.RequiredError(
                'dateTo',
                'Required parameter "dateTo" was null or undefined when calling getCitedSources().'
            );
        }

        const queryParameters: any = {};

        if (requestParameters['dateFrom'] != null) {
            queryParameters['dateFrom'] = (requestParameters['dateFrom'] as any).toISOString().substring(0,10);
        }

        if (requestParameters['dateTo'] != null) {
            queryParameters['dateTo'] = (requestParameters['dateTo'] as any).toISOString().substring(0,10);
        }

        if (requestParameters['engines'] != null) {
            queryParameters['engines'] = requestParameters['engines'];
        }

        if (requestParameters['groupBy'] != null) {
            queryParameters['groupBy'] = requestParameters['groupBy'];
        }

        if (requestParameters['limit'] != null) {
            queryParameters['limit'] = requestParameters['limit'];
        }

        if (requestParameters['offset'] != null) {
            queryParameters['offset'] = requestParameters['offset'];
        }

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("ApiKey", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/api/v1/organizations/{organizationId}/projects/{projectId}/cited-sources`;
        urlPath = urlPath.replace('{organizationId}', encodeURIComponent(String(requestParameters['organizationId'])));
        urlPath = urlPath.replace('{projectId}', encodeURIComponent(String(requestParameters['projectId'])));

        return {
            path: urlPath,
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        };
    }

    /**
     * Minimum role: viewer. The sources the answer engines drew on across a project\'s AI answers over a date window, ranked by how often they were cited. Per source: citationCount, the total number of citations; distinctResponseCount and distinctQueryCount, how many captured answers and tracked queries it appeared in; avgPosition, its average 1-based rank inside the answers\' citation lists, where LOWER is better. A null avgPosition means no citation in the window carried a position, not a rank of zero; a null domain or sampleTitle means the citation never carried one. `total` counts the distinct sources matching the window, before paging. The list is UNFILTERED by ownership: the brand\'s, competitors\' and third-party sources sit in the same ranking. Only the AI answer engines (chatgpt, perplexity, google_ai_overview, google_ai_mode) produce citations, so filtering by google_serp or google_shopping is accepted and returns nothing. Citations still behind an answer engine\'s redirect (a google.com/goto link, whose URL names the engine rather than the source) are excluded, so a source cited only through such links is absent from this list rather than counted as zero.
     * Domains and pages the AI answers cited
     */
    async getCitedSourcesRaw(requestParameters: GetCitedSourcesRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<CitedSourcesResponse>> {
        const requestOptions = await this.getCitedSourcesRequestOpts(requestParameters);
        const response = await this.request(requestOptions, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => CitedSourcesResponseFromJSON(jsonValue));
    }

    /**
     * Minimum role: viewer. The sources the answer engines drew on across a project\'s AI answers over a date window, ranked by how often they were cited. Per source: citationCount, the total number of citations; distinctResponseCount and distinctQueryCount, how many captured answers and tracked queries it appeared in; avgPosition, its average 1-based rank inside the answers\' citation lists, where LOWER is better. A null avgPosition means no citation in the window carried a position, not a rank of zero; a null domain or sampleTitle means the citation never carried one. `total` counts the distinct sources matching the window, before paging. The list is UNFILTERED by ownership: the brand\'s, competitors\' and third-party sources sit in the same ranking. Only the AI answer engines (chatgpt, perplexity, google_ai_overview, google_ai_mode) produce citations, so filtering by google_serp or google_shopping is accepted and returns nothing. Citations still behind an answer engine\'s redirect (a google.com/goto link, whose URL names the engine rather than the source) are excluded, so a source cited only through such links is absent from this list rather than counted as zero.
     * Domains and pages the AI answers cited
     */
    async getCitedSources(requestParameters: GetCitedSourcesRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<CitedSourcesResponse> {
        const response = await this.getCitedSourcesRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Creates request options for getClusterBreakdown without sending the request
     */
    async getClusterBreakdownRequestOpts(requestParameters: GetClusterBreakdownRequest): Promise<runtime.RequestOpts> {
        if (requestParameters['organizationId'] == null) {
            throw new runtime.RequiredError(
                'organizationId',
                'Required parameter "organizationId" was null or undefined when calling getClusterBreakdown().'
            );
        }

        if (requestParameters['projectId'] == null) {
            throw new runtime.RequiredError(
                'projectId',
                'Required parameter "projectId" was null or undefined when calling getClusterBreakdown().'
            );
        }

        if (requestParameters['dateFrom'] == null) {
            throw new runtime.RequiredError(
                'dateFrom',
                'Required parameter "dateFrom" was null or undefined when calling getClusterBreakdown().'
            );
        }

        if (requestParameters['dateTo'] == null) {
            throw new runtime.RequiredError(
                'dateTo',
                'Required parameter "dateTo" was null or undefined when calling getClusterBreakdown().'
            );
        }

        const queryParameters: any = {};

        if (requestParameters['dateFrom'] != null) {
            queryParameters['dateFrom'] = (requestParameters['dateFrom'] as any).toISOString().substring(0,10);
        }

        if (requestParameters['dateTo'] != null) {
            queryParameters['dateTo'] = (requestParameters['dateTo'] as any).toISOString().substring(0,10);
        }

        if (requestParameters['engines'] != null) {
            queryParameters['engines'] = requestParameters['engines'];
        }

        if (requestParameters['countries'] != null) {
            queryParameters['countries'] = requestParameters['countries'];
        }

        if (requestParameters['queryClusterIds'] != null) {
            queryParameters['queryClusterIds'] = requestParameters['queryClusterIds'];
        }

        if (requestParameters['includeUngroupedQueries'] != null) {
            queryParameters['includeUngroupedQueries'] = requestParameters['includeUngroupedQueries'];
        }

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("ApiKey", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/api/v1/organizations/{organizationId}/projects/{projectId}/metrics/clusters`;
        urlPath = urlPath.replace('{organizationId}', encodeURIComponent(String(requestParameters['organizationId'])));
        urlPath = urlPath.replace('{projectId}', encodeURIComponent(String(requestParameters['projectId'])));

        return {
            path: urlPath,
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        };
    }

    /**
     * Minimum role: viewer. One row per keyword cluster over a date window, with its tracked query and keyword counts, average positions, rates, share of voice and sentiment split. A row whose clusterId is null is the ungrouped bucket: the tracked queries belonging to no cluster. Position metrics are 1-based and LOWER is better; rates, positivityIndex and shareOfVoice are 0-100 percentages where higher is better. A null metric means nothing was captured for that cluster in the window — it is not a zero, and averaging or charting it as one would misstate the period. dataDirtySince is non-null while a recalculation is pending, meaning the numbers predate the latest configuration change.
     * Rank-tracking metrics per keyword cluster
     */
    async getClusterBreakdownRaw(requestParameters: GetClusterBreakdownRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<ProjectRankTrackingClusterBreakdown>> {
        const requestOptions = await this.getClusterBreakdownRequestOpts(requestParameters);
        const response = await this.request(requestOptions, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => ProjectRankTrackingClusterBreakdownFromJSON(jsonValue));
    }

    /**
     * Minimum role: viewer. One row per keyword cluster over a date window, with its tracked query and keyword counts, average positions, rates, share of voice and sentiment split. A row whose clusterId is null is the ungrouped bucket: the tracked queries belonging to no cluster. Position metrics are 1-based and LOWER is better; rates, positivityIndex and shareOfVoice are 0-100 percentages where higher is better. A null metric means nothing was captured for that cluster in the window — it is not a zero, and averaging or charting it as one would misstate the period. dataDirtySince is non-null while a recalculation is pending, meaning the numbers predate the latest configuration change.
     * Rank-tracking metrics per keyword cluster
     */
    async getClusterBreakdown(requestParameters: GetClusterBreakdownRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<ProjectRankTrackingClusterBreakdown> {
        const response = await this.getClusterBreakdownRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Creates request options for getCompetitorCoOccurrence without sending the request
     */
    async getCompetitorCoOccurrenceRequestOpts(requestParameters: GetCompetitorCoOccurrenceRequest): Promise<runtime.RequestOpts> {
        if (requestParameters['organizationId'] == null) {
            throw new runtime.RequiredError(
                'organizationId',
                'Required parameter "organizationId" was null or undefined when calling getCompetitorCoOccurrence().'
            );
        }

        if (requestParameters['projectId'] == null) {
            throw new runtime.RequiredError(
                'projectId',
                'Required parameter "projectId" was null or undefined when calling getCompetitorCoOccurrence().'
            );
        }

        if (requestParameters['dateFrom'] == null) {
            throw new runtime.RequiredError(
                'dateFrom',
                'Required parameter "dateFrom" was null or undefined when calling getCompetitorCoOccurrence().'
            );
        }

        if (requestParameters['dateTo'] == null) {
            throw new runtime.RequiredError(
                'dateTo',
                'Required parameter "dateTo" was null or undefined when calling getCompetitorCoOccurrence().'
            );
        }

        const queryParameters: any = {};

        if (requestParameters['dateFrom'] != null) {
            queryParameters['dateFrom'] = (requestParameters['dateFrom'] as any).toISOString().substring(0,10);
        }

        if (requestParameters['dateTo'] != null) {
            queryParameters['dateTo'] = (requestParameters['dateTo'] as any).toISOString().substring(0,10);
        }

        if (requestParameters['engines'] != null) {
            queryParameters['engines'] = requestParameters['engines'];
        }

        if (requestParameters['countries'] != null) {
            queryParameters['countries'] = requestParameters['countries'];
        }

        if (requestParameters['competitorId'] != null) {
            queryParameters['competitorId'] = requestParameters['competitorId'];
        }

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("ApiKey", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/api/v1/organizations/{organizationId}/projects/{projectId}/competitors/co-occurrence`;
        urlPath = urlPath.replace('{organizationId}', encodeURIComponent(String(requestParameters['organizationId'])));
        urlPath = urlPath.replace('{projectId}', encodeURIComponent(String(requestParameters['projectId'])));

        return {
            path: urlPath,
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        };
    }

    /**
     * Minimum role: viewer. Restricted to the AI answers where the brand and a competitor are BOTH mentioned, one row per tracked competitor: sharedResponseCount is how many such answers there are, and brandWins / competitorWins / ties split them by who holds the better (lower) best mention position. winRate is the percentage 0-100 of those answers the brand wins; avgOwnPosition and avgCompetitorPosition are the average best mention position each side held, 1-based, so LOWER is better. exampleQueryText and exampleAiResponseId point at one representative shared answer. Every nullable field means \"not known yet\" rather than zero: a null winRate or average position is the absence of a shared answer in the window, not a record of losing. Tracked competitors only.
     * Head-to-head record of the brand against each tracked competitor
     */
    async getCompetitorCoOccurrenceRaw(requestParameters: GetCompetitorCoOccurrenceRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<CompetitorCoOccurrenceResponse>> {
        const requestOptions = await this.getCompetitorCoOccurrenceRequestOpts(requestParameters);
        const response = await this.request(requestOptions, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => CompetitorCoOccurrenceResponseFromJSON(jsonValue));
    }

    /**
     * Minimum role: viewer. Restricted to the AI answers where the brand and a competitor are BOTH mentioned, one row per tracked competitor: sharedResponseCount is how many such answers there are, and brandWins / competitorWins / ties split them by who holds the better (lower) best mention position. winRate is the percentage 0-100 of those answers the brand wins; avgOwnPosition and avgCompetitorPosition are the average best mention position each side held, 1-based, so LOWER is better. exampleQueryText and exampleAiResponseId point at one representative shared answer. Every nullable field means \"not known yet\" rather than zero: a null winRate or average position is the absence of a shared answer in the window, not a record of losing. Tracked competitors only.
     * Head-to-head record of the brand against each tracked competitor
     */
    async getCompetitorCoOccurrence(requestParameters: GetCompetitorCoOccurrenceRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<CompetitorCoOccurrenceResponse> {
        const response = await this.getCompetitorCoOccurrenceRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Creates request options for getMentionMix without sending the request
     */
    async getMentionMixRequestOpts(requestParameters: GetMentionMixRequest): Promise<runtime.RequestOpts> {
        if (requestParameters['organizationId'] == null) {
            throw new runtime.RequiredError(
                'organizationId',
                'Required parameter "organizationId" was null or undefined when calling getMentionMix().'
            );
        }

        if (requestParameters['projectId'] == null) {
            throw new runtime.RequiredError(
                'projectId',
                'Required parameter "projectId" was null or undefined when calling getMentionMix().'
            );
        }

        if (requestParameters['dateFrom'] == null) {
            throw new runtime.RequiredError(
                'dateFrom',
                'Required parameter "dateFrom" was null or undefined when calling getMentionMix().'
            );
        }

        if (requestParameters['dateTo'] == null) {
            throw new runtime.RequiredError(
                'dateTo',
                'Required parameter "dateTo" was null or undefined when calling getMentionMix().'
            );
        }

        const queryParameters: any = {};

        if (requestParameters['dateFrom'] != null) {
            queryParameters['dateFrom'] = (requestParameters['dateFrom'] as any).toISOString().substring(0,10);
        }

        if (requestParameters['dateTo'] != null) {
            queryParameters['dateTo'] = (requestParameters['dateTo'] as any).toISOString().substring(0,10);
        }

        if (requestParameters['engines'] != null) {
            queryParameters['engines'] = requestParameters['engines'];
        }

        if (requestParameters['countries'] != null) {
            queryParameters['countries'] = requestParameters['countries'];
        }

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("ApiKey", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/api/v1/organizations/{organizationId}/projects/{projectId}/mentions/mix`;
        urlPath = urlPath.replace('{organizationId}', encodeURIComponent(String(requestParameters['organizationId'])));
        urlPath = urlPath.replace('{projectId}', encodeURIComponent(String(requestParameters['projectId'])));

        return {
            path: urlPath,
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        };
    }

    /**
     * Minimum role: viewer. Counts of the project brand\'s own text mentions in AI answers over a date window, grouped three ways: byType (recommendation, comparison, listing, example, reference), byTone (positive, neutral, negative) and byQualifier (direct, conditional — a conditional mention is one the answer hedged with a condition). These are the inputs behind the Share of Voice weighted score. Every bucket is always present and is a plain count, never null: a zero means no mention of that kind was found in the window. The three groupings count the same mentions, so each one sums to the same total. Only the project brand is counted, never a competitor, and only mentions inside the answer text — a citation of the brand\'s URL is not a mention here. Only AI answer engines produce mentions, so restricting engines to google_serp or google_shopping alone returns all zeros. For the positive/neutral/negative split per engine and per competitor use the sentiment endpoint.
     * Composition of a project brand mentions in AI answers
     */
    async getMentionMixRaw(requestParameters: GetMentionMixRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<ProjectMentionMixResponse>> {
        const requestOptions = await this.getMentionMixRequestOpts(requestParameters);
        const response = await this.request(requestOptions, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => ProjectMentionMixResponseFromJSON(jsonValue));
    }

    /**
     * Minimum role: viewer. Counts of the project brand\'s own text mentions in AI answers over a date window, grouped three ways: byType (recommendation, comparison, listing, example, reference), byTone (positive, neutral, negative) and byQualifier (direct, conditional — a conditional mention is one the answer hedged with a condition). These are the inputs behind the Share of Voice weighted score. Every bucket is always present and is a plain count, never null: a zero means no mention of that kind was found in the window. The three groupings count the same mentions, so each one sums to the same total. Only the project brand is counted, never a competitor, and only mentions inside the answer text — a citation of the brand\'s URL is not a mention here. Only AI answer engines produce mentions, so restricting engines to google_serp or google_shopping alone returns all zeros. For the positive/neutral/negative split per engine and per competitor use the sentiment endpoint.
     * Composition of a project brand mentions in AI answers
     */
    async getMentionMix(requestParameters: GetMentionMixRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<ProjectMentionMixResponse> {
        const response = await this.getMentionMixRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Creates request options for getMentionSamples without sending the request
     */
    async getMentionSamplesRequestOpts(requestParameters: GetMentionSamplesRequest): Promise<runtime.RequestOpts> {
        if (requestParameters['organizationId'] == null) {
            throw new runtime.RequiredError(
                'organizationId',
                'Required parameter "organizationId" was null or undefined when calling getMentionSamples().'
            );
        }

        if (requestParameters['projectId'] == null) {
            throw new runtime.RequiredError(
                'projectId',
                'Required parameter "projectId" was null or undefined when calling getMentionSamples().'
            );
        }

        if (requestParameters['dateFrom'] == null) {
            throw new runtime.RequiredError(
                'dateFrom',
                'Required parameter "dateFrom" was null or undefined when calling getMentionSamples().'
            );
        }

        if (requestParameters['dateTo'] == null) {
            throw new runtime.RequiredError(
                'dateTo',
                'Required parameter "dateTo" was null or undefined when calling getMentionSamples().'
            );
        }

        const queryParameters: any = {};

        if (requestParameters['dateFrom'] != null) {
            queryParameters['dateFrom'] = (requestParameters['dateFrom'] as any).toISOString().substring(0,10);
        }

        if (requestParameters['dateTo'] != null) {
            queryParameters['dateTo'] = (requestParameters['dateTo'] as any).toISOString().substring(0,10);
        }

        if (requestParameters['engines'] != null) {
            queryParameters['engines'] = requestParameters['engines'];
        }

        if (requestParameters['countries'] != null) {
            queryParameters['countries'] = requestParameters['countries'];
        }

        if (requestParameters['sentiment'] != null) {
            queryParameters['sentiment'] = requestParameters['sentiment'];
        }

        if (requestParameters['mentionType'] != null) {
            queryParameters['mentionType'] = requestParameters['mentionType'];
        }

        if (requestParameters['competitorId'] != null) {
            queryParameters['competitorId'] = requestParameters['competitorId'];
        }

        if (requestParameters['sortBy'] != null) {
            queryParameters['sortBy'] = requestParameters['sortBy'];
        }

        if (requestParameters['limit'] != null) {
            queryParameters['limit'] = requestParameters['limit'];
        }

        if (requestParameters['offset'] != null) {
            queryParameters['offset'] = requestParameters['offset'];
        }

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("ApiKey", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/api/v1/organizations/{organizationId}/projects/{projectId}/mentions`;
        urlPath = urlPath.replace('{organizationId}', encodeURIComponent(String(requestParameters['organizationId'])));
        urlPath = urlPath.replace('{projectId}', encodeURIComponent(String(requestParameters['projectId'])));

        return {
            path: urlPath,
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        };
    }

    /**
     * Minimum role: viewer. A paginated page of the individual mention texts behind the aggregate numbers, for qualitative review and for checking sentiment labels by eye. Each sample carries the mention text, the engine and country it was seen in, the tracked query that produced it, its sentiment and mention type, and mentionPosition — a 1-based rank inside the answer where LOWER is better, always present. `total` counts every mention matching the filters, not the size of the page returned. Only mentions inside the answer text are returned: a citation of the brand URL is not a mention here, and only AI answer engines produce mentions, so restricting engines to google_serp or google_shopping alone returns an empty page rather than an error. Two fields carry a \"not known\" rather than a zero: country is null and queryText is empty when the tracked query behind the mention has since been deleted, and competitorId is null when the mention row stores no competitor id — which is NOT an assertion that the mention is about your own brand, since an untracked competitor also stores none. Read mentionRelation instead: `own` and `tracked-competitor` are what the scraper resolved to a configured entity, `untracked-competitor` is a rival the project does not track, and null means the row predates the field. brandName carries the mentioned brand, and is the only way to name an untracked competitor, which has no competitor id to resolve one from. Omitting the competitorId filter returns exactly the rows with no competitor id stored — that is, own-brand AND untracked-competitor mentions together, not every competitor; pass a competitor UUID to restrict the page to that competitor, and use mentionRelation to separate the rest. For the aggregate positive/neutral/negative split use the sentiment endpoint, and for weighted mention-type counts the mention mix endpoint.
     * Sample of the raw AI mention texts of a project
     */
    async getMentionSamplesRaw(requestParameters: GetMentionSamplesRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<ProjectMentionSamplesResponse>> {
        const requestOptions = await this.getMentionSamplesRequestOpts(requestParameters);
        const response = await this.request(requestOptions, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => ProjectMentionSamplesResponseFromJSON(jsonValue));
    }

    /**
     * Minimum role: viewer. A paginated page of the individual mention texts behind the aggregate numbers, for qualitative review and for checking sentiment labels by eye. Each sample carries the mention text, the engine and country it was seen in, the tracked query that produced it, its sentiment and mention type, and mentionPosition — a 1-based rank inside the answer where LOWER is better, always present. `total` counts every mention matching the filters, not the size of the page returned. Only mentions inside the answer text are returned: a citation of the brand URL is not a mention here, and only AI answer engines produce mentions, so restricting engines to google_serp or google_shopping alone returns an empty page rather than an error. Two fields carry a \"not known\" rather than a zero: country is null and queryText is empty when the tracked query behind the mention has since been deleted, and competitorId is null when the mention row stores no competitor id — which is NOT an assertion that the mention is about your own brand, since an untracked competitor also stores none. Read mentionRelation instead: `own` and `tracked-competitor` are what the scraper resolved to a configured entity, `untracked-competitor` is a rival the project does not track, and null means the row predates the field. brandName carries the mentioned brand, and is the only way to name an untracked competitor, which has no competitor id to resolve one from. Omitting the competitorId filter returns exactly the rows with no competitor id stored — that is, own-brand AND untracked-competitor mentions together, not every competitor; pass a competitor UUID to restrict the page to that competitor, and use mentionRelation to separate the rest. For the aggregate positive/neutral/negative split use the sentiment endpoint, and for weighted mention-type counts the mention mix endpoint.
     * Sample of the raw AI mention texts of a project
     */
    async getMentionSamples(requestParameters: GetMentionSamplesRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<ProjectMentionSamplesResponse> {
        const response = await this.getMentionSamplesRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Creates request options for getMetricGlossary without sending the request
     */
    async getMetricGlossaryRequestOpts(): Promise<runtime.RequestOpts> {
        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("ApiKey", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/api/v1/metric-glossary`;

        return {
            path: urlPath,
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        };
    }

    /**
     * Static reference, no project data. Each entry gives a metric, the everyday words people use for it, its unit and range, whether higher or lower is better, the operation that returns it, and example questions. Useful when turning a vague or non-technical request into the right call.
     * Map everyday wording to a metric and the operation that serves it
     */
    async getMetricGlossaryRaw(initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<GetMetricGlossary200Response>> {
        const requestOptions = await this.getMetricGlossaryRequestOpts();
        const response = await this.request(requestOptions, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => GetMetricGlossary200ResponseFromJSON(jsonValue));
    }

    /**
     * Static reference, no project data. Each entry gives a metric, the everyday words people use for it, its unit and range, whether higher or lower is better, the operation that returns it, and example questions. Useful when turning a vague or non-technical request into the right call.
     * Map everyday wording to a metric and the operation that serves it
     */
    async getMetricGlossary(initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<GetMetricGlossary200Response> {
        const response = await this.getMetricGlossaryRaw(initOverrides);
        return await response.value();
    }

    /**
     * Creates request options for getOrganizationOverview without sending the request
     */
    async getOrganizationOverviewRequestOpts(requestParameters: GetOrganizationOverviewRequest): Promise<runtime.RequestOpts> {
        if (requestParameters['organizationId'] == null) {
            throw new runtime.RequiredError(
                'organizationId',
                'Required parameter "organizationId" was null or undefined when calling getOrganizationOverview().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("ApiKey", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/api/v1/organizations/{organizationId}/overview`;
        urlPath = urlPath.replace('{organizationId}', encodeURIComponent(String(requestParameters['organizationId'])));

        return {
            path: urlPath,
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        };
    }

    /**
     * Minimum role: viewer. One row per active project — share of voice, mention rate, average mention position, positivity index and tracked-query count — ordered by share of voice, plus an organization-level aggregate of the same metrics. Archived projects are excluded. This is a current-state snapshot and takes no date window; for a date-ranged comparison call the per-project operations (getProjectMetrics, getProjectTimeSeries) for each projectId returned here. Every metric is nullable, and a null means the project has no rank data yet — not a score of zero. Higher is better for shareOfVoice, mentionRate and positivityIndex; avgMentionPosition is a 1-based rank, so LOWER is better.
     * Snapshot rank-health board across an organization active projects
     */
    async getOrganizationOverviewRaw(requestParameters: GetOrganizationOverviewRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<GetOrganizationOverview200Response>> {
        const requestOptions = await this.getOrganizationOverviewRequestOpts(requestParameters);
        const response = await this.request(requestOptions, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => GetOrganizationOverview200ResponseFromJSON(jsonValue));
    }

    /**
     * Minimum role: viewer. One row per active project — share of voice, mention rate, average mention position, positivity index and tracked-query count — ordered by share of voice, plus an organization-level aggregate of the same metrics. Archived projects are excluded. This is a current-state snapshot and takes no date window; for a date-ranged comparison call the per-project operations (getProjectMetrics, getProjectTimeSeries) for each projectId returned here. Every metric is nullable, and a null means the project has no rank data yet — not a score of zero. Higher is better for shareOfVoice, mentionRate and positivityIndex; avgMentionPosition is a 1-based rank, so LOWER is better.
     * Snapshot rank-health board across an organization active projects
     */
    async getOrganizationOverview(requestParameters: GetOrganizationOverviewRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<GetOrganizationOverview200Response> {
        const response = await this.getOrganizationOverviewRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Creates request options for getProjectMetrics without sending the request
     */
    async getProjectMetricsRequestOpts(requestParameters: GetProjectMetricsRequest): Promise<runtime.RequestOpts> {
        if (requestParameters['organizationId'] == null) {
            throw new runtime.RequiredError(
                'organizationId',
                'Required parameter "organizationId" was null or undefined when calling getProjectMetrics().'
            );
        }

        if (requestParameters['projectId'] == null) {
            throw new runtime.RequiredError(
                'projectId',
                'Required parameter "projectId" was null or undefined when calling getProjectMetrics().'
            );
        }

        if (requestParameters['dateFrom'] == null) {
            throw new runtime.RequiredError(
                'dateFrom',
                'Required parameter "dateFrom" was null or undefined when calling getProjectMetrics().'
            );
        }

        if (requestParameters['dateTo'] == null) {
            throw new runtime.RequiredError(
                'dateTo',
                'Required parameter "dateTo" was null or undefined when calling getProjectMetrics().'
            );
        }

        const queryParameters: any = {};

        if (requestParameters['dateFrom'] != null) {
            queryParameters['dateFrom'] = (requestParameters['dateFrom'] as any).toISOString().substring(0,10);
        }

        if (requestParameters['dateTo'] != null) {
            queryParameters['dateTo'] = (requestParameters['dateTo'] as any).toISOString().substring(0,10);
        }

        if (requestParameters['engines'] != null) {
            queryParameters['engines'] = requestParameters['engines'];
        }

        if (requestParameters['countries'] != null) {
            queryParameters['countries'] = requestParameters['countries'];
        }

        if (requestParameters['queryClusterIds'] != null) {
            queryParameters['queryClusterIds'] = requestParameters['queryClusterIds'];
        }

        if (requestParameters['includeUngroupedQueries'] != null) {
            queryParameters['includeUngroupedQueries'] = requestParameters['includeUngroupedQueries'];
        }

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("ApiKey", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/api/v1/organizations/{organizationId}/projects/{projectId}/metrics`;
        urlPath = urlPath.replace('{organizationId}', encodeURIComponent(String(requestParameters['organizationId'])));
        urlPath = urlPath.replace('{projectId}', encodeURIComponent(String(requestParameters['projectId'])));

        return {
            path: urlPath,
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        };
    }

    /**
     * Minimum role: viewer. The project overview over a date window: share of voice (own and per competitor), mention / SERP / shopping rates, average and best positions, position stability, the sentiment split and the position-distribution buckets. Positions are 1-based, so a LOWER number is better; rates, the positivity index and share of voice are percentages from 0 to 100, where HIGHER is better. Every `trend*` field is the signed change against the immediately preceding window of the same length: negative means an improved position, positive means an improved rate or score. A null metric means \"not known yet\", never zero: a scalar is null when the window holds no checks at all, and a `trend*` field is null when there is no earlier window to compare against. The counters (`mentionCount`, `*TrackedQueryCount`, `*QueriesWithResult`, `sentiment*` and `mentionTypeCounts`) are genuine zeros instead, so an empty window reads as zero counts with null rates. `dataDirtySince` is non-null while a recalculation is pending, meaning the figures may still move for dates from then on.
     * Headline visibility metrics of a project
     */
    async getProjectMetricsRaw(requestParameters: GetProjectMetricsRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<ProjectRankTrackingStats>> {
        const requestOptions = await this.getProjectMetricsRequestOpts(requestParameters);
        const response = await this.request(requestOptions, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => ProjectRankTrackingStatsFromJSON(jsonValue));
    }

    /**
     * Minimum role: viewer. The project overview over a date window: share of voice (own and per competitor), mention / SERP / shopping rates, average and best positions, position stability, the sentiment split and the position-distribution buckets. Positions are 1-based, so a LOWER number is better; rates, the positivity index and share of voice are percentages from 0 to 100, where HIGHER is better. Every `trend*` field is the signed change against the immediately preceding window of the same length: negative means an improved position, positive means an improved rate or score. A null metric means \"not known yet\", never zero: a scalar is null when the window holds no checks at all, and a `trend*` field is null when there is no earlier window to compare against. The counters (`mentionCount`, `*TrackedQueryCount`, `*QueriesWithResult`, `sentiment*` and `mentionTypeCounts`) are genuine zeros instead, so an empty window reads as zero counts with null rates. `dataDirtySince` is non-null while a recalculation is pending, meaning the figures may still move for dates from then on.
     * Headline visibility metrics of a project
     */
    async getProjectMetrics(requestParameters: GetProjectMetricsRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<ProjectRankTrackingStats> {
        const response = await this.getProjectMetricsRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Creates request options for getProjectSentiment without sending the request
     */
    async getProjectSentimentRequestOpts(requestParameters: GetProjectSentimentRequest): Promise<runtime.RequestOpts> {
        if (requestParameters['organizationId'] == null) {
            throw new runtime.RequiredError(
                'organizationId',
                'Required parameter "organizationId" was null or undefined when calling getProjectSentiment().'
            );
        }

        if (requestParameters['projectId'] == null) {
            throw new runtime.RequiredError(
                'projectId',
                'Required parameter "projectId" was null or undefined when calling getProjectSentiment().'
            );
        }

        if (requestParameters['dateFrom'] == null) {
            throw new runtime.RequiredError(
                'dateFrom',
                'Required parameter "dateFrom" was null or undefined when calling getProjectSentiment().'
            );
        }

        if (requestParameters['dateTo'] == null) {
            throw new runtime.RequiredError(
                'dateTo',
                'Required parameter "dateTo" was null or undefined when calling getProjectSentiment().'
            );
        }

        const queryParameters: any = {};

        if (requestParameters['dateFrom'] != null) {
            queryParameters['dateFrom'] = (requestParameters['dateFrom'] as any).toISOString().substring(0,10);
        }

        if (requestParameters['dateTo'] != null) {
            queryParameters['dateTo'] = (requestParameters['dateTo'] as any).toISOString().substring(0,10);
        }

        if (requestParameters['engines'] != null) {
            queryParameters['engines'] = requestParameters['engines'];
        }

        if (requestParameters['countries'] != null) {
            queryParameters['countries'] = requestParameters['countries'];
        }

        if (requestParameters['queryClusterIds'] != null) {
            queryParameters['queryClusterIds'] = requestParameters['queryClusterIds'];
        }

        if (requestParameters['includeUngroupedQueries'] != null) {
            queryParameters['includeUngroupedQueries'] = requestParameters['includeUngroupedQueries'];
        }

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("ApiKey", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/api/v1/organizations/{organizationId}/projects/{projectId}/sentiment`;
        urlPath = urlPath.replace('{organizationId}', encodeURIComponent(String(requestParameters['organizationId'])));
        urlPath = urlPath.replace('{projectId}', encodeURIComponent(String(requestParameters['projectId'])));

        return {
            path: urlPath,
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        };
    }

    /**
     * Minimum role: viewer. Positive, neutral and negative split of the brand mentions in AI answers over a date window, per engine and per competitor. A null positivityIndex means no mentions were found in the window, which is not the same as a score of zero.
     * Sentiment breakdown of a project brand mentions
     */
    async getProjectSentimentRaw(requestParameters: GetProjectSentimentRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<ProjectSentimentBreakdown>> {
        const requestOptions = await this.getProjectSentimentRequestOpts(requestParameters);
        const response = await this.request(requestOptions, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => ProjectSentimentBreakdownFromJSON(jsonValue));
    }

    /**
     * Minimum role: viewer. Positive, neutral and negative split of the brand mentions in AI answers over a date window, per engine and per competitor. A null positivityIndex means no mentions were found in the window, which is not the same as a score of zero.
     * Sentiment breakdown of a project brand mentions
     */
    async getProjectSentiment(requestParameters: GetProjectSentimentRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<ProjectSentimentBreakdown> {
        const response = await this.getProjectSentimentRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Creates request options for getProjectTimeSeries without sending the request
     */
    async getProjectTimeSeriesRequestOpts(requestParameters: GetProjectTimeSeriesRequest): Promise<runtime.RequestOpts> {
        if (requestParameters['organizationId'] == null) {
            throw new runtime.RequiredError(
                'organizationId',
                'Required parameter "organizationId" was null or undefined when calling getProjectTimeSeries().'
            );
        }

        if (requestParameters['projectId'] == null) {
            throw new runtime.RequiredError(
                'projectId',
                'Required parameter "projectId" was null or undefined when calling getProjectTimeSeries().'
            );
        }

        if (requestParameters['dateFrom'] == null) {
            throw new runtime.RequiredError(
                'dateFrom',
                'Required parameter "dateFrom" was null or undefined when calling getProjectTimeSeries().'
            );
        }

        if (requestParameters['dateTo'] == null) {
            throw new runtime.RequiredError(
                'dateTo',
                'Required parameter "dateTo" was null or undefined when calling getProjectTimeSeries().'
            );
        }

        const queryParameters: any = {};

        if (requestParameters['dateFrom'] != null) {
            queryParameters['dateFrom'] = (requestParameters['dateFrom'] as any).toISOString().substring(0,10);
        }

        if (requestParameters['dateTo'] != null) {
            queryParameters['dateTo'] = (requestParameters['dateTo'] as any).toISOString().substring(0,10);
        }

        if (requestParameters['granularity'] != null) {
            queryParameters['granularity'] = requestParameters['granularity'];
        }

        if (requestParameters['engines'] != null) {
            queryParameters['engines'] = requestParameters['engines'];
        }

        if (requestParameters['countries'] != null) {
            queryParameters['countries'] = requestParameters['countries'];
        }

        if (requestParameters['queryClusterIds'] != null) {
            queryParameters['queryClusterIds'] = requestParameters['queryClusterIds'];
        }

        if (requestParameters['includeUngroupedQueries'] != null) {
            queryParameters['includeUngroupedQueries'] = requestParameters['includeUngroupedQueries'];
        }

        if (requestParameters['competitorIds'] != null) {
            queryParameters['competitorIds'] = requestParameters['competitorIds'];
        }

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("ApiKey", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/api/v1/organizations/{organizationId}/projects/{projectId}/timeseries`;
        urlPath = urlPath.replace('{organizationId}', encodeURIComponent(String(requestParameters['organizationId'])));
        urlPath = urlPath.replace('{projectId}', encodeURIComponent(String(requestParameters['projectId'])));

        return {
            path: urlPath,
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        };
    }

    /**
     * Minimum role: viewer. One point per bucket over the date window, each carrying the brand metrics and one same-shaped entry per requested competitor. Rank metrics (serp, shopping, mention, link) are 1-based averages where LOWER is better; positivity (0-100), shareOfVoice (0-100), mentionRate (0-100) and serpRate (0-100) are scores where higher is better. Every metric is nullable, and a null means no data was collected for that bucket, which is not the same as a value of zero. A project with no tracked queries returns an empty points list. Prefer weekly or monthly granularity over a long window to keep the response compact.
     * Rank-tracking metrics of a project over time
     */
    async getProjectTimeSeriesRaw(requestParameters: GetProjectTimeSeriesRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<ProjectRankTrackingTimeSeries>> {
        const requestOptions = await this.getProjectTimeSeriesRequestOpts(requestParameters);
        const response = await this.request(requestOptions, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => ProjectRankTrackingTimeSeriesFromJSON(jsonValue));
    }

    /**
     * Minimum role: viewer. One point per bucket over the date window, each carrying the brand metrics and one same-shaped entry per requested competitor. Rank metrics (serp, shopping, mention, link) are 1-based averages where LOWER is better; positivity (0-100), shareOfVoice (0-100), mentionRate (0-100) and serpRate (0-100) are scores where higher is better. Every metric is nullable, and a null means no data was collected for that bucket, which is not the same as a value of zero. A project with no tracked queries returns an empty points list. Prefer weekly or monthly granularity over a long window to keep the response compact.
     * Rank-tracking metrics of a project over time
     */
    async getProjectTimeSeries(requestParameters: GetProjectTimeSeriesRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<ProjectRankTrackingTimeSeries> {
        const response = await this.getProjectTimeSeriesRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Creates request options for getQueryMovers without sending the request
     */
    async getQueryMoversRequestOpts(requestParameters: GetQueryMoversRequest): Promise<runtime.RequestOpts> {
        if (requestParameters['organizationId'] == null) {
            throw new runtime.RequiredError(
                'organizationId',
                'Required parameter "organizationId" was null or undefined when calling getQueryMovers().'
            );
        }

        if (requestParameters['projectId'] == null) {
            throw new runtime.RequiredError(
                'projectId',
                'Required parameter "projectId" was null or undefined when calling getQueryMovers().'
            );
        }

        if (requestParameters['dateFrom'] == null) {
            throw new runtime.RequiredError(
                'dateFrom',
                'Required parameter "dateFrom" was null or undefined when calling getQueryMovers().'
            );
        }

        if (requestParameters['dateTo'] == null) {
            throw new runtime.RequiredError(
                'dateTo',
                'Required parameter "dateTo" was null or undefined when calling getQueryMovers().'
            );
        }

        const queryParameters: any = {};

        if (requestParameters['dateFrom'] != null) {
            queryParameters['dateFrom'] = (requestParameters['dateFrom'] as any).toISOString().substring(0,10);
        }

        if (requestParameters['dateTo'] != null) {
            queryParameters['dateTo'] = (requestParameters['dateTo'] as any).toISOString().substring(0,10);
        }

        if (requestParameters['engines'] != null) {
            queryParameters['engines'] = requestParameters['engines'];
        }

        if (requestParameters['countries'] != null) {
            queryParameters['countries'] = requestParameters['countries'];
        }

        if (requestParameters['sortBy'] != null) {
            queryParameters['sortBy'] = requestParameters['sortBy'];
        }

        if (requestParameters['sortOrder'] != null) {
            queryParameters['sortOrder'] = requestParameters['sortOrder'];
        }

        if (requestParameters['limit'] != null) {
            queryParameters['limit'] = requestParameters['limit'];
        }

        if (requestParameters['offset'] != null) {
            queryParameters['offset'] = requestParameters['offset'];
        }

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("ApiKey", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/api/v1/organizations/{organizationId}/projects/{projectId}/metrics/movers`;
        urlPath = urlPath.replace('{organizationId}', encodeURIComponent(String(requestParameters['organizationId'])));
        urlPath = urlPath.replace('{projectId}', encodeURIComponent(String(requestParameters['projectId'])));

        return {
            path: urlPath,
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        };
    }

    /**
     * Minimum role: viewer. One row per tracked query — a single engine plus country — carrying its current metric envelope and the signed change against the immediately preceding window of equal length: a 7-day window is compared with the 7 days before it. Every trend delta is signed so that POSITIVE means improved, including the position trends, where the underlying avgSerpPosition / avgShoppingPosition / avgMentionPosition / avgLinkPosition are 1-based ranks and therefore LOWER is better. shareOfVoice and positivityIndex are percentages from 0 to 100, where HIGHER is better. Sorting applies to the trend keys only: sortOrder=desc gives the top gainers, asc the top losers. Every nullable field means \"not known yet\" rather than zero — a null position or shareOfVoice is a query with no data in the window, and a null positivityIndex or trend is a period with no mentions to score, neither of which is a record of losing ground. total counts the tracked queries the filters match, not the rows on this page. dataDirtySince is a date from which the rank data is being recomputed, or null when nothing is pending; while it is non-null the deltas may still move.
     * Tracked queries ranked by how much a metric moved
     */
    async getQueryMoversRaw(requestParameters: GetQueryMoversRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<TrackedQueryMoversResponse>> {
        const requestOptions = await this.getQueryMoversRequestOpts(requestParameters);
        const response = await this.request(requestOptions, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => TrackedQueryMoversResponseFromJSON(jsonValue));
    }

    /**
     * Minimum role: viewer. One row per tracked query — a single engine plus country — carrying its current metric envelope and the signed change against the immediately preceding window of equal length: a 7-day window is compared with the 7 days before it. Every trend delta is signed so that POSITIVE means improved, including the position trends, where the underlying avgSerpPosition / avgShoppingPosition / avgMentionPosition / avgLinkPosition are 1-based ranks and therefore LOWER is better. shareOfVoice and positivityIndex are percentages from 0 to 100, where HIGHER is better. Sorting applies to the trend keys only: sortOrder=desc gives the top gainers, asc the top losers. Every nullable field means \"not known yet\" rather than zero — a null position or shareOfVoice is a query with no data in the window, and a null positivityIndex or trend is a period with no mentions to score, neither of which is a record of losing ground. total counts the tracked queries the filters match, not the rows on this page. dataDirtySince is a date from which the rank data is being recomputed, or null when nothing is pending; while it is non-null the deltas may still move.
     * Tracked queries ranked by how much a metric moved
     */
    async getQueryMovers(requestParameters: GetQueryMoversRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<TrackedQueryMoversResponse> {
        const response = await this.getQueryMoversRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Creates request options for getShareOfVoiceFormula without sending the request
     */
    async getShareOfVoiceFormulaRequestOpts(requestParameters: GetShareOfVoiceFormulaRequest): Promise<runtime.RequestOpts> {
        if (requestParameters['organizationId'] == null) {
            throw new runtime.RequiredError(
                'organizationId',
                'Required parameter "organizationId" was null or undefined when calling getShareOfVoiceFormula().'
            );
        }

        if (requestParameters['projectId'] == null) {
            throw new runtime.RequiredError(
                'projectId',
                'Required parameter "projectId" was null or undefined when calling getShareOfVoiceFormula().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("ApiKey", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/api/v1/organizations/{organizationId}/projects/{projectId}/metrics/share-of-voice-formula`;
        urlPath = urlPath.replace('{organizationId}', encodeURIComponent(String(requestParameters['organizationId'])));
        urlPath = urlPath.replace('{projectId}', encodeURIComponent(String(requestParameters['projectId'])));

        return {
            path: urlPath,
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        };
    }

    /**
     * Minimum role: viewer. Static reference data, the same for every project: the weights and multipliers that turn individual brand mentions into a Share of Voice score. Each mention is worth `mentionTypeWeights[type] * sentimentMultipliers[tone] * (conditional ? conditionalMultiplier : directMultiplier)`, and a competitor\'s Share of Voice is its share of the summed weights of every brand in the window, as a percentage. mentionTypeWeights is keyed by mention type (recommendation, comparison, listing, example, reference) and sentimentMultipliers by tone (positive, neutral, negative); a negative mention is discounted, not discarded, because it still evidences presence. A conditional mention is one the answer hedged with a condition (\"if you need X\"). Use this to explain a score, not to recompute one: the counts it applies to come from the mention mix endpoint. Every value is always present and is never null.
     * The constants behind the Share of Voice score
     */
    async getShareOfVoiceFormulaRaw(requestParameters: GetShareOfVoiceFormulaRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<GetShareOfVoiceFormula200Response>> {
        const requestOptions = await this.getShareOfVoiceFormulaRequestOpts(requestParameters);
        const response = await this.request(requestOptions, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => GetShareOfVoiceFormula200ResponseFromJSON(jsonValue));
    }

    /**
     * Minimum role: viewer. Static reference data, the same for every project: the weights and multipliers that turn individual brand mentions into a Share of Voice score. Each mention is worth `mentionTypeWeights[type] * sentimentMultipliers[tone] * (conditional ? conditionalMultiplier : directMultiplier)`, and a competitor\'s Share of Voice is its share of the summed weights of every brand in the window, as a percentage. mentionTypeWeights is keyed by mention type (recommendation, comparison, listing, example, reference) and sentimentMultipliers by tone (positive, neutral, negative); a negative mention is discounted, not discarded, because it still evidences presence. A conditional mention is one the answer hedged with a condition (\"if you need X\"). Use this to explain a score, not to recompute one: the counts it applies to come from the mention mix endpoint. Every value is always present and is never null.
     * The constants behind the Share of Voice score
     */
    async getShareOfVoiceFormula(requestParameters: GetShareOfVoiceFormulaRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<GetShareOfVoiceFormula200Response> {
        const response = await this.getShareOfVoiceFormulaRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Creates request options for getTrackedQueryTimeSeries without sending the request
     */
    async getTrackedQueryTimeSeriesRequestOpts(requestParameters: GetTrackedQueryTimeSeriesRequest): Promise<runtime.RequestOpts> {
        if (requestParameters['organizationId'] == null) {
            throw new runtime.RequiredError(
                'organizationId',
                'Required parameter "organizationId" was null or undefined when calling getTrackedQueryTimeSeries().'
            );
        }

        if (requestParameters['projectId'] == null) {
            throw new runtime.RequiredError(
                'projectId',
                'Required parameter "projectId" was null or undefined when calling getTrackedQueryTimeSeries().'
            );
        }

        if (requestParameters['trackedQueryId'] == null) {
            throw new runtime.RequiredError(
                'trackedQueryId',
                'Required parameter "trackedQueryId" was null or undefined when calling getTrackedQueryTimeSeries().'
            );
        }

        if (requestParameters['dateFrom'] == null) {
            throw new runtime.RequiredError(
                'dateFrom',
                'Required parameter "dateFrom" was null or undefined when calling getTrackedQueryTimeSeries().'
            );
        }

        if (requestParameters['dateTo'] == null) {
            throw new runtime.RequiredError(
                'dateTo',
                'Required parameter "dateTo" was null or undefined when calling getTrackedQueryTimeSeries().'
            );
        }

        const queryParameters: any = {};

        if (requestParameters['dateFrom'] != null) {
            queryParameters['dateFrom'] = (requestParameters['dateFrom'] as any).toISOString().substring(0,10);
        }

        if (requestParameters['dateTo'] != null) {
            queryParameters['dateTo'] = (requestParameters['dateTo'] as any).toISOString().substring(0,10);
        }

        if (requestParameters['granularity'] != null) {
            queryParameters['granularity'] = requestParameters['granularity'];
        }

        if (requestParameters['competitorIds'] != null) {
            queryParameters['competitorIds'] = requestParameters['competitorIds'];
        }

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("ApiKey", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/api/v1/organizations/{organizationId}/projects/{projectId}/tracked-queries/{trackedQueryId}/timeseries`;
        urlPath = urlPath.replace('{organizationId}', encodeURIComponent(String(requestParameters['organizationId'])));
        urlPath = urlPath.replace('{projectId}', encodeURIComponent(String(requestParameters['projectId'])));
        urlPath = urlPath.replace('{trackedQueryId}', encodeURIComponent(String(requestParameters['trackedQueryId'])));

        return {
            path: urlPath,
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        };
    }

    /**
     * Minimum role: viewer. One point per bucket over the date window, each carrying the brand metrics and one same-shaped entry per requested competitor. Positions (serp, shopping, mention, link) are 1-based, so a LOWER number is better; positivity, shareOfVoice, mentionRate and serpRate are percentages from 0 to 100, where higher is better. Every metric is nullable, and a null means nothing was captured for that entity in that bucket — it is not a zero: a null shareOfVoice means no measurement, a shareOfVoice of 0 means measured and never mentioned. The tracked query fixes its own engine and country, so no engine or country filter is accepted. A non-null dataDirtySince is a timestamp warning that tracked queries were deleted from the project and historical buckets may still include their contributions until the nightly refresh rebuilds them.
     * Rank-tracking time series of a single tracked query
     */
    async getTrackedQueryTimeSeriesRaw(requestParameters: GetTrackedQueryTimeSeriesRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<TrackedQueryRankTrackingTimeSeries>> {
        const requestOptions = await this.getTrackedQueryTimeSeriesRequestOpts(requestParameters);
        const response = await this.request(requestOptions, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => TrackedQueryRankTrackingTimeSeriesFromJSON(jsonValue));
    }

    /**
     * Minimum role: viewer. One point per bucket over the date window, each carrying the brand metrics and one same-shaped entry per requested competitor. Positions (serp, shopping, mention, link) are 1-based, so a LOWER number is better; positivity, shareOfVoice, mentionRate and serpRate are percentages from 0 to 100, where higher is better. Every metric is nullable, and a null means nothing was captured for that entity in that bucket — it is not a zero: a null shareOfVoice means no measurement, a shareOfVoice of 0 means measured and never mentioned. The tracked query fixes its own engine and country, so no engine or country filter is accepted. A non-null dataDirtySince is a timestamp warning that tracked queries were deleted from the project and historical buckets may still include their contributions until the nightly refresh rebuilds them.
     * Rank-tracking time series of a single tracked query
     */
    async getTrackedQueryTimeSeries(requestParameters: GetTrackedQueryTimeSeriesRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<TrackedQueryRankTrackingTimeSeries> {
        const response = await this.getTrackedQueryTimeSeriesRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Creates request options for getTrackingCoverage without sending the request
     */
    async getTrackingCoverageRequestOpts(requestParameters: GetTrackingCoverageRequest): Promise<runtime.RequestOpts> {
        if (requestParameters['organizationId'] == null) {
            throw new runtime.RequiredError(
                'organizationId',
                'Required parameter "organizationId" was null or undefined when calling getTrackingCoverage().'
            );
        }

        if (requestParameters['projectId'] == null) {
            throw new runtime.RequiredError(
                'projectId',
                'Required parameter "projectId" was null or undefined when calling getTrackingCoverage().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("ApiKey", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/api/v1/organizations/{organizationId}/projects/{projectId}/coverage`;
        urlPath = urlPath.replace('{organizationId}', encodeURIComponent(String(requestParameters['organizationId'])));
        urlPath = urlPath.replace('{projectId}', encodeURIComponent(String(requestParameters['projectId'])));

        return {
            path: urlPath,
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        };
    }

    /**
     * Minimum role: viewer. A current-state snapshot answering \"what is stale or not being tracked\": total is every tracked query on the project, active and paused split it by status, neverChecked counts the active queries that have never run, and overdue counts the active queries whose last check is older than their own check-frequency interval (daily, weekly or monthly). neverChecked and overdue are disjoint — a query that has never run is never also counted as overdue — and both ignore paused queries, which are not expected to be checked at all. sample lists up to 20 of the overdue queries, most stale first; it is a sample of overdue only, so it never contains a never-checked query and is empty when overdue is 0. Takes no date window: every count describes the project as it stands right now.
     * Coverage and staleness of a project tracked queries
     */
    async getTrackingCoverageRaw(requestParameters: GetTrackingCoverageRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<GetTrackingCoverage200Response>> {
        const requestOptions = await this.getTrackingCoverageRequestOpts(requestParameters);
        const response = await this.request(requestOptions, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => GetTrackingCoverage200ResponseFromJSON(jsonValue));
    }

    /**
     * Minimum role: viewer. A current-state snapshot answering \"what is stale or not being tracked\": total is every tracked query on the project, active and paused split it by status, neverChecked counts the active queries that have never run, and overdue counts the active queries whose last check is older than their own check-frequency interval (daily, weekly or monthly). neverChecked and overdue are disjoint — a query that has never run is never also counted as overdue — and both ignore paused queries, which are not expected to be checked at all. sample lists up to 20 of the overdue queries, most stale first; it is a sample of overdue only, so it never contains a never-checked query and is empty when overdue is 0. Takes no date window: every count describes the project as it stands right now.
     * Coverage and staleness of a project tracked queries
     */
    async getTrackingCoverage(requestParameters: GetTrackingCoverageRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<GetTrackingCoverage200Response> {
        const response = await this.getTrackingCoverageRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Creates request options for listKeywordListings without sending the request
     */
    async listKeywordListingsRequestOpts(requestParameters: ListKeywordListingsRequest): Promise<runtime.RequestOpts> {
        if (requestParameters['organizationId'] == null) {
            throw new runtime.RequiredError(
                'organizationId',
                'Required parameter "organizationId" was null or undefined when calling listKeywordListings().'
            );
        }

        if (requestParameters['projectId'] == null) {
            throw new runtime.RequiredError(
                'projectId',
                'Required parameter "projectId" was null or undefined when calling listKeywordListings().'
            );
        }

        if (requestParameters['dateFrom'] == null) {
            throw new runtime.RequiredError(
                'dateFrom',
                'Required parameter "dateFrom" was null or undefined when calling listKeywordListings().'
            );
        }

        if (requestParameters['dateTo'] == null) {
            throw new runtime.RequiredError(
                'dateTo',
                'Required parameter "dateTo" was null or undefined when calling listKeywordListings().'
            );
        }

        const queryParameters: any = {};

        if (requestParameters['dateFrom'] != null) {
            queryParameters['dateFrom'] = (requestParameters['dateFrom'] as any).toISOString().substring(0,10);
        }

        if (requestParameters['dateTo'] != null) {
            queryParameters['dateTo'] = (requestParameters['dateTo'] as any).toISOString().substring(0,10);
        }

        if (requestParameters['engines'] != null) {
            queryParameters['engines'] = requestParameters['engines'];
        }

        if (requestParameters['countries'] != null) {
            queryParameters['countries'] = requestParameters['countries'];
        }

        if (requestParameters['queryClusterIds'] != null) {
            queryParameters['queryClusterIds'] = requestParameters['queryClusterIds'];
        }

        if (requestParameters['includeUngroupedQueries'] != null) {
            queryParameters['includeUngroupedQueries'] = requestParameters['includeUngroupedQueries'];
        }

        if (requestParameters['status'] != null) {
            queryParameters['status'] = requestParameters['status'];
        }

        if (requestParameters['checkFrequencies'] != null) {
            queryParameters['checkFrequencies'] = requestParameters['checkFrequencies'];
        }

        if (requestParameters['nPasses'] != null) {
            queryParameters['nPasses'] = requestParameters['nPasses'];
        }

        if (requestParameters['search'] != null) {
            queryParameters['search'] = requestParameters['search'];
        }

        if (requestParameters['sortBy'] != null) {
            queryParameters['sortBy'] = requestParameters['sortBy'];
        }

        if (requestParameters['sortOrder'] != null) {
            queryParameters['sortOrder'] = requestParameters['sortOrder'];
        }

        if (requestParameters['limit'] != null) {
            queryParameters['limit'] = requestParameters['limit'];
        }

        if (requestParameters['offset'] != null) {
            queryParameters['offset'] = requestParameters['offset'];
        }

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("ApiKey", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/api/v1/organizations/{organizationId}/projects/{projectId}/keyword-listings`;
        urlPath = urlPath.replace('{organizationId}', encodeURIComponent(String(requestParameters['organizationId'])));
        urlPath = urlPath.replace('{projectId}', encodeURIComponent(String(requestParameters['projectId'])));

        return {
            path: urlPath,
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        };
    }

    /**
     * Minimum role: viewer. One row per distinct keyword text of the project — every tracked query asking that text, on any engine in any country, collapsed into a single row whose variantIds name the tracked queries behind it. Each row carries the metrics of the requested window and a signed trend against the window of equal length immediately before it, where POSITIVE ALWAYS MEANS BETTER whichever direction the metric itself runs. Positions are 1-based and lower is better; rates, positivityIndex and shareOfVoice are 0-100 and higher is better; mentionPositionStability is a day-to-day spread, so lower is steadier. A null metric means nothing was captured for that keyword in the window — it is not a zero. `total` counts the KEYWORDS matching the filters, not the rows on this page and not tracked queries; `totalVariantCount` counts the tracked queries behind the keywords matching every filter EXCEPT `search` — with a text search applied it still counts the project\'s variants, so do not size a force-check budget from it on a searched page. NEITHER IS THE PROJECT\'S TRACKED-QUERY COUNT: the count operation reads the write model, while this listing reads projections refreshed in the background from it, so the numbers legitimately differ while those projections catch up. A project whose cached tracked-query count has not been refreshed yet answers an empty page with total 0 — the same answer as a project with no tracked queries at all — so treat an unexpected empty page right after creating queries as \"not projected yet\", not as \"no data\". dataDirtySince is non-null while a recalculation is pending, meaning the metrics predate the latest configuration change. Filters narrow which VARIANTS count towards a row, so engines, countries, statuses, checkFrequencies and nPassesValues each report what the filters selected rather than everything the keyword has. A filter or sort key this endpoint cannot honour is rejected by name, never ignored, and a limit above the maximum is rejected rather than quietly reduced. This operation answers JSON only: a keyword row carries a nested mentionTypeCounts object, so it is not part of the CSV family.
     * List a project\'s keywords with their windowed metrics
     */
    async listKeywordListingsRaw(requestParameters: ListKeywordListingsRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<ListKeywordListings200Response>> {
        const requestOptions = await this.listKeywordListingsRequestOpts(requestParameters);
        const response = await this.request(requestOptions, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => ListKeywordListings200ResponseFromJSON(jsonValue));
    }

    /**
     * Minimum role: viewer. One row per distinct keyword text of the project — every tracked query asking that text, on any engine in any country, collapsed into a single row whose variantIds name the tracked queries behind it. Each row carries the metrics of the requested window and a signed trend against the window of equal length immediately before it, where POSITIVE ALWAYS MEANS BETTER whichever direction the metric itself runs. Positions are 1-based and lower is better; rates, positivityIndex and shareOfVoice are 0-100 and higher is better; mentionPositionStability is a day-to-day spread, so lower is steadier. A null metric means nothing was captured for that keyword in the window — it is not a zero. `total` counts the KEYWORDS matching the filters, not the rows on this page and not tracked queries; `totalVariantCount` counts the tracked queries behind the keywords matching every filter EXCEPT `search` — with a text search applied it still counts the project\'s variants, so do not size a force-check budget from it on a searched page. NEITHER IS THE PROJECT\'S TRACKED-QUERY COUNT: the count operation reads the write model, while this listing reads projections refreshed in the background from it, so the numbers legitimately differ while those projections catch up. A project whose cached tracked-query count has not been refreshed yet answers an empty page with total 0 — the same answer as a project with no tracked queries at all — so treat an unexpected empty page right after creating queries as \"not projected yet\", not as \"no data\". dataDirtySince is non-null while a recalculation is pending, meaning the metrics predate the latest configuration change. Filters narrow which VARIANTS count towards a row, so engines, countries, statuses, checkFrequencies and nPassesValues each report what the filters selected rather than everything the keyword has. A filter or sort key this endpoint cannot honour is rejected by name, never ignored, and a limit above the maximum is rejected rather than quietly reduced. This operation answers JSON only: a keyword row carries a nested mentionTypeCounts object, so it is not part of the CSV family.
     * List a project\'s keywords with their windowed metrics
     */
    async listKeywordListings(requestParameters: ListKeywordListingsRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<ListKeywordListings200Response> {
        const response = await this.listKeywordListingsRaw(requestParameters, initOverrides);
        return await response.value();
    }

}

/**
 * @export
 */
export const GetCitedSourcesEnginesEnum = {
    Chatgpt: 'chatgpt',
    Perplexity: 'perplexity',
    GoogleAiOverview: 'google_ai_overview',
    GoogleAiMode: 'google_ai_mode',
    GoogleSerp: 'google_serp',
    GoogleShopping: 'google_shopping'
} as const;
export type GetCitedSourcesEnginesEnum = typeof GetCitedSourcesEnginesEnum[keyof typeof GetCitedSourcesEnginesEnum];
/**
 * @export
 */
export const GetCitedSourcesGroupByEnum = {
    Domain: 'domain',
    Page: 'page'
} as const;
export type GetCitedSourcesGroupByEnum = typeof GetCitedSourcesGroupByEnum[keyof typeof GetCitedSourcesGroupByEnum];
/**
 * @export
 */
export const GetClusterBreakdownEnginesEnum = {
    Chatgpt: 'chatgpt',
    Perplexity: 'perplexity',
    GoogleAiOverview: 'google_ai_overview',
    GoogleAiMode: 'google_ai_mode',
    GoogleSerp: 'google_serp',
    GoogleShopping: 'google_shopping'
} as const;
export type GetClusterBreakdownEnginesEnum = typeof GetClusterBreakdownEnginesEnum[keyof typeof GetClusterBreakdownEnginesEnum];
/**
 * @export
 */
export const GetCompetitorCoOccurrenceEnginesEnum = {
    Chatgpt: 'chatgpt',
    Perplexity: 'perplexity',
    GoogleAiOverview: 'google_ai_overview',
    GoogleAiMode: 'google_ai_mode',
    GoogleSerp: 'google_serp',
    GoogleShopping: 'google_shopping'
} as const;
export type GetCompetitorCoOccurrenceEnginesEnum = typeof GetCompetitorCoOccurrenceEnginesEnum[keyof typeof GetCompetitorCoOccurrenceEnginesEnum];
/**
 * @export
 */
export const GetMentionMixEnginesEnum = {
    Chatgpt: 'chatgpt',
    Perplexity: 'perplexity',
    GoogleAiOverview: 'google_ai_overview',
    GoogleAiMode: 'google_ai_mode',
    GoogleSerp: 'google_serp',
    GoogleShopping: 'google_shopping'
} as const;
export type GetMentionMixEnginesEnum = typeof GetMentionMixEnginesEnum[keyof typeof GetMentionMixEnginesEnum];
/**
 * @export
 */
export const GetMentionSamplesEnginesEnum = {
    Chatgpt: 'chatgpt',
    Perplexity: 'perplexity',
    GoogleAiOverview: 'google_ai_overview',
    GoogleAiMode: 'google_ai_mode',
    GoogleSerp: 'google_serp',
    GoogleShopping: 'google_shopping'
} as const;
export type GetMentionSamplesEnginesEnum = typeof GetMentionSamplesEnginesEnum[keyof typeof GetMentionSamplesEnginesEnum];
/**
 * @export
 */
export const GetMentionSamplesSentimentEnum = {
    Positive: 'positive',
    Neutral: 'neutral',
    Negative: 'negative'
} as const;
export type GetMentionSamplesSentimentEnum = typeof GetMentionSamplesSentimentEnum[keyof typeof GetMentionSamplesSentimentEnum];
/**
 * @export
 */
export const GetMentionSamplesMentionTypeEnum = {
    Recommendation: 'recommendation',
    Comparison: 'comparison',
    Listing: 'listing',
    Example: 'example',
    Reference: 'reference'
} as const;
export type GetMentionSamplesMentionTypeEnum = typeof GetMentionSamplesMentionTypeEnum[keyof typeof GetMentionSamplesMentionTypeEnum];
/**
 * @export
 */
export const GetMentionSamplesSortByEnum = {
    Recent: 'recent',
    Negative: 'negative',
    Engine: 'engine',
    Country: 'country'
} as const;
export type GetMentionSamplesSortByEnum = typeof GetMentionSamplesSortByEnum[keyof typeof GetMentionSamplesSortByEnum];
/**
 * @export
 */
export const GetProjectMetricsEnginesEnum = {
    Chatgpt: 'chatgpt',
    Perplexity: 'perplexity',
    GoogleAiOverview: 'google_ai_overview',
    GoogleAiMode: 'google_ai_mode',
    GoogleSerp: 'google_serp',
    GoogleShopping: 'google_shopping'
} as const;
export type GetProjectMetricsEnginesEnum = typeof GetProjectMetricsEnginesEnum[keyof typeof GetProjectMetricsEnginesEnum];
/**
 * @export
 */
export const GetProjectSentimentEnginesEnum = {
    Chatgpt: 'chatgpt',
    Perplexity: 'perplexity',
    GoogleAiOverview: 'google_ai_overview',
    GoogleAiMode: 'google_ai_mode',
    GoogleSerp: 'google_serp',
    GoogleShopping: 'google_shopping'
} as const;
export type GetProjectSentimentEnginesEnum = typeof GetProjectSentimentEnginesEnum[keyof typeof GetProjectSentimentEnginesEnum];
/**
 * @export
 */
export const GetProjectTimeSeriesGranularityEnum = {
    Daily: 'daily',
    Weekly: 'weekly',
    Monthly: 'monthly'
} as const;
export type GetProjectTimeSeriesGranularityEnum = typeof GetProjectTimeSeriesGranularityEnum[keyof typeof GetProjectTimeSeriesGranularityEnum];
/**
 * @export
 */
export const GetProjectTimeSeriesEnginesEnum = {
    Chatgpt: 'chatgpt',
    Perplexity: 'perplexity',
    GoogleAiOverview: 'google_ai_overview',
    GoogleAiMode: 'google_ai_mode',
    GoogleSerp: 'google_serp',
    GoogleShopping: 'google_shopping'
} as const;
export type GetProjectTimeSeriesEnginesEnum = typeof GetProjectTimeSeriesEnginesEnum[keyof typeof GetProjectTimeSeriesEnginesEnum];
/**
 * @export
 */
export const GetQueryMoversEnginesEnum = {
    Chatgpt: 'chatgpt',
    Perplexity: 'perplexity',
    GoogleAiOverview: 'google_ai_overview',
    GoogleAiMode: 'google_ai_mode',
    GoogleSerp: 'google_serp',
    GoogleShopping: 'google_shopping'
} as const;
export type GetQueryMoversEnginesEnum = typeof GetQueryMoversEnginesEnum[keyof typeof GetQueryMoversEnginesEnum];
/**
 * @export
 */
export const GetQueryMoversSortByEnum = {
    TrendSerp: 'trend_serp',
    TrendShopping: 'trend_shopping',
    TrendMention: 'trend_mention',
    TrendLink: 'trend_link',
    TrendShareOfVoice: 'trend_share_of_voice'
} as const;
export type GetQueryMoversSortByEnum = typeof GetQueryMoversSortByEnum[keyof typeof GetQueryMoversSortByEnum];
/**
 * @export
 */
export const GetQueryMoversSortOrderEnum = {
    Asc: 'asc',
    Desc: 'desc'
} as const;
export type GetQueryMoversSortOrderEnum = typeof GetQueryMoversSortOrderEnum[keyof typeof GetQueryMoversSortOrderEnum];
/**
 * @export
 */
export const GetTrackedQueryTimeSeriesGranularityEnum = {
    Daily: 'daily',
    Weekly: 'weekly',
    Monthly: 'monthly'
} as const;
export type GetTrackedQueryTimeSeriesGranularityEnum = typeof GetTrackedQueryTimeSeriesGranularityEnum[keyof typeof GetTrackedQueryTimeSeriesGranularityEnum];
/**
 * @export
 */
export const ListKeywordListingsEnginesEnum = {
    Chatgpt: 'chatgpt',
    Perplexity: 'perplexity',
    GoogleAiOverview: 'google_ai_overview',
    GoogleAiMode: 'google_ai_mode',
    GoogleSerp: 'google_serp',
    GoogleShopping: 'google_shopping'
} as const;
export type ListKeywordListingsEnginesEnum = typeof ListKeywordListingsEnginesEnum[keyof typeof ListKeywordListingsEnginesEnum];
/**
 * @export
 */
export const ListKeywordListingsStatusEnum = {
    Active: 'active',
    Paused: 'paused'
} as const;
export type ListKeywordListingsStatusEnum = typeof ListKeywordListingsStatusEnum[keyof typeof ListKeywordListingsStatusEnum];
/**
 * @export
 */
export const ListKeywordListingsCheckFrequenciesEnum = {
    Daily: 'daily',
    Weekly: 'weekly',
    Monthly: 'monthly'
} as const;
export type ListKeywordListingsCheckFrequenciesEnum = typeof ListKeywordListingsCheckFrequenciesEnum[keyof typeof ListKeywordListingsCheckFrequenciesEnum];
/**
 * @export
 */
export const ListKeywordListingsSortByEnum = {
    Keyword: 'keyword',
    VariantCount: 'variantCount',
    LastCheckedAt: 'lastCheckedAt',
    StatusSummary: 'statusSummary',
    PositivityIndex: 'positivityIndex',
    ShareOfVoice: 'shareOfVoice',
    AvgMentionPosition: 'avgMentionPosition',
    AvgLinkPosition: 'avgLinkPosition',
    MentionPositionStability: 'mentionPositionStability',
    AvgSerpPosition: 'avgSerpPosition',
    AvgShoppingPosition: 'avgShoppingPosition'
} as const;
export type ListKeywordListingsSortByEnum = typeof ListKeywordListingsSortByEnum[keyof typeof ListKeywordListingsSortByEnum];
/**
 * @export
 */
export const ListKeywordListingsSortOrderEnum = {
    Asc: 'asc',
    Desc: 'desc'
} as const;
export type ListKeywordListingsSortOrderEnum = typeof ListKeywordListingsSortOrderEnum[keyof typeof ListKeywordListingsSortOrderEnum];

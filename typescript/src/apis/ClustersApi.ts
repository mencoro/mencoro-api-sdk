/* tslint:disable */
/* eslint-disable */
/**
 * Mencoro API
 * Public API for Mencoro customers. Authenticate with an API key: `Authorization: Bearer mencoro_sk_...`.
 *
 * The version of the OpenAPI document: 1.0.0
 * Contact: support@mencoro.com
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */

import * as runtime from '../runtime';
import {
    type AcceptedJobResource,
    AcceptedJobResourceFromJSON,
    AcceptedJobResourceToJSON,
} from '../models/AcceptedJobResource';
import {
    type ApplyClusteringJobOutcome,
    ApplyClusteringJobOutcomeFromJSON,
    ApplyClusteringJobOutcomeToJSON,
} from '../models/ApplyClusteringJobOutcome';
import {
    type BatchCreateQueryClustersOutcome,
    BatchCreateQueryClustersOutcomeFromJSON,
    BatchCreateQueryClustersOutcomeToJSON,
} from '../models/BatchCreateQueryClustersOutcome';
import {
    type BatchCreateQueryClustersRequestData,
    BatchCreateQueryClustersRequestDataFromJSON,
    BatchCreateQueryClustersRequestDataToJSON,
} from '../models/BatchCreateQueryClustersRequestData';
import {
    type CreateQueryCluster409Response,
    CreateQueryCluster409ResponseFromJSON,
    CreateQueryCluster409ResponseToJSON,
} from '../models/CreateQueryCluster409Response';
import {
    type CreateQueryClusterRequest,
    CreateQueryClusterRequestFromJSON,
    CreateQueryClusterRequestToJSON,
} from '../models/CreateQueryClusterRequest';
import {
    type DeleteQueryCluster200Response,
    DeleteQueryCluster200ResponseFromJSON,
    DeleteQueryCluster200ResponseToJSON,
} from '../models/DeleteQueryCluster200Response';
import {
    type QueryClusterResource,
    QueryClusterResourceFromJSON,
    QueryClusterResourceToJSON,
} from '../models/QueryClusterResource';
import {
    type StartClusteringJobRequestData,
    StartClusteringJobRequestDataFromJSON,
    StartClusteringJobRequestDataToJSON,
} from '../models/StartClusteringJobRequestData';

export interface ApplyClusteringJobRequest {
    organizationId: string;
    projectId: string;
    jobId: string;
    idempotencyKey: string;
}

export interface BatchCreateQueryClustersRequest {
    organizationId: string;
    projectId: string;
    idempotencyKey: string;
    batchCreateQueryClustersRequestData: BatchCreateQueryClustersRequestData;
}

export interface CreateQueryClusterOperationRequest {
    organizationId: string;
    projectId: string;
    idempotencyKey: string;
    createQueryClusterRequest: CreateQueryClusterRequest;
}

export interface DeleteQueryClusterRequest {
    organizationId: string;
    projectId: string;
    clusterId: string;
    idempotencyKey: string;
}

export interface RenameQueryClusterRequest {
    organizationId: string;
    projectId: string;
    clusterId: string;
    idempotencyKey: string;
    createQueryClusterRequest: CreateQueryClusterRequest;
}

export interface StartClusteringJobRequest {
    organizationId: string;
    projectId: string;
    idempotencyKey: string;
    startClusteringJobRequestData: StartClusteringJobRequestData;
}

/**
 * ClustersApi - interface
 * 
 * @export
 * @interface ClustersApiInterface
 */
export interface ClustersApiInterface {
    /**
     * Creates request options for applyClusteringJob without sending the request
     * @param {string} organizationId 
     * @param {string} projectId 
     * @param {string} jobId Must be a clustering job started for this organization and project.
     * @param {string} idempotencyKey Repeat it to retry a lost response without applying the job twice.
     * @throws {RequiredError}
     * @memberof ClustersApiInterface
     */
    applyClusteringJobRequestOpts(requestParameters: ApplyClusteringJobRequest): Promise<runtime.RequestOpts>;

    /**
     * Minimum role: manager. Writes a completed clustering job onto the tracked queries it was computed for: it creates the clusters the job proposed that the project does not have yet, then assigns each tracked query according to the merge mode the job was started with — fill_gaps leaves already grouped queries alone, add_on_top only adds, full_regroup replaces a query\'s clusters with the proposed set and therefore REMOVES clusters that are not in it — but only for a tracked query the job actually returned an assignment for. A tracked query the job was started over and produced no assignment for is listed under `unassigned` and left exactly as it was, under every mode; it is not treated as \"belongs to no cluster\" and is never stripped. That matters most with restrictToExistingClusters, where the model is expected to return nothing for queries that fit no existing cluster. The body must be empty: the assignments, the target tracked queries and the mode all come from the job, so this call cannot apply something the job did not produce. Synchronous and partially successful — always 200, with each tracked query under `successful` or `failed`, and nothing rolled back because one failed. A job that is not completed, or one that produced no assignments, is refused with 409. Requires the write capability and an Idempotency-Key header; repeating the key returns the first answer rather than applying twice.
     * @summary Apply the result of a clustering job
     * @param {string} organizationId 
     * @param {string} projectId 
     * @param {string} jobId Must be a clustering job started for this organization and project.
     * @param {string} idempotencyKey Repeat it to retry a lost response without applying the job twice.
     * @param {*} [options] Override http request option.
     * @throws {RequiredError}
     * @memberof ClustersApiInterface
     */
    applyClusteringJobRaw(requestParameters: ApplyClusteringJobRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<ApplyClusteringJobOutcome>>;

    /**
     * Minimum role: manager. Writes a completed clustering job onto the tracked queries it was computed for: it creates the clusters the job proposed that the project does not have yet, then assigns each tracked query according to the merge mode the job was started with — fill_gaps leaves already grouped queries alone, add_on_top only adds, full_regroup replaces a query\'s clusters with the proposed set and therefore REMOVES clusters that are not in it — but only for a tracked query the job actually returned an assignment for. A tracked query the job was started over and produced no assignment for is listed under `unassigned` and left exactly as it was, under every mode; it is not treated as \"belongs to no cluster\" and is never stripped. That matters most with restrictToExistingClusters, where the model is expected to return nothing for queries that fit no existing cluster. The body must be empty: the assignments, the target tracked queries and the mode all come from the job, so this call cannot apply something the job did not produce. Synchronous and partially successful — always 200, with each tracked query under `successful` or `failed`, and nothing rolled back because one failed. A job that is not completed, or one that produced no assignments, is refused with 409. Requires the write capability and an Idempotency-Key header; repeating the key returns the first answer rather than applying twice.
     * Apply the result of a clustering job
     */
    applyClusteringJob(requestParameters: ApplyClusteringJobRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<ApplyClusteringJobOutcome>;

    /**
     * Creates request options for batchCreateQueryClusters without sending the request
     * @param {string} organizationId 
     * @param {string} projectId 
     * @param {string} idempotencyKey Repeat it to retry a lost response without creating the batch twice.
     * @param {BatchCreateQueryClustersRequestData} batchCreateQueryClustersRequestData 
     * @throws {RequiredError}
     * @memberof ClustersApiInterface
     */
    batchCreateQueryClustersRequestOpts(requestParameters: BatchCreateQueryClustersRequest): Promise<runtime.RequestOpts>;

    /**
     * Minimum role: manager. Creates up to 100 empty keyword clusters in one call. Names are trimmed and lower-cased and repeated names in the body are collapsed before anything is written. Partial success: the answer is always 200 and reports every name under `successful` or under `failed` — a name already taken in the project fails with query_cluster_name_already_exists and is NOT resolved to the existing cluster, while the other names are still created. Nothing is rolled back because one name failed. The clusters are created empty; no tracked query is assigned to them. The organization must not be archived; an archived project IS refused with 409, stricter than the Mencoro app, which lets cluster writes into one. Requires the write capability and an Idempotency-Key header.
     * @summary Create several keyword clusters at once
     * @param {string} organizationId 
     * @param {string} projectId 
     * @param {string} idempotencyKey Repeat it to retry a lost response without creating the batch twice.
     * @param {BatchCreateQueryClustersRequestData} batchCreateQueryClustersRequestData 
     * @param {*} [options] Override http request option.
     * @throws {RequiredError}
     * @memberof ClustersApiInterface
     */
    batchCreateQueryClustersRaw(requestParameters: BatchCreateQueryClustersRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<BatchCreateQueryClustersOutcome>>;

    /**
     * Minimum role: manager. Creates up to 100 empty keyword clusters in one call. Names are trimmed and lower-cased and repeated names in the body are collapsed before anything is written. Partial success: the answer is always 200 and reports every name under `successful` or under `failed` — a name already taken in the project fails with query_cluster_name_already_exists and is NOT resolved to the existing cluster, while the other names are still created. Nothing is rolled back because one name failed. The clusters are created empty; no tracked query is assigned to them. The organization must not be archived; an archived project IS refused with 409, stricter than the Mencoro app, which lets cluster writes into one. Requires the write capability and an Idempotency-Key header.
     * Create several keyword clusters at once
     */
    batchCreateQueryClusters(requestParameters: BatchCreateQueryClustersRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<BatchCreateQueryClustersOutcome>;

    /**
     * Creates request options for createQueryCluster without sending the request
     * @param {string} organizationId 
     * @param {string} projectId 
     * @param {string} idempotencyKey A token you choose per operation, 8 to 255 printable ASCII characters with no spaces; a UUID is the obvious choice. Repeat it to retry a lost response without creating a second cluster. The key is scoped to your API key, and it is bound to the method, the path and the body of the first attempt: repeating it with anything else changed is refused with 409 rather than replayed.
     * @param {CreateQueryClusterRequest} createQueryClusterRequest 
     * @throws {RequiredError}
     * @memberof ClustersApiInterface
     */
    createQueryClusterRequestOpts(requestParameters: CreateQueryClusterOperationRequest): Promise<runtime.RequestOpts>;

    /**
     * Minimum role: manager. Creates one empty keyword cluster in the project. The name is trimmed and lower-cased before it is stored, and it must be unique within the project: a name that already exists is refused with 409 and nothing is merged into the existing cluster. The cluster starts with no tracked queries in it — this operation does not assign anything to it. The organization must not be archived; an archived project IS refused with 409, stricter than the Mencoro app, which lets cluster writes into one. Requires the write capability and an Idempotency-Key header.
     * @summary Create a keyword cluster
     * @param {string} organizationId 
     * @param {string} projectId 
     * @param {string} idempotencyKey A token you choose per operation, 8 to 255 printable ASCII characters with no spaces; a UUID is the obvious choice. Repeat it to retry a lost response without creating a second cluster. The key is scoped to your API key, and it is bound to the method, the path and the body of the first attempt: repeating it with anything else changed is refused with 409 rather than replayed.
     * @param {CreateQueryClusterRequest} createQueryClusterRequest 
     * @param {*} [options] Override http request option.
     * @throws {RequiredError}
     * @memberof ClustersApiInterface
     */
    createQueryClusterRaw(requestParameters: CreateQueryClusterOperationRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<QueryClusterResource>>;

    /**
     * Minimum role: manager. Creates one empty keyword cluster in the project. The name is trimmed and lower-cased before it is stored, and it must be unique within the project: a name that already exists is refused with 409 and nothing is merged into the existing cluster. The cluster starts with no tracked queries in it — this operation does not assign anything to it. The organization must not be archived; an archived project IS refused with 409, stricter than the Mencoro app, which lets cluster writes into one. Requires the write capability and an Idempotency-Key header.
     * Create a keyword cluster
     */
    createQueryCluster(requestParameters: CreateQueryClusterOperationRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<QueryClusterResource>;

    /**
     * Creates request options for deleteQueryCluster without sending the request
     * @param {string} organizationId 
     * @param {string} projectId 
     * @param {string} clusterId Must be a cluster of the project in the path.
     * @param {string} idempotencyKey Repeat it to retry a lost response; a fresh key answers 404 once the cluster is gone.
     * @throws {RequiredError}
     * @memberof ClustersApiInterface
     */
    deleteQueryClusterRequestOpts(requestParameters: DeleteQueryClusterRequest): Promise<runtime.RequestOpts>;

    /**
     * Minimum role: manager. Deletes one keyword cluster. The tracked queries that were in it are NOT deleted: they are unassigned from this cluster and keep every other cluster they belong to. Analytics filtered by this cluster id return nothing afterwards, including for dates before the deletion, because that filter reads current membership. This cannot be undone — recreating a cluster with the same name produces a new id and an empty cluster. The organization must not be archived, and neither must the project: an archived project IS refused with 409, stricter than the Mencoro app, which lets cluster writes into one. Requires the write capability and an Idempotency-Key header.
     * @summary Delete a keyword cluster
     * @param {string} organizationId 
     * @param {string} projectId 
     * @param {string} clusterId Must be a cluster of the project in the path.
     * @param {string} idempotencyKey Repeat it to retry a lost response; a fresh key answers 404 once the cluster is gone.
     * @param {*} [options] Override http request option.
     * @throws {RequiredError}
     * @memberof ClustersApiInterface
     */
    deleteQueryClusterRaw(requestParameters: DeleteQueryClusterRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<DeleteQueryCluster200Response>>;

    /**
     * Minimum role: manager. Deletes one keyword cluster. The tracked queries that were in it are NOT deleted: they are unassigned from this cluster and keep every other cluster they belong to. Analytics filtered by this cluster id return nothing afterwards, including for dates before the deletion, because that filter reads current membership. This cannot be undone — recreating a cluster with the same name produces a new id and an empty cluster. The organization must not be archived, and neither must the project: an archived project IS refused with 409, stricter than the Mencoro app, which lets cluster writes into one. Requires the write capability and an Idempotency-Key header.
     * Delete a keyword cluster
     */
    deleteQueryCluster(requestParameters: DeleteQueryClusterRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<DeleteQueryCluster200Response>;

    /**
     * Creates request options for renameQueryCluster without sending the request
     * @param {string} organizationId 
     * @param {string} projectId 
     * @param {string} clusterId Must be a cluster of the project in the path.
     * @param {string} idempotencyKey Repeat it to retry a lost response without renaming twice.
     * @param {CreateQueryClusterRequest} createQueryClusterRequest 
     * @throws {RequiredError}
     * @memberof ClustersApiInterface
     */
    renameQueryClusterRequestOpts(requestParameters: RenameQueryClusterRequest): Promise<runtime.RequestOpts>;

    /**
     * Minimum role: manager. Changes the name of one keyword cluster and nothing else: the tracked queries assigned to it are untouched, and its id does not change, so nothing a client stored breaks. The new name is trimmed and lower-cased and must be unique within the project; a collision is refused with 409. Renaming to the name the cluster already has is accepted and is a no-op. The organization must not be archived; an archived project IS refused with 409, stricter than the Mencoro app, which lets cluster writes into one. Requires the write capability and an Idempotency-Key header.
     * @summary Rename a keyword cluster
     * @param {string} organizationId 
     * @param {string} projectId 
     * @param {string} clusterId Must be a cluster of the project in the path.
     * @param {string} idempotencyKey Repeat it to retry a lost response without renaming twice.
     * @param {CreateQueryClusterRequest} createQueryClusterRequest 
     * @param {*} [options] Override http request option.
     * @throws {RequiredError}
     * @memberof ClustersApiInterface
     */
    renameQueryClusterRaw(requestParameters: RenameQueryClusterRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<QueryClusterResource>>;

    /**
     * Minimum role: manager. Changes the name of one keyword cluster and nothing else: the tracked queries assigned to it are untouched, and its id does not change, so nothing a client stored breaks. The new name is trimmed and lower-cased and must be unique within the project; a collision is refused with 409. Renaming to the name the cluster already has is accepted and is a no-op. The organization must not be archived; an archived project IS refused with 409, stricter than the Mencoro app, which lets cluster writes into one. Requires the write capability and an Idempotency-Key header.
     * Rename a keyword cluster
     */
    renameQueryCluster(requestParameters: RenameQueryClusterRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<QueryClusterResource>;

    /**
     * Creates request options for startClusteringJob without sending the request
     * @param {string} organizationId 
     * @param {string} projectId 
     * @param {string} idempotencyKey Repeat it to retry a lost response without starting a second job.
     * @param {StartClusteringJobRequestData} startClusteringJobRequestData 
     * @throws {RequiredError}
     * @memberof ClustersApiInterface
     */
    startClusteringJobRequestOpts(requestParameters: StartClusteringJobRequest): Promise<runtime.RequestOpts>;

    /**
     * Minimum role: manager. Asks the clustering service to propose keyword clusters for up to 500 tracked queries of the project, and answers 202 with the job to poll — the work runs in the background and a 202 says it was accepted, never that it succeeded. THE JOB WRITES NOTHING: it produces a proposal, and nothing changes until it is applied through the apply operation, which is a separate call. Duplicate query texts within the selection are sent once. Requires an entitled subscription (402 otherwise) and the organization must be active and the project not archived. Rate limited to 10 starts per minute per organization, shared with the same operation in the web application. Requires the write capability and an Idempotency-Key header; repeating the key returns the first job rather than starting a second.
     * @summary Start a keyword clustering job
     * @param {string} organizationId 
     * @param {string} projectId 
     * @param {string} idempotencyKey Repeat it to retry a lost response without starting a second job.
     * @param {StartClusteringJobRequestData} startClusteringJobRequestData 
     * @param {*} [options] Override http request option.
     * @throws {RequiredError}
     * @memberof ClustersApiInterface
     */
    startClusteringJobRaw(requestParameters: StartClusteringJobRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<AcceptedJobResource>>;

    /**
     * Minimum role: manager. Asks the clustering service to propose keyword clusters for up to 500 tracked queries of the project, and answers 202 with the job to poll — the work runs in the background and a 202 says it was accepted, never that it succeeded. THE JOB WRITES NOTHING: it produces a proposal, and nothing changes until it is applied through the apply operation, which is a separate call. Duplicate query texts within the selection are sent once. Requires an entitled subscription (402 otherwise) and the organization must be active and the project not archived. Rate limited to 10 starts per minute per organization, shared with the same operation in the web application. Requires the write capability and an Idempotency-Key header; repeating the key returns the first job rather than starting a second.
     * Start a keyword clustering job
     */
    startClusteringJob(requestParameters: StartClusteringJobRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<AcceptedJobResource>;

}

/**
 * 
 */
export class ClustersApi extends runtime.BaseAPI implements ClustersApiInterface {

    /**
     * Creates request options for applyClusteringJob without sending the request
     */
    async applyClusteringJobRequestOpts(requestParameters: ApplyClusteringJobRequest): Promise<runtime.RequestOpts> {
        if (requestParameters['organizationId'] == null) {
            throw new runtime.RequiredError(
                'organizationId',
                'Required parameter "organizationId" was null or undefined when calling applyClusteringJob().'
            );
        }

        if (requestParameters['projectId'] == null) {
            throw new runtime.RequiredError(
                'projectId',
                'Required parameter "projectId" was null or undefined when calling applyClusteringJob().'
            );
        }

        if (requestParameters['jobId'] == null) {
            throw new runtime.RequiredError(
                'jobId',
                'Required parameter "jobId" was null or undefined when calling applyClusteringJob().'
            );
        }

        if (requestParameters['idempotencyKey'] == null) {
            throw new runtime.RequiredError(
                'idempotencyKey',
                'Required parameter "idempotencyKey" was null or undefined when calling applyClusteringJob().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (requestParameters['idempotencyKey'] != null) {
            headerParameters['Idempotency-Key'] = String(requestParameters['idempotencyKey']);
        }

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("ApiKey", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/api/v1/organizations/{organizationId}/projects/{projectId}/clusters/jobs/{jobId}/apply`;
        urlPath = urlPath.replace('{organizationId}', encodeURIComponent(String(requestParameters['organizationId'])));
        urlPath = urlPath.replace('{projectId}', encodeURIComponent(String(requestParameters['projectId'])));
        urlPath = urlPath.replace('{jobId}', encodeURIComponent(String(requestParameters['jobId'])));

        return {
            path: urlPath,
            method: 'POST',
            headers: headerParameters,
            query: queryParameters,
        };
    }

    /**
     * Minimum role: manager. Writes a completed clustering job onto the tracked queries it was computed for: it creates the clusters the job proposed that the project does not have yet, then assigns each tracked query according to the merge mode the job was started with — fill_gaps leaves already grouped queries alone, add_on_top only adds, full_regroup replaces a query\'s clusters with the proposed set and therefore REMOVES clusters that are not in it — but only for a tracked query the job actually returned an assignment for. A tracked query the job was started over and produced no assignment for is listed under `unassigned` and left exactly as it was, under every mode; it is not treated as \"belongs to no cluster\" and is never stripped. That matters most with restrictToExistingClusters, where the model is expected to return nothing for queries that fit no existing cluster. The body must be empty: the assignments, the target tracked queries and the mode all come from the job, so this call cannot apply something the job did not produce. Synchronous and partially successful — always 200, with each tracked query under `successful` or `failed`, and nothing rolled back because one failed. A job that is not completed, or one that produced no assignments, is refused with 409. Requires the write capability and an Idempotency-Key header; repeating the key returns the first answer rather than applying twice.
     * Apply the result of a clustering job
     */
    async applyClusteringJobRaw(requestParameters: ApplyClusteringJobRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<ApplyClusteringJobOutcome>> {
        const requestOptions = await this.applyClusteringJobRequestOpts(requestParameters);
        const response = await this.request(requestOptions, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => ApplyClusteringJobOutcomeFromJSON(jsonValue));
    }

    /**
     * Minimum role: manager. Writes a completed clustering job onto the tracked queries it was computed for: it creates the clusters the job proposed that the project does not have yet, then assigns each tracked query according to the merge mode the job was started with — fill_gaps leaves already grouped queries alone, add_on_top only adds, full_regroup replaces a query\'s clusters with the proposed set and therefore REMOVES clusters that are not in it — but only for a tracked query the job actually returned an assignment for. A tracked query the job was started over and produced no assignment for is listed under `unassigned` and left exactly as it was, under every mode; it is not treated as \"belongs to no cluster\" and is never stripped. That matters most with restrictToExistingClusters, where the model is expected to return nothing for queries that fit no existing cluster. The body must be empty: the assignments, the target tracked queries and the mode all come from the job, so this call cannot apply something the job did not produce. Synchronous and partially successful — always 200, with each tracked query under `successful` or `failed`, and nothing rolled back because one failed. A job that is not completed, or one that produced no assignments, is refused with 409. Requires the write capability and an Idempotency-Key header; repeating the key returns the first answer rather than applying twice.
     * Apply the result of a clustering job
     */
    async applyClusteringJob(requestParameters: ApplyClusteringJobRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<ApplyClusteringJobOutcome> {
        const response = await this.applyClusteringJobRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Creates request options for batchCreateQueryClusters without sending the request
     */
    async batchCreateQueryClustersRequestOpts(requestParameters: BatchCreateQueryClustersRequest): Promise<runtime.RequestOpts> {
        if (requestParameters['organizationId'] == null) {
            throw new runtime.RequiredError(
                'organizationId',
                'Required parameter "organizationId" was null or undefined when calling batchCreateQueryClusters().'
            );
        }

        if (requestParameters['projectId'] == null) {
            throw new runtime.RequiredError(
                'projectId',
                'Required parameter "projectId" was null or undefined when calling batchCreateQueryClusters().'
            );
        }

        if (requestParameters['idempotencyKey'] == null) {
            throw new runtime.RequiredError(
                'idempotencyKey',
                'Required parameter "idempotencyKey" was null or undefined when calling batchCreateQueryClusters().'
            );
        }

        if (requestParameters['batchCreateQueryClustersRequestData'] == null) {
            throw new runtime.RequiredError(
                'batchCreateQueryClustersRequestData',
                'Required parameter "batchCreateQueryClustersRequestData" was null or undefined when calling batchCreateQueryClusters().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        headerParameters['Content-Type'] = 'application/json';

        if (requestParameters['idempotencyKey'] != null) {
            headerParameters['Idempotency-Key'] = String(requestParameters['idempotencyKey']);
        }

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("ApiKey", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/api/v1/organizations/{organizationId}/projects/{projectId}/clusters/batch`;
        urlPath = urlPath.replace('{organizationId}', encodeURIComponent(String(requestParameters['organizationId'])));
        urlPath = urlPath.replace('{projectId}', encodeURIComponent(String(requestParameters['projectId'])));

        return {
            path: urlPath,
            method: 'POST',
            headers: headerParameters,
            query: queryParameters,
            body: BatchCreateQueryClustersRequestDataToJSON(requestParameters['batchCreateQueryClustersRequestData']),
        };
    }

    /**
     * Minimum role: manager. Creates up to 100 empty keyword clusters in one call. Names are trimmed and lower-cased and repeated names in the body are collapsed before anything is written. Partial success: the answer is always 200 and reports every name under `successful` or under `failed` — a name already taken in the project fails with query_cluster_name_already_exists and is NOT resolved to the existing cluster, while the other names are still created. Nothing is rolled back because one name failed. The clusters are created empty; no tracked query is assigned to them. The organization must not be archived; an archived project IS refused with 409, stricter than the Mencoro app, which lets cluster writes into one. Requires the write capability and an Idempotency-Key header.
     * Create several keyword clusters at once
     */
    async batchCreateQueryClustersRaw(requestParameters: BatchCreateQueryClustersRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<BatchCreateQueryClustersOutcome>> {
        const requestOptions = await this.batchCreateQueryClustersRequestOpts(requestParameters);
        const response = await this.request(requestOptions, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => BatchCreateQueryClustersOutcomeFromJSON(jsonValue));
    }

    /**
     * Minimum role: manager. Creates up to 100 empty keyword clusters in one call. Names are trimmed and lower-cased and repeated names in the body are collapsed before anything is written. Partial success: the answer is always 200 and reports every name under `successful` or under `failed` — a name already taken in the project fails with query_cluster_name_already_exists and is NOT resolved to the existing cluster, while the other names are still created. Nothing is rolled back because one name failed. The clusters are created empty; no tracked query is assigned to them. The organization must not be archived; an archived project IS refused with 409, stricter than the Mencoro app, which lets cluster writes into one. Requires the write capability and an Idempotency-Key header.
     * Create several keyword clusters at once
     */
    async batchCreateQueryClusters(requestParameters: BatchCreateQueryClustersRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<BatchCreateQueryClustersOutcome> {
        const response = await this.batchCreateQueryClustersRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Creates request options for createQueryCluster without sending the request
     */
    async createQueryClusterRequestOpts(requestParameters: CreateQueryClusterOperationRequest): Promise<runtime.RequestOpts> {
        if (requestParameters['organizationId'] == null) {
            throw new runtime.RequiredError(
                'organizationId',
                'Required parameter "organizationId" was null or undefined when calling createQueryCluster().'
            );
        }

        if (requestParameters['projectId'] == null) {
            throw new runtime.RequiredError(
                'projectId',
                'Required parameter "projectId" was null or undefined when calling createQueryCluster().'
            );
        }

        if (requestParameters['idempotencyKey'] == null) {
            throw new runtime.RequiredError(
                'idempotencyKey',
                'Required parameter "idempotencyKey" was null or undefined when calling createQueryCluster().'
            );
        }

        if (requestParameters['createQueryClusterRequest'] == null) {
            throw new runtime.RequiredError(
                'createQueryClusterRequest',
                'Required parameter "createQueryClusterRequest" was null or undefined when calling createQueryCluster().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        headerParameters['Content-Type'] = 'application/json';

        if (requestParameters['idempotencyKey'] != null) {
            headerParameters['Idempotency-Key'] = String(requestParameters['idempotencyKey']);
        }

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("ApiKey", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/api/v1/organizations/{organizationId}/projects/{projectId}/clusters`;
        urlPath = urlPath.replace('{organizationId}', encodeURIComponent(String(requestParameters['organizationId'])));
        urlPath = urlPath.replace('{projectId}', encodeURIComponent(String(requestParameters['projectId'])));

        return {
            path: urlPath,
            method: 'POST',
            headers: headerParameters,
            query: queryParameters,
            body: CreateQueryClusterRequestToJSON(requestParameters['createQueryClusterRequest']),
        };
    }

    /**
     * Minimum role: manager. Creates one empty keyword cluster in the project. The name is trimmed and lower-cased before it is stored, and it must be unique within the project: a name that already exists is refused with 409 and nothing is merged into the existing cluster. The cluster starts with no tracked queries in it — this operation does not assign anything to it. The organization must not be archived; an archived project IS refused with 409, stricter than the Mencoro app, which lets cluster writes into one. Requires the write capability and an Idempotency-Key header.
     * Create a keyword cluster
     */
    async createQueryClusterRaw(requestParameters: CreateQueryClusterOperationRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<QueryClusterResource>> {
        const requestOptions = await this.createQueryClusterRequestOpts(requestParameters);
        const response = await this.request(requestOptions, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => QueryClusterResourceFromJSON(jsonValue));
    }

    /**
     * Minimum role: manager. Creates one empty keyword cluster in the project. The name is trimmed and lower-cased before it is stored, and it must be unique within the project: a name that already exists is refused with 409 and nothing is merged into the existing cluster. The cluster starts with no tracked queries in it — this operation does not assign anything to it. The organization must not be archived; an archived project IS refused with 409, stricter than the Mencoro app, which lets cluster writes into one. Requires the write capability and an Idempotency-Key header.
     * Create a keyword cluster
     */
    async createQueryCluster(requestParameters: CreateQueryClusterOperationRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<QueryClusterResource> {
        const response = await this.createQueryClusterRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Creates request options for deleteQueryCluster without sending the request
     */
    async deleteQueryClusterRequestOpts(requestParameters: DeleteQueryClusterRequest): Promise<runtime.RequestOpts> {
        if (requestParameters['organizationId'] == null) {
            throw new runtime.RequiredError(
                'organizationId',
                'Required parameter "organizationId" was null or undefined when calling deleteQueryCluster().'
            );
        }

        if (requestParameters['projectId'] == null) {
            throw new runtime.RequiredError(
                'projectId',
                'Required parameter "projectId" was null or undefined when calling deleteQueryCluster().'
            );
        }

        if (requestParameters['clusterId'] == null) {
            throw new runtime.RequiredError(
                'clusterId',
                'Required parameter "clusterId" was null or undefined when calling deleteQueryCluster().'
            );
        }

        if (requestParameters['idempotencyKey'] == null) {
            throw new runtime.RequiredError(
                'idempotencyKey',
                'Required parameter "idempotencyKey" was null or undefined when calling deleteQueryCluster().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (requestParameters['idempotencyKey'] != null) {
            headerParameters['Idempotency-Key'] = String(requestParameters['idempotencyKey']);
        }

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("ApiKey", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/api/v1/organizations/{organizationId}/projects/{projectId}/clusters/{clusterId}`;
        urlPath = urlPath.replace('{organizationId}', encodeURIComponent(String(requestParameters['organizationId'])));
        urlPath = urlPath.replace('{projectId}', encodeURIComponent(String(requestParameters['projectId'])));
        urlPath = urlPath.replace('{clusterId}', encodeURIComponent(String(requestParameters['clusterId'])));

        return {
            path: urlPath,
            method: 'DELETE',
            headers: headerParameters,
            query: queryParameters,
        };
    }

    /**
     * Minimum role: manager. Deletes one keyword cluster. The tracked queries that were in it are NOT deleted: they are unassigned from this cluster and keep every other cluster they belong to. Analytics filtered by this cluster id return nothing afterwards, including for dates before the deletion, because that filter reads current membership. This cannot be undone — recreating a cluster with the same name produces a new id and an empty cluster. The organization must not be archived, and neither must the project: an archived project IS refused with 409, stricter than the Mencoro app, which lets cluster writes into one. Requires the write capability and an Idempotency-Key header.
     * Delete a keyword cluster
     */
    async deleteQueryClusterRaw(requestParameters: DeleteQueryClusterRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<DeleteQueryCluster200Response>> {
        const requestOptions = await this.deleteQueryClusterRequestOpts(requestParameters);
        const response = await this.request(requestOptions, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => DeleteQueryCluster200ResponseFromJSON(jsonValue));
    }

    /**
     * Minimum role: manager. Deletes one keyword cluster. The tracked queries that were in it are NOT deleted: they are unassigned from this cluster and keep every other cluster they belong to. Analytics filtered by this cluster id return nothing afterwards, including for dates before the deletion, because that filter reads current membership. This cannot be undone — recreating a cluster with the same name produces a new id and an empty cluster. The organization must not be archived, and neither must the project: an archived project IS refused with 409, stricter than the Mencoro app, which lets cluster writes into one. Requires the write capability and an Idempotency-Key header.
     * Delete a keyword cluster
     */
    async deleteQueryCluster(requestParameters: DeleteQueryClusterRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<DeleteQueryCluster200Response> {
        const response = await this.deleteQueryClusterRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Creates request options for renameQueryCluster without sending the request
     */
    async renameQueryClusterRequestOpts(requestParameters: RenameQueryClusterRequest): Promise<runtime.RequestOpts> {
        if (requestParameters['organizationId'] == null) {
            throw new runtime.RequiredError(
                'organizationId',
                'Required parameter "organizationId" was null or undefined when calling renameQueryCluster().'
            );
        }

        if (requestParameters['projectId'] == null) {
            throw new runtime.RequiredError(
                'projectId',
                'Required parameter "projectId" was null or undefined when calling renameQueryCluster().'
            );
        }

        if (requestParameters['clusterId'] == null) {
            throw new runtime.RequiredError(
                'clusterId',
                'Required parameter "clusterId" was null or undefined when calling renameQueryCluster().'
            );
        }

        if (requestParameters['idempotencyKey'] == null) {
            throw new runtime.RequiredError(
                'idempotencyKey',
                'Required parameter "idempotencyKey" was null or undefined when calling renameQueryCluster().'
            );
        }

        if (requestParameters['createQueryClusterRequest'] == null) {
            throw new runtime.RequiredError(
                'createQueryClusterRequest',
                'Required parameter "createQueryClusterRequest" was null or undefined when calling renameQueryCluster().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        headerParameters['Content-Type'] = 'application/json';

        if (requestParameters['idempotencyKey'] != null) {
            headerParameters['Idempotency-Key'] = String(requestParameters['idempotencyKey']);
        }

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("ApiKey", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/api/v1/organizations/{organizationId}/projects/{projectId}/clusters/{clusterId}`;
        urlPath = urlPath.replace('{organizationId}', encodeURIComponent(String(requestParameters['organizationId'])));
        urlPath = urlPath.replace('{projectId}', encodeURIComponent(String(requestParameters['projectId'])));
        urlPath = urlPath.replace('{clusterId}', encodeURIComponent(String(requestParameters['clusterId'])));

        return {
            path: urlPath,
            method: 'PATCH',
            headers: headerParameters,
            query: queryParameters,
            body: CreateQueryClusterRequestToJSON(requestParameters['createQueryClusterRequest']),
        };
    }

    /**
     * Minimum role: manager. Changes the name of one keyword cluster and nothing else: the tracked queries assigned to it are untouched, and its id does not change, so nothing a client stored breaks. The new name is trimmed and lower-cased and must be unique within the project; a collision is refused with 409. Renaming to the name the cluster already has is accepted and is a no-op. The organization must not be archived; an archived project IS refused with 409, stricter than the Mencoro app, which lets cluster writes into one. Requires the write capability and an Idempotency-Key header.
     * Rename a keyword cluster
     */
    async renameQueryClusterRaw(requestParameters: RenameQueryClusterRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<QueryClusterResource>> {
        const requestOptions = await this.renameQueryClusterRequestOpts(requestParameters);
        const response = await this.request(requestOptions, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => QueryClusterResourceFromJSON(jsonValue));
    }

    /**
     * Minimum role: manager. Changes the name of one keyword cluster and nothing else: the tracked queries assigned to it are untouched, and its id does not change, so nothing a client stored breaks. The new name is trimmed and lower-cased and must be unique within the project; a collision is refused with 409. Renaming to the name the cluster already has is accepted and is a no-op. The organization must not be archived; an archived project IS refused with 409, stricter than the Mencoro app, which lets cluster writes into one. Requires the write capability and an Idempotency-Key header.
     * Rename a keyword cluster
     */
    async renameQueryCluster(requestParameters: RenameQueryClusterRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<QueryClusterResource> {
        const response = await this.renameQueryClusterRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Creates request options for startClusteringJob without sending the request
     */
    async startClusteringJobRequestOpts(requestParameters: StartClusteringJobRequest): Promise<runtime.RequestOpts> {
        if (requestParameters['organizationId'] == null) {
            throw new runtime.RequiredError(
                'organizationId',
                'Required parameter "organizationId" was null or undefined when calling startClusteringJob().'
            );
        }

        if (requestParameters['projectId'] == null) {
            throw new runtime.RequiredError(
                'projectId',
                'Required parameter "projectId" was null or undefined when calling startClusteringJob().'
            );
        }

        if (requestParameters['idempotencyKey'] == null) {
            throw new runtime.RequiredError(
                'idempotencyKey',
                'Required parameter "idempotencyKey" was null or undefined when calling startClusteringJob().'
            );
        }

        if (requestParameters['startClusteringJobRequestData'] == null) {
            throw new runtime.RequiredError(
                'startClusteringJobRequestData',
                'Required parameter "startClusteringJobRequestData" was null or undefined when calling startClusteringJob().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        headerParameters['Content-Type'] = 'application/json';

        if (requestParameters['idempotencyKey'] != null) {
            headerParameters['Idempotency-Key'] = String(requestParameters['idempotencyKey']);
        }

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("ApiKey", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/api/v1/organizations/{organizationId}/projects/{projectId}/clusters/jobs`;
        urlPath = urlPath.replace('{organizationId}', encodeURIComponent(String(requestParameters['organizationId'])));
        urlPath = urlPath.replace('{projectId}', encodeURIComponent(String(requestParameters['projectId'])));

        return {
            path: urlPath,
            method: 'POST',
            headers: headerParameters,
            query: queryParameters,
            body: StartClusteringJobRequestDataToJSON(requestParameters['startClusteringJobRequestData']),
        };
    }

    /**
     * Minimum role: manager. Asks the clustering service to propose keyword clusters for up to 500 tracked queries of the project, and answers 202 with the job to poll — the work runs in the background and a 202 says it was accepted, never that it succeeded. THE JOB WRITES NOTHING: it produces a proposal, and nothing changes until it is applied through the apply operation, which is a separate call. Duplicate query texts within the selection are sent once. Requires an entitled subscription (402 otherwise) and the organization must be active and the project not archived. Rate limited to 10 starts per minute per organization, shared with the same operation in the web application. Requires the write capability and an Idempotency-Key header; repeating the key returns the first job rather than starting a second.
     * Start a keyword clustering job
     */
    async startClusteringJobRaw(requestParameters: StartClusteringJobRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<AcceptedJobResource>> {
        const requestOptions = await this.startClusteringJobRequestOpts(requestParameters);
        const response = await this.request(requestOptions, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => AcceptedJobResourceFromJSON(jsonValue));
    }

    /**
     * Minimum role: manager. Asks the clustering service to propose keyword clusters for up to 500 tracked queries of the project, and answers 202 with the job to poll — the work runs in the background and a 202 says it was accepted, never that it succeeded. THE JOB WRITES NOTHING: it produces a proposal, and nothing changes until it is applied through the apply operation, which is a separate call. Duplicate query texts within the selection are sent once. Requires an entitled subscription (402 otherwise) and the organization must be active and the project not archived. Rate limited to 10 starts per minute per organization, shared with the same operation in the web application. Requires the write capability and an Idempotency-Key header; repeating the key returns the first job rather than starting a second.
     * Start a keyword clustering job
     */
    async startClusteringJob(requestParameters: StartClusteringJobRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<AcceptedJobResource> {
        const response = await this.startClusteringJobRaw(requestParameters, initOverrides);
        return await response.value();
    }

}

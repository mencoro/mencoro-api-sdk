/* tslint:disable */
/* eslint-disable */
/**
 * Mencoro API
 * Public API for Mencoro customers. Authenticate with an API key: `Authorization: Bearer mencoro_sk_...`.
 *
 * The version of the OpenAPI document: 1.0.0
 * Contact: support@mencoro.com
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */

import * as runtime from '../runtime';
import {
    type AiResponseReportResource,
    AiResponseReportResourceFromJSON,
    AiResponseReportResourceToJSON,
} from '../models/AiResponseReportResource';
import {
    type BatchChangeTrackedQueryCheckFrequencyRequestData,
    BatchChangeTrackedQueryCheckFrequencyRequestDataFromJSON,
    BatchChangeTrackedQueryCheckFrequencyRequestDataToJSON,
} from '../models/BatchChangeTrackedQueryCheckFrequencyRequestData';
import {
    type BatchChangeTrackedQueryPassesRequestData,
    BatchChangeTrackedQueryPassesRequestDataFromJSON,
    BatchChangeTrackedQueryPassesRequestDataToJSON,
} from '../models/BatchChangeTrackedQueryPassesRequestData';
import {
    type BatchCreateTrackedQueriesRequestData,
    BatchCreateTrackedQueriesRequestDataFromJSON,
    BatchCreateTrackedQueriesRequestDataToJSON,
} from '../models/BatchCreateTrackedQueriesRequestData';
import {
    type BatchCreateTrackedQueriesResultResource,
    BatchCreateTrackedQueriesResultResourceFromJSON,
    BatchCreateTrackedQueriesResultResourceToJSON,
} from '../models/BatchCreateTrackedQueriesResultResource';
import {
    type BatchForceCheckTrackedQueries200Response,
    BatchForceCheckTrackedQueries200ResponseFromJSON,
    BatchForceCheckTrackedQueries200ResponseToJSON,
} from '../models/BatchForceCheckTrackedQueries200Response';
import {
    type BatchPauseTrackedQueries200Response,
    BatchPauseTrackedQueries200ResponseFromJSON,
    BatchPauseTrackedQueries200ResponseToJSON,
} from '../models/BatchPauseTrackedQueries200Response';
import {
    type BatchResumeTrackedQueries200Response,
    BatchResumeTrackedQueries200ResponseFromJSON,
    BatchResumeTrackedQueries200ResponseToJSON,
} from '../models/BatchResumeTrackedQueries200Response';
import {
    type BatchTargetsRequestData,
    BatchTargetsRequestDataFromJSON,
    BatchTargetsRequestDataToJSON,
} from '../models/BatchTargetsRequestData';
import {
    type BatchWriteOutcome,
    BatchWriteOutcomeFromJSON,
    BatchWriteOutcomeToJSON,
} from '../models/BatchWriteOutcome';
import {
    type BulkAddClustersToTrackedQueries200Response,
    BulkAddClustersToTrackedQueries200ResponseFromJSON,
    BulkAddClustersToTrackedQueries200ResponseToJSON,
} from '../models/BulkAddClustersToTrackedQueries200Response';
import {
    type BulkAddClustersToTrackedQueriesRequest,
    BulkAddClustersToTrackedQueriesRequestFromJSON,
    BulkAddClustersToTrackedQueriesRequestToJSON,
} from '../models/BulkAddClustersToTrackedQueriesRequest';
import {
    type BulkRemoveClustersFromTrackedQueries200Response,
    BulkRemoveClustersFromTrackedQueries200ResponseFromJSON,
    BulkRemoveClustersFromTrackedQueries200ResponseToJSON,
} from '../models/BulkRemoveClustersFromTrackedQueries200Response';
import {
    type BulkRemoveClustersFromTrackedQueriesRequest,
    BulkRemoveClustersFromTrackedQueriesRequestFromJSON,
    BulkRemoveClustersFromTrackedQueriesRequestToJSON,
} from '../models/BulkRemoveClustersFromTrackedQueriesRequest';
import {
    type ChangeTrackedQueryCheckFrequencyRequestData,
    ChangeTrackedQueryCheckFrequencyRequestDataFromJSON,
    ChangeTrackedQueryCheckFrequencyRequestDataToJSON,
} from '../models/ChangeTrackedQueryCheckFrequencyRequestData';
import {
    type ChangeTrackedQueryPassesRequestData,
    ChangeTrackedQueryPassesRequestDataFromJSON,
    ChangeTrackedQueryPassesRequestDataToJSON,
} from '../models/ChangeTrackedQueryPassesRequestData';
import {
    type ClusterMembershipRequestData,
    ClusterMembershipRequestDataFromJSON,
    ClusterMembershipRequestDataToJSON,
} from '../models/ClusterMembershipRequestData';
import {
    type ReportAiResponseRequest,
    ReportAiResponseRequestFromJSON,
    ReportAiResponseRequestToJSON,
} from '../models/ReportAiResponseRequest';
import {
    type SearchTrackedQueries200Response,
    SearchTrackedQueries200ResponseFromJSON,
    SearchTrackedQueries200ResponseToJSON,
} from '../models/SearchTrackedQueries200Response';
import {
    type SubmittedChecksResource,
    SubmittedChecksResourceFromJSON,
    SubmittedChecksResourceToJSON,
} from '../models/SubmittedChecksResource';
import {
    type TrackedQueryCountResource,
    TrackedQueryCountResourceFromJSON,
    TrackedQueryCountResourceToJSON,
} from '../models/TrackedQueryCountResource';
import {
    type TrackedQueryDetailResource,
    TrackedQueryDetailResourceFromJSON,
    TrackedQueryDetailResourceToJSON,
} from '../models/TrackedQueryDetailResource';

export interface AddClustersToTrackedQueryRequest {
    organizationId: string;
    projectId: string;
    trackedQueryId: string;
    idempotencyKey: string;
    clusterMembershipRequestData: ClusterMembershipRequestData;
}

export interface BatchChangeTrackedQueriesCheckFrequencyRequest {
    organizationId: string;
    projectId: string;
    idempotencyKey: string;
    batchChangeTrackedQueryCheckFrequencyRequestData: BatchChangeTrackedQueryCheckFrequencyRequestData;
}

export interface BatchChangeTrackedQueriesNPassesRequest {
    organizationId: string;
    projectId: string;
    idempotencyKey: string;
    batchChangeTrackedQueryPassesRequestData: BatchChangeTrackedQueryPassesRequestData;
}

export interface BatchCreateTrackedQueriesRequest {
    organizationId: string;
    projectId: string;
    idempotencyKey: string;
    batchCreateTrackedQueriesRequestData: BatchCreateTrackedQueriesRequestData;
}

export interface BatchForceCheckTrackedQueriesRequest {
    organizationId: string;
    projectId: string;
    idempotencyKey: string;
    batchTargetsRequestData: BatchTargetsRequestData;
}

export interface BatchPauseTrackedQueriesRequest {
    organizationId: string;
    projectId: string;
    idempotencyKey: string;
    batchTargetsRequestData: BatchTargetsRequestData;
}

export interface BatchResumeTrackedQueriesRequest {
    organizationId: string;
    projectId: string;
    idempotencyKey: string;
    batchTargetsRequestData: BatchTargetsRequestData;
}

export interface BulkAddClustersToTrackedQueriesOperationRequest {
    organizationId: string;
    projectId: string;
    idempotencyKey: string;
    bulkAddClustersToTrackedQueriesRequest: BulkAddClustersToTrackedQueriesRequest;
}

export interface BulkDeleteTrackedQueriesRequest {
    organizationId: string;
    projectId: string;
    idempotencyKey: string;
    batchTargetsRequestData: BatchTargetsRequestData;
}

export interface BulkRemoveClustersFromTrackedQueriesOperationRequest {
    organizationId: string;
    projectId: string;
    idempotencyKey: string;
    bulkRemoveClustersFromTrackedQueriesRequest: BulkRemoveClustersFromTrackedQueriesRequest;
}

export interface ChangeTrackedQueryCheckFrequencyRequest {
    organizationId: string;
    projectId: string;
    trackedQueryId: string;
    idempotencyKey: string;
    changeTrackedQueryCheckFrequencyRequestData: ChangeTrackedQueryCheckFrequencyRequestData;
}

export interface ChangeTrackedQueryNPassesRequest {
    organizationId: string;
    projectId: string;
    trackedQueryId: string;
    idempotencyKey: string;
    changeTrackedQueryPassesRequestData: ChangeTrackedQueryPassesRequestData;
}

export interface CountTrackedQueriesRequest {
    organizationId: string;
    projectId: string;
    status?: CountTrackedQueriesStatusEnum;
}

export interface ForceCheckAllActiveTrackedQueriesRequest {
    organizationId: string;
    projectId: string;
    idempotencyKey: string;
}

export interface GetTrackedQueryRequest {
    organizationId: string;
    projectId: string;
    trackedQueryId: string;
}

export interface PauseTrackedQueryRequest {
    organizationId: string;
    projectId: string;
    trackedQueryId: string;
    idempotencyKey: string;
}

export interface RemoveClustersFromTrackedQueryRequest {
    organizationId: string;
    projectId: string;
    trackedQueryId: string;
    idempotencyKey: string;
    clusterMembershipRequestData: ClusterMembershipRequestData;
}

export interface ReportAiResponseOperationRequest {
    organizationId: string;
    projectId: string;
    trackedQueryId: string;
    aiResponseId: string;
    idempotencyKey: string;
    reportAiResponseRequest: ReportAiResponseRequest;
}

export interface ResumeTrackedQueryRequest {
    organizationId: string;
    projectId: string;
    trackedQueryId: string;
    idempotencyKey: string;
}

export interface SearchTrackedQueriesRequest {
    organizationId: string;
    projectId: string;
    limit?: number;
    offset?: number;
    search?: string;
    status?: SearchTrackedQueriesStatusEnum;
    engines?: Array<SearchTrackedQueriesEnginesEnum>;
    countries?: Array<string>;
    sortBy?: SearchTrackedQueriesSortByEnum;
    sortOrder?: SearchTrackedQueriesSortOrderEnum;
}

/**
 * TrackedQueriesApi - interface
 * 
 * @export
 * @interface TrackedQueriesApiInterface
 */
export interface TrackedQueriesApiInterface {
    /**
     * Creates request options for addClustersToTrackedQuery without sending the request
     * @param {string} organizationId 
     * @param {string} projectId 
     * @param {string} trackedQueryId Must belong to the project in the path.
     * @param {string} idempotencyKey A client-chosen key, unique per operation, so a lost response can be retried without repeating the write.
     * @param {ClusterMembershipRequestData} clusterMembershipRequestData 
     * @throws {RequiredError}
     * @memberof TrackedQueriesApiInterface
     */
    addClustersToTrackedQueryRequestOpts(requestParameters: AddClustersToTrackedQueryRequest): Promise<runtime.RequestOpts>;

    /**
     * Minimum role: manager. Adds the tracked query to every cluster named in \"queryClusterIds\" and answers with the query in its new state. Membership is a set: a cluster the query already belongs to is skipped, not reported as an error, and the response still lists it. Clusters the query belongs to and this call does not name are left alone — this adds, it does not replace the membership. Every cluster must belong to the project in the path; one that does not is rejected with its field named, and nothing is written. The \"Idempotency-Key\" header is required, and a repeat of the same key and body returns the recorded answer without adding anything again.
     * @summary Add a tracked query to clusters
     * @param {string} organizationId 
     * @param {string} projectId 
     * @param {string} trackedQueryId Must belong to the project in the path.
     * @param {string} idempotencyKey A client-chosen key, unique per operation, so a lost response can be retried without repeating the write.
     * @param {ClusterMembershipRequestData} clusterMembershipRequestData 
     * @param {*} [options] Override http request option.
     * @throws {RequiredError}
     * @memberof TrackedQueriesApiInterface
     */
    addClustersToTrackedQueryRaw(requestParameters: AddClustersToTrackedQueryRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<TrackedQueryDetailResource>>;

    /**
     * Minimum role: manager. Adds the tracked query to every cluster named in \"queryClusterIds\" and answers with the query in its new state. Membership is a set: a cluster the query already belongs to is skipped, not reported as an error, and the response still lists it. Clusters the query belongs to and this call does not name are left alone — this adds, it does not replace the membership. Every cluster must belong to the project in the path; one that does not is rejected with its field named, and nothing is written. The \"Idempotency-Key\" header is required, and a repeat of the same key and body returns the recorded answer without adding anything again.
     * Add a tracked query to clusters
     */
    addClustersToTrackedQuery(requestParameters: AddClustersToTrackedQueryRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<TrackedQueryDetailResource>;

    /**
     * Creates request options for batchChangeTrackedQueriesCheckFrequency without sending the request
     * @param {string} organizationId 
     * @param {string} projectId 
     * @param {string} idempotencyKey A client-chosen key, unique per operation. Replaying it returns the first answer instead of applying anything again.
     * @param {BatchChangeTrackedQueryCheckFrequencyRequestData} batchChangeTrackedQueryCheckFrequencyRequestData 
     * @throws {RequiredError}
     * @memberof TrackedQueriesApiInterface
     */
    batchChangeTrackedQueriesCheckFrequencyRequestOpts(requestParameters: BatchChangeTrackedQueriesCheckFrequencyRequest): Promise<runtime.RequestOpts>;

    /**
     * Minimum role: manager. Sets the same check frequency on every tracked query named in `ids`, at most 100 distinct ids per call. The organization must be active and the project must not be archived. Partial success: an id that is not a tracked query of this project — unknown, malformed, already deleted, or belonging to somewhere else — is reported under `failed` with `tracked_query_not_found` while the rest are changed, and the call still answers 200. Nothing is rolled back because an item failed. Setting the frequency a query already has is accepted and changes nothing. This does NOT run a check and does not reschedule one already in flight. It never changes more than the ids it is given: there is no \"change everything matching a filter\" mode. The Idempotency-Key header is required; a retry with the same key and the same body returns this same answer without applying anything again.
     * @summary Change how often several tracked queries are checked
     * @param {string} organizationId 
     * @param {string} projectId 
     * @param {string} idempotencyKey A client-chosen key, unique per operation. Replaying it returns the first answer instead of applying anything again.
     * @param {BatchChangeTrackedQueryCheckFrequencyRequestData} batchChangeTrackedQueryCheckFrequencyRequestData 
     * @param {*} [options] Override http request option.
     * @throws {RequiredError}
     * @memberof TrackedQueriesApiInterface
     */
    batchChangeTrackedQueriesCheckFrequencyRaw(requestParameters: BatchChangeTrackedQueriesCheckFrequencyRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<BatchWriteOutcome>>;

    /**
     * Minimum role: manager. Sets the same check frequency on every tracked query named in `ids`, at most 100 distinct ids per call. The organization must be active and the project must not be archived. Partial success: an id that is not a tracked query of this project — unknown, malformed, already deleted, or belonging to somewhere else — is reported under `failed` with `tracked_query_not_found` while the rest are changed, and the call still answers 200. Nothing is rolled back because an item failed. Setting the frequency a query already has is accepted and changes nothing. This does NOT run a check and does not reschedule one already in flight. It never changes more than the ids it is given: there is no \"change everything matching a filter\" mode. The Idempotency-Key header is required; a retry with the same key and the same body returns this same answer without applying anything again.
     * Change how often several tracked queries are checked
     */
    batchChangeTrackedQueriesCheckFrequency(requestParameters: BatchChangeTrackedQueriesCheckFrequencyRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<BatchWriteOutcome>;

    /**
     * Creates request options for batchChangeTrackedQueriesNPasses without sending the request
     * @param {string} organizationId 
     * @param {string} projectId 
     * @param {string} idempotencyKey A client-chosen key, unique per operation. Replaying it returns the first answer instead of applying anything again.
     * @param {BatchChangeTrackedQueryPassesRequestData} batchChangeTrackedQueryPassesRequestData 
     * @throws {RequiredError}
     * @memberof TrackedQueriesApiInterface
     */
    batchChangeTrackedQueriesNPassesRequestOpts(requestParameters: BatchChangeTrackedQueriesNPassesRequest): Promise<runtime.RequestOpts>;

    /**
     * Minimum role: manager. Sets the same passes-per-check on every tracked query named in `ids`, at most 100 distinct ids per call. The organization must be active and the project must not be archived. Partial success, and two distinct reasons appear under `failed`: an id that is not a tracked query of this project answers `tracked_query_not_found`, and a `google_serp` or `google_shopping` query asked for more than one pass answers `n_passes_not_supported_for_engine` — those engines run a single pass. Both leave the rest of the batch changed and the call still answers 200. Setting the value back to 1 is always allowed, so a batch that lowers sampling never fails on engine grounds. This does NOT run a check and does not change history already captured. More passes cost proportionally more of the plan\'s check budget. It never changes more than the ids it is given: there is no \"change everything matching a filter\" mode. The Idempotency-Key header is required; a retry with the same key and the same body returns this same answer without applying anything again.
     * @summary Change how many passes several tracked queries run per check
     * @param {string} organizationId 
     * @param {string} projectId 
     * @param {string} idempotencyKey A client-chosen key, unique per operation. Replaying it returns the first answer instead of applying anything again.
     * @param {BatchChangeTrackedQueryPassesRequestData} batchChangeTrackedQueryPassesRequestData 
     * @param {*} [options] Override http request option.
     * @throws {RequiredError}
     * @memberof TrackedQueriesApiInterface
     */
    batchChangeTrackedQueriesNPassesRaw(requestParameters: BatchChangeTrackedQueriesNPassesRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<BatchWriteOutcome>>;

    /**
     * Minimum role: manager. Sets the same passes-per-check on every tracked query named in `ids`, at most 100 distinct ids per call. The organization must be active and the project must not be archived. Partial success, and two distinct reasons appear under `failed`: an id that is not a tracked query of this project answers `tracked_query_not_found`, and a `google_serp` or `google_shopping` query asked for more than one pass answers `n_passes_not_supported_for_engine` — those engines run a single pass. Both leave the rest of the batch changed and the call still answers 200. Setting the value back to 1 is always allowed, so a batch that lowers sampling never fails on engine grounds. This does NOT run a check and does not change history already captured. More passes cost proportionally more of the plan\'s check budget. It never changes more than the ids it is given: there is no \"change everything matching a filter\" mode. The Idempotency-Key header is required; a retry with the same key and the same body returns this same answer without applying anything again.
     * Change how many passes several tracked queries run per check
     */
    batchChangeTrackedQueriesNPasses(requestParameters: BatchChangeTrackedQueriesNPassesRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<BatchWriteOutcome>;

    /**
     * Creates request options for batchCreateTrackedQueries without sending the request
     * @param {string} organizationId 
     * @param {string} projectId 
     * @param {string} idempotencyKey A client-chosen key, unique per operation. Replaying it returns the first answer instead of creating anything again.
     * @param {BatchCreateTrackedQueriesRequestData} batchCreateTrackedQueriesRequestData 
     * @throws {RequiredError}
     * @memberof TrackedQueriesApiInterface
     */
    batchCreateTrackedQueriesRequestOpts(requestParameters: BatchCreateTrackedQueriesRequest): Promise<runtime.RequestOpts>;

    /**
     * Minimum role: manager. Creates the cross product of `queryTexts` x `engines` x `countries`: three texts, two engines and two countries create twelve tracked queries, not three. At most 100 combinations per call. The organization must be active and the project must not be archived. Partial success: the response lists, per combination, either the id it was created under or why nothing was created for it, and the status code never reports item-level outcomes. A combination the project already tracks is NOT created again and NOT re-identified — it appears under `failed` with `tracked_query_already_exists`, keeps the id it already had, and has any `queryClusterIds` in this request merged into it. Query text is normalised before it is compared and stored (lower-cased, whitespace collapsed, leading list markers stripped), so two texts differing only in those respects are one tracked query: the first of them owns the outcome and every later one appears under `failed` with `duplicate_combination_in_request`, naming the entry it repeats. `nPasses` applies to AI engines only: `google_serp` and `google_shopping` rows are always created with one pass, whatever is sent. Google AI Mode is unavailable in a few countries and those combinations are reported under `failed` rather than created. Creating a tracked query does not run a check: the first check happens on the normal schedule for the `checkFrequency` chosen. The Idempotency-Key header is required; a retry with the same key and the same body returns this same answer without creating anything again.
     * @summary Create tracked queries
     * @param {string} organizationId 
     * @param {string} projectId 
     * @param {string} idempotencyKey A client-chosen key, unique per operation. Replaying it returns the first answer instead of creating anything again.
     * @param {BatchCreateTrackedQueriesRequestData} batchCreateTrackedQueriesRequestData 
     * @param {*} [options] Override http request option.
     * @throws {RequiredError}
     * @memberof TrackedQueriesApiInterface
     */
    batchCreateTrackedQueriesRaw(requestParameters: BatchCreateTrackedQueriesRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<BatchCreateTrackedQueriesResultResource>>;

    /**
     * Minimum role: manager. Creates the cross product of `queryTexts` x `engines` x `countries`: three texts, two engines and two countries create twelve tracked queries, not three. At most 100 combinations per call. The organization must be active and the project must not be archived. Partial success: the response lists, per combination, either the id it was created under or why nothing was created for it, and the status code never reports item-level outcomes. A combination the project already tracks is NOT created again and NOT re-identified — it appears under `failed` with `tracked_query_already_exists`, keeps the id it already had, and has any `queryClusterIds` in this request merged into it. Query text is normalised before it is compared and stored (lower-cased, whitespace collapsed, leading list markers stripped), so two texts differing only in those respects are one tracked query: the first of them owns the outcome and every later one appears under `failed` with `duplicate_combination_in_request`, naming the entry it repeats. `nPasses` applies to AI engines only: `google_serp` and `google_shopping` rows are always created with one pass, whatever is sent. Google AI Mode is unavailable in a few countries and those combinations are reported under `failed` rather than created. Creating a tracked query does not run a check: the first check happens on the normal schedule for the `checkFrequency` chosen. The Idempotency-Key header is required; a retry with the same key and the same body returns this same answer without creating anything again.
     * Create tracked queries
     */
    batchCreateTrackedQueries(requestParameters: BatchCreateTrackedQueriesRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<BatchCreateTrackedQueriesResultResource>;

    /**
     * Creates request options for batchForceCheckTrackedQueries without sending the request
     * @param {string} organizationId 
     * @param {string} projectId 
     * @param {string} idempotencyKey Repeating a request with the same key and the same ids answers with the first attempt\&#39;s result instead of submitting again.
     * @param {BatchTargetsRequestData} batchTargetsRequestData 
     * @throws {RequiredError}
     * @memberof TrackedQueriesApiInterface
     */
    batchForceCheckTrackedQueriesRequestOpts(requestParameters: BatchForceCheckTrackedQueriesRequest): Promise<runtime.RequestOpts>;

    /**
     * Minimum role: manager. Asks for a fresh check of up to 100 named tracked queries straight away, ignoring how recently each was last checked. Answers 200 with per-item results and NO job id: a check that is already in flight for a query is reused rather than started again, so there is no id this operation could hand back that is guaranteed to exist. Follow progress on the tracked query itself — its lastCheckedAt advances when the check completes. A successful item means the check was accepted for submission with budget available for it at that moment; it does not mean the check has run. A checked query costs one budget unit per pass (nPasses), and items that do not fit the remaining budget are reported as failed with `check_budget_forecast_exhausted`, or `subscription_not_found` when the organization has no entitled subscription — they are never reported as successful. That budget figure is a forecast for this batch, and a pessimistic one: a tracked query already being checked is joined to the check in flight and costs nothing, but it is still debited here, so an item refused this way may have fitted. Resubmit it in a later batch rather than treating the refusal as a statement about your subscription. A paused query is reported as failed with `tracked_query_already_paused`: paused queries are never checked. An id that is not a tracked query of this project is reported as failed with `tracked_query_not_found`, exactly as an id that does not exist. Requires an Idempotency-Key header; a retry must repeat the same ids in the same order.
     * @summary Check several tracked queries now
     * @param {string} organizationId 
     * @param {string} projectId 
     * @param {string} idempotencyKey Repeating a request with the same key and the same ids answers with the first attempt\&#39;s result instead of submitting again.
     * @param {BatchTargetsRequestData} batchTargetsRequestData 
     * @param {*} [options] Override http request option.
     * @throws {RequiredError}
     * @memberof TrackedQueriesApiInterface
     */
    batchForceCheckTrackedQueriesRaw(requestParameters: BatchForceCheckTrackedQueriesRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<BatchForceCheckTrackedQueries200Response>>;

    /**
     * Minimum role: manager. Asks for a fresh check of up to 100 named tracked queries straight away, ignoring how recently each was last checked. Answers 200 with per-item results and NO job id: a check that is already in flight for a query is reused rather than started again, so there is no id this operation could hand back that is guaranteed to exist. Follow progress on the tracked query itself — its lastCheckedAt advances when the check completes. A successful item means the check was accepted for submission with budget available for it at that moment; it does not mean the check has run. A checked query costs one budget unit per pass (nPasses), and items that do not fit the remaining budget are reported as failed with `check_budget_forecast_exhausted`, or `subscription_not_found` when the organization has no entitled subscription — they are never reported as successful. That budget figure is a forecast for this batch, and a pessimistic one: a tracked query already being checked is joined to the check in flight and costs nothing, but it is still debited here, so an item refused this way may have fitted. Resubmit it in a later batch rather than treating the refusal as a statement about your subscription. A paused query is reported as failed with `tracked_query_already_paused`: paused queries are never checked. An id that is not a tracked query of this project is reported as failed with `tracked_query_not_found`, exactly as an id that does not exist. Requires an Idempotency-Key header; a retry must repeat the same ids in the same order.
     * Check several tracked queries now
     */
    batchForceCheckTrackedQueries(requestParameters: BatchForceCheckTrackedQueriesRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<BatchForceCheckTrackedQueries200Response>;

    /**
     * Creates request options for batchPauseTrackedQueries without sending the request
     * @param {string} organizationId 
     * @param {string} projectId 
     * @param {string} idempotencyKey Repeating a request with the same key and the same ids answers with the first attempt\&#39;s result.
     * @param {BatchTargetsRequestData} batchTargetsRequestData 
     * @throws {RequiredError}
     * @memberof TrackedQueriesApiInterface
     */
    batchPauseTrackedQueriesRequestOpts(requestParameters: BatchPauseTrackedQueriesRequest): Promise<runtime.RequestOpts>;

    /**
     * Minimum role: manager. Pauses up to 100 tracked queries of one project, each independently. Always answers 200 when the batch itself was processed: read `failed` to find out which items were not paused, never the status code. Items are never rolled back because a later one failed. An id that is already paused is reported as successful — pausing asserts a state, not a transition. An id that is not a tracked query of this project is reported as failed with `tracked_query_not_found`, exactly as an id that does not exist at all, and nothing is written for it. Pausing does not cancel a check that is already running. Requires an Idempotency-Key header; a retry must repeat the same ids in the same order to be recognised as a retry rather than refused as a reused key.
     * @summary Pause several tracked queries
     * @param {string} organizationId 
     * @param {string} projectId 
     * @param {string} idempotencyKey Repeating a request with the same key and the same ids answers with the first attempt\&#39;s result.
     * @param {BatchTargetsRequestData} batchTargetsRequestData 
     * @param {*} [options] Override http request option.
     * @throws {RequiredError}
     * @memberof TrackedQueriesApiInterface
     */
    batchPauseTrackedQueriesRaw(requestParameters: BatchPauseTrackedQueriesRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<BatchPauseTrackedQueries200Response>>;

    /**
     * Minimum role: manager. Pauses up to 100 tracked queries of one project, each independently. Always answers 200 when the batch itself was processed: read `failed` to find out which items were not paused, never the status code. Items are never rolled back because a later one failed. An id that is already paused is reported as successful — pausing asserts a state, not a transition. An id that is not a tracked query of this project is reported as failed with `tracked_query_not_found`, exactly as an id that does not exist at all, and nothing is written for it. Pausing does not cancel a check that is already running. Requires an Idempotency-Key header; a retry must repeat the same ids in the same order to be recognised as a retry rather than refused as a reused key.
     * Pause several tracked queries
     */
    batchPauseTrackedQueries(requestParameters: BatchPauseTrackedQueriesRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<BatchPauseTrackedQueries200Response>;

    /**
     * Creates request options for batchResumeTrackedQueries without sending the request
     * @param {string} organizationId 
     * @param {string} projectId 
     * @param {string} idempotencyKey Repeating a request with the same key and the same ids answers with the first attempt\&#39;s result.
     * @param {BatchTargetsRequestData} batchTargetsRequestData 
     * @throws {RequiredError}
     * @memberof TrackedQueriesApiInterface
     */
    batchResumeTrackedQueriesRequestOpts(requestParameters: BatchResumeTrackedQueriesRequest): Promise<runtime.RequestOpts>;

    /**
     * Minimum role: manager. Puts up to 100 paused tracked queries of one project back under the scheduler, each independently. Always answers 200 when the batch itself was processed: read `failed` to find out which items were not resumed, never the status code. Items are never rolled back because a later one failed. An id that is already active is reported as successful — resuming asserts a state, not a transition. An id that is not a tracked query of this project is reported as failed with `tracked_query_not_found`, exactly as an id that does not exist at all, and nothing is written for it. No check is run by this operation and nothing is back-filled for the time the queries spent paused. Requires an Idempotency-Key header; a retry must repeat the same ids in the same order to be recognised as a retry rather than refused as a reused key.
     * @summary Resume several tracked queries
     * @param {string} organizationId 
     * @param {string} projectId 
     * @param {string} idempotencyKey Repeating a request with the same key and the same ids answers with the first attempt\&#39;s result.
     * @param {BatchTargetsRequestData} batchTargetsRequestData 
     * @param {*} [options] Override http request option.
     * @throws {RequiredError}
     * @memberof TrackedQueriesApiInterface
     */
    batchResumeTrackedQueriesRaw(requestParameters: BatchResumeTrackedQueriesRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<BatchResumeTrackedQueries200Response>>;

    /**
     * Minimum role: manager. Puts up to 100 paused tracked queries of one project back under the scheduler, each independently. Always answers 200 when the batch itself was processed: read `failed` to find out which items were not resumed, never the status code. Items are never rolled back because a later one failed. An id that is already active is reported as successful — resuming asserts a state, not a transition. An id that is not a tracked query of this project is reported as failed with `tracked_query_not_found`, exactly as an id that does not exist at all, and nothing is written for it. No check is run by this operation and nothing is back-filled for the time the queries spent paused. Requires an Idempotency-Key header; a retry must repeat the same ids in the same order to be recognised as a retry rather than refused as a reused key.
     * Resume several tracked queries
     */
    batchResumeTrackedQueries(requestParameters: BatchResumeTrackedQueriesRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<BatchResumeTrackedQueries200Response>;

    /**
     * Creates request options for bulkAddClustersToTrackedQueries without sending the request
     * @param {string} organizationId 
     * @param {string} projectId 
     * @param {string} idempotencyKey A client-chosen key, unique per operation, so a lost response can be retried without repeating the batch.
     * @param {BulkAddClustersToTrackedQueriesRequest} bulkAddClustersToTrackedQueriesRequest 
     * @throws {RequiredError}
     * @memberof TrackedQueriesApiInterface
     */
    bulkAddClustersToTrackedQueriesRequestOpts(requestParameters: BulkAddClustersToTrackedQueriesOperationRequest): Promise<runtime.RequestOpts>;

    /**
     * Minimum role: manager. Adds every tracked query named in \"ids\" to every cluster named in \"queryClusterIds\". At most 100 distinct tracked queries per call; duplicates in \"ids\" are collapsed. Partial success: the answer is 200 with a per-item \"successful\" and \"failed\" list even when some items failed, nothing is rolled back, and an id that is not a tracked query of this project is reported as a failed item rather than dropped. Clusters not named are left alone — this adds, it does not replace membership. Every cluster must belong to the project in the path; one that does not is rejected with its field named and nothing is written at all. The \"Idempotency-Key\" header is required, and a repeat of the same key and body returns the recorded answer without running the batch again.
     * @summary Add many tracked queries to clusters
     * @param {string} organizationId 
     * @param {string} projectId 
     * @param {string} idempotencyKey A client-chosen key, unique per operation, so a lost response can be retried without repeating the batch.
     * @param {BulkAddClustersToTrackedQueriesRequest} bulkAddClustersToTrackedQueriesRequest 
     * @param {*} [options] Override http request option.
     * @throws {RequiredError}
     * @memberof TrackedQueriesApiInterface
     */
    bulkAddClustersToTrackedQueriesRaw(requestParameters: BulkAddClustersToTrackedQueriesOperationRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<BulkAddClustersToTrackedQueries200Response>>;

    /**
     * Minimum role: manager. Adds every tracked query named in \"ids\" to every cluster named in \"queryClusterIds\". At most 100 distinct tracked queries per call; duplicates in \"ids\" are collapsed. Partial success: the answer is 200 with a per-item \"successful\" and \"failed\" list even when some items failed, nothing is rolled back, and an id that is not a tracked query of this project is reported as a failed item rather than dropped. Clusters not named are left alone — this adds, it does not replace membership. Every cluster must belong to the project in the path; one that does not is rejected with its field named and nothing is written at all. The \"Idempotency-Key\" header is required, and a repeat of the same key and body returns the recorded answer without running the batch again.
     * Add many tracked queries to clusters
     */
    bulkAddClustersToTrackedQueries(requestParameters: BulkAddClustersToTrackedQueriesOperationRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<BulkAddClustersToTrackedQueries200Response>;

    /**
     * Creates request options for bulkDeleteTrackedQueries without sending the request
     * @param {string} organizationId 
     * @param {string} projectId 
     * @param {string} idempotencyKey A client-chosen key, unique per operation. Replaying it returns the first answer instead of deleting anything again.
     * @param {BatchTargetsRequestData} batchTargetsRequestData 
     * @throws {RequiredError}
     * @memberof TrackedQueriesApiInterface
     */
    bulkDeleteTrackedQueriesRequestOpts(requestParameters: BulkDeleteTrackedQueriesRequest): Promise<runtime.RequestOpts>;

    /**
     * Minimum role: manager. Permanently deletes the tracked queries named in `ids`, at most 100 distinct ids per call. The organization must be active and the project must not be archived. Deletion is hard and cannot be undone: the tracked query is removed along with its captured answers, matches, search pages and rank history, and any check already in flight for it is cancelled. Those cascades run in the background, so a 200 means the tracked queries were deleted, not that every derived record has finished being purged. Partial success: an id that is not a tracked query of this project — unknown, malformed, already deleted, or belonging to somewhere else — is reported under `failed` with `tracked_query_not_found` while the rest are deleted. It never deletes more than the ids it is given: there is no \"delete everything matching a filter\" mode. The Idempotency-Key header is required; a retry with the same key and the same body returns this same answer without deleting anything again.
     * @summary Delete tracked queries
     * @param {string} organizationId 
     * @param {string} projectId 
     * @param {string} idempotencyKey A client-chosen key, unique per operation. Replaying it returns the first answer instead of deleting anything again.
     * @param {BatchTargetsRequestData} batchTargetsRequestData 
     * @param {*} [options] Override http request option.
     * @throws {RequiredError}
     * @memberof TrackedQueriesApiInterface
     */
    bulkDeleteTrackedQueriesRaw(requestParameters: BulkDeleteTrackedQueriesRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<BatchWriteOutcome>>;

    /**
     * Minimum role: manager. Permanently deletes the tracked queries named in `ids`, at most 100 distinct ids per call. The organization must be active and the project must not be archived. Deletion is hard and cannot be undone: the tracked query is removed along with its captured answers, matches, search pages and rank history, and any check already in flight for it is cancelled. Those cascades run in the background, so a 200 means the tracked queries were deleted, not that every derived record has finished being purged. Partial success: an id that is not a tracked query of this project — unknown, malformed, already deleted, or belonging to somewhere else — is reported under `failed` with `tracked_query_not_found` while the rest are deleted. It never deletes more than the ids it is given: there is no \"delete everything matching a filter\" mode. The Idempotency-Key header is required; a retry with the same key and the same body returns this same answer without deleting anything again.
     * Delete tracked queries
     */
    bulkDeleteTrackedQueries(requestParameters: BulkDeleteTrackedQueriesRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<BatchWriteOutcome>;

    /**
     * Creates request options for bulkRemoveClustersFromTrackedQueries without sending the request
     * @param {string} organizationId 
     * @param {string} projectId 
     * @param {string} idempotencyKey A client-chosen key, unique per operation, so a lost response can be retried without repeating the batch.
     * @param {BulkRemoveClustersFromTrackedQueriesRequest} bulkRemoveClustersFromTrackedQueriesRequest Required. A DELETE with no body is rejected.
     * @throws {RequiredError}
     * @memberof TrackedQueriesApiInterface
     */
    bulkRemoveClustersFromTrackedQueriesRequestOpts(requestParameters: BulkRemoveClustersFromTrackedQueriesOperationRequest): Promise<runtime.RequestOpts>;

    /**
     * Minimum role: manager. Removes every tracked query named in \"ids\" from every cluster named in \"queryClusterIds\". At most 100 distinct tracked queries per call; duplicates in \"ids\" are collapsed. Partial success: the answer is 200 with a per-item \"successful\" and \"failed\" list even when some items failed, nothing is rolled back, and an id that is not a tracked query of this project is reported as a failed item rather than dropped. Neither the clusters nor the tracked queries are deleted: only the membership between them. Every cluster must belong to the project in the path; one that does not is rejected with its field named and nothing is written at all. NOTE: this DELETE requires a request body — some HTTP client libraries and proxies strip bodies from DELETE, and a stripped body is refused with a validation error rather than interpreted as \"remove everything\". The \"Idempotency-Key\" header is required, and a repeat of the same key and body returns the recorded answer without running the batch again.
     * @summary Remove many tracked queries from clusters
     * @param {string} organizationId 
     * @param {string} projectId 
     * @param {string} idempotencyKey A client-chosen key, unique per operation, so a lost response can be retried without repeating the batch.
     * @param {BulkRemoveClustersFromTrackedQueriesRequest} bulkRemoveClustersFromTrackedQueriesRequest Required. A DELETE with no body is rejected.
     * @param {*} [options] Override http request option.
     * @throws {RequiredError}
     * @memberof TrackedQueriesApiInterface
     */
    bulkRemoveClustersFromTrackedQueriesRaw(requestParameters: BulkRemoveClustersFromTrackedQueriesOperationRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<BulkRemoveClustersFromTrackedQueries200Response>>;

    /**
     * Minimum role: manager. Removes every tracked query named in \"ids\" from every cluster named in \"queryClusterIds\". At most 100 distinct tracked queries per call; duplicates in \"ids\" are collapsed. Partial success: the answer is 200 with a per-item \"successful\" and \"failed\" list even when some items failed, nothing is rolled back, and an id that is not a tracked query of this project is reported as a failed item rather than dropped. Neither the clusters nor the tracked queries are deleted: only the membership between them. Every cluster must belong to the project in the path; one that does not is rejected with its field named and nothing is written at all. NOTE: this DELETE requires a request body — some HTTP client libraries and proxies strip bodies from DELETE, and a stripped body is refused with a validation error rather than interpreted as \"remove everything\". The \"Idempotency-Key\" header is required, and a repeat of the same key and body returns the recorded answer without running the batch again.
     * Remove many tracked queries from clusters
     */
    bulkRemoveClustersFromTrackedQueries(requestParameters: BulkRemoveClustersFromTrackedQueriesOperationRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<BulkRemoveClustersFromTrackedQueries200Response>;

    /**
     * Creates request options for changeTrackedQueryCheckFrequency without sending the request
     * @param {string} organizationId 
     * @param {string} projectId 
     * @param {string} trackedQueryId Must belong to the project in the path.
     * @param {string} idempotencyKey A client-chosen key, unique per operation. Replaying it returns the first answer instead of applying anything again.
     * @param {ChangeTrackedQueryCheckFrequencyRequestData} changeTrackedQueryCheckFrequencyRequestData 
     * @throws {RequiredError}
     * @memberof TrackedQueriesApiInterface
     */
    changeTrackedQueryCheckFrequencyRequestOpts(requestParameters: ChangeTrackedQueryCheckFrequencyRequest): Promise<runtime.RequestOpts>;

    /**
     * Minimum role: manager. Sets how often one tracked query is checked while it is active. The organization must be active and the project must not be archived. Setting the frequency it already has is accepted and changes nothing. This does NOT run a check, does not backfill history, and does not reschedule a check already in flight: the new cadence applies from the next time the query is considered. A paused query keeps the setting but is not checked until it is resumed. A tracked query belonging to another project answers 404, the same answer an unknown id gets. The Idempotency-Key header is required; a retry with the same key and the same body returns this same answer without applying anything again.
     * @summary Change how often a tracked query is checked
     * @param {string} organizationId 
     * @param {string} projectId 
     * @param {string} trackedQueryId Must belong to the project in the path.
     * @param {string} idempotencyKey A client-chosen key, unique per operation. Replaying it returns the first answer instead of applying anything again.
     * @param {ChangeTrackedQueryCheckFrequencyRequestData} changeTrackedQueryCheckFrequencyRequestData 
     * @param {*} [options] Override http request option.
     * @throws {RequiredError}
     * @memberof TrackedQueriesApiInterface
     */
    changeTrackedQueryCheckFrequencyRaw(requestParameters: ChangeTrackedQueryCheckFrequencyRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<TrackedQueryDetailResource>>;

    /**
     * Minimum role: manager. Sets how often one tracked query is checked while it is active. The organization must be active and the project must not be archived. Setting the frequency it already has is accepted and changes nothing. This does NOT run a check, does not backfill history, and does not reschedule a check already in flight: the new cadence applies from the next time the query is considered. A paused query keeps the setting but is not checked until it is resumed. A tracked query belonging to another project answers 404, the same answer an unknown id gets. The Idempotency-Key header is required; a retry with the same key and the same body returns this same answer without applying anything again.
     * Change how often a tracked query is checked
     */
    changeTrackedQueryCheckFrequency(requestParameters: ChangeTrackedQueryCheckFrequencyRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<TrackedQueryDetailResource>;

    /**
     * Creates request options for changeTrackedQueryNPasses without sending the request
     * @param {string} organizationId 
     * @param {string} projectId 
     * @param {string} trackedQueryId Must belong to the project in the path.
     * @param {string} idempotencyKey A client-chosen key, unique per operation. Replaying it returns the first answer instead of applying anything again.
     * @param {ChangeTrackedQueryPassesRequestData} changeTrackedQueryPassesRequestData 
     * @throws {RequiredError}
     * @memberof TrackedQueriesApiInterface
     */
    changeTrackedQueryNPassesRequestOpts(requestParameters: ChangeTrackedQueryNPassesRequest): Promise<runtime.RequestOpts>;

    /**
     * Minimum role: manager. Sets how many times one tracked query is asked per check. AI engines are not deterministic, so several passes are averaged; `google_serp` and `google_shopping` run a single pass and refuse any value above one with 409 `n_passes_not_supported_for_engine`. Setting the value back to 1 is always allowed. The organization must be active and the project must not be archived. Setting the value it already has is accepted and changes nothing. This does NOT run a check and does not change history already captured: it applies from the next check. More passes cost proportionally more of the plan\'s check budget. A tracked query belonging to another project answers 404, the same answer an unknown id gets. The Idempotency-Key header is required; a retry with the same key and the same body returns this same answer without applying anything again.
     * @summary Change how many passes a tracked query runs per check
     * @param {string} organizationId 
     * @param {string} projectId 
     * @param {string} trackedQueryId Must belong to the project in the path.
     * @param {string} idempotencyKey A client-chosen key, unique per operation. Replaying it returns the first answer instead of applying anything again.
     * @param {ChangeTrackedQueryPassesRequestData} changeTrackedQueryPassesRequestData 
     * @param {*} [options] Override http request option.
     * @throws {RequiredError}
     * @memberof TrackedQueriesApiInterface
     */
    changeTrackedQueryNPassesRaw(requestParameters: ChangeTrackedQueryNPassesRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<TrackedQueryDetailResource>>;

    /**
     * Minimum role: manager. Sets how many times one tracked query is asked per check. AI engines are not deterministic, so several passes are averaged; `google_serp` and `google_shopping` run a single pass and refuse any value above one with 409 `n_passes_not_supported_for_engine`. Setting the value back to 1 is always allowed. The organization must be active and the project must not be archived. Setting the value it already has is accepted and changes nothing. This does NOT run a check and does not change history already captured: it applies from the next check. More passes cost proportionally more of the plan\'s check budget. A tracked query belonging to another project answers 404, the same answer an unknown id gets. The Idempotency-Key header is required; a retry with the same key and the same body returns this same answer without applying anything again.
     * Change how many passes a tracked query runs per check
     */
    changeTrackedQueryNPasses(requestParameters: ChangeTrackedQueryNPassesRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<TrackedQueryDetailResource>;

    /**
     * Creates request options for countTrackedQueries without sending the request
     * @param {string} organizationId 
     * @param {string} projectId 
     * @param {'active' | 'paused'} [status] Restrict both numbers to one status. Lower-case; an unknown value is rejected, not ignored. Omitted, every status is counted and priced.
     * @throws {RequiredError}
     * @memberof TrackedQueriesApiInterface
     */
    countTrackedQueriesRequestOpts(requestParameters: CountTrackedQueriesRequest): Promise<runtime.RequestOpts>;

    /**
     * Minimum role: viewer. Two numbers about one project: how many tracked queries it holds, and what force-checking that same set would cost. Omit `status` to count every tracked query whatever its status; send `active` or `paused` to count and price only those. `checkCost` is a PRICED DRY RUN expressed in check budget units — one unit per pass, the same unit the plan allowance is counted in, so it is directly comparable with `checksAvailable` from the entitlements operation — and it is the sum of each matched tracked query\'s configured passes. Asking reserves nothing, debits nothing and starts no check. It is deliberately NOT a forecast of what the check-all operation will consume: that operation skips paused queries, skips a query whose check is already pending or running, re-runs a check awaiting retry without charging for it again, and stops at whatever budget is left — none of which is subtracted here. Count with `status=active` for the figure closest to a full check-all. Both numbers are read from the write model, so a tracked query created moments ago is already in them; that is why they can be AHEAD of the `total` returned by the tracked-queries listing, which counts a search projection, and ahead of the keyword listings, which read projections refreshed in the background. Summing the unfiltered count over every project of an organization, archived projects included, reproduces countOrganizationTrackedQueries, which counts through the same counter in the same store. An archived project still answers, because this is a read, but no check can be submitted there while it stays archived, so its cost is hypothetical. For a month of scheduled rounds across the whole organization instead of one round over one project, read getOrganizationProjectedMonthlyChecks. No monetary amount is published or implied: the cost of a check is published only in budget units.
     * @summary Count a project\'s tracked queries and price checking them
     * @param {string} organizationId 
     * @param {string} projectId 
     * @param {'active' | 'paused'} [status] Restrict both numbers to one status. Lower-case; an unknown value is rejected, not ignored. Omitted, every status is counted and priced.
     * @param {*} [options] Override http request option.
     * @throws {RequiredError}
     * @memberof TrackedQueriesApiInterface
     */
    countTrackedQueriesRaw(requestParameters: CountTrackedQueriesRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<TrackedQueryCountResource>>;

    /**
     * Minimum role: viewer. Two numbers about one project: how many tracked queries it holds, and what force-checking that same set would cost. Omit `status` to count every tracked query whatever its status; send `active` or `paused` to count and price only those. `checkCost` is a PRICED DRY RUN expressed in check budget units — one unit per pass, the same unit the plan allowance is counted in, so it is directly comparable with `checksAvailable` from the entitlements operation — and it is the sum of each matched tracked query\'s configured passes. Asking reserves nothing, debits nothing and starts no check. It is deliberately NOT a forecast of what the check-all operation will consume: that operation skips paused queries, skips a query whose check is already pending or running, re-runs a check awaiting retry without charging for it again, and stops at whatever budget is left — none of which is subtracted here. Count with `status=active` for the figure closest to a full check-all. Both numbers are read from the write model, so a tracked query created moments ago is already in them; that is why they can be AHEAD of the `total` returned by the tracked-queries listing, which counts a search projection, and ahead of the keyword listings, which read projections refreshed in the background. Summing the unfiltered count over every project of an organization, archived projects included, reproduces countOrganizationTrackedQueries, which counts through the same counter in the same store. An archived project still answers, because this is a read, but no check can be submitted there while it stays archived, so its cost is hypothetical. For a month of scheduled rounds across the whole organization instead of one round over one project, read getOrganizationProjectedMonthlyChecks. No monetary amount is published or implied: the cost of a check is published only in budget units.
     * Count a project\'s tracked queries and price checking them
     */
    countTrackedQueries(requestParameters: CountTrackedQueriesRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<TrackedQueryCountResource>;

    /**
     * Creates request options for forceCheckAllActiveTrackedQueries without sending the request
     * @param {string} organizationId 
     * @param {string} projectId 
     * @param {string} idempotencyKey Repeating a request with the same key answers with the first attempt\&#39;s result instead of submitting a second wave.
     * @throws {RequiredError}
     * @memberof TrackedQueriesApiInterface
     */
    forceCheckAllActiveTrackedQueriesRequestOpts(requestParameters: ForceCheckAllActiveTrackedQueriesRequest): Promise<runtime.RequestOpts>;

    /**
     * Minimum role: manager. Submits a fresh check for every eligible tracked query of the project, ignoring how recently each was last checked, least-recently-checked first, and at most 1000 tracked queries per call. Eligible is narrower than active: a paused query is skipped, and so is one whose check is already pending or running for the same engine. A query whose check is awaiting a retry is included and, WHEN THE ENGINE HAS NOT CHANGED SINCE, costs nothing extra, because its first submission already paid for it; if the engine did change, the pending run is replaced and the replacement is paid for. What is left is trimmed to what the organization\'s remaining check budget can pay for — a check costs one budget unit per pass. While a subscription is cancelled but still inside its paid grace window nothing is submitted at all and `submitted` is 0; name the queries explicitly through the check operation to run them in that window. CALLING AGAIN DOES NOT CONTINUE WHERE THIS CALL STOPPED: submissions are handed to a worker, and a tracked query stops being selected only once that worker has started its check, so a second call made before the queue drains selects and submits the same tracked queries again. That is safe while the first check is still running — the duplicate is collapsed, and nothing is checked or charged twice — but a check that has already finished is run again and charged again, because this operation ignores staleness by design. To cover a project with more than 1000 eligible tracked queries, wait for the submitted wave to be picked up — each tracked query\'s lastCheckedAt advances when its check completes — and call again then. The response lists the tracked queries submitted and carries NO job id: a check already in flight is reused rather than started again, so no id could be guaranteed to exist. Submission is accepted, not completed: a listed tracked query can still lose the last budget units to another caller before the worker reaches it. The request takes no body, and any field sent is rejected. Requires an Idempotency-Key header.
     * @summary Check every eligible tracked query of a project now
     * @param {string} organizationId 
     * @param {string} projectId 
     * @param {string} idempotencyKey Repeating a request with the same key answers with the first attempt\&#39;s result instead of submitting a second wave.
     * @param {*} [options] Override http request option.
     * @throws {RequiredError}
     * @memberof TrackedQueriesApiInterface
     */
    forceCheckAllActiveTrackedQueriesRaw(requestParameters: ForceCheckAllActiveTrackedQueriesRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<SubmittedChecksResource>>;

    /**
     * Minimum role: manager. Submits a fresh check for every eligible tracked query of the project, ignoring how recently each was last checked, least-recently-checked first, and at most 1000 tracked queries per call. Eligible is narrower than active: a paused query is skipped, and so is one whose check is already pending or running for the same engine. A query whose check is awaiting a retry is included and, WHEN THE ENGINE HAS NOT CHANGED SINCE, costs nothing extra, because its first submission already paid for it; if the engine did change, the pending run is replaced and the replacement is paid for. What is left is trimmed to what the organization\'s remaining check budget can pay for — a check costs one budget unit per pass. While a subscription is cancelled but still inside its paid grace window nothing is submitted at all and `submitted` is 0; name the queries explicitly through the check operation to run them in that window. CALLING AGAIN DOES NOT CONTINUE WHERE THIS CALL STOPPED: submissions are handed to a worker, and a tracked query stops being selected only once that worker has started its check, so a second call made before the queue drains selects and submits the same tracked queries again. That is safe while the first check is still running — the duplicate is collapsed, and nothing is checked or charged twice — but a check that has already finished is run again and charged again, because this operation ignores staleness by design. To cover a project with more than 1000 eligible tracked queries, wait for the submitted wave to be picked up — each tracked query\'s lastCheckedAt advances when its check completes — and call again then. The response lists the tracked queries submitted and carries NO job id: a check already in flight is reused rather than started again, so no id could be guaranteed to exist. Submission is accepted, not completed: a listed tracked query can still lose the last budget units to another caller before the worker reaches it. The request takes no body, and any field sent is rejected. Requires an Idempotency-Key header.
     * Check every eligible tracked query of a project now
     */
    forceCheckAllActiveTrackedQueries(requestParameters: ForceCheckAllActiveTrackedQueriesRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<SubmittedChecksResource>;

    /**
     * Creates request options for getTrackedQuery without sending the request
     * @param {string} organizationId 
     * @param {string} projectId 
     * @param {string} trackedQueryId Must belong to the project in the path.
     * @throws {RequiredError}
     * @memberof TrackedQueriesApiInterface
     */
    getTrackedQueryRequestOpts(requestParameters: GetTrackedQueryRequest): Promise<runtime.RequestOpts>;

    /**
     * Minimum role: viewer. The configuration of one tracked query: the text sent to the engine, the engine, locale and country it is asked in, the clusters it belongs to, and how often it is checked. A lastCheckedAt of null means no check has completed yet — it is not a check that found nothing. A tracked query belonging to another project answers 404, the same answer an unknown id gets, so the API never confirms that an inaccessible tracked query exists. Rank positions, share of voice and sentiment are not part of this response: they belong to a date window and are served by the analytics endpoints.
     * @summary Get a tracked query
     * @param {string} organizationId 
     * @param {string} projectId 
     * @param {string} trackedQueryId Must belong to the project in the path.
     * @param {*} [options] Override http request option.
     * @throws {RequiredError}
     * @memberof TrackedQueriesApiInterface
     */
    getTrackedQueryRaw(requestParameters: GetTrackedQueryRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<TrackedQueryDetailResource>>;

    /**
     * Minimum role: viewer. The configuration of one tracked query: the text sent to the engine, the engine, locale and country it is asked in, the clusters it belongs to, and how often it is checked. A lastCheckedAt of null means no check has completed yet — it is not a check that found nothing. A tracked query belonging to another project answers 404, the same answer an unknown id gets, so the API never confirms that an inaccessible tracked query exists. Rank positions, share of voice and sentiment are not part of this response: they belong to a date window and are served by the analytics endpoints.
     * Get a tracked query
     */
    getTrackedQuery(requestParameters: GetTrackedQueryRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<TrackedQueryDetailResource>;

    /**
     * Creates request options for pauseTrackedQuery without sending the request
     * @param {string} organizationId 
     * @param {string} projectId 
     * @param {string} trackedQueryId Must belong to the project in the path.
     * @param {string} idempotencyKey Repeating a request with the same key answers with the first attempt\&#39;s result instead of pausing again.
     * @throws {RequiredError}
     * @memberof TrackedQueriesApiInterface
     */
    pauseTrackedQueryRequestOpts(requestParameters: PauseTrackedQueryRequest): Promise<runtime.RequestOpts>;

    /**
     * Minimum role: manager. Stops the scheduler from checking this tracked query; it keeps its configuration, its clusters and every result already collected, and nothing is deleted. Pausing a query that is already paused succeeds and answers the same body — this is a PUT asserting a state, not a transition, so it is safe to repeat. It does NOT cancel a check that is already running: a check in flight when the pause lands still completes and still consumes the budget unit it reserved. The request takes no body, and any field sent is rejected. Requires an Idempotency-Key header.
     * @summary Pause a tracked query
     * @param {string} organizationId 
     * @param {string} projectId 
     * @param {string} trackedQueryId Must belong to the project in the path.
     * @param {string} idempotencyKey Repeating a request with the same key answers with the first attempt\&#39;s result instead of pausing again.
     * @param {*} [options] Override http request option.
     * @throws {RequiredError}
     * @memberof TrackedQueriesApiInterface
     */
    pauseTrackedQueryRaw(requestParameters: PauseTrackedQueryRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<TrackedQueryDetailResource>>;

    /**
     * Minimum role: manager. Stops the scheduler from checking this tracked query; it keeps its configuration, its clusters and every result already collected, and nothing is deleted. Pausing a query that is already paused succeeds and answers the same body — this is a PUT asserting a state, not a transition, so it is safe to repeat. It does NOT cancel a check that is already running: a check in flight when the pause lands still completes and still consumes the budget unit it reserved. The request takes no body, and any field sent is rejected. Requires an Idempotency-Key header.
     * Pause a tracked query
     */
    pauseTrackedQuery(requestParameters: PauseTrackedQueryRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<TrackedQueryDetailResource>;

    /**
     * Creates request options for removeClustersFromTrackedQuery without sending the request
     * @param {string} organizationId 
     * @param {string} projectId 
     * @param {string} trackedQueryId Must belong to the project in the path.
     * @param {string} idempotencyKey A client-chosen key, unique per operation, so a lost response can be retried without repeating the write.
     * @param {ClusterMembershipRequestData} clusterMembershipRequestData Required. A DELETE with no body is rejected.
     * @throws {RequiredError}
     * @memberof TrackedQueriesApiInterface
     */
    removeClustersFromTrackedQueryRequestOpts(requestParameters: RemoveClustersFromTrackedQueryRequest): Promise<runtime.RequestOpts>;

    /**
     * Minimum role: manager. Removes the tracked query from every cluster named in \"queryClusterIds\" and answers with the query in its new state. A cluster the query does not belong to is skipped, not reported as an error. Neither the clusters nor the tracked query are deleted: only the membership between them. Every cluster must belong to the project in the path. NOTE: this DELETE requires a request body — some HTTP client libraries and proxies strip bodies from DELETE, and a stripped body is refused with a validation error rather than interpreted as \"remove all clusters\". The \"Idempotency-Key\" header is required, and a repeat of the same key and body returns the recorded answer without removing anything again.
     * @summary Remove a tracked query from clusters
     * @param {string} organizationId 
     * @param {string} projectId 
     * @param {string} trackedQueryId Must belong to the project in the path.
     * @param {string} idempotencyKey A client-chosen key, unique per operation, so a lost response can be retried without repeating the write.
     * @param {ClusterMembershipRequestData} clusterMembershipRequestData Required. A DELETE with no body is rejected.
     * @param {*} [options] Override http request option.
     * @throws {RequiredError}
     * @memberof TrackedQueriesApiInterface
     */
    removeClustersFromTrackedQueryRaw(requestParameters: RemoveClustersFromTrackedQueryRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<TrackedQueryDetailResource>>;

    /**
     * Minimum role: manager. Removes the tracked query from every cluster named in \"queryClusterIds\" and answers with the query in its new state. A cluster the query does not belong to is skipped, not reported as an error. Neither the clusters nor the tracked query are deleted: only the membership between them. Every cluster must belong to the project in the path. NOTE: this DELETE requires a request body — some HTTP client libraries and proxies strip bodies from DELETE, and a stripped body is refused with a validation error rather than interpreted as \"remove all clusters\". The \"Idempotency-Key\" header is required, and a repeat of the same key and body returns the recorded answer without removing anything again.
     * Remove a tracked query from clusters
     */
    removeClustersFromTrackedQuery(requestParameters: RemoveClustersFromTrackedQueryRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<TrackedQueryDetailResource>;

    /**
     * Creates request options for reportAiResponse without sending the request
     * @param {string} organizationId 
     * @param {string} projectId 
     * @param {string} trackedQueryId Must belong to the project in the path.
     * @param {string} aiResponseId A capture id from the AI responses listing. Must belong to the tracked query in the path.
     * @param {string} idempotencyKey A client-chosen key, unique per report, so a lost response can be retried without filing the report twice.
     * @param {ReportAiResponseRequest} reportAiResponseRequest 
     * @throws {RequiredError}
     * @memberof TrackedQueriesApiInterface
     */
    reportAiResponseRequestOpts(requestParameters: ReportAiResponseOperationRequest): Promise<runtime.RequestOpts>;

    /**
     * Minimum role: viewer — deliberately lower than the other tracked-query writes, because a report changes nothing a viewer cannot already read. The key still needs the \"write\" capability. The report is forwarded to the team that reviews the scrape run behind the capture; it does not change the capture, the tracked query, or any metric derived from them, and nothing in this API will show the report afterwards. A 200 means the report was accepted for review, not that anything was corrected, and there is no identifier to poll. A capture that belongs to another tracked query or project answers 404, the same answer an unknown id gets. A capture that cannot be routed back to its scrape run answers 409 \"ai_check_session_unavailable\", and it cannot be reported. That code covers two moments, which behave differently for your key: when the capture carries no scrape run at all the refusal is decided BEFORE anything is sent, so the idempotency key is left unused and the same key answers the same way however often it is presented; when the review system itself rejects the run — its own retention having elapsed — the refusal comes after the forward was attempted, so the key is spent like any outcome we cannot confirm. You can tell them apart without guessing: retry the same key, and a reply of \"operation_outcome_uncertain\" means the refusal came from the review system. Delivery is at-most-once: the \"Idempotency-Key\" header is required and a repeat of the same key and body returns the recorded answer, but a delivery whose outcome is unknown refuses replay on that key rather than risk filing the report twice.
     * @summary Report a problem with a captured AI answer
     * @param {string} organizationId 
     * @param {string} projectId 
     * @param {string} trackedQueryId Must belong to the project in the path.
     * @param {string} aiResponseId A capture id from the AI responses listing. Must belong to the tracked query in the path.
     * @param {string} idempotencyKey A client-chosen key, unique per report, so a lost response can be retried without filing the report twice.
     * @param {ReportAiResponseRequest} reportAiResponseRequest 
     * @param {*} [options] Override http request option.
     * @throws {RequiredError}
     * @memberof TrackedQueriesApiInterface
     */
    reportAiResponseRaw(requestParameters: ReportAiResponseOperationRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<AiResponseReportResource>>;

    /**
     * Minimum role: viewer — deliberately lower than the other tracked-query writes, because a report changes nothing a viewer cannot already read. The key still needs the \"write\" capability. The report is forwarded to the team that reviews the scrape run behind the capture; it does not change the capture, the tracked query, or any metric derived from them, and nothing in this API will show the report afterwards. A 200 means the report was accepted for review, not that anything was corrected, and there is no identifier to poll. A capture that belongs to another tracked query or project answers 404, the same answer an unknown id gets. A capture that cannot be routed back to its scrape run answers 409 \"ai_check_session_unavailable\", and it cannot be reported. That code covers two moments, which behave differently for your key: when the capture carries no scrape run at all the refusal is decided BEFORE anything is sent, so the idempotency key is left unused and the same key answers the same way however often it is presented; when the review system itself rejects the run — its own retention having elapsed — the refusal comes after the forward was attempted, so the key is spent like any outcome we cannot confirm. You can tell them apart without guessing: retry the same key, and a reply of \"operation_outcome_uncertain\" means the refusal came from the review system. Delivery is at-most-once: the \"Idempotency-Key\" header is required and a repeat of the same key and body returns the recorded answer, but a delivery whose outcome is unknown refuses replay on that key rather than risk filing the report twice.
     * Report a problem with a captured AI answer
     */
    reportAiResponse(requestParameters: ReportAiResponseOperationRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<AiResponseReportResource>;

    /**
     * Creates request options for resumeTrackedQuery without sending the request
     * @param {string} organizationId 
     * @param {string} projectId 
     * @param {string} trackedQueryId Must belong to the project in the path.
     * @param {string} idempotencyKey Repeating a request with the same key answers with the first attempt\&#39;s result instead of resuming again.
     * @throws {RequiredError}
     * @memberof TrackedQueriesApiInterface
     */
    resumeTrackedQueryRequestOpts(requestParameters: ResumeTrackedQueryRequest): Promise<runtime.RequestOpts>;

    /**
     * Minimum role: manager. Puts a paused tracked query back under the scheduler. Resuming a query that is already active succeeds and answers the same body — this is a PUT asserting a state, not a transition. It does NOT run a check: the query is checked when it next falls due under its own checkFrequency, and results collected while it was paused are unaffected. Nothing is back-filled for the time it spent paused. The request takes no body, and any field sent is rejected. Requires an Idempotency-Key header.
     * @summary Resume a tracked query
     * @param {string} organizationId 
     * @param {string} projectId 
     * @param {string} trackedQueryId Must belong to the project in the path.
     * @param {string} idempotencyKey Repeating a request with the same key answers with the first attempt\&#39;s result instead of resuming again.
     * @param {*} [options] Override http request option.
     * @throws {RequiredError}
     * @memberof TrackedQueriesApiInterface
     */
    resumeTrackedQueryRaw(requestParameters: ResumeTrackedQueryRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<TrackedQueryDetailResource>>;

    /**
     * Minimum role: manager. Puts a paused tracked query back under the scheduler. Resuming a query that is already active succeeds and answers the same body — this is a PUT asserting a state, not a transition. It does NOT run a check: the query is checked when it next falls due under its own checkFrequency, and results collected while it was paused are unaffected. Nothing is back-filled for the time it spent paused. The request takes no body, and any field sent is rejected. Requires an Idempotency-Key header.
     * Resume a tracked query
     */
    resumeTrackedQuery(requestParameters: ResumeTrackedQueryRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<TrackedQueryDetailResource>;

    /**
     * Creates request options for searchTrackedQueries without sending the request
     * @param {string} organizationId 
     * @param {string} projectId 
     * @param {number} [limit] Page size. A value above the maximum is rejected, never clamped.
     * @param {number} [offset] 
     * @param {string} [search] Free-text search over the keyword.
     * @param {'active' | 'paused'} [status] 
     * @param {Array<'chatgpt' | 'perplexity' | 'google_ai_overview' | 'google_ai_mode' | 'google_serp' | 'google_shopping'>} [engines] Repeatable, or comma-separated.
     * @param {Array<string>} [countries] ISO-3166 alpha-2 codes or English names. Must be configured on the project.
     * @param {'queryText' | 'lastSerpPosition' | 'lastMentionPosition' | 'lastShoppingPosition' | 'lastShareOfVoice' | 'lastPositivityIndex' | 'lastMentionCount' | 'lastCheckedAt'} [sortBy] 
     * @param {'asc' | 'desc'} [sortOrder] 
     * @throws {RequiredError}
     * @memberof TrackedQueriesApiInterface
     */
    searchTrackedQueriesRequestOpts(requestParameters: SearchTrackedQueriesRequest): Promise<runtime.RequestOpts>;

    /**
     * Minimum role: viewer. One row per tracked query — a single keyword on a single engine in a single country — carrying the metrics of its most recent completed check. Filter by status, engine, country and a free-text search over the keyword, and sort by any of the returned metrics. Positions (lastSerpPosition, lastMentionPosition, lastLinkPosition, lastShoppingPosition) are 1-based ranks, so LOWER is better; lastShareOfVoice and lastPositivityIndex are percentages from 0 to 100, where HIGHER is better. Every nullable field means \"not known yet\" rather than zero: a null position is a query with no data for that surface, a null lastPositivityIndex is a check with no mentions to score, and a null lastCheckedAt is a query that has never been checked — none of them is a score of zero. This listing reads a projection refreshed by background subscribers, not the write model, so a tracked query created or changed moments ago may not appear here yet or may still show its previous settings. It catches up on its own; nothing is lost. If you need to read back what you just wrote, the creation response carries the new ids and the single tracked-query operation reads the write model directly. total counts the tracked queries the filters match, not the rows on this page. A limit above the maximum is rejected, never clamped, and a filter this endpoint does not support is rejected rather than ignored.Send `Accept: text/csv` to receive the same page as a CSV download instead of JSON: same filters, same authorization, same page window and the same maximum of 100 rows — it is this page in another format, not a bulk export, so a whole collection is still read by paging. The CSV carries no `total`, because a table whose every row is a record has nowhere to put one; read it from the JSON representation of the same request. A list-valued field is joined into one cell with `; ` as a display projection — parse the JSON if you need the structure. Cells beginning with `=`, `+`, `-` or `@` are prefixed with an apostrophe so a spreadsheet treats them as text rather than running them as formulas.
     * @summary Search a project\'s tracked queries
     * @param {string} organizationId 
     * @param {string} projectId 
     * @param {number} [limit] Page size. A value above the maximum is rejected, never clamped.
     * @param {number} [offset] 
     * @param {string} [search] Free-text search over the keyword.
     * @param {'active' | 'paused'} [status] 
     * @param {Array<'chatgpt' | 'perplexity' | 'google_ai_overview' | 'google_ai_mode' | 'google_serp' | 'google_shopping'>} [engines] Repeatable, or comma-separated.
     * @param {Array<string>} [countries] ISO-3166 alpha-2 codes or English names. Must be configured on the project.
     * @param {'queryText' | 'lastSerpPosition' | 'lastMentionPosition' | 'lastShoppingPosition' | 'lastShareOfVoice' | 'lastPositivityIndex' | 'lastMentionCount' | 'lastCheckedAt'} [sortBy] 
     * @param {'asc' | 'desc'} [sortOrder] 
     * @param {*} [options] Override http request option.
     * @throws {RequiredError}
     * @memberof TrackedQueriesApiInterface
     */
    searchTrackedQueriesRaw(requestParameters: SearchTrackedQueriesRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<SearchTrackedQueries200Response>>;

    /**
     * Minimum role: viewer. One row per tracked query — a single keyword on a single engine in a single country — carrying the metrics of its most recent completed check. Filter by status, engine, country and a free-text search over the keyword, and sort by any of the returned metrics. Positions (lastSerpPosition, lastMentionPosition, lastLinkPosition, lastShoppingPosition) are 1-based ranks, so LOWER is better; lastShareOfVoice and lastPositivityIndex are percentages from 0 to 100, where HIGHER is better. Every nullable field means \"not known yet\" rather than zero: a null position is a query with no data for that surface, a null lastPositivityIndex is a check with no mentions to score, and a null lastCheckedAt is a query that has never been checked — none of them is a score of zero. This listing reads a projection refreshed by background subscribers, not the write model, so a tracked query created or changed moments ago may not appear here yet or may still show its previous settings. It catches up on its own; nothing is lost. If you need to read back what you just wrote, the creation response carries the new ids and the single tracked-query operation reads the write model directly. total counts the tracked queries the filters match, not the rows on this page. A limit above the maximum is rejected, never clamped, and a filter this endpoint does not support is rejected rather than ignored.Send `Accept: text/csv` to receive the same page as a CSV download instead of JSON: same filters, same authorization, same page window and the same maximum of 100 rows — it is this page in another format, not a bulk export, so a whole collection is still read by paging. The CSV carries no `total`, because a table whose every row is a record has nowhere to put one; read it from the JSON representation of the same request. A list-valued field is joined into one cell with `; ` as a display projection — parse the JSON if you need the structure. Cells beginning with `=`, `+`, `-` or `@` are prefixed with an apostrophe so a spreadsheet treats them as text rather than running them as formulas.
     * Search a project\'s tracked queries
     */
    searchTrackedQueries(requestParameters: SearchTrackedQueriesRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<SearchTrackedQueries200Response>;

}

/**
 * 
 */
export class TrackedQueriesApi extends runtime.BaseAPI implements TrackedQueriesApiInterface {

    /**
     * Creates request options for addClustersToTrackedQuery without sending the request
     */
    async addClustersToTrackedQueryRequestOpts(requestParameters: AddClustersToTrackedQueryRequest): Promise<runtime.RequestOpts> {
        if (requestParameters['organizationId'] == null) {
            throw new runtime.RequiredError(
                'organizationId',
                'Required parameter "organizationId" was null or undefined when calling addClustersToTrackedQuery().'
            );
        }

        if (requestParameters['projectId'] == null) {
            throw new runtime.RequiredError(
                'projectId',
                'Required parameter "projectId" was null or undefined when calling addClustersToTrackedQuery().'
            );
        }

        if (requestParameters['trackedQueryId'] == null) {
            throw new runtime.RequiredError(
                'trackedQueryId',
                'Required parameter "trackedQueryId" was null or undefined when calling addClustersToTrackedQuery().'
            );
        }

        if (requestParameters['idempotencyKey'] == null) {
            throw new runtime.RequiredError(
                'idempotencyKey',
                'Required parameter "idempotencyKey" was null or undefined when calling addClustersToTrackedQuery().'
            );
        }

        if (requestParameters['clusterMembershipRequestData'] == null) {
            throw new runtime.RequiredError(
                'clusterMembershipRequestData',
                'Required parameter "clusterMembershipRequestData" was null or undefined when calling addClustersToTrackedQuery().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        headerParameters['Content-Type'] = 'application/json';

        if (requestParameters['idempotencyKey'] != null) {
            headerParameters['Idempotency-Key'] = String(requestParameters['idempotencyKey']);
        }

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("ApiKey", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/api/v1/organizations/{organizationId}/projects/{projectId}/tracked-queries/{trackedQueryId}/clusters`;
        urlPath = urlPath.replace('{organizationId}', encodeURIComponent(String(requestParameters['organizationId'])));
        urlPath = urlPath.replace('{projectId}', encodeURIComponent(String(requestParameters['projectId'])));
        urlPath = urlPath.replace('{trackedQueryId}', encodeURIComponent(String(requestParameters['trackedQueryId'])));

        return {
            path: urlPath,
            method: 'PUT',
            headers: headerParameters,
            query: queryParameters,
            body: ClusterMembershipRequestDataToJSON(requestParameters['clusterMembershipRequestData']),
        };
    }

    /**
     * Minimum role: manager. Adds the tracked query to every cluster named in \"queryClusterIds\" and answers with the query in its new state. Membership is a set: a cluster the query already belongs to is skipped, not reported as an error, and the response still lists it. Clusters the query belongs to and this call does not name are left alone — this adds, it does not replace the membership. Every cluster must belong to the project in the path; one that does not is rejected with its field named, and nothing is written. The \"Idempotency-Key\" header is required, and a repeat of the same key and body returns the recorded answer without adding anything again.
     * Add a tracked query to clusters
     */
    async addClustersToTrackedQueryRaw(requestParameters: AddClustersToTrackedQueryRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<TrackedQueryDetailResource>> {
        const requestOptions = await this.addClustersToTrackedQueryRequestOpts(requestParameters);
        const response = await this.request(requestOptions, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => TrackedQueryDetailResourceFromJSON(jsonValue));
    }

    /**
     * Minimum role: manager. Adds the tracked query to every cluster named in \"queryClusterIds\" and answers with the query in its new state. Membership is a set: a cluster the query already belongs to is skipped, not reported as an error, and the response still lists it. Clusters the query belongs to and this call does not name are left alone — this adds, it does not replace the membership. Every cluster must belong to the project in the path; one that does not is rejected with its field named, and nothing is written. The \"Idempotency-Key\" header is required, and a repeat of the same key and body returns the recorded answer without adding anything again.
     * Add a tracked query to clusters
     */
    async addClustersToTrackedQuery(requestParameters: AddClustersToTrackedQueryRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<TrackedQueryDetailResource> {
        const response = await this.addClustersToTrackedQueryRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Creates request options for batchChangeTrackedQueriesCheckFrequency without sending the request
     */
    async batchChangeTrackedQueriesCheckFrequencyRequestOpts(requestParameters: BatchChangeTrackedQueriesCheckFrequencyRequest): Promise<runtime.RequestOpts> {
        if (requestParameters['organizationId'] == null) {
            throw new runtime.RequiredError(
                'organizationId',
                'Required parameter "organizationId" was null or undefined when calling batchChangeTrackedQueriesCheckFrequency().'
            );
        }

        if (requestParameters['projectId'] == null) {
            throw new runtime.RequiredError(
                'projectId',
                'Required parameter "projectId" was null or undefined when calling batchChangeTrackedQueriesCheckFrequency().'
            );
        }

        if (requestParameters['idempotencyKey'] == null) {
            throw new runtime.RequiredError(
                'idempotencyKey',
                'Required parameter "idempotencyKey" was null or undefined when calling batchChangeTrackedQueriesCheckFrequency().'
            );
        }

        if (requestParameters['batchChangeTrackedQueryCheckFrequencyRequestData'] == null) {
            throw new runtime.RequiredError(
                'batchChangeTrackedQueryCheckFrequencyRequestData',
                'Required parameter "batchChangeTrackedQueryCheckFrequencyRequestData" was null or undefined when calling batchChangeTrackedQueriesCheckFrequency().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        headerParameters['Content-Type'] = 'application/json';

        if (requestParameters['idempotencyKey'] != null) {
            headerParameters['Idempotency-Key'] = String(requestParameters['idempotencyKey']);
        }

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("ApiKey", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/api/v1/organizations/{organizationId}/projects/{projectId}/tracked-queries/batch/check-frequency`;
        urlPath = urlPath.replace('{organizationId}', encodeURIComponent(String(requestParameters['organizationId'])));
        urlPath = urlPath.replace('{projectId}', encodeURIComponent(String(requestParameters['projectId'])));

        return {
            path: urlPath,
            method: 'PUT',
            headers: headerParameters,
            query: queryParameters,
            body: BatchChangeTrackedQueryCheckFrequencyRequestDataToJSON(requestParameters['batchChangeTrackedQueryCheckFrequencyRequestData']),
        };
    }

    /**
     * Minimum role: manager. Sets the same check frequency on every tracked query named in `ids`, at most 100 distinct ids per call. The organization must be active and the project must not be archived. Partial success: an id that is not a tracked query of this project — unknown, malformed, already deleted, or belonging to somewhere else — is reported under `failed` with `tracked_query_not_found` while the rest are changed, and the call still answers 200. Nothing is rolled back because an item failed. Setting the frequency a query already has is accepted and changes nothing. This does NOT run a check and does not reschedule one already in flight. It never changes more than the ids it is given: there is no \"change everything matching a filter\" mode. The Idempotency-Key header is required; a retry with the same key and the same body returns this same answer without applying anything again.
     * Change how often several tracked queries are checked
     */
    async batchChangeTrackedQueriesCheckFrequencyRaw(requestParameters: BatchChangeTrackedQueriesCheckFrequencyRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<BatchWriteOutcome>> {
        const requestOptions = await this.batchChangeTrackedQueriesCheckFrequencyRequestOpts(requestParameters);
        const response = await this.request(requestOptions, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => BatchWriteOutcomeFromJSON(jsonValue));
    }

    /**
     * Minimum role: manager. Sets the same check frequency on every tracked query named in `ids`, at most 100 distinct ids per call. The organization must be active and the project must not be archived. Partial success: an id that is not a tracked query of this project — unknown, malformed, already deleted, or belonging to somewhere else — is reported under `failed` with `tracked_query_not_found` while the rest are changed, and the call still answers 200. Nothing is rolled back because an item failed. Setting the frequency a query already has is accepted and changes nothing. This does NOT run a check and does not reschedule one already in flight. It never changes more than the ids it is given: there is no \"change everything matching a filter\" mode. The Idempotency-Key header is required; a retry with the same key and the same body returns this same answer without applying anything again.
     * Change how often several tracked queries are checked
     */
    async batchChangeTrackedQueriesCheckFrequency(requestParameters: BatchChangeTrackedQueriesCheckFrequencyRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<BatchWriteOutcome> {
        const response = await this.batchChangeTrackedQueriesCheckFrequencyRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Creates request options for batchChangeTrackedQueriesNPasses without sending the request
     */
    async batchChangeTrackedQueriesNPassesRequestOpts(requestParameters: BatchChangeTrackedQueriesNPassesRequest): Promise<runtime.RequestOpts> {
        if (requestParameters['organizationId'] == null) {
            throw new runtime.RequiredError(
                'organizationId',
                'Required parameter "organizationId" was null or undefined when calling batchChangeTrackedQueriesNPasses().'
            );
        }

        if (requestParameters['projectId'] == null) {
            throw new runtime.RequiredError(
                'projectId',
                'Required parameter "projectId" was null or undefined when calling batchChangeTrackedQueriesNPasses().'
            );
        }

        if (requestParameters['idempotencyKey'] == null) {
            throw new runtime.RequiredError(
                'idempotencyKey',
                'Required parameter "idempotencyKey" was null or undefined when calling batchChangeTrackedQueriesNPasses().'
            );
        }

        if (requestParameters['batchChangeTrackedQueryPassesRequestData'] == null) {
            throw new runtime.RequiredError(
                'batchChangeTrackedQueryPassesRequestData',
                'Required parameter "batchChangeTrackedQueryPassesRequestData" was null or undefined when calling batchChangeTrackedQueriesNPasses().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        headerParameters['Content-Type'] = 'application/json';

        if (requestParameters['idempotencyKey'] != null) {
            headerParameters['Idempotency-Key'] = String(requestParameters['idempotencyKey']);
        }

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("ApiKey", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/api/v1/organizations/{organizationId}/projects/{projectId}/tracked-queries/batch/passes`;
        urlPath = urlPath.replace('{organizationId}', encodeURIComponent(String(requestParameters['organizationId'])));
        urlPath = urlPath.replace('{projectId}', encodeURIComponent(String(requestParameters['projectId'])));

        return {
            path: urlPath,
            method: 'PUT',
            headers: headerParameters,
            query: queryParameters,
            body: BatchChangeTrackedQueryPassesRequestDataToJSON(requestParameters['batchChangeTrackedQueryPassesRequestData']),
        };
    }

    /**
     * Minimum role: manager. Sets the same passes-per-check on every tracked query named in `ids`, at most 100 distinct ids per call. The organization must be active and the project must not be archived. Partial success, and two distinct reasons appear under `failed`: an id that is not a tracked query of this project answers `tracked_query_not_found`, and a `google_serp` or `google_shopping` query asked for more than one pass answers `n_passes_not_supported_for_engine` — those engines run a single pass. Both leave the rest of the batch changed and the call still answers 200. Setting the value back to 1 is always allowed, so a batch that lowers sampling never fails on engine grounds. This does NOT run a check and does not change history already captured. More passes cost proportionally more of the plan\'s check budget. It never changes more than the ids it is given: there is no \"change everything matching a filter\" mode. The Idempotency-Key header is required; a retry with the same key and the same body returns this same answer without applying anything again.
     * Change how many passes several tracked queries run per check
     */
    async batchChangeTrackedQueriesNPassesRaw(requestParameters: BatchChangeTrackedQueriesNPassesRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<BatchWriteOutcome>> {
        const requestOptions = await this.batchChangeTrackedQueriesNPassesRequestOpts(requestParameters);
        const response = await this.request(requestOptions, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => BatchWriteOutcomeFromJSON(jsonValue));
    }

    /**
     * Minimum role: manager. Sets the same passes-per-check on every tracked query named in `ids`, at most 100 distinct ids per call. The organization must be active and the project must not be archived. Partial success, and two distinct reasons appear under `failed`: an id that is not a tracked query of this project answers `tracked_query_not_found`, and a `google_serp` or `google_shopping` query asked for more than one pass answers `n_passes_not_supported_for_engine` — those engines run a single pass. Both leave the rest of the batch changed and the call still answers 200. Setting the value back to 1 is always allowed, so a batch that lowers sampling never fails on engine grounds. This does NOT run a check and does not change history already captured. More passes cost proportionally more of the plan\'s check budget. It never changes more than the ids it is given: there is no \"change everything matching a filter\" mode. The Idempotency-Key header is required; a retry with the same key and the same body returns this same answer without applying anything again.
     * Change how many passes several tracked queries run per check
     */
    async batchChangeTrackedQueriesNPasses(requestParameters: BatchChangeTrackedQueriesNPassesRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<BatchWriteOutcome> {
        const response = await this.batchChangeTrackedQueriesNPassesRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Creates request options for batchCreateTrackedQueries without sending the request
     */
    async batchCreateTrackedQueriesRequestOpts(requestParameters: BatchCreateTrackedQueriesRequest): Promise<runtime.RequestOpts> {
        if (requestParameters['organizationId'] == null) {
            throw new runtime.RequiredError(
                'organizationId',
                'Required parameter "organizationId" was null or undefined when calling batchCreateTrackedQueries().'
            );
        }

        if (requestParameters['projectId'] == null) {
            throw new runtime.RequiredError(
                'projectId',
                'Required parameter "projectId" was null or undefined when calling batchCreateTrackedQueries().'
            );
        }

        if (requestParameters['idempotencyKey'] == null) {
            throw new runtime.RequiredError(
                'idempotencyKey',
                'Required parameter "idempotencyKey" was null or undefined when calling batchCreateTrackedQueries().'
            );
        }

        if (requestParameters['batchCreateTrackedQueriesRequestData'] == null) {
            throw new runtime.RequiredError(
                'batchCreateTrackedQueriesRequestData',
                'Required parameter "batchCreateTrackedQueriesRequestData" was null or undefined when calling batchCreateTrackedQueries().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        headerParameters['Content-Type'] = 'application/json';

        if (requestParameters['idempotencyKey'] != null) {
            headerParameters['Idempotency-Key'] = String(requestParameters['idempotencyKey']);
        }

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("ApiKey", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/api/v1/organizations/{organizationId}/projects/{projectId}/tracked-queries`;
        urlPath = urlPath.replace('{organizationId}', encodeURIComponent(String(requestParameters['organizationId'])));
        urlPath = urlPath.replace('{projectId}', encodeURIComponent(String(requestParameters['projectId'])));

        return {
            path: urlPath,
            method: 'POST',
            headers: headerParameters,
            query: queryParameters,
            body: BatchCreateTrackedQueriesRequestDataToJSON(requestParameters['batchCreateTrackedQueriesRequestData']),
        };
    }

    /**
     * Minimum role: manager. Creates the cross product of `queryTexts` x `engines` x `countries`: three texts, two engines and two countries create twelve tracked queries, not three. At most 100 combinations per call. The organization must be active and the project must not be archived. Partial success: the response lists, per combination, either the id it was created under or why nothing was created for it, and the status code never reports item-level outcomes. A combination the project already tracks is NOT created again and NOT re-identified — it appears under `failed` with `tracked_query_already_exists`, keeps the id it already had, and has any `queryClusterIds` in this request merged into it. Query text is normalised before it is compared and stored (lower-cased, whitespace collapsed, leading list markers stripped), so two texts differing only in those respects are one tracked query: the first of them owns the outcome and every later one appears under `failed` with `duplicate_combination_in_request`, naming the entry it repeats. `nPasses` applies to AI engines only: `google_serp` and `google_shopping` rows are always created with one pass, whatever is sent. Google AI Mode is unavailable in a few countries and those combinations are reported under `failed` rather than created. Creating a tracked query does not run a check: the first check happens on the normal schedule for the `checkFrequency` chosen. The Idempotency-Key header is required; a retry with the same key and the same body returns this same answer without creating anything again.
     * Create tracked queries
     */
    async batchCreateTrackedQueriesRaw(requestParameters: BatchCreateTrackedQueriesRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<BatchCreateTrackedQueriesResultResource>> {
        const requestOptions = await this.batchCreateTrackedQueriesRequestOpts(requestParameters);
        const response = await this.request(requestOptions, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => BatchCreateTrackedQueriesResultResourceFromJSON(jsonValue));
    }

    /**
     * Minimum role: manager. Creates the cross product of `queryTexts` x `engines` x `countries`: three texts, two engines and two countries create twelve tracked queries, not three. At most 100 combinations per call. The organization must be active and the project must not be archived. Partial success: the response lists, per combination, either the id it was created under or why nothing was created for it, and the status code never reports item-level outcomes. A combination the project already tracks is NOT created again and NOT re-identified — it appears under `failed` with `tracked_query_already_exists`, keeps the id it already had, and has any `queryClusterIds` in this request merged into it. Query text is normalised before it is compared and stored (lower-cased, whitespace collapsed, leading list markers stripped), so two texts differing only in those respects are one tracked query: the first of them owns the outcome and every later one appears under `failed` with `duplicate_combination_in_request`, naming the entry it repeats. `nPasses` applies to AI engines only: `google_serp` and `google_shopping` rows are always created with one pass, whatever is sent. Google AI Mode is unavailable in a few countries and those combinations are reported under `failed` rather than created. Creating a tracked query does not run a check: the first check happens on the normal schedule for the `checkFrequency` chosen. The Idempotency-Key header is required; a retry with the same key and the same body returns this same answer without creating anything again.
     * Create tracked queries
     */
    async batchCreateTrackedQueries(requestParameters: BatchCreateTrackedQueriesRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<BatchCreateTrackedQueriesResultResource> {
        const response = await this.batchCreateTrackedQueriesRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Creates request options for batchForceCheckTrackedQueries without sending the request
     */
    async batchForceCheckTrackedQueriesRequestOpts(requestParameters: BatchForceCheckTrackedQueriesRequest): Promise<runtime.RequestOpts> {
        if (requestParameters['organizationId'] == null) {
            throw new runtime.RequiredError(
                'organizationId',
                'Required parameter "organizationId" was null or undefined when calling batchForceCheckTrackedQueries().'
            );
        }

        if (requestParameters['projectId'] == null) {
            throw new runtime.RequiredError(
                'projectId',
                'Required parameter "projectId" was null or undefined when calling batchForceCheckTrackedQueries().'
            );
        }

        if (requestParameters['idempotencyKey'] == null) {
            throw new runtime.RequiredError(
                'idempotencyKey',
                'Required parameter "idempotencyKey" was null or undefined when calling batchForceCheckTrackedQueries().'
            );
        }

        if (requestParameters['batchTargetsRequestData'] == null) {
            throw new runtime.RequiredError(
                'batchTargetsRequestData',
                'Required parameter "batchTargetsRequestData" was null or undefined when calling batchForceCheckTrackedQueries().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        headerParameters['Content-Type'] = 'application/json';

        if (requestParameters['idempotencyKey'] != null) {
            headerParameters['Idempotency-Key'] = String(requestParameters['idempotencyKey']);
        }

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("ApiKey", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/api/v1/organizations/{organizationId}/projects/{projectId}/tracked-queries/check`;
        urlPath = urlPath.replace('{organizationId}', encodeURIComponent(String(requestParameters['organizationId'])));
        urlPath = urlPath.replace('{projectId}', encodeURIComponent(String(requestParameters['projectId'])));

        return {
            path: urlPath,
            method: 'PUT',
            headers: headerParameters,
            query: queryParameters,
            body: BatchTargetsRequestDataToJSON(requestParameters['batchTargetsRequestData']),
        };
    }

    /**
     * Minimum role: manager. Asks for a fresh check of up to 100 named tracked queries straight away, ignoring how recently each was last checked. Answers 200 with per-item results and NO job id: a check that is already in flight for a query is reused rather than started again, so there is no id this operation could hand back that is guaranteed to exist. Follow progress on the tracked query itself — its lastCheckedAt advances when the check completes. A successful item means the check was accepted for submission with budget available for it at that moment; it does not mean the check has run. A checked query costs one budget unit per pass (nPasses), and items that do not fit the remaining budget are reported as failed with `check_budget_forecast_exhausted`, or `subscription_not_found` when the organization has no entitled subscription — they are never reported as successful. That budget figure is a forecast for this batch, and a pessimistic one: a tracked query already being checked is joined to the check in flight and costs nothing, but it is still debited here, so an item refused this way may have fitted. Resubmit it in a later batch rather than treating the refusal as a statement about your subscription. A paused query is reported as failed with `tracked_query_already_paused`: paused queries are never checked. An id that is not a tracked query of this project is reported as failed with `tracked_query_not_found`, exactly as an id that does not exist. Requires an Idempotency-Key header; a retry must repeat the same ids in the same order.
     * Check several tracked queries now
     */
    async batchForceCheckTrackedQueriesRaw(requestParameters: BatchForceCheckTrackedQueriesRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<BatchForceCheckTrackedQueries200Response>> {
        const requestOptions = await this.batchForceCheckTrackedQueriesRequestOpts(requestParameters);
        const response = await this.request(requestOptions, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => BatchForceCheckTrackedQueries200ResponseFromJSON(jsonValue));
    }

    /**
     * Minimum role: manager. Asks for a fresh check of up to 100 named tracked queries straight away, ignoring how recently each was last checked. Answers 200 with per-item results and NO job id: a check that is already in flight for a query is reused rather than started again, so there is no id this operation could hand back that is guaranteed to exist. Follow progress on the tracked query itself — its lastCheckedAt advances when the check completes. A successful item means the check was accepted for submission with budget available for it at that moment; it does not mean the check has run. A checked query costs one budget unit per pass (nPasses), and items that do not fit the remaining budget are reported as failed with `check_budget_forecast_exhausted`, or `subscription_not_found` when the organization has no entitled subscription — they are never reported as successful. That budget figure is a forecast for this batch, and a pessimistic one: a tracked query already being checked is joined to the check in flight and costs nothing, but it is still debited here, so an item refused this way may have fitted. Resubmit it in a later batch rather than treating the refusal as a statement about your subscription. A paused query is reported as failed with `tracked_query_already_paused`: paused queries are never checked. An id that is not a tracked query of this project is reported as failed with `tracked_query_not_found`, exactly as an id that does not exist. Requires an Idempotency-Key header; a retry must repeat the same ids in the same order.
     * Check several tracked queries now
     */
    async batchForceCheckTrackedQueries(requestParameters: BatchForceCheckTrackedQueriesRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<BatchForceCheckTrackedQueries200Response> {
        const response = await this.batchForceCheckTrackedQueriesRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Creates request options for batchPauseTrackedQueries without sending the request
     */
    async batchPauseTrackedQueriesRequestOpts(requestParameters: BatchPauseTrackedQueriesRequest): Promise<runtime.RequestOpts> {
        if (requestParameters['organizationId'] == null) {
            throw new runtime.RequiredError(
                'organizationId',
                'Required parameter "organizationId" was null or undefined when calling batchPauseTrackedQueries().'
            );
        }

        if (requestParameters['projectId'] == null) {
            throw new runtime.RequiredError(
                'projectId',
                'Required parameter "projectId" was null or undefined when calling batchPauseTrackedQueries().'
            );
        }

        if (requestParameters['idempotencyKey'] == null) {
            throw new runtime.RequiredError(
                'idempotencyKey',
                'Required parameter "idempotencyKey" was null or undefined when calling batchPauseTrackedQueries().'
            );
        }

        if (requestParameters['batchTargetsRequestData'] == null) {
            throw new runtime.RequiredError(
                'batchTargetsRequestData',
                'Required parameter "batchTargetsRequestData" was null or undefined when calling batchPauseTrackedQueries().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        headerParameters['Content-Type'] = 'application/json';

        if (requestParameters['idempotencyKey'] != null) {
            headerParameters['Idempotency-Key'] = String(requestParameters['idempotencyKey']);
        }

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("ApiKey", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/api/v1/organizations/{organizationId}/projects/{projectId}/tracked-queries/batch/pause`;
        urlPath = urlPath.replace('{organizationId}', encodeURIComponent(String(requestParameters['organizationId'])));
        urlPath = urlPath.replace('{projectId}', encodeURIComponent(String(requestParameters['projectId'])));

        return {
            path: urlPath,
            method: 'PUT',
            headers: headerParameters,
            query: queryParameters,
            body: BatchTargetsRequestDataToJSON(requestParameters['batchTargetsRequestData']),
        };
    }

    /**
     * Minimum role: manager. Pauses up to 100 tracked queries of one project, each independently. Always answers 200 when the batch itself was processed: read `failed` to find out which items were not paused, never the status code. Items are never rolled back because a later one failed. An id that is already paused is reported as successful — pausing asserts a state, not a transition. An id that is not a tracked query of this project is reported as failed with `tracked_query_not_found`, exactly as an id that does not exist at all, and nothing is written for it. Pausing does not cancel a check that is already running. Requires an Idempotency-Key header; a retry must repeat the same ids in the same order to be recognised as a retry rather than refused as a reused key.
     * Pause several tracked queries
     */
    async batchPauseTrackedQueriesRaw(requestParameters: BatchPauseTrackedQueriesRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<BatchPauseTrackedQueries200Response>> {
        const requestOptions = await this.batchPauseTrackedQueriesRequestOpts(requestParameters);
        const response = await this.request(requestOptions, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => BatchPauseTrackedQueries200ResponseFromJSON(jsonValue));
    }

    /**
     * Minimum role: manager. Pauses up to 100 tracked queries of one project, each independently. Always answers 200 when the batch itself was processed: read `failed` to find out which items were not paused, never the status code. Items are never rolled back because a later one failed. An id that is already paused is reported as successful — pausing asserts a state, not a transition. An id that is not a tracked query of this project is reported as failed with `tracked_query_not_found`, exactly as an id that does not exist at all, and nothing is written for it. Pausing does not cancel a check that is already running. Requires an Idempotency-Key header; a retry must repeat the same ids in the same order to be recognised as a retry rather than refused as a reused key.
     * Pause several tracked queries
     */
    async batchPauseTrackedQueries(requestParameters: BatchPauseTrackedQueriesRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<BatchPauseTrackedQueries200Response> {
        const response = await this.batchPauseTrackedQueriesRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Creates request options for batchResumeTrackedQueries without sending the request
     */
    async batchResumeTrackedQueriesRequestOpts(requestParameters: BatchResumeTrackedQueriesRequest): Promise<runtime.RequestOpts> {
        if (requestParameters['organizationId'] == null) {
            throw new runtime.RequiredError(
                'organizationId',
                'Required parameter "organizationId" was null or undefined when calling batchResumeTrackedQueries().'
            );
        }

        if (requestParameters['projectId'] == null) {
            throw new runtime.RequiredError(
                'projectId',
                'Required parameter "projectId" was null or undefined when calling batchResumeTrackedQueries().'
            );
        }

        if (requestParameters['idempotencyKey'] == null) {
            throw new runtime.RequiredError(
                'idempotencyKey',
                'Required parameter "idempotencyKey" was null or undefined when calling batchResumeTrackedQueries().'
            );
        }

        if (requestParameters['batchTargetsRequestData'] == null) {
            throw new runtime.RequiredError(
                'batchTargetsRequestData',
                'Required parameter "batchTargetsRequestData" was null or undefined when calling batchResumeTrackedQueries().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        headerParameters['Content-Type'] = 'application/json';

        if (requestParameters['idempotencyKey'] != null) {
            headerParameters['Idempotency-Key'] = String(requestParameters['idempotencyKey']);
        }

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("ApiKey", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/api/v1/organizations/{organizationId}/projects/{projectId}/tracked-queries/batch/resume`;
        urlPath = urlPath.replace('{organizationId}', encodeURIComponent(String(requestParameters['organizationId'])));
        urlPath = urlPath.replace('{projectId}', encodeURIComponent(String(requestParameters['projectId'])));

        return {
            path: urlPath,
            method: 'PUT',
            headers: headerParameters,
            query: queryParameters,
            body: BatchTargetsRequestDataToJSON(requestParameters['batchTargetsRequestData']),
        };
    }

    /**
     * Minimum role: manager. Puts up to 100 paused tracked queries of one project back under the scheduler, each independently. Always answers 200 when the batch itself was processed: read `failed` to find out which items were not resumed, never the status code. Items are never rolled back because a later one failed. An id that is already active is reported as successful — resuming asserts a state, not a transition. An id that is not a tracked query of this project is reported as failed with `tracked_query_not_found`, exactly as an id that does not exist at all, and nothing is written for it. No check is run by this operation and nothing is back-filled for the time the queries spent paused. Requires an Idempotency-Key header; a retry must repeat the same ids in the same order to be recognised as a retry rather than refused as a reused key.
     * Resume several tracked queries
     */
    async batchResumeTrackedQueriesRaw(requestParameters: BatchResumeTrackedQueriesRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<BatchResumeTrackedQueries200Response>> {
        const requestOptions = await this.batchResumeTrackedQueriesRequestOpts(requestParameters);
        const response = await this.request(requestOptions, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => BatchResumeTrackedQueries200ResponseFromJSON(jsonValue));
    }

    /**
     * Minimum role: manager. Puts up to 100 paused tracked queries of one project back under the scheduler, each independently. Always answers 200 when the batch itself was processed: read `failed` to find out which items were not resumed, never the status code. Items are never rolled back because a later one failed. An id that is already active is reported as successful — resuming asserts a state, not a transition. An id that is not a tracked query of this project is reported as failed with `tracked_query_not_found`, exactly as an id that does not exist at all, and nothing is written for it. No check is run by this operation and nothing is back-filled for the time the queries spent paused. Requires an Idempotency-Key header; a retry must repeat the same ids in the same order to be recognised as a retry rather than refused as a reused key.
     * Resume several tracked queries
     */
    async batchResumeTrackedQueries(requestParameters: BatchResumeTrackedQueriesRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<BatchResumeTrackedQueries200Response> {
        const response = await this.batchResumeTrackedQueriesRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Creates request options for bulkAddClustersToTrackedQueries without sending the request
     */
    async bulkAddClustersToTrackedQueriesRequestOpts(requestParameters: BulkAddClustersToTrackedQueriesOperationRequest): Promise<runtime.RequestOpts> {
        if (requestParameters['organizationId'] == null) {
            throw new runtime.RequiredError(
                'organizationId',
                'Required parameter "organizationId" was null or undefined when calling bulkAddClustersToTrackedQueries().'
            );
        }

        if (requestParameters['projectId'] == null) {
            throw new runtime.RequiredError(
                'projectId',
                'Required parameter "projectId" was null or undefined when calling bulkAddClustersToTrackedQueries().'
            );
        }

        if (requestParameters['idempotencyKey'] == null) {
            throw new runtime.RequiredError(
                'idempotencyKey',
                'Required parameter "idempotencyKey" was null or undefined when calling bulkAddClustersToTrackedQueries().'
            );
        }

        if (requestParameters['bulkAddClustersToTrackedQueriesRequest'] == null) {
            throw new runtime.RequiredError(
                'bulkAddClustersToTrackedQueriesRequest',
                'Required parameter "bulkAddClustersToTrackedQueriesRequest" was null or undefined when calling bulkAddClustersToTrackedQueries().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        headerParameters['Content-Type'] = 'application/json';

        if (requestParameters['idempotencyKey'] != null) {
            headerParameters['Idempotency-Key'] = String(requestParameters['idempotencyKey']);
        }

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("ApiKey", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/api/v1/organizations/{organizationId}/projects/{projectId}/tracked-queries/bulk/clusters`;
        urlPath = urlPath.replace('{organizationId}', encodeURIComponent(String(requestParameters['organizationId'])));
        urlPath = urlPath.replace('{projectId}', encodeURIComponent(String(requestParameters['projectId'])));

        return {
            path: urlPath,
            method: 'PUT',
            headers: headerParameters,
            query: queryParameters,
            body: BulkAddClustersToTrackedQueriesRequestToJSON(requestParameters['bulkAddClustersToTrackedQueriesRequest']),
        };
    }

    /**
     * Minimum role: manager. Adds every tracked query named in \"ids\" to every cluster named in \"queryClusterIds\". At most 100 distinct tracked queries per call; duplicates in \"ids\" are collapsed. Partial success: the answer is 200 with a per-item \"successful\" and \"failed\" list even when some items failed, nothing is rolled back, and an id that is not a tracked query of this project is reported as a failed item rather than dropped. Clusters not named are left alone — this adds, it does not replace membership. Every cluster must belong to the project in the path; one that does not is rejected with its field named and nothing is written at all. The \"Idempotency-Key\" header is required, and a repeat of the same key and body returns the recorded answer without running the batch again.
     * Add many tracked queries to clusters
     */
    async bulkAddClustersToTrackedQueriesRaw(requestParameters: BulkAddClustersToTrackedQueriesOperationRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<BulkAddClustersToTrackedQueries200Response>> {
        const requestOptions = await this.bulkAddClustersToTrackedQueriesRequestOpts(requestParameters);
        const response = await this.request(requestOptions, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => BulkAddClustersToTrackedQueries200ResponseFromJSON(jsonValue));
    }

    /**
     * Minimum role: manager. Adds every tracked query named in \"ids\" to every cluster named in \"queryClusterIds\". At most 100 distinct tracked queries per call; duplicates in \"ids\" are collapsed. Partial success: the answer is 200 with a per-item \"successful\" and \"failed\" list even when some items failed, nothing is rolled back, and an id that is not a tracked query of this project is reported as a failed item rather than dropped. Clusters not named are left alone — this adds, it does not replace membership. Every cluster must belong to the project in the path; one that does not is rejected with its field named and nothing is written at all. The \"Idempotency-Key\" header is required, and a repeat of the same key and body returns the recorded answer without running the batch again.
     * Add many tracked queries to clusters
     */
    async bulkAddClustersToTrackedQueries(requestParameters: BulkAddClustersToTrackedQueriesOperationRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<BulkAddClustersToTrackedQueries200Response> {
        const response = await this.bulkAddClustersToTrackedQueriesRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Creates request options for bulkDeleteTrackedQueries without sending the request
     */
    async bulkDeleteTrackedQueriesRequestOpts(requestParameters: BulkDeleteTrackedQueriesRequest): Promise<runtime.RequestOpts> {
        if (requestParameters['organizationId'] == null) {
            throw new runtime.RequiredError(
                'organizationId',
                'Required parameter "organizationId" was null or undefined when calling bulkDeleteTrackedQueries().'
            );
        }

        if (requestParameters['projectId'] == null) {
            throw new runtime.RequiredError(
                'projectId',
                'Required parameter "projectId" was null or undefined when calling bulkDeleteTrackedQueries().'
            );
        }

        if (requestParameters['idempotencyKey'] == null) {
            throw new runtime.RequiredError(
                'idempotencyKey',
                'Required parameter "idempotencyKey" was null or undefined when calling bulkDeleteTrackedQueries().'
            );
        }

        if (requestParameters['batchTargetsRequestData'] == null) {
            throw new runtime.RequiredError(
                'batchTargetsRequestData',
                'Required parameter "batchTargetsRequestData" was null or undefined when calling bulkDeleteTrackedQueries().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        headerParameters['Content-Type'] = 'application/json';

        if (requestParameters['idempotencyKey'] != null) {
            headerParameters['Idempotency-Key'] = String(requestParameters['idempotencyKey']);
        }

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("ApiKey", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/api/v1/organizations/{organizationId}/projects/{projectId}/tracked-queries/bulk-delete`;
        urlPath = urlPath.replace('{organizationId}', encodeURIComponent(String(requestParameters['organizationId'])));
        urlPath = urlPath.replace('{projectId}', encodeURIComponent(String(requestParameters['projectId'])));

        return {
            path: urlPath,
            method: 'POST',
            headers: headerParameters,
            query: queryParameters,
            body: BatchTargetsRequestDataToJSON(requestParameters['batchTargetsRequestData']),
        };
    }

    /**
     * Minimum role: manager. Permanently deletes the tracked queries named in `ids`, at most 100 distinct ids per call. The organization must be active and the project must not be archived. Deletion is hard and cannot be undone: the tracked query is removed along with its captured answers, matches, search pages and rank history, and any check already in flight for it is cancelled. Those cascades run in the background, so a 200 means the tracked queries were deleted, not that every derived record has finished being purged. Partial success: an id that is not a tracked query of this project — unknown, malformed, already deleted, or belonging to somewhere else — is reported under `failed` with `tracked_query_not_found` while the rest are deleted. It never deletes more than the ids it is given: there is no \"delete everything matching a filter\" mode. The Idempotency-Key header is required; a retry with the same key and the same body returns this same answer without deleting anything again.
     * Delete tracked queries
     */
    async bulkDeleteTrackedQueriesRaw(requestParameters: BulkDeleteTrackedQueriesRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<BatchWriteOutcome>> {
        const requestOptions = await this.bulkDeleteTrackedQueriesRequestOpts(requestParameters);
        const response = await this.request(requestOptions, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => BatchWriteOutcomeFromJSON(jsonValue));
    }

    /**
     * Minimum role: manager. Permanently deletes the tracked queries named in `ids`, at most 100 distinct ids per call. The organization must be active and the project must not be archived. Deletion is hard and cannot be undone: the tracked query is removed along with its captured answers, matches, search pages and rank history, and any check already in flight for it is cancelled. Those cascades run in the background, so a 200 means the tracked queries were deleted, not that every derived record has finished being purged. Partial success: an id that is not a tracked query of this project — unknown, malformed, already deleted, or belonging to somewhere else — is reported under `failed` with `tracked_query_not_found` while the rest are deleted. It never deletes more than the ids it is given: there is no \"delete everything matching a filter\" mode. The Idempotency-Key header is required; a retry with the same key and the same body returns this same answer without deleting anything again.
     * Delete tracked queries
     */
    async bulkDeleteTrackedQueries(requestParameters: BulkDeleteTrackedQueriesRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<BatchWriteOutcome> {
        const response = await this.bulkDeleteTrackedQueriesRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Creates request options for bulkRemoveClustersFromTrackedQueries without sending the request
     */
    async bulkRemoveClustersFromTrackedQueriesRequestOpts(requestParameters: BulkRemoveClustersFromTrackedQueriesOperationRequest): Promise<runtime.RequestOpts> {
        if (requestParameters['organizationId'] == null) {
            throw new runtime.RequiredError(
                'organizationId',
                'Required parameter "organizationId" was null or undefined when calling bulkRemoveClustersFromTrackedQueries().'
            );
        }

        if (requestParameters['projectId'] == null) {
            throw new runtime.RequiredError(
                'projectId',
                'Required parameter "projectId" was null or undefined when calling bulkRemoveClustersFromTrackedQueries().'
            );
        }

        if (requestParameters['idempotencyKey'] == null) {
            throw new runtime.RequiredError(
                'idempotencyKey',
                'Required parameter "idempotencyKey" was null or undefined when calling bulkRemoveClustersFromTrackedQueries().'
            );
        }

        if (requestParameters['bulkRemoveClustersFromTrackedQueriesRequest'] == null) {
            throw new runtime.RequiredError(
                'bulkRemoveClustersFromTrackedQueriesRequest',
                'Required parameter "bulkRemoveClustersFromTrackedQueriesRequest" was null or undefined when calling bulkRemoveClustersFromTrackedQueries().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        headerParameters['Content-Type'] = 'application/json';

        if (requestParameters['idempotencyKey'] != null) {
            headerParameters['Idempotency-Key'] = String(requestParameters['idempotencyKey']);
        }

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("ApiKey", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/api/v1/organizations/{organizationId}/projects/{projectId}/tracked-queries/bulk/clusters`;
        urlPath = urlPath.replace('{organizationId}', encodeURIComponent(String(requestParameters['organizationId'])));
        urlPath = urlPath.replace('{projectId}', encodeURIComponent(String(requestParameters['projectId'])));

        return {
            path: urlPath,
            method: 'DELETE',
            headers: headerParameters,
            query: queryParameters,
            body: BulkRemoveClustersFromTrackedQueriesRequestToJSON(requestParameters['bulkRemoveClustersFromTrackedQueriesRequest']),
        };
    }

    /**
     * Minimum role: manager. Removes every tracked query named in \"ids\" from every cluster named in \"queryClusterIds\". At most 100 distinct tracked queries per call; duplicates in \"ids\" are collapsed. Partial success: the answer is 200 with a per-item \"successful\" and \"failed\" list even when some items failed, nothing is rolled back, and an id that is not a tracked query of this project is reported as a failed item rather than dropped. Neither the clusters nor the tracked queries are deleted: only the membership between them. Every cluster must belong to the project in the path; one that does not is rejected with its field named and nothing is written at all. NOTE: this DELETE requires a request body — some HTTP client libraries and proxies strip bodies from DELETE, and a stripped body is refused with a validation error rather than interpreted as \"remove everything\". The \"Idempotency-Key\" header is required, and a repeat of the same key and body returns the recorded answer without running the batch again.
     * Remove many tracked queries from clusters
     */
    async bulkRemoveClustersFromTrackedQueriesRaw(requestParameters: BulkRemoveClustersFromTrackedQueriesOperationRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<BulkRemoveClustersFromTrackedQueries200Response>> {
        const requestOptions = await this.bulkRemoveClustersFromTrackedQueriesRequestOpts(requestParameters);
        const response = await this.request(requestOptions, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => BulkRemoveClustersFromTrackedQueries200ResponseFromJSON(jsonValue));
    }

    /**
     * Minimum role: manager. Removes every tracked query named in \"ids\" from every cluster named in \"queryClusterIds\". At most 100 distinct tracked queries per call; duplicates in \"ids\" are collapsed. Partial success: the answer is 200 with a per-item \"successful\" and \"failed\" list even when some items failed, nothing is rolled back, and an id that is not a tracked query of this project is reported as a failed item rather than dropped. Neither the clusters nor the tracked queries are deleted: only the membership between them. Every cluster must belong to the project in the path; one that does not is rejected with its field named and nothing is written at all. NOTE: this DELETE requires a request body — some HTTP client libraries and proxies strip bodies from DELETE, and a stripped body is refused with a validation error rather than interpreted as \"remove everything\". The \"Idempotency-Key\" header is required, and a repeat of the same key and body returns the recorded answer without running the batch again.
     * Remove many tracked queries from clusters
     */
    async bulkRemoveClustersFromTrackedQueries(requestParameters: BulkRemoveClustersFromTrackedQueriesOperationRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<BulkRemoveClustersFromTrackedQueries200Response> {
        const response = await this.bulkRemoveClustersFromTrackedQueriesRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Creates request options for changeTrackedQueryCheckFrequency without sending the request
     */
    async changeTrackedQueryCheckFrequencyRequestOpts(requestParameters: ChangeTrackedQueryCheckFrequencyRequest): Promise<runtime.RequestOpts> {
        if (requestParameters['organizationId'] == null) {
            throw new runtime.RequiredError(
                'organizationId',
                'Required parameter "organizationId" was null or undefined when calling changeTrackedQueryCheckFrequency().'
            );
        }

        if (requestParameters['projectId'] == null) {
            throw new runtime.RequiredError(
                'projectId',
                'Required parameter "projectId" was null or undefined when calling changeTrackedQueryCheckFrequency().'
            );
        }

        if (requestParameters['trackedQueryId'] == null) {
            throw new runtime.RequiredError(
                'trackedQueryId',
                'Required parameter "trackedQueryId" was null or undefined when calling changeTrackedQueryCheckFrequency().'
            );
        }

        if (requestParameters['idempotencyKey'] == null) {
            throw new runtime.RequiredError(
                'idempotencyKey',
                'Required parameter "idempotencyKey" was null or undefined when calling changeTrackedQueryCheckFrequency().'
            );
        }

        if (requestParameters['changeTrackedQueryCheckFrequencyRequestData'] == null) {
            throw new runtime.RequiredError(
                'changeTrackedQueryCheckFrequencyRequestData',
                'Required parameter "changeTrackedQueryCheckFrequencyRequestData" was null or undefined when calling changeTrackedQueryCheckFrequency().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        headerParameters['Content-Type'] = 'application/json';

        if (requestParameters['idempotencyKey'] != null) {
            headerParameters['Idempotency-Key'] = String(requestParameters['idempotencyKey']);
        }

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("ApiKey", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/api/v1/organizations/{organizationId}/projects/{projectId}/tracked-queries/{trackedQueryId}/check-frequency`;
        urlPath = urlPath.replace('{organizationId}', encodeURIComponent(String(requestParameters['organizationId'])));
        urlPath = urlPath.replace('{projectId}', encodeURIComponent(String(requestParameters['projectId'])));
        urlPath = urlPath.replace('{trackedQueryId}', encodeURIComponent(String(requestParameters['trackedQueryId'])));

        return {
            path: urlPath,
            method: 'PUT',
            headers: headerParameters,
            query: queryParameters,
            body: ChangeTrackedQueryCheckFrequencyRequestDataToJSON(requestParameters['changeTrackedQueryCheckFrequencyRequestData']),
        };
    }

    /**
     * Minimum role: manager. Sets how often one tracked query is checked while it is active. The organization must be active and the project must not be archived. Setting the frequency it already has is accepted and changes nothing. This does NOT run a check, does not backfill history, and does not reschedule a check already in flight: the new cadence applies from the next time the query is considered. A paused query keeps the setting but is not checked until it is resumed. A tracked query belonging to another project answers 404, the same answer an unknown id gets. The Idempotency-Key header is required; a retry with the same key and the same body returns this same answer without applying anything again.
     * Change how often a tracked query is checked
     */
    async changeTrackedQueryCheckFrequencyRaw(requestParameters: ChangeTrackedQueryCheckFrequencyRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<TrackedQueryDetailResource>> {
        const requestOptions = await this.changeTrackedQueryCheckFrequencyRequestOpts(requestParameters);
        const response = await this.request(requestOptions, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => TrackedQueryDetailResourceFromJSON(jsonValue));
    }

    /**
     * Minimum role: manager. Sets how often one tracked query is checked while it is active. The organization must be active and the project must not be archived. Setting the frequency it already has is accepted and changes nothing. This does NOT run a check, does not backfill history, and does not reschedule a check already in flight: the new cadence applies from the next time the query is considered. A paused query keeps the setting but is not checked until it is resumed. A tracked query belonging to another project answers 404, the same answer an unknown id gets. The Idempotency-Key header is required; a retry with the same key and the same body returns this same answer without applying anything again.
     * Change how often a tracked query is checked
     */
    async changeTrackedQueryCheckFrequency(requestParameters: ChangeTrackedQueryCheckFrequencyRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<TrackedQueryDetailResource> {
        const response = await this.changeTrackedQueryCheckFrequencyRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Creates request options for changeTrackedQueryNPasses without sending the request
     */
    async changeTrackedQueryNPassesRequestOpts(requestParameters: ChangeTrackedQueryNPassesRequest): Promise<runtime.RequestOpts> {
        if (requestParameters['organizationId'] == null) {
            throw new runtime.RequiredError(
                'organizationId',
                'Required parameter "organizationId" was null or undefined when calling changeTrackedQueryNPasses().'
            );
        }

        if (requestParameters['projectId'] == null) {
            throw new runtime.RequiredError(
                'projectId',
                'Required parameter "projectId" was null or undefined when calling changeTrackedQueryNPasses().'
            );
        }

        if (requestParameters['trackedQueryId'] == null) {
            throw new runtime.RequiredError(
                'trackedQueryId',
                'Required parameter "trackedQueryId" was null or undefined when calling changeTrackedQueryNPasses().'
            );
        }

        if (requestParameters['idempotencyKey'] == null) {
            throw new runtime.RequiredError(
                'idempotencyKey',
                'Required parameter "idempotencyKey" was null or undefined when calling changeTrackedQueryNPasses().'
            );
        }

        if (requestParameters['changeTrackedQueryPassesRequestData'] == null) {
            throw new runtime.RequiredError(
                'changeTrackedQueryPassesRequestData',
                'Required parameter "changeTrackedQueryPassesRequestData" was null or undefined when calling changeTrackedQueryNPasses().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        headerParameters['Content-Type'] = 'application/json';

        if (requestParameters['idempotencyKey'] != null) {
            headerParameters['Idempotency-Key'] = String(requestParameters['idempotencyKey']);
        }

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("ApiKey", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/api/v1/organizations/{organizationId}/projects/{projectId}/tracked-queries/{trackedQueryId}/passes`;
        urlPath = urlPath.replace('{organizationId}', encodeURIComponent(String(requestParameters['organizationId'])));
        urlPath = urlPath.replace('{projectId}', encodeURIComponent(String(requestParameters['projectId'])));
        urlPath = urlPath.replace('{trackedQueryId}', encodeURIComponent(String(requestParameters['trackedQueryId'])));

        return {
            path: urlPath,
            method: 'PUT',
            headers: headerParameters,
            query: queryParameters,
            body: ChangeTrackedQueryPassesRequestDataToJSON(requestParameters['changeTrackedQueryPassesRequestData']),
        };
    }

    /**
     * Minimum role: manager. Sets how many times one tracked query is asked per check. AI engines are not deterministic, so several passes are averaged; `google_serp` and `google_shopping` run a single pass and refuse any value above one with 409 `n_passes_not_supported_for_engine`. Setting the value back to 1 is always allowed. The organization must be active and the project must not be archived. Setting the value it already has is accepted and changes nothing. This does NOT run a check and does not change history already captured: it applies from the next check. More passes cost proportionally more of the plan\'s check budget. A tracked query belonging to another project answers 404, the same answer an unknown id gets. The Idempotency-Key header is required; a retry with the same key and the same body returns this same answer without applying anything again.
     * Change how many passes a tracked query runs per check
     */
    async changeTrackedQueryNPassesRaw(requestParameters: ChangeTrackedQueryNPassesRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<TrackedQueryDetailResource>> {
        const requestOptions = await this.changeTrackedQueryNPassesRequestOpts(requestParameters);
        const response = await this.request(requestOptions, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => TrackedQueryDetailResourceFromJSON(jsonValue));
    }

    /**
     * Minimum role: manager. Sets how many times one tracked query is asked per check. AI engines are not deterministic, so several passes are averaged; `google_serp` and `google_shopping` run a single pass and refuse any value above one with 409 `n_passes_not_supported_for_engine`. Setting the value back to 1 is always allowed. The organization must be active and the project must not be archived. Setting the value it already has is accepted and changes nothing. This does NOT run a check and does not change history already captured: it applies from the next check. More passes cost proportionally more of the plan\'s check budget. A tracked query belonging to another project answers 404, the same answer an unknown id gets. The Idempotency-Key header is required; a retry with the same key and the same body returns this same answer without applying anything again.
     * Change how many passes a tracked query runs per check
     */
    async changeTrackedQueryNPasses(requestParameters: ChangeTrackedQueryNPassesRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<TrackedQueryDetailResource> {
        const response = await this.changeTrackedQueryNPassesRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Creates request options for countTrackedQueries without sending the request
     */
    async countTrackedQueriesRequestOpts(requestParameters: CountTrackedQueriesRequest): Promise<runtime.RequestOpts> {
        if (requestParameters['organizationId'] == null) {
            throw new runtime.RequiredError(
                'organizationId',
                'Required parameter "organizationId" was null or undefined when calling countTrackedQueries().'
            );
        }

        if (requestParameters['projectId'] == null) {
            throw new runtime.RequiredError(
                'projectId',
                'Required parameter "projectId" was null or undefined when calling countTrackedQueries().'
            );
        }

        const queryParameters: any = {};

        if (requestParameters['status'] != null) {
            queryParameters['status'] = requestParameters['status'];
        }

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("ApiKey", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/api/v1/organizations/{organizationId}/projects/{projectId}/tracked-queries/count`;
        urlPath = urlPath.replace('{organizationId}', encodeURIComponent(String(requestParameters['organizationId'])));
        urlPath = urlPath.replace('{projectId}', encodeURIComponent(String(requestParameters['projectId'])));

        return {
            path: urlPath,
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        };
    }

    /**
     * Minimum role: viewer. Two numbers about one project: how many tracked queries it holds, and what force-checking that same set would cost. Omit `status` to count every tracked query whatever its status; send `active` or `paused` to count and price only those. `checkCost` is a PRICED DRY RUN expressed in check budget units — one unit per pass, the same unit the plan allowance is counted in, so it is directly comparable with `checksAvailable` from the entitlements operation — and it is the sum of each matched tracked query\'s configured passes. Asking reserves nothing, debits nothing and starts no check. It is deliberately NOT a forecast of what the check-all operation will consume: that operation skips paused queries, skips a query whose check is already pending or running, re-runs a check awaiting retry without charging for it again, and stops at whatever budget is left — none of which is subtracted here. Count with `status=active` for the figure closest to a full check-all. Both numbers are read from the write model, so a tracked query created moments ago is already in them; that is why they can be AHEAD of the `total` returned by the tracked-queries listing, which counts a search projection, and ahead of the keyword listings, which read projections refreshed in the background. Summing the unfiltered count over every project of an organization, archived projects included, reproduces countOrganizationTrackedQueries, which counts through the same counter in the same store. An archived project still answers, because this is a read, but no check can be submitted there while it stays archived, so its cost is hypothetical. For a month of scheduled rounds across the whole organization instead of one round over one project, read getOrganizationProjectedMonthlyChecks. No monetary amount is published or implied: the cost of a check is published only in budget units.
     * Count a project\'s tracked queries and price checking them
     */
    async countTrackedQueriesRaw(requestParameters: CountTrackedQueriesRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<TrackedQueryCountResource>> {
        const requestOptions = await this.countTrackedQueriesRequestOpts(requestParameters);
        const response = await this.request(requestOptions, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => TrackedQueryCountResourceFromJSON(jsonValue));
    }

    /**
     * Minimum role: viewer. Two numbers about one project: how many tracked queries it holds, and what force-checking that same set would cost. Omit `status` to count every tracked query whatever its status; send `active` or `paused` to count and price only those. `checkCost` is a PRICED DRY RUN expressed in check budget units — one unit per pass, the same unit the plan allowance is counted in, so it is directly comparable with `checksAvailable` from the entitlements operation — and it is the sum of each matched tracked query\'s configured passes. Asking reserves nothing, debits nothing and starts no check. It is deliberately NOT a forecast of what the check-all operation will consume: that operation skips paused queries, skips a query whose check is already pending or running, re-runs a check awaiting retry without charging for it again, and stops at whatever budget is left — none of which is subtracted here. Count with `status=active` for the figure closest to a full check-all. Both numbers are read from the write model, so a tracked query created moments ago is already in them; that is why they can be AHEAD of the `total` returned by the tracked-queries listing, which counts a search projection, and ahead of the keyword listings, which read projections refreshed in the background. Summing the unfiltered count over every project of an organization, archived projects included, reproduces countOrganizationTrackedQueries, which counts through the same counter in the same store. An archived project still answers, because this is a read, but no check can be submitted there while it stays archived, so its cost is hypothetical. For a month of scheduled rounds across the whole organization instead of one round over one project, read getOrganizationProjectedMonthlyChecks. No monetary amount is published or implied: the cost of a check is published only in budget units.
     * Count a project\'s tracked queries and price checking them
     */
    async countTrackedQueries(requestParameters: CountTrackedQueriesRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<TrackedQueryCountResource> {
        const response = await this.countTrackedQueriesRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Creates request options for forceCheckAllActiveTrackedQueries without sending the request
     */
    async forceCheckAllActiveTrackedQueriesRequestOpts(requestParameters: ForceCheckAllActiveTrackedQueriesRequest): Promise<runtime.RequestOpts> {
        if (requestParameters['organizationId'] == null) {
            throw new runtime.RequiredError(
                'organizationId',
                'Required parameter "organizationId" was null or undefined when calling forceCheckAllActiveTrackedQueries().'
            );
        }

        if (requestParameters['projectId'] == null) {
            throw new runtime.RequiredError(
                'projectId',
                'Required parameter "projectId" was null or undefined when calling forceCheckAllActiveTrackedQueries().'
            );
        }

        if (requestParameters['idempotencyKey'] == null) {
            throw new runtime.RequiredError(
                'idempotencyKey',
                'Required parameter "idempotencyKey" was null or undefined when calling forceCheckAllActiveTrackedQueries().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (requestParameters['idempotencyKey'] != null) {
            headerParameters['Idempotency-Key'] = String(requestParameters['idempotencyKey']);
        }

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("ApiKey", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/api/v1/organizations/{organizationId}/projects/{projectId}/tracked-queries/check-all`;
        urlPath = urlPath.replace('{organizationId}', encodeURIComponent(String(requestParameters['organizationId'])));
        urlPath = urlPath.replace('{projectId}', encodeURIComponent(String(requestParameters['projectId'])));

        return {
            path: urlPath,
            method: 'PUT',
            headers: headerParameters,
            query: queryParameters,
        };
    }

    /**
     * Minimum role: manager. Submits a fresh check for every eligible tracked query of the project, ignoring how recently each was last checked, least-recently-checked first, and at most 1000 tracked queries per call. Eligible is narrower than active: a paused query is skipped, and so is one whose check is already pending or running for the same engine. A query whose check is awaiting a retry is included and, WHEN THE ENGINE HAS NOT CHANGED SINCE, costs nothing extra, because its first submission already paid for it; if the engine did change, the pending run is replaced and the replacement is paid for. What is left is trimmed to what the organization\'s remaining check budget can pay for — a check costs one budget unit per pass. While a subscription is cancelled but still inside its paid grace window nothing is submitted at all and `submitted` is 0; name the queries explicitly through the check operation to run them in that window. CALLING AGAIN DOES NOT CONTINUE WHERE THIS CALL STOPPED: submissions are handed to a worker, and a tracked query stops being selected only once that worker has started its check, so a second call made before the queue drains selects and submits the same tracked queries again. That is safe while the first check is still running — the duplicate is collapsed, and nothing is checked or charged twice — but a check that has already finished is run again and charged again, because this operation ignores staleness by design. To cover a project with more than 1000 eligible tracked queries, wait for the submitted wave to be picked up — each tracked query\'s lastCheckedAt advances when its check completes — and call again then. The response lists the tracked queries submitted and carries NO job id: a check already in flight is reused rather than started again, so no id could be guaranteed to exist. Submission is accepted, not completed: a listed tracked query can still lose the last budget units to another caller before the worker reaches it. The request takes no body, and any field sent is rejected. Requires an Idempotency-Key header.
     * Check every eligible tracked query of a project now
     */
    async forceCheckAllActiveTrackedQueriesRaw(requestParameters: ForceCheckAllActiveTrackedQueriesRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<SubmittedChecksResource>> {
        const requestOptions = await this.forceCheckAllActiveTrackedQueriesRequestOpts(requestParameters);
        const response = await this.request(requestOptions, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => SubmittedChecksResourceFromJSON(jsonValue));
    }

    /**
     * Minimum role: manager. Submits a fresh check for every eligible tracked query of the project, ignoring how recently each was last checked, least-recently-checked first, and at most 1000 tracked queries per call. Eligible is narrower than active: a paused query is skipped, and so is one whose check is already pending or running for the same engine. A query whose check is awaiting a retry is included and, WHEN THE ENGINE HAS NOT CHANGED SINCE, costs nothing extra, because its first submission already paid for it; if the engine did change, the pending run is replaced and the replacement is paid for. What is left is trimmed to what the organization\'s remaining check budget can pay for — a check costs one budget unit per pass. While a subscription is cancelled but still inside its paid grace window nothing is submitted at all and `submitted` is 0; name the queries explicitly through the check operation to run them in that window. CALLING AGAIN DOES NOT CONTINUE WHERE THIS CALL STOPPED: submissions are handed to a worker, and a tracked query stops being selected only once that worker has started its check, so a second call made before the queue drains selects and submits the same tracked queries again. That is safe while the first check is still running — the duplicate is collapsed, and nothing is checked or charged twice — but a check that has already finished is run again and charged again, because this operation ignores staleness by design. To cover a project with more than 1000 eligible tracked queries, wait for the submitted wave to be picked up — each tracked query\'s lastCheckedAt advances when its check completes — and call again then. The response lists the tracked queries submitted and carries NO job id: a check already in flight is reused rather than started again, so no id could be guaranteed to exist. Submission is accepted, not completed: a listed tracked query can still lose the last budget units to another caller before the worker reaches it. The request takes no body, and any field sent is rejected. Requires an Idempotency-Key header.
     * Check every eligible tracked query of a project now
     */
    async forceCheckAllActiveTrackedQueries(requestParameters: ForceCheckAllActiveTrackedQueriesRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<SubmittedChecksResource> {
        const response = await this.forceCheckAllActiveTrackedQueriesRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Creates request options for getTrackedQuery without sending the request
     */
    async getTrackedQueryRequestOpts(requestParameters: GetTrackedQueryRequest): Promise<runtime.RequestOpts> {
        if (requestParameters['organizationId'] == null) {
            throw new runtime.RequiredError(
                'organizationId',
                'Required parameter "organizationId" was null or undefined when calling getTrackedQuery().'
            );
        }

        if (requestParameters['projectId'] == null) {
            throw new runtime.RequiredError(
                'projectId',
                'Required parameter "projectId" was null or undefined when calling getTrackedQuery().'
            );
        }

        if (requestParameters['trackedQueryId'] == null) {
            throw new runtime.RequiredError(
                'trackedQueryId',
                'Required parameter "trackedQueryId" was null or undefined when calling getTrackedQuery().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("ApiKey", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/api/v1/organizations/{organizationId}/projects/{projectId}/tracked-queries/{trackedQueryId}`;
        urlPath = urlPath.replace('{organizationId}', encodeURIComponent(String(requestParameters['organizationId'])));
        urlPath = urlPath.replace('{projectId}', encodeURIComponent(String(requestParameters['projectId'])));
        urlPath = urlPath.replace('{trackedQueryId}', encodeURIComponent(String(requestParameters['trackedQueryId'])));

        return {
            path: urlPath,
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        };
    }

    /**
     * Minimum role: viewer. The configuration of one tracked query: the text sent to the engine, the engine, locale and country it is asked in, the clusters it belongs to, and how often it is checked. A lastCheckedAt of null means no check has completed yet — it is not a check that found nothing. A tracked query belonging to another project answers 404, the same answer an unknown id gets, so the API never confirms that an inaccessible tracked query exists. Rank positions, share of voice and sentiment are not part of this response: they belong to a date window and are served by the analytics endpoints.
     * Get a tracked query
     */
    async getTrackedQueryRaw(requestParameters: GetTrackedQueryRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<TrackedQueryDetailResource>> {
        const requestOptions = await this.getTrackedQueryRequestOpts(requestParameters);
        const response = await this.request(requestOptions, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => TrackedQueryDetailResourceFromJSON(jsonValue));
    }

    /**
     * Minimum role: viewer. The configuration of one tracked query: the text sent to the engine, the engine, locale and country it is asked in, the clusters it belongs to, and how often it is checked. A lastCheckedAt of null means no check has completed yet — it is not a check that found nothing. A tracked query belonging to another project answers 404, the same answer an unknown id gets, so the API never confirms that an inaccessible tracked query exists. Rank positions, share of voice and sentiment are not part of this response: they belong to a date window and are served by the analytics endpoints.
     * Get a tracked query
     */
    async getTrackedQuery(requestParameters: GetTrackedQueryRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<TrackedQueryDetailResource> {
        const response = await this.getTrackedQueryRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Creates request options for pauseTrackedQuery without sending the request
     */
    async pauseTrackedQueryRequestOpts(requestParameters: PauseTrackedQueryRequest): Promise<runtime.RequestOpts> {
        if (requestParameters['organizationId'] == null) {
            throw new runtime.RequiredError(
                'organizationId',
                'Required parameter "organizationId" was null or undefined when calling pauseTrackedQuery().'
            );
        }

        if (requestParameters['projectId'] == null) {
            throw new runtime.RequiredError(
                'projectId',
                'Required parameter "projectId" was null or undefined when calling pauseTrackedQuery().'
            );
        }

        if (requestParameters['trackedQueryId'] == null) {
            throw new runtime.RequiredError(
                'trackedQueryId',
                'Required parameter "trackedQueryId" was null or undefined when calling pauseTrackedQuery().'
            );
        }

        if (requestParameters['idempotencyKey'] == null) {
            throw new runtime.RequiredError(
                'idempotencyKey',
                'Required parameter "idempotencyKey" was null or undefined when calling pauseTrackedQuery().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (requestParameters['idempotencyKey'] != null) {
            headerParameters['Idempotency-Key'] = String(requestParameters['idempotencyKey']);
        }

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("ApiKey", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/api/v1/organizations/{organizationId}/projects/{projectId}/tracked-queries/{trackedQueryId}/pause`;
        urlPath = urlPath.replace('{organizationId}', encodeURIComponent(String(requestParameters['organizationId'])));
        urlPath = urlPath.replace('{projectId}', encodeURIComponent(String(requestParameters['projectId'])));
        urlPath = urlPath.replace('{trackedQueryId}', encodeURIComponent(String(requestParameters['trackedQueryId'])));

        return {
            path: urlPath,
            method: 'PUT',
            headers: headerParameters,
            query: queryParameters,
        };
    }

    /**
     * Minimum role: manager. Stops the scheduler from checking this tracked query; it keeps its configuration, its clusters and every result already collected, and nothing is deleted. Pausing a query that is already paused succeeds and answers the same body — this is a PUT asserting a state, not a transition, so it is safe to repeat. It does NOT cancel a check that is already running: a check in flight when the pause lands still completes and still consumes the budget unit it reserved. The request takes no body, and any field sent is rejected. Requires an Idempotency-Key header.
     * Pause a tracked query
     */
    async pauseTrackedQueryRaw(requestParameters: PauseTrackedQueryRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<TrackedQueryDetailResource>> {
        const requestOptions = await this.pauseTrackedQueryRequestOpts(requestParameters);
        const response = await this.request(requestOptions, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => TrackedQueryDetailResourceFromJSON(jsonValue));
    }

    /**
     * Minimum role: manager. Stops the scheduler from checking this tracked query; it keeps its configuration, its clusters and every result already collected, and nothing is deleted. Pausing a query that is already paused succeeds and answers the same body — this is a PUT asserting a state, not a transition, so it is safe to repeat. It does NOT cancel a check that is already running: a check in flight when the pause lands still completes and still consumes the budget unit it reserved. The request takes no body, and any field sent is rejected. Requires an Idempotency-Key header.
     * Pause a tracked query
     */
    async pauseTrackedQuery(requestParameters: PauseTrackedQueryRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<TrackedQueryDetailResource> {
        const response = await this.pauseTrackedQueryRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Creates request options for removeClustersFromTrackedQuery without sending the request
     */
    async removeClustersFromTrackedQueryRequestOpts(requestParameters: RemoveClustersFromTrackedQueryRequest): Promise<runtime.RequestOpts> {
        if (requestParameters['organizationId'] == null) {
            throw new runtime.RequiredError(
                'organizationId',
                'Required parameter "organizationId" was null or undefined when calling removeClustersFromTrackedQuery().'
            );
        }

        if (requestParameters['projectId'] == null) {
            throw new runtime.RequiredError(
                'projectId',
                'Required parameter "projectId" was null or undefined when calling removeClustersFromTrackedQuery().'
            );
        }

        if (requestParameters['trackedQueryId'] == null) {
            throw new runtime.RequiredError(
                'trackedQueryId',
                'Required parameter "trackedQueryId" was null or undefined when calling removeClustersFromTrackedQuery().'
            );
        }

        if (requestParameters['idempotencyKey'] == null) {
            throw new runtime.RequiredError(
                'idempotencyKey',
                'Required parameter "idempotencyKey" was null or undefined when calling removeClustersFromTrackedQuery().'
            );
        }

        if (requestParameters['clusterMembershipRequestData'] == null) {
            throw new runtime.RequiredError(
                'clusterMembershipRequestData',
                'Required parameter "clusterMembershipRequestData" was null or undefined when calling removeClustersFromTrackedQuery().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        headerParameters['Content-Type'] = 'application/json';

        if (requestParameters['idempotencyKey'] != null) {
            headerParameters['Idempotency-Key'] = String(requestParameters['idempotencyKey']);
        }

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("ApiKey", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/api/v1/organizations/{organizationId}/projects/{projectId}/tracked-queries/{trackedQueryId}/clusters`;
        urlPath = urlPath.replace('{organizationId}', encodeURIComponent(String(requestParameters['organizationId'])));
        urlPath = urlPath.replace('{projectId}', encodeURIComponent(String(requestParameters['projectId'])));
        urlPath = urlPath.replace('{trackedQueryId}', encodeURIComponent(String(requestParameters['trackedQueryId'])));

        return {
            path: urlPath,
            method: 'DELETE',
            headers: headerParameters,
            query: queryParameters,
            body: ClusterMembershipRequestDataToJSON(requestParameters['clusterMembershipRequestData']),
        };
    }

    /**
     * Minimum role: manager. Removes the tracked query from every cluster named in \"queryClusterIds\" and answers with the query in its new state. A cluster the query does not belong to is skipped, not reported as an error. Neither the clusters nor the tracked query are deleted: only the membership between them. Every cluster must belong to the project in the path. NOTE: this DELETE requires a request body — some HTTP client libraries and proxies strip bodies from DELETE, and a stripped body is refused with a validation error rather than interpreted as \"remove all clusters\". The \"Idempotency-Key\" header is required, and a repeat of the same key and body returns the recorded answer without removing anything again.
     * Remove a tracked query from clusters
     */
    async removeClustersFromTrackedQueryRaw(requestParameters: RemoveClustersFromTrackedQueryRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<TrackedQueryDetailResource>> {
        const requestOptions = await this.removeClustersFromTrackedQueryRequestOpts(requestParameters);
        const response = await this.request(requestOptions, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => TrackedQueryDetailResourceFromJSON(jsonValue));
    }

    /**
     * Minimum role: manager. Removes the tracked query from every cluster named in \"queryClusterIds\" and answers with the query in its new state. A cluster the query does not belong to is skipped, not reported as an error. Neither the clusters nor the tracked query are deleted: only the membership between them. Every cluster must belong to the project in the path. NOTE: this DELETE requires a request body — some HTTP client libraries and proxies strip bodies from DELETE, and a stripped body is refused with a validation error rather than interpreted as \"remove all clusters\". The \"Idempotency-Key\" header is required, and a repeat of the same key and body returns the recorded answer without removing anything again.
     * Remove a tracked query from clusters
     */
    async removeClustersFromTrackedQuery(requestParameters: RemoveClustersFromTrackedQueryRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<TrackedQueryDetailResource> {
        const response = await this.removeClustersFromTrackedQueryRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Creates request options for reportAiResponse without sending the request
     */
    async reportAiResponseRequestOpts(requestParameters: ReportAiResponseOperationRequest): Promise<runtime.RequestOpts> {
        if (requestParameters['organizationId'] == null) {
            throw new runtime.RequiredError(
                'organizationId',
                'Required parameter "organizationId" was null or undefined when calling reportAiResponse().'
            );
        }

        if (requestParameters['projectId'] == null) {
            throw new runtime.RequiredError(
                'projectId',
                'Required parameter "projectId" was null or undefined when calling reportAiResponse().'
            );
        }

        if (requestParameters['trackedQueryId'] == null) {
            throw new runtime.RequiredError(
                'trackedQueryId',
                'Required parameter "trackedQueryId" was null or undefined when calling reportAiResponse().'
            );
        }

        if (requestParameters['aiResponseId'] == null) {
            throw new runtime.RequiredError(
                'aiResponseId',
                'Required parameter "aiResponseId" was null or undefined when calling reportAiResponse().'
            );
        }

        if (requestParameters['idempotencyKey'] == null) {
            throw new runtime.RequiredError(
                'idempotencyKey',
                'Required parameter "idempotencyKey" was null or undefined when calling reportAiResponse().'
            );
        }

        if (requestParameters['reportAiResponseRequest'] == null) {
            throw new runtime.RequiredError(
                'reportAiResponseRequest',
                'Required parameter "reportAiResponseRequest" was null or undefined when calling reportAiResponse().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        headerParameters['Content-Type'] = 'application/json';

        if (requestParameters['idempotencyKey'] != null) {
            headerParameters['Idempotency-Key'] = String(requestParameters['idempotencyKey']);
        }

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("ApiKey", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/api/v1/organizations/{organizationId}/projects/{projectId}/tracked-queries/{trackedQueryId}/responses/{aiResponseId}/report`;
        urlPath = urlPath.replace('{organizationId}', encodeURIComponent(String(requestParameters['organizationId'])));
        urlPath = urlPath.replace('{projectId}', encodeURIComponent(String(requestParameters['projectId'])));
        urlPath = urlPath.replace('{trackedQueryId}', encodeURIComponent(String(requestParameters['trackedQueryId'])));
        urlPath = urlPath.replace('{aiResponseId}', encodeURIComponent(String(requestParameters['aiResponseId'])));

        return {
            path: urlPath,
            method: 'POST',
            headers: headerParameters,
            query: queryParameters,
            body: ReportAiResponseRequestToJSON(requestParameters['reportAiResponseRequest']),
        };
    }

    /**
     * Minimum role: viewer — deliberately lower than the other tracked-query writes, because a report changes nothing a viewer cannot already read. The key still needs the \"write\" capability. The report is forwarded to the team that reviews the scrape run behind the capture; it does not change the capture, the tracked query, or any metric derived from them, and nothing in this API will show the report afterwards. A 200 means the report was accepted for review, not that anything was corrected, and there is no identifier to poll. A capture that belongs to another tracked query or project answers 404, the same answer an unknown id gets. A capture that cannot be routed back to its scrape run answers 409 \"ai_check_session_unavailable\", and it cannot be reported. That code covers two moments, which behave differently for your key: when the capture carries no scrape run at all the refusal is decided BEFORE anything is sent, so the idempotency key is left unused and the same key answers the same way however often it is presented; when the review system itself rejects the run — its own retention having elapsed — the refusal comes after the forward was attempted, so the key is spent like any outcome we cannot confirm. You can tell them apart without guessing: retry the same key, and a reply of \"operation_outcome_uncertain\" means the refusal came from the review system. Delivery is at-most-once: the \"Idempotency-Key\" header is required and a repeat of the same key and body returns the recorded answer, but a delivery whose outcome is unknown refuses replay on that key rather than risk filing the report twice.
     * Report a problem with a captured AI answer
     */
    async reportAiResponseRaw(requestParameters: ReportAiResponseOperationRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<AiResponseReportResource>> {
        const requestOptions = await this.reportAiResponseRequestOpts(requestParameters);
        const response = await this.request(requestOptions, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => AiResponseReportResourceFromJSON(jsonValue));
    }

    /**
     * Minimum role: viewer — deliberately lower than the other tracked-query writes, because a report changes nothing a viewer cannot already read. The key still needs the \"write\" capability. The report is forwarded to the team that reviews the scrape run behind the capture; it does not change the capture, the tracked query, or any metric derived from them, and nothing in this API will show the report afterwards. A 200 means the report was accepted for review, not that anything was corrected, and there is no identifier to poll. A capture that belongs to another tracked query or project answers 404, the same answer an unknown id gets. A capture that cannot be routed back to its scrape run answers 409 \"ai_check_session_unavailable\", and it cannot be reported. That code covers two moments, which behave differently for your key: when the capture carries no scrape run at all the refusal is decided BEFORE anything is sent, so the idempotency key is left unused and the same key answers the same way however often it is presented; when the review system itself rejects the run — its own retention having elapsed — the refusal comes after the forward was attempted, so the key is spent like any outcome we cannot confirm. You can tell them apart without guessing: retry the same key, and a reply of \"operation_outcome_uncertain\" means the refusal came from the review system. Delivery is at-most-once: the \"Idempotency-Key\" header is required and a repeat of the same key and body returns the recorded answer, but a delivery whose outcome is unknown refuses replay on that key rather than risk filing the report twice.
     * Report a problem with a captured AI answer
     */
    async reportAiResponse(requestParameters: ReportAiResponseOperationRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<AiResponseReportResource> {
        const response = await this.reportAiResponseRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Creates request options for resumeTrackedQuery without sending the request
     */
    async resumeTrackedQueryRequestOpts(requestParameters: ResumeTrackedQueryRequest): Promise<runtime.RequestOpts> {
        if (requestParameters['organizationId'] == null) {
            throw new runtime.RequiredError(
                'organizationId',
                'Required parameter "organizationId" was null or undefined when calling resumeTrackedQuery().'
            );
        }

        if (requestParameters['projectId'] == null) {
            throw new runtime.RequiredError(
                'projectId',
                'Required parameter "projectId" was null or undefined when calling resumeTrackedQuery().'
            );
        }

        if (requestParameters['trackedQueryId'] == null) {
            throw new runtime.RequiredError(
                'trackedQueryId',
                'Required parameter "trackedQueryId" was null or undefined when calling resumeTrackedQuery().'
            );
        }

        if (requestParameters['idempotencyKey'] == null) {
            throw new runtime.RequiredError(
                'idempotencyKey',
                'Required parameter "idempotencyKey" was null or undefined when calling resumeTrackedQuery().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (requestParameters['idempotencyKey'] != null) {
            headerParameters['Idempotency-Key'] = String(requestParameters['idempotencyKey']);
        }

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("ApiKey", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/api/v1/organizations/{organizationId}/projects/{projectId}/tracked-queries/{trackedQueryId}/resume`;
        urlPath = urlPath.replace('{organizationId}', encodeURIComponent(String(requestParameters['organizationId'])));
        urlPath = urlPath.replace('{projectId}', encodeURIComponent(String(requestParameters['projectId'])));
        urlPath = urlPath.replace('{trackedQueryId}', encodeURIComponent(String(requestParameters['trackedQueryId'])));

        return {
            path: urlPath,
            method: 'PUT',
            headers: headerParameters,
            query: queryParameters,
        };
    }

    /**
     * Minimum role: manager. Puts a paused tracked query back under the scheduler. Resuming a query that is already active succeeds and answers the same body — this is a PUT asserting a state, not a transition. It does NOT run a check: the query is checked when it next falls due under its own checkFrequency, and results collected while it was paused are unaffected. Nothing is back-filled for the time it spent paused. The request takes no body, and any field sent is rejected. Requires an Idempotency-Key header.
     * Resume a tracked query
     */
    async resumeTrackedQueryRaw(requestParameters: ResumeTrackedQueryRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<TrackedQueryDetailResource>> {
        const requestOptions = await this.resumeTrackedQueryRequestOpts(requestParameters);
        const response = await this.request(requestOptions, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => TrackedQueryDetailResourceFromJSON(jsonValue));
    }

    /**
     * Minimum role: manager. Puts a paused tracked query back under the scheduler. Resuming a query that is already active succeeds and answers the same body — this is a PUT asserting a state, not a transition. It does NOT run a check: the query is checked when it next falls due under its own checkFrequency, and results collected while it was paused are unaffected. Nothing is back-filled for the time it spent paused. The request takes no body, and any field sent is rejected. Requires an Idempotency-Key header.
     * Resume a tracked query
     */
    async resumeTrackedQuery(requestParameters: ResumeTrackedQueryRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<TrackedQueryDetailResource> {
        const response = await this.resumeTrackedQueryRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Creates request options for searchTrackedQueries without sending the request
     */
    async searchTrackedQueriesRequestOpts(requestParameters: SearchTrackedQueriesRequest): Promise<runtime.RequestOpts> {
        if (requestParameters['organizationId'] == null) {
            throw new runtime.RequiredError(
                'organizationId',
                'Required parameter "organizationId" was null or undefined when calling searchTrackedQueries().'
            );
        }

        if (requestParameters['projectId'] == null) {
            throw new runtime.RequiredError(
                'projectId',
                'Required parameter "projectId" was null or undefined when calling searchTrackedQueries().'
            );
        }

        const queryParameters: any = {};

        if (requestParameters['limit'] != null) {
            queryParameters['limit'] = requestParameters['limit'];
        }

        if (requestParameters['offset'] != null) {
            queryParameters['offset'] = requestParameters['offset'];
        }

        if (requestParameters['search'] != null) {
            queryParameters['search'] = requestParameters['search'];
        }

        if (requestParameters['status'] != null) {
            queryParameters['status'] = requestParameters['status'];
        }

        if (requestParameters['engines'] != null) {
            queryParameters['engines'] = requestParameters['engines'];
        }

        if (requestParameters['countries'] != null) {
            queryParameters['countries'] = requestParameters['countries'];
        }

        if (requestParameters['sortBy'] != null) {
            queryParameters['sortBy'] = requestParameters['sortBy'];
        }

        if (requestParameters['sortOrder'] != null) {
            queryParameters['sortOrder'] = requestParameters['sortOrder'];
        }

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("ApiKey", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/api/v1/organizations/{organizationId}/projects/{projectId}/tracked-queries`;
        urlPath = urlPath.replace('{organizationId}', encodeURIComponent(String(requestParameters['organizationId'])));
        urlPath = urlPath.replace('{projectId}', encodeURIComponent(String(requestParameters['projectId'])));

        return {
            path: urlPath,
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        };
    }

    /**
     * Minimum role: viewer. One row per tracked query — a single keyword on a single engine in a single country — carrying the metrics of its most recent completed check. Filter by status, engine, country and a free-text search over the keyword, and sort by any of the returned metrics. Positions (lastSerpPosition, lastMentionPosition, lastLinkPosition, lastShoppingPosition) are 1-based ranks, so LOWER is better; lastShareOfVoice and lastPositivityIndex are percentages from 0 to 100, where HIGHER is better. Every nullable field means \"not known yet\" rather than zero: a null position is a query with no data for that surface, a null lastPositivityIndex is a check with no mentions to score, and a null lastCheckedAt is a query that has never been checked — none of them is a score of zero. This listing reads a projection refreshed by background subscribers, not the write model, so a tracked query created or changed moments ago may not appear here yet or may still show its previous settings. It catches up on its own; nothing is lost. If you need to read back what you just wrote, the creation response carries the new ids and the single tracked-query operation reads the write model directly. total counts the tracked queries the filters match, not the rows on this page. A limit above the maximum is rejected, never clamped, and a filter this endpoint does not support is rejected rather than ignored.Send `Accept: text/csv` to receive the same page as a CSV download instead of JSON: same filters, same authorization, same page window and the same maximum of 100 rows — it is this page in another format, not a bulk export, so a whole collection is still read by paging. The CSV carries no `total`, because a table whose every row is a record has nowhere to put one; read it from the JSON representation of the same request. A list-valued field is joined into one cell with `; ` as a display projection — parse the JSON if you need the structure. Cells beginning with `=`, `+`, `-` or `@` are prefixed with an apostrophe so a spreadsheet treats them as text rather than running them as formulas.
     * Search a project\'s tracked queries
     */
    async searchTrackedQueriesRaw(requestParameters: SearchTrackedQueriesRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<SearchTrackedQueries200Response>> {
        const requestOptions = await this.searchTrackedQueriesRequestOpts(requestParameters);
        const response = await this.request(requestOptions, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => SearchTrackedQueries200ResponseFromJSON(jsonValue));
    }

    /**
     * Minimum role: viewer. One row per tracked query — a single keyword on a single engine in a single country — carrying the metrics of its most recent completed check. Filter by status, engine, country and a free-text search over the keyword, and sort by any of the returned metrics. Positions (lastSerpPosition, lastMentionPosition, lastLinkPosition, lastShoppingPosition) are 1-based ranks, so LOWER is better; lastShareOfVoice and lastPositivityIndex are percentages from 0 to 100, where HIGHER is better. Every nullable field means \"not known yet\" rather than zero: a null position is a query with no data for that surface, a null lastPositivityIndex is a check with no mentions to score, and a null lastCheckedAt is a query that has never been checked — none of them is a score of zero. This listing reads a projection refreshed by background subscribers, not the write model, so a tracked query created or changed moments ago may not appear here yet or may still show its previous settings. It catches up on its own; nothing is lost. If you need to read back what you just wrote, the creation response carries the new ids and the single tracked-query operation reads the write model directly. total counts the tracked queries the filters match, not the rows on this page. A limit above the maximum is rejected, never clamped, and a filter this endpoint does not support is rejected rather than ignored.Send `Accept: text/csv` to receive the same page as a CSV download instead of JSON: same filters, same authorization, same page window and the same maximum of 100 rows — it is this page in another format, not a bulk export, so a whole collection is still read by paging. The CSV carries no `total`, because a table whose every row is a record has nowhere to put one; read it from the JSON representation of the same request. A list-valued field is joined into one cell with `; ` as a display projection — parse the JSON if you need the structure. Cells beginning with `=`, `+`, `-` or `@` are prefixed with an apostrophe so a spreadsheet treats them as text rather than running them as formulas.
     * Search a project\'s tracked queries
     */
    async searchTrackedQueries(requestParameters: SearchTrackedQueriesRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<SearchTrackedQueries200Response> {
        const response = await this.searchTrackedQueriesRaw(requestParameters, initOverrides);
        return await response.value();
    }

}

/**
 * @export
 */
export const CountTrackedQueriesStatusEnum = {
    Active: 'active',
    Paused: 'paused'
} as const;
export type CountTrackedQueriesStatusEnum = typeof CountTrackedQueriesStatusEnum[keyof typeof CountTrackedQueriesStatusEnum];
/**
 * @export
 */
export const SearchTrackedQueriesStatusEnum = {
    Active: 'active',
    Paused: 'paused'
} as const;
export type SearchTrackedQueriesStatusEnum = typeof SearchTrackedQueriesStatusEnum[keyof typeof SearchTrackedQueriesStatusEnum];
/**
 * @export
 */
export const SearchTrackedQueriesEnginesEnum = {
    Chatgpt: 'chatgpt',
    Perplexity: 'perplexity',
    GoogleAiOverview: 'google_ai_overview',
    GoogleAiMode: 'google_ai_mode',
    GoogleSerp: 'google_serp',
    GoogleShopping: 'google_shopping'
} as const;
export type SearchTrackedQueriesEnginesEnum = typeof SearchTrackedQueriesEnginesEnum[keyof typeof SearchTrackedQueriesEnginesEnum];
/**
 * @export
 */
export const SearchTrackedQueriesSortByEnum = {
    QueryText: 'queryText',
    LastSerpPosition: 'lastSerpPosition',
    LastMentionPosition: 'lastMentionPosition',
    LastShoppingPosition: 'lastShoppingPosition',
    LastShareOfVoice: 'lastShareOfVoice',
    LastPositivityIndex: 'lastPositivityIndex',
    LastMentionCount: 'lastMentionCount',
    LastCheckedAt: 'lastCheckedAt'
} as const;
export type SearchTrackedQueriesSortByEnum = typeof SearchTrackedQueriesSortByEnum[keyof typeof SearchTrackedQueriesSortByEnum];
/**
 * @export
 */
export const SearchTrackedQueriesSortOrderEnum = {
    Asc: 'asc',
    Desc: 'desc'
} as const;
export type SearchTrackedQueriesSortOrderEnum = typeof SearchTrackedQueriesSortOrderEnum[keyof typeof SearchTrackedQueriesSortOrderEnum];

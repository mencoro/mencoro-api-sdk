# coding: utf-8

"""
    Mencoro API

    Public API for Mencoro customers. Authenticate with an API key: `Authorization: Bearer mencoro_sk_...`.

    The version of the OpenAPI document: 1.0.0
    Contact: support@mencoro.com
    Generated by OpenAPI Generator (https://openapi-generator.tech)

    Do not edit the class manually.
"""  # noqa: E501


from __future__ import annotations
import pprint
import re  # noqa: F401
import json

from datetime import datetime
from pydantic import BaseModel, ConfigDict, Field, StrictBool, StrictInt, StrictStr, field_validator
from typing import Any, ClassVar, Dict, List, Optional
from typing import Optional, Set
from typing_extensions import Self
from pydantic_core import to_jsonable_python

class EntitlementsResource(BaseModel):
    """
    What an organization's plan allows and what it has consumed
    """ # noqa: E501
    status: StrictStr = Field(description="\"none\" when the organization has never had a subscription contract.")
    is_entitled: StrictBool = Field(description="Whether the organization may use paid features right now: an active contract, or a cancelled one still inside its paid grace window.", alias="isEntitled")
    check_budget: Optional[StrictInt] = Field(default=None, description="Checks the current cycle allows. Null when there is no plan on file, which is not the same as a budget of zero.", alias="checkBudget")
    checks_consumed: Optional[StrictInt] = Field(default=None, description="Checks consumed so far in the current cycle. Null when there is no plan on file.", alias="checksConsumed")
    checks_available: Optional[StrictInt] = Field(default=None, description="Checks left in the current cycle. Null when there is no plan on file.", alias="checksAvailable")
    billing_cycle_type: Optional[StrictStr] = Field(default=None, description="How often the allowance renews. Null when there is no plan on file.", alias="billingCycleType")
    billing_cycle_anchor: Optional[datetime] = Field(default=None, description="Start of the current cycle. Null when there is no plan on file.", alias="billingCycleAnchor")
    next_reset_at: Optional[datetime] = Field(default=None, description="When the check budget next resets. Null when there is no plan on file.", alias="nextResetAt")
    cancelled_at: Optional[datetime] = Field(default=None, description="When the subscription was terminally cancelled. Null while it has not been.", alias="cancelledAt")
    scheduled_to_cancel_at: Optional[datetime] = Field(default=None, description="When a scheduled cancellation takes effect, while the subscription is still running. Null when none is scheduled.", alias="scheduledToCancelAt")
    grace_period_ends_at: Optional[datetime] = Field(default=None, description="When paid access ends after a cancellation. Null when the subscription has not been cancelled.", alias="gracePeriodEndsAt")
    __properties: ClassVar[List[str]] = ["status", "isEntitled", "checkBudget", "checksConsumed", "checksAvailable", "billingCycleType", "billingCycleAnchor", "nextResetAt", "cancelledAt", "scheduledToCancelAt", "gracePeriodEndsAt"]

    @field_validator('status')
    def status_validate_enum(cls, value):
        """Validates the enum"""
        if value not in set(['active', 'inactive', 'none']):
            raise ValueError("must be one of enum values ('active', 'inactive', 'none')")
        return value

    @field_validator('billing_cycle_type')
    def billing_cycle_type_validate_enum(cls, value):
        """Validates the enum"""
        if value is None:
            return value

        if value not in set(['monthly', 'yearly']):
            raise ValueError("must be one of enum values ('monthly', 'yearly')")
        return value

    model_config = ConfigDict(
        validate_by_name=True,
        validate_by_alias=True,
        validate_assignment=True,
        protected_namespaces=(),
    )


    def to_str(self) -> str:
        """Returns the string representation of the model using alias"""
        return pprint.pformat(self.model_dump(by_alias=True))

    def to_json(self) -> str:
        """Returns the JSON representation of the model using alias"""
        return json.dumps(to_jsonable_python(self.to_dict()))

    @classmethod
    def from_json(cls, json_str: str) -> Optional[Self]:
        """Create an instance of EntitlementsResource from a JSON string"""
        return cls.from_dict(json.loads(json_str))

    def to_dict(self) -> Dict[str, Any]:
        """Return the dictionary representation of the model using alias.

        This has the following differences from calling pydantic's
        `self.model_dump(by_alias=True)`:

        * `None` is only added to the output dict for nullable fields that
          were set at model initialization. Other fields with value `None`
          are ignored.
        """
        excluded_fields: Set[str] = set([
        ])

        _dict = self.model_dump(
            by_alias=True,
            exclude=excluded_fields,
            exclude_none=True,
        )
        # set to None if check_budget (nullable) is None
        # and model_fields_set contains the field
        if self.check_budget is None and "check_budget" in self.model_fields_set:
            _dict['checkBudget'] = None

        # set to None if checks_consumed (nullable) is None
        # and model_fields_set contains the field
        if self.checks_consumed is None and "checks_consumed" in self.model_fields_set:
            _dict['checksConsumed'] = None

        # set to None if checks_available (nullable) is None
        # and model_fields_set contains the field
        if self.checks_available is None and "checks_available" in self.model_fields_set:
            _dict['checksAvailable'] = None

        # set to None if billing_cycle_type (nullable) is None
        # and model_fields_set contains the field
        if self.billing_cycle_type is None and "billing_cycle_type" in self.model_fields_set:
            _dict['billingCycleType'] = None

        # set to None if billing_cycle_anchor (nullable) is None
        # and model_fields_set contains the field
        if self.billing_cycle_anchor is None and "billing_cycle_anchor" in self.model_fields_set:
            _dict['billingCycleAnchor'] = None

        # set to None if next_reset_at (nullable) is None
        # and model_fields_set contains the field
        if self.next_reset_at is None and "next_reset_at" in self.model_fields_set:
            _dict['nextResetAt'] = None

        # set to None if cancelled_at (nullable) is None
        # and model_fields_set contains the field
        if self.cancelled_at is None and "cancelled_at" in self.model_fields_set:
            _dict['cancelledAt'] = None

        # set to None if scheduled_to_cancel_at (nullable) is None
        # and model_fields_set contains the field
        if self.scheduled_to_cancel_at is None and "scheduled_to_cancel_at" in self.model_fields_set:
            _dict['scheduledToCancelAt'] = None

        # set to None if grace_period_ends_at (nullable) is None
        # and model_fields_set contains the field
        if self.grace_period_ends_at is None and "grace_period_ends_at" in self.model_fields_set:
            _dict['gracePeriodEndsAt'] = None

        return _dict

    @classmethod
    def from_dict(cls, obj: Optional[Dict[str, Any]]) -> Optional[Self]:
        """Create an instance of EntitlementsResource from a dict"""
        if obj is None:
            return None

        if not isinstance(obj, dict):
            return cls.model_validate(obj)

        _obj = cls.model_validate({
            "status": obj.get("status"),
            "isEntitled": obj.get("isEntitled"),
            "checkBudget": obj.get("checkBudget"),
            "checksConsumed": obj.get("checksConsumed"),
            "checksAvailable": obj.get("checksAvailable"),
            "billingCycleType": obj.get("billingCycleType"),
            "billingCycleAnchor": obj.get("billingCycleAnchor"),
            "nextResetAt": obj.get("nextResetAt"),
            "cancelledAt": obj.get("cancelledAt"),
            "scheduledToCancelAt": obj.get("scheduledToCancelAt"),
            "gracePeriodEndsAt": obj.get("gracePeriodEndsAt")
        })
        return _obj



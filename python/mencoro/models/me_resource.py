# coding: utf-8

"""
    Mencoro API

    Public API for Mencoro customers. Authenticate with an API key: `Authorization: Bearer mencoro_sk_...`.

    The version of the OpenAPI document: 1.0.0
    Contact: support@mencoro.com
    Generated by OpenAPI Generator (https://openapi-generator.tech)

    Do not edit the class manually.
"""  # noqa: E501


from __future__ import annotations
import pprint
import re  # noqa: F401
import json

from datetime import datetime
from pydantic import BaseModel, ConfigDict, Field, StrictStr, field_validator
from typing import Any, ClassVar, Dict, List
from uuid import UUID
from typing import Optional, Set
from typing_extensions import Self
from pydantic_core import to_jsonable_python

class MeResource(BaseModel):
    """
    The authenticated user and the API key the request was made with
    """ # noqa: E501
    user_id: UUID = Field(alias="userId")
    full_name: StrictStr = Field(alias="fullName")
    email: StrictStr
    language: StrictStr = Field(description="IETF language tag the user reads the product in", json_schema_extra={"examples": ["en"]})
    created_at: datetime = Field(alias="createdAt")
    api_key_id: UUID = Field(description="The API key this request authenticated with", alias="apiKeyId")
    capabilities: List[StrictStr]
    scope_mode: StrictStr = Field(alias="scopeMode")
    organization_ids: List[UUID] = Field(description="Organizations the key names. Empty for a key scoped to all organizations, which instead follows the owner's membership.", alias="organizationIds")
    __properties: ClassVar[List[str]] = ["userId", "fullName", "email", "language", "createdAt", "apiKeyId", "capabilities", "scopeMode", "organizationIds"]

    @field_validator('capabilities')
    def capabilities_validate_enum(cls, value):
        """Validates the enum"""
        for i in value:
            if i not in set(['read', 'write', 'organization:manage']):
                raise ValueError("each list item must be one of ('read', 'write', 'organization:manage')")
        return value

    @field_validator('scope_mode')
    def scope_mode_validate_enum(cls, value):
        """Validates the enum"""
        if value not in set(['selected', 'all_organizations']):
            raise ValueError("must be one of enum values ('selected', 'all_organizations')")
        return value

    model_config = ConfigDict(
        validate_by_name=True,
        validate_by_alias=True,
        validate_assignment=True,
        protected_namespaces=(),
    )


    def to_str(self) -> str:
        """Returns the string representation of the model using alias"""
        return pprint.pformat(self.model_dump(by_alias=True))

    def to_json(self) -> str:
        """Returns the JSON representation of the model using alias"""
        return json.dumps(to_jsonable_python(self.to_dict()))

    @classmethod
    def from_json(cls, json_str: str) -> Optional[Self]:
        """Create an instance of MeResource from a JSON string"""
        return cls.from_dict(json.loads(json_str))

    def to_dict(self) -> Dict[str, Any]:
        """Return the dictionary representation of the model using alias.

        This has the following differences from calling pydantic's
        `self.model_dump(by_alias=True)`:

        * `None` is only added to the output dict for nullable fields that
          were set at model initialization. Other fields with value `None`
          are ignored.
        """
        excluded_fields: Set[str] = set([
        ])

        _dict = self.model_dump(
            by_alias=True,
            exclude=excluded_fields,
            exclude_none=True,
        )
        return _dict

    @classmethod
    def from_dict(cls, obj: Optional[Dict[str, Any]]) -> Optional[Self]:
        """Create an instance of MeResource from a dict"""
        if obj is None:
            return None

        if not isinstance(obj, dict):
            return cls.model_validate(obj)

        _obj = cls.model_validate({
            "userId": obj.get("userId"),
            "fullName": obj.get("fullName"),
            "email": obj.get("email"),
            "language": obj.get("language"),
            "createdAt": obj.get("createdAt"),
            "apiKeyId": obj.get("apiKeyId"),
            "capabilities": obj.get("capabilities"),
            "scopeMode": obj.get("scopeMode"),
            "organizationIds": obj.get("organizationIds")
        })
        return _obj



# coding: utf-8

"""
    Mencoro API

    Public API for Mencoro customers. Authenticate with an API key: `Authorization: Bearer mencoro_sk_...`.

    The version of the OpenAPI document: 1.0.0
    Contact: support@mencoro.com
    Generated by OpenAPI Generator (https://openapi-generator.tech)

    Do not edit the class manually.
"""  # noqa: E501


from __future__ import annotations
import pprint
import re  # noqa: F401
import json

from datetime import datetime
from pydantic import BaseModel, ConfigDict, Field, StrictFloat, StrictInt, StrictStr, field_validator
from typing import Any, ClassVar, Dict, List, Optional, Union
from uuid import UUID
from typing import Optional, Set
from typing_extensions import Self
from pydantic_core import to_jsonable_python

class TrackedQueryResource(BaseModel):
    """
    A tracked query and the metrics of its most recent check
    """ # noqa: E501
    id: UUID
    query_text: StrictStr = Field(description="The keyword or prompt being tracked", alias="queryText")
    engine: StrictStr
    country: StrictStr = Field(description="ISO-3166 alpha-2 country code the query is tracked in")
    status: StrictStr
    query_cluster_ids: List[UUID] = Field(description="Ids of the keyword clusters this query belongs to", alias="queryClusterIds")
    check_frequency: StrictStr = Field(description="How often the query is checked", alias="checkFrequency")
    n_passes: StrictInt = Field(description="How many times the query is asked per check", alias="nPasses")
    last_serp_position: Optional[StrictInt] = Field(default=None, description="Position in traditional search results at the last check. 1-based, LOWER is better. Null when not known yet.", alias="lastSerpPosition")
    last_mention_position: Optional[StrictInt] = Field(default=None, description="Position of the brand mention inside the AI answer at the last check. 1-based, LOWER is better. Null when not known yet.", alias="lastMentionPosition")
    last_link_position: Optional[StrictInt] = Field(default=None, description="Position of a cited link to the brand at the last check. 1-based, LOWER is better. Null when not known yet.", alias="lastLinkPosition")
    last_shopping_position: Optional[StrictInt] = Field(default=None, description="Position in shopping results at the last check. 1-based, LOWER is better. Null when not known yet.", alias="lastShoppingPosition")
    last_share_of_voice: Optional[Union[StrictFloat, StrictInt]] = Field(default=None, description="0-100 share of the weighted AI mentions against every tracked brand; HIGHER is better. Null when not known yet, which is not a share of zero.", alias="lastShareOfVoice")
    last_positivity_index: Optional[StrictInt] = Field(default=None, description="0-100 sentiment score of the brand mentions; HIGHER is better. Null when there were no mentions to score, which is not a score of zero.", alias="lastPositivityIndex")
    last_positive_mention_count: Optional[StrictInt] = Field(default=None, description="Positive brand mentions at the last check. Null when not known yet.", alias="lastPositiveMentionCount")
    last_neutral_mention_count: Optional[StrictInt] = Field(default=None, description="Neutral brand mentions at the last check. Null when not known yet.", alias="lastNeutralMentionCount")
    last_negative_mention_count: Optional[StrictInt] = Field(default=None, description="Negative brand mentions at the last check. Null when not known yet.", alias="lastNegativeMentionCount")
    last_checked_at: Optional[datetime] = Field(default=None, description="When the query was last checked. Null when it never has been.", alias="lastCheckedAt")
    __properties: ClassVar[List[str]] = ["id", "queryText", "engine", "country", "status", "queryClusterIds", "checkFrequency", "nPasses", "lastSerpPosition", "lastMentionPosition", "lastLinkPosition", "lastShoppingPosition", "lastShareOfVoice", "lastPositivityIndex", "lastPositiveMentionCount", "lastNeutralMentionCount", "lastNegativeMentionCount", "lastCheckedAt"]

    @field_validator('engine')
    def engine_validate_enum(cls, value):
        """Validates the enum"""
        if value not in set(['chatgpt', 'perplexity', 'google_ai_overview', 'google_ai_mode', 'google_serp', 'google_shopping']):
            raise ValueError("must be one of enum values ('chatgpt', 'perplexity', 'google_ai_overview', 'google_ai_mode', 'google_serp', 'google_shopping')")
        return value

    @field_validator('status')
    def status_validate_enum(cls, value):
        """Validates the enum"""
        if value not in set(['active', 'paused']):
            raise ValueError("must be one of enum values ('active', 'paused')")
        return value

    @field_validator('check_frequency')
    def check_frequency_validate_enum(cls, value):
        """Validates the enum"""
        if value not in set(['daily', 'weekly', 'monthly']):
            raise ValueError("must be one of enum values ('daily', 'weekly', 'monthly')")
        return value

    model_config = ConfigDict(
        validate_by_name=True,
        validate_by_alias=True,
        validate_assignment=True,
        protected_namespaces=(),
    )


    def to_str(self) -> str:
        """Returns the string representation of the model using alias"""
        return pprint.pformat(self.model_dump(by_alias=True))

    def to_json(self) -> str:
        """Returns the JSON representation of the model using alias"""
        return json.dumps(to_jsonable_python(self.to_dict()))

    @classmethod
    def from_json(cls, json_str: str) -> Optional[Self]:
        """Create an instance of TrackedQueryResource from a JSON string"""
        return cls.from_dict(json.loads(json_str))

    def to_dict(self) -> Dict[str, Any]:
        """Return the dictionary representation of the model using alias.

        This has the following differences from calling pydantic's
        `self.model_dump(by_alias=True)`:

        * `None` is only added to the output dict for nullable fields that
          were set at model initialization. Other fields with value `None`
          are ignored.
        """
        excluded_fields: Set[str] = set([
        ])

        _dict = self.model_dump(
            by_alias=True,
            exclude=excluded_fields,
            exclude_none=True,
        )
        # set to None if last_serp_position (nullable) is None
        # and model_fields_set contains the field
        if self.last_serp_position is None and "last_serp_position" in self.model_fields_set:
            _dict['lastSerpPosition'] = None

        # set to None if last_mention_position (nullable) is None
        # and model_fields_set contains the field
        if self.last_mention_position is None and "last_mention_position" in self.model_fields_set:
            _dict['lastMentionPosition'] = None

        # set to None if last_link_position (nullable) is None
        # and model_fields_set contains the field
        if self.last_link_position is None and "last_link_position" in self.model_fields_set:
            _dict['lastLinkPosition'] = None

        # set to None if last_shopping_position (nullable) is None
        # and model_fields_set contains the field
        if self.last_shopping_position is None and "last_shopping_position" in self.model_fields_set:
            _dict['lastShoppingPosition'] = None

        # set to None if last_share_of_voice (nullable) is None
        # and model_fields_set contains the field
        if self.last_share_of_voice is None and "last_share_of_voice" in self.model_fields_set:
            _dict['lastShareOfVoice'] = None

        # set to None if last_positivity_index (nullable) is None
        # and model_fields_set contains the field
        if self.last_positivity_index is None and "last_positivity_index" in self.model_fields_set:
            _dict['lastPositivityIndex'] = None

        # set to None if last_positive_mention_count (nullable) is None
        # and model_fields_set contains the field
        if self.last_positive_mention_count is None and "last_positive_mention_count" in self.model_fields_set:
            _dict['lastPositiveMentionCount'] = None

        # set to None if last_neutral_mention_count (nullable) is None
        # and model_fields_set contains the field
        if self.last_neutral_mention_count is None and "last_neutral_mention_count" in self.model_fields_set:
            _dict['lastNeutralMentionCount'] = None

        # set to None if last_negative_mention_count (nullable) is None
        # and model_fields_set contains the field
        if self.last_negative_mention_count is None and "last_negative_mention_count" in self.model_fields_set:
            _dict['lastNegativeMentionCount'] = None

        # set to None if last_checked_at (nullable) is None
        # and model_fields_set contains the field
        if self.last_checked_at is None and "last_checked_at" in self.model_fields_set:
            _dict['lastCheckedAt'] = None

        return _dict

    @classmethod
    def from_dict(cls, obj: Optional[Dict[str, Any]]) -> Optional[Self]:
        """Create an instance of TrackedQueryResource from a dict"""
        if obj is None:
            return None

        if not isinstance(obj, dict):
            return cls.model_validate(obj)

        _obj = cls.model_validate({
            "id": obj.get("id"),
            "queryText": obj.get("queryText"),
            "engine": obj.get("engine"),
            "country": obj.get("country"),
            "status": obj.get("status"),
            "queryClusterIds": obj.get("queryClusterIds"),
            "checkFrequency": obj.get("checkFrequency"),
            "nPasses": obj.get("nPasses"),
            "lastSerpPosition": obj.get("lastSerpPosition"),
            "lastMentionPosition": obj.get("lastMentionPosition"),
            "lastLinkPosition": obj.get("lastLinkPosition"),
            "lastShoppingPosition": obj.get("lastShoppingPosition"),
            "lastShareOfVoice": obj.get("lastShareOfVoice"),
            "lastPositivityIndex": obj.get("lastPositivityIndex"),
            "lastPositiveMentionCount": obj.get("lastPositiveMentionCount"),
            "lastNeutralMentionCount": obj.get("lastNeutralMentionCount"),
            "lastNegativeMentionCount": obj.get("lastNegativeMentionCount"),
            "lastCheckedAt": obj.get("lastCheckedAt")
        })
        return _obj



# Mencoro::ChangeTrackedQueryPassesRequestData

## Properties

| Name | Type | Description | Notes |
| ---- | ---- | ----------- | ----- |
| **n_passes** | **Integer** | Passes run per check. Only an AI engine accepts more than one. |  |

## Example

```ruby
require 'mencoro'

instance = Mencoro::ChangeTrackedQueryPassesRequestData.new(
  n_passes: null
)
```


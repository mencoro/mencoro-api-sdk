openapi: 3.0.0
info:
  contact:
    email: support@mencoro.com
    name: Mencoro Support
    url: https://mencoro.com/contact/
  description: "Public API for Mencoro customers. Authenticate with an API key: `Authorization:\
    \ Bearer mencoro_sk_...`."
  license:
    name: Proprietary
    url: https://mencoro.com/legal/
  termsOfService: https://mencoro.com/legal/
  title: Mencoro API
  version: 1.0.0
servers:
- description: Production
  url: https://api.mencoro.com
security:
- ApiKey: []
tags:
- description: Tracked Queries
  name: Tracked Queries
- description: Analytics
  name: Analytics
- description: Organizations
  name: Organizations
- description: Billing
  name: Billing
- description: Captures
  name: Captures
- description: Clusters
  name: Clusters
- description: Projects
  name: Projects
- description: Discovery
  name: Discovery
- description: Invitations
  name: Invitations
- description: Jobs
  name: Jobs
- description: Account
  name: Account
- description: Members
  name: Members
- description: Organization operations
  name: Organization operations
paths:
  /api/v1/organizations/{organizationId}/projects/{projectId}/tracked-queries/batch/check-frequency:
    put:
      description: "Minimum role: manager. Sets the same check frequency on every\
        \ tracked query named in `ids`, at most 100 distinct ids per call. The organization\
        \ must be active and the project must not be archived. Partial success: an\
        \ id that is not a tracked query of this project — unknown, malformed, already\
        \ deleted, or belonging to somewhere else — is reported under `failed` with\
        \ `tracked_query_not_found` while the rest are changed, and the call still\
        \ answers 200. Nothing is rolled back because an item failed. Setting the\
        \ frequency a query already has is accepted and changes nothing. This does\
        \ NOT run a check and does not reschedule one already in flight. It never\
        \ changes more than the ids it is given: there is no \"change everything matching\
        \ a filter\" mode. The Idempotency-Key header is required; a retry with the\
        \ same key and the same body returns this same answer without applying anything\
        \ again."
      operationId: batchChangeTrackedQueriesCheckFrequency
      parameters:
      - explode: false
        in: path
        name: organizationId
        required: true
        schema:
          format: uuid
          type: string
        style: simple
      - explode: false
        in: path
        name: projectId
        required: true
        schema:
          format: uuid
          type: string
        style: simple
      - description: "A client-chosen key, unique per operation. Replaying it returns\
          \ the first answer instead of applying anything again."
        explode: false
        in: header
        name: Idempotency-Key
        required: true
        schema:
          type: string
        style: simple
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/BatchChangeTrackedQueryCheckFrequencyRequestData"
        required: true
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BatchWriteOutcome"
          description: "Per-id results. Read `failed`: a non-empty `failed` with a\
            \ 200 is the normal partial-success answer."
        "400":
          description: "The body was rejected, or the Idempotency-Key header is missing;\
            \ `details` names the field"
        "401":
          description: Missing or invalid API key
        "403":
          description: The key lacks the write capability
        "404":
          description: No organization or project the caller can access under these
            ids
        "409":
          description: "The organization is archived, the project is archived, or\
            \ the idempotency key was already used for a different request, a concurrent\
            \ request with the same key is still running, or a previous attempt with\
            \ it ended without a known outcome"
      summary: Change how often several tracked queries are checked
      tags:
      - Tracked Queries
      x-content-type: application/json
      x-accepts:
      - application/json
  /api/v1/organizations/{organizationId}/projects/{projectId}/tracked-queries/batch/passes:
    put:
      description: "Minimum role: manager. Sets the same passes-per-check on every\
        \ tracked query named in `ids`, at most 100 distinct ids per call. The organization\
        \ must be active and the project must not be archived. Partial success, and\
        \ two distinct reasons appear under `failed`: an id that is not a tracked\
        \ query of this project answers `tracked_query_not_found`, and a `google_serp`\
        \ or `google_shopping` query asked for more than one pass answers `n_passes_not_supported_for_engine`\
        \ — those engines run a single pass. Both leave the rest of the batch changed\
        \ and the call still answers 200. Setting the value back to 1 is always allowed,\
        \ so a batch that lowers sampling never fails on engine grounds. This does\
        \ NOT run a check and does not change history already captured. More passes\
        \ cost proportionally more of the plan's check budget. It never changes more\
        \ than the ids it is given: there is no \"change everything matching a filter\"\
        \ mode. The Idempotency-Key header is required; a retry with the same key\
        \ and the same body returns this same answer without applying anything again."
      operationId: batchChangeTrackedQueriesNPasses
      parameters:
      - explode: false
        in: path
        name: organizationId
        required: true
        schema:
          format: uuid
          type: string
        style: simple
      - explode: false
        in: path
        name: projectId
        required: true
        schema:
          format: uuid
          type: string
        style: simple
      - description: "A client-chosen key, unique per operation. Replaying it returns\
          \ the first answer instead of applying anything again."
        explode: false
        in: header
        name: Idempotency-Key
        required: true
        schema:
          type: string
        style: simple
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/BatchChangeTrackedQueryPassesRequestData"
        required: true
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BatchWriteOutcome"
          description: "Per-id results. Read `failed`: a non-empty `failed` with a\
            \ 200 is the normal partial-success answer."
        "400":
          description: "The body was rejected, or the Idempotency-Key header is missing;\
            \ `details` names the field"
        "401":
          description: Missing or invalid API key
        "403":
          description: The key lacks the write capability
        "404":
          description: No organization or project the caller can access under these
            ids
        "409":
          description: "The organization is archived, the project is archived, or\
            \ the idempotency key was already used for a different request, a concurrent\
            \ request with the same key is still running, or a previous attempt with\
            \ it ended without a known outcome"
      summary: Change how many passes several tracked queries run per check
      tags:
      - Tracked Queries
      x-content-type: application/json
      x-accepts:
      - application/json
  /api/v1/organizations/{organizationId}/projects/{projectId}/tracked-queries/batch/pause:
    put:
      description: "Minimum role: manager. Pauses up to 100 tracked queries of one\
        \ project, each independently. Always answers 200 when the batch itself was\
        \ processed: read `failed` to find out which items were not paused, never\
        \ the status code. Items are never rolled back because a later one failed.\
        \ An id that is already paused is reported as successful — pausing asserts\
        \ a state, not a transition. An id that is not a tracked query of this project\
        \ is reported as failed with `tracked_query_not_found`, exactly as an id that\
        \ does not exist at all, and nothing is written for it. Pausing does not cancel\
        \ a check that is already running. Requires an Idempotency-Key header; a retry\
        \ must repeat the same ids in the same order to be recognised as a retry rather\
        \ than refused as a reused key."
      operationId: batchPauseTrackedQueries
      parameters:
      - explode: false
        in: path
        name: organizationId
        required: true
        schema:
          format: uuid
          type: string
        style: simple
      - explode: false
        in: path
        name: projectId
        required: true
        schema:
          format: uuid
          type: string
        style: simple
      - description: Repeating a request with the same key and the same ids answers
          with the first attempt's result.
        explode: false
        in: header
        name: Idempotency-Key
        required: true
        schema:
          type: string
        style: simple
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/BatchTargetsRequestData"
        required: true
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/batchPauseTrackedQueries_200_response"
          description: "Per-item results. `failed` is always present, empty list included:\
            \ read it rather than inferring success from the status."
        "400":
          description: "The Idempotency-Key header is missing, the body carried an\
            \ unknown field, or \"ids\" was absent, malformed or larger than 100 distinct\
            \ entries"
        "401":
          description: Missing or invalid API key
        "403":
          description: The key lacks the write capability
        "404":
          description: No organization or project the caller can access under these
            ids
        "409":
          description: "The organization or the project is archived, or the idempotency\
            \ key was already used for a different request, a concurrent request with\
            \ the same key is still running, or a previous attempt with it ended without\
            \ a known outcome"
      summary: Pause several tracked queries
      tags:
      - Tracked Queries
      x-content-type: application/json
      x-accepts:
      - application/json
  /api/v1/organizations/{organizationId}/projects/{projectId}/tracked-queries/batch/resume:
    put:
      description: "Minimum role: manager. Puts up to 100 paused tracked queries of\
        \ one project back under the scheduler, each independently. Always answers\
        \ 200 when the batch itself was processed: read `failed` to find out which\
        \ items were not resumed, never the status code. Items are never rolled back\
        \ because a later one failed. An id that is already active is reported as\
        \ successful — resuming asserts a state, not a transition. An id that is not\
        \ a tracked query of this project is reported as failed with `tracked_query_not_found`,\
        \ exactly as an id that does not exist at all, and nothing is written for\
        \ it. No check is run by this operation and nothing is back-filled for the\
        \ time the queries spent paused. Requires an Idempotency-Key header; a retry\
        \ must repeat the same ids in the same order to be recognised as a retry rather\
        \ than refused as a reused key."
      operationId: batchResumeTrackedQueries
      parameters:
      - explode: false
        in: path
        name: organizationId
        required: true
        schema:
          format: uuid
          type: string
        style: simple
      - explode: false
        in: path
        name: projectId
        required: true
        schema:
          format: uuid
          type: string
        style: simple
      - description: Repeating a request with the same key and the same ids answers
          with the first attempt's result.
        explode: false
        in: header
        name: Idempotency-Key
        required: true
        schema:
          type: string
        style: simple
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/BatchTargetsRequestData"
        required: true
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/batchResumeTrackedQueries_200_response"
          description: "Per-item results. `failed` is always present, empty list included:\
            \ read it rather than inferring success from the status."
        "400":
          description: "The Idempotency-Key header is missing, the body carried an\
            \ unknown field, or \"ids\" was absent, malformed or larger than 100 distinct\
            \ entries"
        "401":
          description: Missing or invalid API key
        "403":
          description: The key lacks the write capability
        "404":
          description: No organization or project the caller can access under these
            ids
        "409":
          description: "The organization or the project is archived, or the idempotency\
            \ key was already used for a different request, a concurrent request with\
            \ the same key is still running, or a previous attempt with it ended without\
            \ a known outcome"
      summary: Resume several tracked queries
      tags:
      - Tracked Queries
      x-content-type: application/json
      x-accepts:
      - application/json
  /api/v1/organizations/{organizationId}/projects/{projectId}/tracked-queries/bulk/clusters:
    delete:
      description: "Minimum role: manager. Removes every tracked query named in \"\
        ids\" from every cluster named in \"queryClusterIds\". At most 100 distinct\
        \ tracked queries per call; duplicates in \"ids\" are collapsed. Partial success:\
        \ the answer is 200 with a per-item \"successful\" and \"failed\" list even\
        \ when some items failed, nothing is rolled back, and an id that is not a\
        \ tracked query of this project is reported as a failed item rather than dropped.\
        \ Neither the clusters nor the tracked queries are deleted: only the membership\
        \ between them. Every cluster must belong to the project in the path; one\
        \ that does not is rejected with its field named and nothing is written at\
        \ all. NOTE: this DELETE requires a request body — some HTTP client libraries\
        \ and proxies strip bodies from DELETE, and a stripped body is refused with\
        \ a validation error rather than interpreted as \"remove everything\". The\
        \ \"Idempotency-Key\" header is required, and a repeat of the same key and\
        \ body returns the recorded answer without running the batch again."
      operationId: bulkRemoveClustersFromTrackedQueries
      parameters:
      - explode: false
        in: path
        name: organizationId
        required: true
        schema:
          format: uuid
          type: string
        style: simple
      - explode: false
        in: path
        name: projectId
        required: true
        schema:
          format: uuid
          type: string
        style: simple
      - description: "A client-chosen key, unique per operation, so a lost response\
          \ can be retried without repeating the batch."
        explode: false
        in: header
        name: Idempotency-Key
        required: true
        schema:
          type: string
        style: simple
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/bulkRemoveClustersFromTrackedQueries_request"
        description: Required. A DELETE with no body is rejected.
        required: true
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/bulkRemoveClustersFromTrackedQueries_200_response"
          description: "Per-item results. Read \"failed\": a 200 does not mean every\
            \ item succeeded."
        "400":
          description: "The body was rejected or missing: an unknown field, a malformed\
            \ or missing id, more than 100 distinct ids, a cluster outside this project,\
            \ a stripped DELETE body, or a missing Idempotency-Key"
        "401":
          description: Missing or invalid API key
        "403":
          description: The key lacks the write capability
        "404":
          description: No organization or project the caller can access under these
            ids
        "409":
          description: "The organization is archived, the project is archived, or\
            \ the idempotency key was reused for a different body, a concurrent request\
            \ with the same key is still running, or a previous attempt with it ended\
            \ without a known outcome"
      summary: Remove many tracked queries from clusters
      tags:
      - Tracked Queries
      x-content-type: application/json
      x-accepts:
      - application/json
    put:
      description: "Minimum role: manager. Adds every tracked query named in \"ids\"\
        \ to every cluster named in \"queryClusterIds\". At most 100 distinct tracked\
        \ queries per call; duplicates in \"ids\" are collapsed. Partial success:\
        \ the answer is 200 with a per-item \"successful\" and \"failed\" list even\
        \ when some items failed, nothing is rolled back, and an id that is not a\
        \ tracked query of this project is reported as a failed item rather than dropped.\
        \ Clusters not named are left alone — this adds, it does not replace membership.\
        \ Every cluster must belong to the project in the path; one that does not\
        \ is rejected with its field named and nothing is written at all. The \"Idempotency-Key\"\
        \ header is required, and a repeat of the same key and body returns the recorded\
        \ answer without running the batch again."
      operationId: bulkAddClustersToTrackedQueries
      parameters:
      - explode: false
        in: path
        name: organizationId
        required: true
        schema:
          format: uuid
          type: string
        style: simple
      - explode: false
        in: path
        name: projectId
        required: true
        schema:
          format: uuid
          type: string
        style: simple
      - description: "A client-chosen key, unique per operation, so a lost response\
          \ can be retried without repeating the batch."
        explode: false
        in: header
        name: Idempotency-Key
        required: true
        schema:
          type: string
        style: simple
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/bulkAddClustersToTrackedQueries_request"
        required: true
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/bulkAddClustersToTrackedQueries_200_response"
          description: "Per-item results. Read \"failed\": a 200 does not mean every\
            \ item succeeded."
        "400":
          description: "The body was rejected: an unknown field, a malformed or missing\
            \ id, more than 100 distinct ids, a cluster outside this project, or a\
            \ missing Idempotency-Key"
        "401":
          description: Missing or invalid API key
        "403":
          description: The key lacks the write capability
        "404":
          description: No organization or project the caller can access under these
            ids
        "409":
          description: "The organization is archived, the project is archived, or\
            \ the idempotency key was reused for a different body, a concurrent request\
            \ with the same key is still running, or a previous attempt with it ended\
            \ without a known outcome"
      summary: Add many tracked queries to clusters
      tags:
      - Tracked Queries
      x-content-type: application/json
      x-accepts:
      - application/json
  /api/v1/organizations/{organizationId}/projects/{projectId}/tracked-queries/count:
    get:
      description: "Minimum role: viewer. Two numbers about one project: how many\
        \ tracked queries it holds, and what force-checking that same set would cost.\
        \ Omit `status` to count every tracked query whatever its status; send `active`\
        \ or `paused` to count and price only those. `checkCost` is a PRICED DRY RUN\
        \ expressed in check budget units — one unit per pass, the same unit the plan\
        \ allowance is counted in, so it is directly comparable with `checksAvailable`\
        \ from the entitlements operation — and it is the sum of each matched tracked\
        \ query's configured passes. Asking reserves nothing, debits nothing and starts\
        \ no check. It is deliberately NOT a forecast of what the check-all operation\
        \ will consume: that operation skips paused queries, skips a query whose check\
        \ is already pending or running, re-runs a check awaiting retry without charging\
        \ for it again, and stops at whatever budget is left — none of which is subtracted\
        \ here. Count with `status=active` for the figure closest to a full check-all.\
        \ Both numbers are read from the write model, so a tracked query created moments\
        \ ago is already in them; that is why they can be AHEAD of the `total` returned\
        \ by the tracked-queries listing, which counts a search projection, and ahead\
        \ of the keyword listings, which read projections refreshed in the background.\
        \ Summing the unfiltered count over every project of an organization, archived\
        \ projects included, reproduces countOrganizationTrackedQueries, which counts\
        \ through the same counter in the same store. An archived project still answers,\
        \ because this is a read, but no check can be submitted there while it stays\
        \ archived, so its cost is hypothetical. For a month of scheduled rounds across\
        \ the whole organization instead of one round over one project, read getOrganizationProjectedMonthlyChecks.\
        \ No monetary amount is published or implied: the cost of a check is published\
        \ only in budget units."
      operationId: countTrackedQueries
      parameters:
      - explode: false
        in: path
        name: organizationId
        required: true
        schema:
          format: uuid
          type: string
        style: simple
      - explode: false
        in: path
        name: projectId
        required: true
        schema:
          format: uuid
          type: string
        style: simple
      - description: "Restrict both numbers to one status. Lower-case; an unknown\
          \ value is rejected, not ignored. Omitted, every status is counted and priced."
        explode: true
        in: query
        name: status
        required: false
        schema:
          enum:
          - active
          - paused
          type: string
        style: form
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/TrackedQueryCountResource"
          description: The tracked query count and the budget cost of checking that
            set
        "400":
          description: The status parameter was rejected; the details name the field
        "401":
          description: Missing or invalid API key
        "403":
          description: The key lacks the read capability
        "404":
          description: No organization or project the caller can access under these
            ids
      summary: Count a project's tracked queries and price checking them
      tags:
      - Tracked Queries
      x-accepts:
      - application/json
  /api/v1/organizations/{organizationId}/projects/{projectId}/tracked-queries/check-all:
    put:
      description: "Minimum role: manager. Submits a fresh check for every eligible\
        \ tracked query of the project, ignoring how recently each was last checked,\
        \ least-recently-checked first, and at most 1000 tracked queries per call.\
        \ Eligible is narrower than active: a paused query is skipped, and so is one\
        \ whose check is already pending or running for the same engine. A query whose\
        \ check is awaiting a retry is included and, WHEN THE ENGINE HAS NOT CHANGED\
        \ SINCE, costs nothing extra, because its first submission already paid for\
        \ it; if the engine did change, the pending run is replaced and the replacement\
        \ is paid for. What is left is trimmed to what the organization's remaining\
        \ check budget can pay for — a check costs one budget unit per pass. While\
        \ a subscription is cancelled but still inside its paid grace window nothing\
        \ is submitted at all and `submitted` is 0; name the queries explicitly through\
        \ the check operation to run them in that window. CALLING AGAIN DOES NOT CONTINUE\
        \ WHERE THIS CALL STOPPED: submissions are handed to a worker, and a tracked\
        \ query stops being selected only once that worker has started its check,\
        \ so a second call made before the queue drains selects and submits the same\
        \ tracked queries again. That is safe while the first check is still running\
        \ — the duplicate is collapsed, and nothing is checked or charged twice —\
        \ but a check that has already finished is run again and charged again, because\
        \ this operation ignores staleness by design. To cover a project with more\
        \ than 1000 eligible tracked queries, wait for the submitted wave to be picked\
        \ up — each tracked query's lastCheckedAt advances when its check completes\
        \ — and call again then. The response lists the tracked queries submitted\
        \ and carries NO job id: a check already in flight is reused rather than started\
        \ again, so no id could be guaranteed to exist. Submission is accepted, not\
        \ completed: a listed tracked query can still lose the last budget units to\
        \ another caller before the worker reaches it. The request takes no body,\
        \ and any field sent is rejected. Requires an Idempotency-Key header."
      operationId: forceCheckAllActiveTrackedQueries
      parameters:
      - explode: false
        in: path
        name: organizationId
        required: true
        schema:
          format: uuid
          type: string
        style: simple
      - explode: false
        in: path
        name: projectId
        required: true
        schema:
          format: uuid
          type: string
        style: simple
      - description: Repeating a request with the same key answers with the first
          attempt's result instead of submitting a second wave.
        explode: false
        in: header
        name: Idempotency-Key
        required: true
        schema:
          type: string
        style: simple
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/SubmittedChecksResource"
          description: The tracked queries submitted for a check
        "400":
          description: "The Idempotency-Key header is missing or malformed, or the\
            \ request carried a body"
        "401":
          description: Missing or invalid API key
        "403":
          description: The key lacks the write capability
        "404":
          description: No organization or project the caller can access under these
            ids
        "409":
          description: "The organization or the project is archived, or the idempotency\
            \ key was already used for a different request, a concurrent request with\
            \ the same key is still running, or a previous attempt with it ended without\
            \ a known outcome"
      summary: Check every eligible tracked query of a project now
      tags:
      - Tracked Queries
      x-accepts:
      - application/json
  /api/v1/organizations/{organizationId}/projects/{projectId}/tracked-queries/check:
    put:
      description: "Minimum role: manager. Asks for a fresh check of up to 100 named\
        \ tracked queries straight away, ignoring how recently each was last checked.\
        \ Answers 200 with per-item results and NO job id: a check that is already\
        \ in flight for a query is reused rather than started again, so there is no\
        \ id this operation could hand back that is guaranteed to exist. Follow progress\
        \ on the tracked query itself — its lastCheckedAt advances when the check\
        \ completes. A successful item means the check was accepted for submission\
        \ with budget available for it at that moment; it does not mean the check\
        \ has run. A checked query costs one budget unit per pass (nPasses), and items\
        \ that do not fit the remaining budget are reported as failed with `check_budget_forecast_exhausted`,\
        \ or `subscription_not_found` when the organization has no entitled subscription\
        \ — they are never reported as successful. That budget figure is a forecast\
        \ for this batch, and a pessimistic one: a tracked query already being checked\
        \ is joined to the check in flight and costs nothing, but it is still debited\
        \ here, so an item refused this way may have fitted. Resubmit it in a later\
        \ batch rather than treating the refusal as a statement about your subscription.\
        \ A paused query is reported as failed with `tracked_query_already_paused`:\
        \ paused queries are never checked. An id that is not a tracked query of this\
        \ project is reported as failed with `tracked_query_not_found`, exactly as\
        \ an id that does not exist. Requires an Idempotency-Key header; a retry must\
        \ repeat the same ids in the same order."
      operationId: batchForceCheckTrackedQueries
      parameters:
      - explode: false
        in: path
        name: organizationId
        required: true
        schema:
          format: uuid
          type: string
        style: simple
      - explode: false
        in: path
        name: projectId
        required: true
        schema:
          format: uuid
          type: string
        style: simple
      - description: Repeating a request with the same key and the same ids answers
          with the first attempt's result instead of submitting again.
        explode: false
        in: header
        name: Idempotency-Key
        required: true
        schema:
          type: string
        style: simple
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/BatchTargetsRequestData"
        required: true
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/batchForceCheckTrackedQueries_200_response"
          description: "Per-item results. `failed` is always present, empty list included:\
            \ read it rather than inferring success from the status."
        "400":
          description: "The Idempotency-Key header is missing, the body carried an\
            \ unknown field, or \"ids\" was absent, malformed or larger than 100 distinct\
            \ entries"
        "401":
          description: Missing or invalid API key
        "403":
          description: The key lacks the write capability
        "404":
          description: No organization or project the caller can access under these
            ids
        "409":
          description: "The organization or the project is archived, or the idempotency\
            \ key was already used for a different request, a concurrent request with\
            \ the same key is still running, or a previous attempt with it ended without\
            \ a known outcome"
      summary: Check several tracked queries now
      tags:
      - Tracked Queries
      x-content-type: application/json
      x-accepts:
      - application/json
  /api/v1/organizations/{organizationId}/projects/{projectId}/available-filters:
    get:
      description: "Minimum role: viewer. Call this first: it is where every other\
        \ analytics operation sends you for the valid engines, countries and keyword\
        \ clusters of a project, and the values it returns are the exact strings the\
        \ engines, countries and queryClusterIds parameters accept — anything else\
        \ is rejected as a 400. Engines are engine codes, countries are ISO-3166 alpha-2\
        \ codes, and clusters are {id, name} objects whose id goes in queryClusterIds.\
        \ Takes no date window: it describes how the project is configured right now,\
        \ so a value is listed as soon as a tracked query uses it, even when no response\
        \ has been captured for it yet. An empty list therefore means nothing is configured\
        \ for it, not that no data was collected. Competitor ids are not part of this\
        \ response."
      operationId: getAvailableFilters
      parameters:
      - explode: false
        in: path
        name: organizationId
        required: true
        schema:
          format: uuid
          type: string
        style: simple
      - explode: false
        in: path
        name: projectId
        required: true
        schema:
          format: uuid
          type: string
        style: simple
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/getAvailableFilters_200_response"
          description: "The engines, countries and clusters configured on the project"
        "401":
          description: Missing or invalid API key
        "403":
          description: The key lacks the read capability
        "404":
          description: No organization or project the caller can access under these
            ids
      summary: Filter values a project is configured for
      tags:
      - Analytics
      x-accepts:
      - application/json
  /api/v1/organizations/{organizationId}/projects/{projectId}/cited-sources:
    get:
      description: "Minimum role: viewer. The sources the answer engines drew on across\
        \ a project's AI answers over a date window, ranked by how often they were\
        \ cited. Per source: citationCount, the total number of citations; distinctResponseCount\
        \ and distinctQueryCount, how many captured answers and tracked queries it\
        \ appeared in; avgPosition, its average 1-based rank inside the answers' citation\
        \ lists, where LOWER is better. A null avgPosition means no citation in the\
        \ window carried a position, not a rank of zero; a null domain or sampleTitle\
        \ means the citation never carried one. `total` counts the distinct sources\
        \ matching the window, before paging. The list is UNFILTERED by ownership:\
        \ the brand's, competitors' and third-party sources sit in the same ranking.\
        \ Only the AI answer engines (chatgpt, perplexity, google_ai_overview, google_ai_mode)\
        \ produce citations, so filtering by google_serp or google_shopping is accepted\
        \ and returns nothing. Citations still behind an answer engine's redirect\
        \ (a google.com/goto link, whose URL names the engine rather than the source)\
        \ are excluded, so a source cited only through such links is absent from this\
        \ list rather than counted as zero."
      operationId: getCitedSources
      parameters:
      - explode: false
        in: path
        name: organizationId
        required: true
        schema:
          format: uuid
          type: string
        style: simple
      - explode: false
        in: path
        name: projectId
        required: true
        schema:
          format: uuid
          type: string
        style: simple
      - description: "Inclusive start of the window, Y-m-d. Must fall inside the data\
          \ retention window."
        explode: true
        in: query
        name: dateFrom
        required: true
        schema:
          format: date
          type: string
        style: form
      - description: "Inclusive end of the window, Y-m-d."
        explode: true
        in: query
        name: dateTo
        required: true
        schema:
          format: date
          type: string
        style: form
      - description: "Repeatable, or comma-separated. Only the AI engines carry citations."
        explode: true
        in: query
        name: engines
        required: false
        schema:
          items:
            enum:
            - chatgpt
            - perplexity
            - google_ai_overview
            - google_ai_mode
            - google_serp
            - google_shopping
            type: string
          type: array
        style: form
      - description: "Grain of the roll-up: \"domain\" by host, \"page\" by exact\
          \ URL."
        explode: true
        in: query
        name: groupBy
        required: false
        schema:
          default: domain
          enum:
          - domain
          - page
          type: string
        style: form
      - description: "Page size. A larger value is rejected, never silently reduced."
        explode: true
        in: query
        name: limit
        required: false
        schema:
          default: 20
          maximum: 100
          minimum: 1
          type: integer
        style: form
      - description: Number of sources to skip.
        explode: true
        in: query
        name: offset
        required: false
        schema:
          default: 0
          minimum: 0
          type: integer
        style: form
      responses:
        "200":
          description: "The cited sources for this page, plus the total number of\
            \ distinct sources in the window"
        "401":
          description: Missing or invalid API key
        "400":
          description: A filter or page bound was rejected; the details name the field
        "403":
          description: The key lacks the read capability
        "404":
          description: No organization or project the caller can access under these
            ids
      summary: Domains and pages the AI answers cited
      tags:
      - Analytics
      x-accepts:
      - application/json
  /api/v1/organizations/{organizationId}/projects/{projectId}/metrics/clusters:
    get:
      description: "Minimum role: viewer. One row per keyword cluster over a date\
        \ window, with its tracked query and keyword counts, average positions, rates,\
        \ share of voice and sentiment split. A row whose clusterId is null is the\
        \ ungrouped bucket: the tracked queries belonging to no cluster. Position\
        \ metrics are 1-based and LOWER is better; rates, positivityIndex and shareOfVoice\
        \ are 0-100 percentages where higher is better. A null metric means nothing\
        \ was captured for that cluster in the window — it is not a zero, and averaging\
        \ or charting it as one would misstate the period. dataDirtySince is non-null\
        \ while a recalculation is pending, meaning the numbers predate the latest\
        \ configuration change."
      operationId: getClusterBreakdown
      parameters:
      - explode: false
        in: path
        name: organizationId
        required: true
        schema:
          format: uuid
          type: string
        style: simple
      - explode: false
        in: path
        name: projectId
        required: true
        schema:
          format: uuid
          type: string
        style: simple
      - description: "Inclusive start of the window, Y-m-d. Must fall inside the data\
          \ retention window."
        explode: true
        in: query
        name: dateFrom
        required: true
        schema:
          format: date
          type: string
        style: form
      - description: "Inclusive end of the window, Y-m-d."
        explode: true
        in: query
        name: dateTo
        required: true
        schema:
          format: date
          type: string
        style: form
      - description: "Repeatable, or comma-separated."
        explode: true
        in: query
        name: engines
        required: false
        schema:
          items:
            enum:
            - chatgpt
            - perplexity
            - google_ai_overview
            - google_ai_mode
            - google_serp
            - google_shopping
            type: string
          type: array
        style: form
      - description: ISO-3166 alpha-2 codes or English names. Must be configured on
          the project.
        explode: true
        in: query
        name: countries
        required: false
        schema:
          items:
            type: string
          type: array
        style: form
      - description: Restrict to these clusters. Each must belong to the project.
        explode: true
        in: query
        name: queryClusterIds
        required: false
        schema:
          items:
            format: uuid
            type: string
          type: array
        style: form
      - description: "Sent alone, returns only the ungrouped bucket rather than adding\
          \ it to every cluster."
        explode: true
        in: query
        name: includeUngroupedQueries
        required: false
        schema:
          default: false
          type: boolean
        style: form
      responses:
        "200":
          description: "One row per keyword cluster, plus the ungrouped bucket where\
            \ it applies"
        "401":
          description: Missing or invalid API key
        "400":
          description: A filter was rejected; the details name the field
        "403":
          description: The key lacks the read capability
        "404":
          description: No organization or project the caller can access under these
            ids
      summary: Rank-tracking metrics per keyword cluster
      tags:
      - Analytics
      x-accepts:
      - application/json
  /api/v1/organizations/{organizationId}/projects/{projectId}/competitors/co-occurrence:
    get:
      description: "Minimum role: viewer. Restricted to the AI answers where the brand\
        \ and a competitor are BOTH mentioned, one row per tracked competitor: sharedResponseCount\
        \ is how many such answers there are, and brandWins / competitorWins / ties\
        \ split them by who holds the better (lower) best mention position. winRate\
        \ is the percentage 0-100 of those answers the brand wins; avgOwnPosition\
        \ and avgCompetitorPosition are the average best mention position each side\
        \ held, 1-based, so LOWER is better. exampleQueryText and exampleAiResponseId\
        \ point at one representative shared answer. Every nullable field means \"\
        not known yet\" rather than zero: a null winRate or average position is the\
        \ absence of a shared answer in the window, not a record of losing. Tracked\
        \ competitors only."
      operationId: getCompetitorCoOccurrence
      parameters:
      - explode: false
        in: path
        name: organizationId
        required: true
        schema:
          format: uuid
          type: string
        style: simple
      - explode: false
        in: path
        name: projectId
        required: true
        schema:
          format: uuid
          type: string
        style: simple
      - description: "Inclusive start of the window, Y-m-d. Must fall inside the data\
          \ retention window."
        explode: true
        in: query
        name: dateFrom
        required: true
        schema:
          format: date
          type: string
        style: form
      - description: "Inclusive end of the window, Y-m-d."
        explode: true
        in: query
        name: dateTo
        required: true
        schema:
          format: date
          type: string
        style: form
      - description: "Repeatable, or comma-separated. SERP and Shopping carry no AI\
          \ answer text, so they contribute no co-occurrence."
        explode: true
        in: query
        name: engines
        required: false
        schema:
          items:
            enum:
            - chatgpt
            - perplexity
            - google_ai_overview
            - google_ai_mode
            - google_serp
            - google_shopping
            type: string
          type: array
        style: form
      - description: ISO-3166 alpha-2 codes or English names. Must be configured on
          the project.
        explode: true
        in: query
        name: countries
        required: false
        schema:
          items:
            type: string
          type: array
        style: form
      - description: Restricts the answer to a single tracked competitor. Omit it
          for every tracked competitor. The available-filters endpoint lists the valid
          ids.
        explode: true
        in: query
        name: competitorId
        required: false
        schema:
          format: uuid
          type: string
        style: form
      responses:
        "200":
          description: "One head-to-head row per tracked competitor, under \"competitors\""
        "401":
          description: Missing or invalid API key
        "400":
          description: A filter was rejected; the details name the field
        "403":
          description: The key lacks the read capability
        "404":
          description: No organization or project the caller can access under these
            ids
      summary: Head-to-head record of the brand against each tracked competitor
      tags:
      - Analytics
      x-accepts:
      - application/json
  /api/v1/organizations/{organizationId}/projects/{projectId}/mentions/mix:
    get:
      description: "Minimum role: viewer. Counts of the project brand's own text mentions\
        \ in AI answers over a date window, grouped three ways: byType (recommendation,\
        \ comparison, listing, example, reference), byTone (positive, neutral, negative)\
        \ and byQualifier (direct, conditional — a conditional mention is one the\
        \ answer hedged with a condition). These are the inputs behind the Share of\
        \ Voice weighted score. Every bucket is always present and is a plain count,\
        \ never null: a zero means no mention of that kind was found in the window.\
        \ The three groupings count the same mentions, so each one sums to the same\
        \ total. Only the project brand is counted, never a competitor, and only mentions\
        \ inside the answer text — a citation of the brand's URL is not a mention\
        \ here. Only AI answer engines produce mentions, so restricting engines to\
        \ google_serp or google_shopping alone returns all zeros. For the positive/neutral/negative\
        \ split per engine and per competitor use the sentiment endpoint."
      operationId: getMentionMix
      parameters:
      - explode: false
        in: path
        name: organizationId
        required: true
        schema:
          format: uuid
          type: string
        style: simple
      - explode: false
        in: path
        name: projectId
        required: true
        schema:
          format: uuid
          type: string
        style: simple
      - description: "Inclusive start of the window, Y-m-d. Must fall inside the data\
          \ retention window."
        explode: true
        in: query
        name: dateFrom
        required: true
        schema:
          format: date
          type: string
        style: form
      - description: "Inclusive end of the window, Y-m-d."
        explode: true
        in: query
        name: dateTo
        required: true
        schema:
          format: date
          type: string
        style: form
      - description: "Repeatable, or comma-separated. Non-AI engines contribute no\
          \ mentions."
        explode: true
        in: query
        name: engines
        required: false
        schema:
          items:
            enum:
            - chatgpt
            - perplexity
            - google_ai_overview
            - google_ai_mode
            - google_serp
            - google_shopping
            type: string
          type: array
        style: form
      - description: ISO-3166 alpha-2 codes or English names. Must be configured on
          the project.
        explode: true
        in: query
        name: countries
        required: false
        schema:
          items:
            type: string
          type: array
        style: form
      responses:
        "200":
          description: "Mention counts by type, by tone and by qualifier"
        "401":
          description: Missing or invalid API key
        "400":
          description: A filter was rejected; the details name the field
        "403":
          description: The key lacks the read capability
        "404":
          description: No organization or project the caller can access under these
            ids
      summary: Composition of a project brand mentions in AI answers
      tags:
      - Analytics
      x-accepts:
      - application/json
  /api/v1/organizations/{organizationId}/projects/{projectId}/mentions:
    get:
      description: "Minimum role: viewer. A paginated page of the individual mention\
        \ texts behind the aggregate numbers, for qualitative review and for checking\
        \ sentiment labels by eye. Each sample carries the mention text, the engine\
        \ and country it was seen in, the tracked query that produced it, its sentiment\
        \ and mention type, and mentionPosition — a 1-based rank inside the answer\
        \ where LOWER is better, always present. `total` counts every mention matching\
        \ the filters, not the size of the page returned. Only mentions inside the\
        \ answer text are returned: a citation of the brand URL is not a mention here,\
        \ and only AI answer engines produce mentions, so restricting engines to google_serp\
        \ or google_shopping alone returns an empty page rather than an error. Two\
        \ fields carry a \"not known\" rather than a zero: country is null and queryText\
        \ is empty when the tracked query behind the mention has since been deleted,\
        \ and competitorId is null when the mention row stores no competitor id —\
        \ which is NOT an assertion that the mention is about your own brand, since\
        \ an untracked competitor also stores none. Read mentionRelation instead:\
        \ `own` and `tracked-competitor` are what the scraper resolved to a configured\
        \ entity, `untracked-competitor` is a rival the project does not track, and\
        \ null means the row predates the field. brandName carries the mentioned brand,\
        \ and is the only way to name an untracked competitor, which has no competitor\
        \ id to resolve one from. Omitting the competitorId filter returns exactly\
        \ the rows with no competitor id stored — that is, own-brand AND untracked-competitor\
        \ mentions together, not every competitor; pass a competitor UUID to restrict\
        \ the page to that competitor, and use mentionRelation to separate the rest.\
        \ For the aggregate positive/neutral/negative split use the sentiment endpoint,\
        \ and for weighted mention-type counts the mention mix endpoint."
      operationId: getMentionSamples
      parameters:
      - explode: false
        in: path
        name: organizationId
        required: true
        schema:
          format: uuid
          type: string
        style: simple
      - explode: false
        in: path
        name: projectId
        required: true
        schema:
          format: uuid
          type: string
        style: simple
      - description: "Inclusive start of the window, Y-m-d. Must fall inside the data\
          \ retention window."
        explode: true
        in: query
        name: dateFrom
        required: true
        schema:
          format: date
          type: string
        style: form
      - description: "Inclusive end of the window, Y-m-d."
        explode: true
        in: query
        name: dateTo
        required: true
        schema:
          format: date
          type: string
        style: form
      - description: "Repeatable, or comma-separated. Non-AI engines contribute no\
          \ mentions."
        explode: true
        in: query
        name: engines
        required: false
        schema:
          items:
            enum:
            - chatgpt
            - perplexity
            - google_ai_overview
            - google_ai_mode
            - google_serp
            - google_shopping
            type: string
          type: array
        style: form
      - description: ISO-3166 alpha-2 codes or English names. Must be configured on
          the project.
        explode: true
        in: query
        name: countries
        required: false
        schema:
          items:
            type: string
          type: array
        style: form
      - description: Restrict to one sentiment label. Omit for every sentiment.
        explode: true
        in: query
        name: sentiment
        required: false
        schema:
          enum:
          - positive
          - neutral
          - negative
          type: string
        style: form
      - description: Restrict to one mention type. Omit for every type.
        explode: true
        in: query
        name: mentionType
        required: false
        schema:
          enum:
          - recommendation
          - comparison
          - listing
          - example
          - reference
          type: string
        style: form
      - description: "UUID of a single competitor, as listed by the available-filters\
          \ endpoint (competitors[].id). Omitted, the page is restricted to mentions\
          \ storing no competitor id, which is own-brand and untracked-competitor\
          \ mentions together — see the operation description and read mentionRelation\
          \ to tell them apart."
        explode: true
        in: query
        name: competitorId
        required: false
        schema:
          format: uuid
          type: string
        style: form
      - description: "recent: newest first. negative: negative sentiment first, then\
          \ neutral, then positive, newest first inside each. engine and country:\
          \ grouped alphabetically, newest first inside each group. An unknown value\
          \ is rejected, not replaced by the default."
        explode: true
        in: query
        name: sortBy
        required: false
        schema:
          default: recent
          enum:
          - recent
          - negative
          - engine
          - country
          type: string
        style: form
      - description: "Page size. A larger value is rejected, never silently reduced."
        explode: true
        in: query
        name: limit
        required: false
        schema:
          default: 20
          maximum: 100
          minimum: 1
          type: integer
        style: form
      - description: Number of matching mentions to skip before the page starts.
        explode: true
        in: query
        name: offset
        required: false
        schema:
          default: 0
          minimum: 0
          type: integer
        style: form
      responses:
        "200":
          description: A page of mention samples and the total matching the filters
        "401":
          description: Missing or invalid API key
        "400":
          content:
            application/json:
              examples:
                validationError:
                  description: "Produced by `?dateFrom=2026-02-01&dateTo=2026-03-01&engines=bard&limit=500`.\
                    \ Every error carries `code`, `message` and `requestId`; `details`\
                    \ is added only when a request failed validation, and names every\
                    \ field at fault in one response rather than one per round trip.\
                    \ `code` is `validation_error` for a rejected request and the\
                    \ domain error code otherwise - `organization_not_found`, `idempotency_key_reused`,\
                    \ `rate_limit_exceeded`. `requestId` is also returned in the X-Request-Id\
                    \ header; quote it when reporting a failure. `parameters` carries\
                    \ the placeholder values behind a message and is an empty array\
                    \ when a rule needs none, as both of these do."
                  summary: "The error envelope every failure of this API uses, here\
                    \ a request with two bad filters"
                  value:
                    code: validation_error
                    message: The request failed validation.
                    requestId: 0193c4f2-77b1-7e5a-9c03-2d8e4f6a1b90
                    details:
                      engines:
                      - code: invalid_value
                        message: "\"engines\" values must be drawn from: chatgpt,\
                          \ perplexity, google_ai_overview, google_ai_mode, google_serp,\
                          \ google_shopping."
                        parameters: []
                      limit:
                      - code: out_of_range
                        message: '"limit" must be between 1 and 100.'
                        parameters: []
              schema:
                $ref: "#/components/schemas/getMentionSamples_400_response"
          description: A filter was rejected; the details name the field
        "403":
          description: The key lacks the read capability
        "404":
          description: No organization or project the caller can access under these
            ids
      summary: Sample of the raw AI mention texts of a project
      tags:
      - Analytics
      x-accepts:
      - application/json
  /api/v1/metric-glossary:
    get:
      description: "Static reference, no project data. Each entry gives a metric,\
        \ the everyday words people use for it, its unit and range, whether higher\
        \ or lower is better, the operation that returns it, and example questions.\
        \ Useful when turning a vague or non-technical request into the right call."
      operationId: getMetricGlossary
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/getMetricGlossary_200_response"
          description: The metric catalogue
        "403":
          description: The key lacks the read capability
        "401":
          description: Missing or invalid API key
      summary: Map everyday wording to a metric and the operation that serves it
      tags:
      - Analytics
      x-accepts:
      - application/json
  /api/v1/organizations/{organizationId}/overview:
    get:
      description: "Minimum role: viewer. One row per active project — share of voice,\
        \ mention rate, average mention position, positivity index and tracked-query\
        \ count — ordered by share of voice, plus an organization-level aggregate\
        \ of the same metrics. Archived projects are excluded. This is a current-state\
        \ snapshot and takes no date window; for a date-ranged comparison call the\
        \ per-project operations (getProjectMetrics, getProjectTimeSeries) for each\
        \ projectId returned here. Every metric is nullable, and a null means the\
        \ project has no rank data yet — not a score of zero. Higher is better for\
        \ shareOfVoice, mentionRate and positivityIndex; avgMentionPosition is a 1-based\
        \ rank, so LOWER is better."
      operationId: getOrganizationOverview
      parameters:
      - explode: false
        in: path
        name: organizationId
        required: true
        schema:
          format: uuid
          type: string
        style: simple
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/getOrganizationOverview_200_response"
          description: Per-project snapshot rows and the organization aggregate
        "401":
          description: Missing or invalid API key
        "403":
          description: The key lacks the read capability
        "404":
          description: No organization the caller can access under this id
      summary: Snapshot rank-health board across an organization active projects
      tags:
      - Analytics
      x-accepts:
      - application/json
  /api/v1/organizations/{organizationId}/projects/{projectId}/metrics:
    get:
      description: "Minimum role: viewer. The project overview over a date window:\
        \ share of voice (own and per competitor), mention / SERP / shopping rates,\
        \ average and best positions, position stability, the sentiment split and\
        \ the position-distribution buckets. Positions are 1-based, so a LOWER number\
        \ is better; rates, the positivity index and share of voice are percentages\
        \ from 0 to 100, where HIGHER is better. Every `trend*` field is the signed\
        \ change against the immediately preceding window of the same length: negative\
        \ means an improved position, positive means an improved rate or score. A\
        \ null metric means \"not known yet\", never zero: a scalar is null when the\
        \ window holds no checks at all, and a `trend*` field is null when there is\
        \ no earlier window to compare against. The counters (`mentionCount`, `*TrackedQueryCount`,\
        \ `*QueriesWithResult`, `sentiment*` and `mentionTypeCounts`) are genuine\
        \ zeros instead, so an empty window reads as zero counts with null rates.\
        \ `dataDirtySince` is non-null while a recalculation is pending, meaning the\
        \ figures may still move for dates from then on."
      operationId: getProjectMetrics
      parameters:
      - explode: false
        in: path
        name: organizationId
        required: true
        schema:
          format: uuid
          type: string
        style: simple
      - explode: false
        in: path
        name: projectId
        required: true
        schema:
          format: uuid
          type: string
        style: simple
      - description: "Inclusive start of the window, Y-m-d. Must fall inside the data\
          \ retention window."
        explode: true
        in: query
        name: dateFrom
        required: true
        schema:
          format: date
          type: string
        style: form
      - description: "Inclusive end of the window, Y-m-d."
        explode: true
        in: query
        name: dateTo
        required: true
        schema:
          format: date
          type: string
        style: form
      - description: "Repeatable, or comma-separated."
        explode: true
        in: query
        name: engines
        required: false
        schema:
          items:
            enum:
            - chatgpt
            - perplexity
            - google_ai_overview
            - google_ai_mode
            - google_serp
            - google_shopping
            type: string
          type: array
        style: form
      - description: ISO-3166 alpha-2 codes or English names. Must be configured on
          the project.
        explode: true
        in: query
        name: countries
        required: false
        schema:
          items:
            type: string
          type: array
        style: form
      - description: Restrict to these keyword clusters. Each must belong to this
          project.
        explode: true
        in: query
        name: queryClusterIds
        required: false
        schema:
          items:
            format: uuid
            type: string
          type: array
        style: form
      - description: "Only meaningful together with queryClusterIds: also counts the\
          \ tracked queries that belong to no cluster."
        explode: true
        in: query
        name: includeUngroupedQueries
        required: false
        schema:
          default: false
          type: boolean
        style: form
      responses:
        "200":
          description: "Headline metrics, trends, sentiment split, per-competitor\
            \ share of voice and position distributions"
        "401":
          description: Missing or invalid API key
        "400":
          description: A filter was rejected; the details name the field
        "403":
          description: The key lacks the read capability
        "404":
          description: No organization or project the caller can access under these
            ids
      summary: Headline visibility metrics of a project
      tags:
      - Analytics
      x-accepts:
      - application/json
  /api/v1/organizations/{organizationId}/projects/{projectId}/sentiment:
    get:
      description: "Minimum role: viewer. Positive, neutral and negative split of\
        \ the brand mentions in AI answers over a date window, per engine and per\
        \ competitor. A null positivityIndex means no mentions were found in the window,\
        \ which is not the same as a score of zero."
      operationId: getProjectSentiment
      parameters:
      - explode: false
        in: path
        name: organizationId
        required: true
        schema:
          format: uuid
          type: string
        style: simple
      - explode: false
        in: path
        name: projectId
        required: true
        schema:
          format: uuid
          type: string
        style: simple
      - description: "Inclusive start of the window, Y-m-d. Must fall inside the data\
          \ retention window."
        explode: true
        in: query
        name: dateFrom
        required: true
        schema:
          format: date
          type: string
        style: form
      - description: "Inclusive end of the window, Y-m-d."
        explode: true
        in: query
        name: dateTo
        required: true
        schema:
          format: date
          type: string
        style: form
      - description: "Repeatable, or comma-separated."
        explode: true
        in: query
        name: engines
        required: false
        schema:
          items:
            enum:
            - chatgpt
            - perplexity
            - google_ai_overview
            - google_ai_mode
            - google_serp
            - google_shopping
            type: string
          type: array
        style: form
      - description: ISO-3166 alpha-2 codes or English names. Must be configured on
          the project.
        explode: true
        in: query
        name: countries
        required: false
        schema:
          items:
            type: string
          type: array
        style: form
      - explode: true
        in: query
        name: queryClusterIds
        required: false
        schema:
          items:
            format: uuid
            type: string
          type: array
        style: form
      - explode: true
        in: query
        name: includeUngroupedQueries
        required: false
        schema:
          default: false
          type: boolean
        style: form
      responses:
        "200":
          description: Sentiment per engine and per competitor
        "401":
          description: Missing or invalid API key
        "400":
          description: A filter was rejected; the details name the field
        "403":
          description: The key lacks the read capability
        "404":
          description: No organization or project the caller can access under these
            ids
      summary: Sentiment breakdown of a project brand mentions
      tags:
      - Analytics
      x-accepts:
      - application/json
  /api/v1/organizations/{organizationId}/projects/{projectId}/timeseries:
    get:
      description: "Minimum role: viewer. One point per bucket over the date window,\
        \ each carrying the brand metrics and one same-shaped entry per requested\
        \ competitor. Rank metrics (serp, shopping, mention, link) are 1-based averages\
        \ where LOWER is better; positivity (0-100), shareOfVoice (0-100), mentionRate\
        \ (0-100) and serpRate (0-100) are scores where higher is better. Every metric\
        \ is nullable, and a null means no data was collected for that bucket, which\
        \ is not the same as a value of zero. A project with no tracked queries returns\
        \ an empty points list. Prefer weekly or monthly granularity over a long window\
        \ to keep the response compact."
      operationId: getProjectTimeSeries
      parameters:
      - explode: false
        in: path
        name: organizationId
        required: true
        schema:
          format: uuid
          type: string
        style: simple
      - explode: false
        in: path
        name: projectId
        required: true
        schema:
          format: uuid
          type: string
        style: simple
      - description: "Inclusive start of the window, Y-m-d. Must fall inside the data\
          \ retention window."
        explode: true
        in: query
        name: dateFrom
        required: true
        schema:
          format: date
          type: string
        style: form
      - description: "Inclusive end of the window, Y-m-d."
        explode: true
        in: query
        name: dateTo
        required: true
        schema:
          format: date
          type: string
        style: form
      - description: Bucket size of each point.
        explode: true
        in: query
        name: granularity
        required: false
        schema:
          default: daily
          enum:
          - daily
          - weekly
          - monthly
          type: string
        style: form
      - description: "Repeatable, or comma-separated."
        explode: true
        in: query
        name: engines
        required: false
        schema:
          items:
            enum:
            - chatgpt
            - perplexity
            - google_ai_overview
            - google_ai_mode
            - google_serp
            - google_shopping
            type: string
          type: array
        style: form
      - description: ISO-3166 alpha-2 codes or English names. Must be configured on
          the project.
        explode: true
        in: query
        name: countries
        required: false
        schema:
          items:
            type: string
          type: array
        style: form
      - explode: true
        in: query
        name: queryClusterIds
        required: false
        schema:
          items:
            format: uuid
            type: string
          type: array
        style: form
      - description: On its own this NARROWS the series to tracked queries that belong
          to no cluster; combined with queryClusterIds it widens those clusters to
          also cover them.
        explode: true
        in: query
        name: includeUngroupedQueries
        required: false
        schema:
          default: false
          type: boolean
        style: form
      - description: "Repeatable, or comma-separated. Each id adds one series under\
          \ the competitors map of every point."
        explode: true
        in: query
        name: competitorIds
        required: false
        schema:
          items:
            format: uuid
            type: string
          type: array
        style: form
      responses:
        "200":
          description: "Points in bucket order, plus dataDirtySince: a date from which\
            \ the rank data is being recomputed, or null when nothing is pending"
        "401":
          description: Missing or invalid API key
        "400":
          description: A filter was rejected; the details name the field
        "403":
          description: The key lacks the read capability
        "404":
          description: No organization or project the caller can access under these
            ids
      summary: Rank-tracking metrics of a project over time
      tags:
      - Analytics
      x-accepts:
      - application/json
  /api/v1/organizations/{organizationId}/projects/{projectId}/metrics/movers:
    get:
      description: "Minimum role: viewer. One row per tracked query — a single engine\
        \ plus country — carrying its current metric envelope and the signed change\
        \ against the immediately preceding window of equal length: a 7-day window\
        \ is compared with the 7 days before it. Every trend delta is signed so that\
        \ POSITIVE means improved, including the position trends, where the underlying\
        \ avgSerpPosition / avgShoppingPosition / avgMentionPosition / avgLinkPosition\
        \ are 1-based ranks and therefore LOWER is better. shareOfVoice and positivityIndex\
        \ are percentages from 0 to 100, where HIGHER is better. Sorting applies to\
        \ the trend keys only: sortOrder=desc gives the top gainers, asc the top losers.\
        \ Every nullable field means \"not known yet\" rather than zero — a null position\
        \ or shareOfVoice is a query with no data in the window, and a null positivityIndex\
        \ or trend is a period with no mentions to score, neither of which is a record\
        \ of losing ground. total counts the tracked queries the filters match, not\
        \ the rows on this page. dataDirtySince is a date from which the rank data\
        \ is being recomputed, or null when nothing is pending; while it is non-null\
        \ the deltas may still move."
      operationId: getQueryMovers
      parameters:
      - explode: false
        in: path
        name: organizationId
        required: true
        schema:
          format: uuid
          type: string
        style: simple
      - explode: false
        in: path
        name: projectId
        required: true
        schema:
          format: uuid
          type: string
        style: simple
      - description: "Inclusive start of the window, Y-m-d. Must fall inside the data\
          \ retention window. The comparison window is the equally long stretch immediately\
          \ before it."
        explode: true
        in: query
        name: dateFrom
        required: true
        schema:
          format: date
          type: string
        style: form
      - description: "Inclusive end of the window, Y-m-d."
        explode: true
        in: query
        name: dateTo
        required: true
        schema:
          format: date
          type: string
        style: form
      - description: "Repeatable, or comma-separated."
        explode: true
        in: query
        name: engines
        required: false
        schema:
          items:
            enum:
            - chatgpt
            - perplexity
            - google_ai_overview
            - google_ai_mode
            - google_serp
            - google_shopping
            type: string
          type: array
        style: form
      - description: ISO-3166 alpha-2 codes or English names. Must be configured on
          the project.
        explode: true
        in: query
        name: countries
        required: false
        schema:
          items:
            type: string
          type: array
        style: form
      - description: "Which trend delta ranks the rows. Positions improve as they\
          \ fall, so a positive delta is always an improvement whichever key you pick.\
          \ An unknown value is rejected, not replaced by the default."
        explode: true
        in: query
        name: sortBy
        required: false
        schema:
          default: trend_share_of_voice
          enum:
          - trend_serp
          - trend_shopping
          - trend_mention
          - trend_link
          - trend_share_of_voice
          type: string
        style: form
      - description: "desc for the top gainers, asc for the top losers."
        explode: true
        in: query
        name: sortOrder
        required: false
        schema:
          default: desc
          enum:
          - asc
          - desc
          type: string
        style: form
      - description: "Page size. A value above the maximum is rejected, never clamped."
        explode: true
        in: query
        name: limit
        required: false
        schema:
          default: 20
          maximum: 100
          minimum: 1
          type: integer
        style: form
      - explode: true
        in: query
        name: offset
        required: false
        schema:
          default: 0
          minimum: 0
          type: integer
        style: form
      responses:
        "200":
          description: "The ranked tracked queries under \"rows\", with \"total\"\
            \ and \"dataDirtySince\""
        "401":
          description: Missing or invalid API key
        "400":
          description: A filter or page bound was rejected; the details name the field
        "403":
          description: The key lacks the read capability
        "404":
          description: No organization or project the caller can access under these
            ids
      summary: Tracked queries ranked by how much a metric moved
      tags:
      - Analytics
      x-accepts:
      - application/json
  /api/v1/organizations/{organizationId}/projects/{projectId}/metrics/share-of-voice-formula:
    get:
      description: "Minimum role: viewer. Static reference data, the same for every\
        \ project: the weights and multipliers that turn individual brand mentions\
        \ into a Share of Voice score. Each mention is worth `mentionTypeWeights[type]\
        \ * sentimentMultipliers[tone] * (conditional ? conditionalMultiplier : directMultiplier)`,\
        \ and a competitor's Share of Voice is its share of the summed weights of\
        \ every brand in the window, as a percentage. mentionTypeWeights is keyed\
        \ by mention type (recommendation, comparison, listing, example, reference)\
        \ and sentimentMultipliers by tone (positive, neutral, negative); a negative\
        \ mention is discounted, not discarded, because it still evidences presence.\
        \ A conditional mention is one the answer hedged with a condition (\"if you\
        \ need X\"). Use this to explain a score, not to recompute one: the counts\
        \ it applies to come from the mention mix endpoint. Every value is always\
        \ present and is never null."
      operationId: getShareOfVoiceFormula
      parameters:
      - explode: false
        in: path
        name: organizationId
        required: true
        schema:
          format: uuid
          type: string
        style: simple
      - explode: false
        in: path
        name: projectId
        required: true
        schema:
          format: uuid
          type: string
        style: simple
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/getShareOfVoiceFormula_200_response"
          description: The Share of Voice weighting constants
        "401":
          description: Missing or invalid API key
        "400":
          description: "Not reachable here: the endpoint accepts no query parameters,\
            \ so it has nothing to reject. Listed because the error envelope is shared\
            \ across the API"
        "403":
          description: The key lacks the read capability
        "404":
          description: No organization or project the caller can access under these
            ids
      summary: The constants behind the Share of Voice score
      tags:
      - Analytics
      x-accepts:
      - application/json
  /api/v1/organizations/{organizationId}/projects/{projectId}/tracked-queries/{trackedQueryId}/timeseries:
    get:
      description: "Minimum role: viewer. One point per bucket over the date window,\
        \ each carrying the brand metrics and one same-shaped entry per requested\
        \ competitor. Positions (serp, shopping, mention, link) are 1-based, so a\
        \ LOWER number is better; positivity, shareOfVoice, mentionRate and serpRate\
        \ are percentages from 0 to 100, where higher is better. Every metric is nullable,\
        \ and a null means nothing was captured for that entity in that bucket — it\
        \ is not a zero: a null shareOfVoice means no measurement, a shareOfVoice\
        \ of 0 means measured and never mentioned. The tracked query fixes its own\
        \ engine and country, so no engine or country filter is accepted. A non-null\
        \ dataDirtySince is a timestamp warning that tracked queries were deleted\
        \ from the project and historical buckets may still include their contributions\
        \ until the nightly refresh rebuilds them."
      operationId: getTrackedQueryTimeSeries
      parameters:
      - explode: false
        in: path
        name: organizationId
        required: true
        schema:
          format: uuid
          type: string
        style: simple
      - explode: false
        in: path
        name: projectId
        required: true
        schema:
          format: uuid
          type: string
        style: simple
      - description: Must belong to the project in the path.
        explode: false
        in: path
        name: trackedQueryId
        required: true
        schema:
          format: uuid
          type: string
        style: simple
      - description: "Inclusive start of the window, Y-m-d. Must fall inside the data\
          \ retention window."
        explode: true
        in: query
        name: dateFrom
        required: true
        schema:
          format: date
          type: string
        style: form
      - description: "Inclusive end of the window, Y-m-d."
        explode: true
        in: query
        name: dateTo
        required: true
        schema:
          format: date
          type: string
        style: form
      - description: Bucket size. Prefer weekly or monthly for long windows.
        explode: true
        in: query
        name: granularity
        required: false
        schema:
          default: daily
          enum:
          - daily
          - weekly
          - monthly
          type: string
        style: form
      - description: "Competitors to add as extra series, repeatable or comma-separated.\
          \ Valid ids come from the available-filters endpoint."
        explode: true
        in: query
        name: competitorIds
        required: false
        schema:
          items:
            format: uuid
            type: string
          type: array
        style: form
      responses:
        "200":
          description: "One point per bucket, with brand and per-competitor metrics"
        "401":
          description: Missing or invalid API key
        "400":
          description: A filter was rejected; the details name the field
        "403":
          description: The key lacks the read capability
        "404":
          description: "No organization, project or tracked query the caller can access\
            \ under these ids"
      summary: Rank-tracking time series of a single tracked query
      tags:
      - Analytics
      x-accepts:
      - application/json
  /api/v1/organizations/{organizationId}/projects/{projectId}/coverage:
    get:
      description: "Minimum role: viewer. A current-state snapshot answering \"what\
        \ is stale or not being tracked\": total is every tracked query on the project,\
        \ active and paused split it by status, neverChecked counts the active queries\
        \ that have never run, and overdue counts the active queries whose last check\
        \ is older than their own check-frequency interval (daily, weekly or monthly).\
        \ neverChecked and overdue are disjoint — a query that has never run is never\
        \ also counted as overdue — and both ignore paused queries, which are not\
        \ expected to be checked at all. sample lists up to 20 of the overdue queries,\
        \ most stale first; it is a sample of overdue only, so it never contains a\
        \ never-checked query and is empty when overdue is 0. Takes no date window:\
        \ every count describes the project as it stands right now."
      operationId: getTrackingCoverage
      parameters:
      - explode: false
        in: path
        name: organizationId
        required: true
        schema:
          format: uuid
          type: string
        style: simple
      - explode: false
        in: path
        name: projectId
        required: true
        schema:
          format: uuid
          type: string
        style: simple
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/getTrackingCoverage_200_response"
          description: Coverage counts and a sample of the most-overdue queries
        "401":
          description: Missing or invalid API key
        "400":
          description: "The request was rejected before it reached the project; this\
            \ endpoint accepts no query parameters, so no filter can be refused here"
        "403":
          description: The key lacks the read capability
        "404":
          description: No organization or project the caller can access under these
            ids
      summary: Coverage and staleness of a project tracked queries
      tags:
      - Analytics
      x-accepts:
      - application/json
  /api/v1/organizations/{organizationId}/entitlements:
    get:
      description: "Minimum role: owner. Returns what the current plan allows, how\
        \ much of it has been consumed and when the allowance next resets, taken from\
        \ the most recent subscription contract whether it is running or cancelled.\
        \ An organization that has never subscribed answers `status: \"none\"` with\
        \ every budget and consumption field null — null means \"no plan on file,\
        \ so not known\", which is deliberately distinct from a budget or a consumption\
        \ of zero. Commercial and provider details (prices, Stripe identifiers, internal\
        \ tier codes) are not part of this contract."
      operationId: getEntitlements
      parameters:
      - explode: false
        in: path
        name: organizationId
        required: true
        schema:
          format: uuid
          type: string
        style: simple
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EntitlementsResource"
          description: The organization's allowance and consumption
        "401":
          description: Missing or invalid API key
        "403":
          description: The key lacks the read capability
        "404":
          description: No organization the caller owns under this id
      summary: Get an organization's plan allowance and consumption
      tags:
      - Organizations
      x-accepts:
      - application/json
  /api/v1/organizations/{organizationId}/subscription:
    get:
      description: "Minimum role: viewer. The most recent subscription contract of\
        \ the organization, whatever its state: tier, billing interval, check budget\
        \ and consumption, and the cancellation and grace dates. An organization that\
        \ has never subscribed answers 200 with \"status\": \"none\" and every other\
        \ field null - a null is \"not applicable\", never a stand-in for a zero budget\
        \ or zero consumption. Stripe identifiers, prices and payment methods are\
        \ not part of this API."
      operationId: getSubscription
      parameters:
      - explode: false
        in: path
        name: organizationId
        required: true
        schema:
          format: uuid
          type: string
        style: simple
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/SubscriptionResource"
          description: "The subscription contract, or the \"none\" state"
        "401":
          description: Missing or invalid API key
        "403":
          description: The key lacks the read capability
        "404":
          description: No organization the caller can access under this id
      summary: Get the subscription of an organization
      tags:
      - Billing
      x-accepts:
      - application/json
  /api/v1/organizations/{organizationId}/projects/{projectId}/ai-responses:
    get:
      description: "Minimum role: viewer. Every AI answer captured for a project,\
        \ newest first, one page at a time. This is the evidence under the analytics\
        \ operations: the full answer text and the sources the engine cited, exactly\
        \ as captured. Read `unresolved` on a citation before attributing its domain\
        \ — a true value means the URL is still a redirector and the domain belongs\
        \ to that redirector, not to the publisher; count the citation, do not credit\
        \ the site. `passIndex` and `passCount` describe multi-pass sampling: rows\
        \ sharing a `trackedQueryId` and `capturedAt` are passes of one run, not duplicates,\
        \ so aggregating across them without dividing by `passCount` double-counts\
        \ that run. `responseText` is the whole answer, so a page of 100 is a large\
        \ response — lower `limit` rather than paging blind. Captures are kept for\
        \ 16 months and purged after that; a `dateFrom` before the window is rejected\
        \ rather than answered with an empty page. `total` counts every capture the\
        \ filter matches, not the size of this page."
      operationId: listAiResponses
      parameters:
      - explode: false
        in: path
        name: organizationId
        required: true
        schema:
          format: uuid
          type: string
        style: simple
      - explode: false
        in: path
        name: projectId
        required: true
        schema:
          format: uuid
          type: string
        style: simple
      - description: "Inclusive lower bound, widened to 00:00:00 UTC of the named\
          \ day. Must be inside the retention window."
        explode: true
        in: query
        name: dateFrom
        required: false
        schema:
          format: date
          type: string
        style: form
      - description: "Inclusive upper bound, widened to 23:59:59 UTC of the named\
          \ day."
        explode: true
        in: query
        name: dateTo
        required: false
        schema:
          format: date
          type: string
        style: form
      - description: Only captures of this tracked query. Must belong to the project
          in the path.
        explode: true
        in: query
        name: trackedQueryId
        required: false
        schema:
          format: uuid
          type: string
        style: form
      - description: Only captures from these AI engines. Repeat the parameter or
          pass a comma-separated list. An empty filter means every engine.
        explode: true
        in: query
        name: engines
        required: false
        schema:
          items:
            enum:
            - chatgpt
            - perplexity
            - google_ai_overview
            - google_ai_mode
            type: string
          type: array
        style: form
      - description: "Page size. A larger value is rejected, never silently reduced."
        explode: true
        in: query
        name: limit
        required: false
        schema:
          default: 20
          maximum: 100
          minimum: 1
          type: integer
        style: form
      - description: Number of captures to skip before the page starts.
        explode: true
        in: query
        name: offset
        required: false
        schema:
          default: 0
          minimum: 0
          type: integer
        style: form
      - description: "Direction of the capture-time ordering. An unknown value is\
          \ rejected, not replaced by the default."
        explode: true
        in: query
        name: sortOrder
        required: false
        schema:
          default: desc
          enum:
          - asc
          - desc
          type: string
        style: form
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/listAiResponses_200_response"
          description: A page of captured AI answers and the total the filter matches
        "400":
          description: "A filter, pagination or ordering parameter was rejected; the\
            \ details name the field"
        "401":
          description: Missing or invalid API key
        "403":
          description: The key lacks the read capability
        "404":
          description: No organization or project the caller can access under these
            ids
      summary: List captured AI answers
      tags:
      - Captures
      x-accepts:
      - application/json
  /api/v1/organizations/{organizationId}/projects/{projectId}/search-snapshots:
    get:
      description: "Minimum role: viewer. Every organic search page captured for a\
        \ project, newest first, one page at a time. This is what the rank figures\
        \ in the analytics operations were computed from: the ranked results as they\
        \ stood at `capturedAt`, not as they stand now. Read `unresolved` on a result\
        \ before attributing its domain — a true value means the URL is still a redirector\
        \ and the domain belongs to that redirector; count the result, do not credit\
        \ the site. `rating` and `ratingVotes` come from a rich-result star rating\
        \ when Google showed one and are absent far more often than present; their\
        \ absence says nothing about the page. There is no `engines` filter because\
        \ every capture here comes from Google SERP. Captures are kept for 16 months\
        \ and purged after that; a `dateFrom` before the window is rejected rather\
        \ than answered with an empty page. `total` counts every capture the filter\
        \ matches, not the size of this page."
      operationId: listSearchSnapshots
      parameters:
      - explode: false
        in: path
        name: organizationId
        required: true
        schema:
          format: uuid
          type: string
        style: simple
      - explode: false
        in: path
        name: projectId
        required: true
        schema:
          format: uuid
          type: string
        style: simple
      - description: "Inclusive lower bound, widened to 00:00:00 UTC of the named\
          \ day. Must be inside the retention window."
        explode: true
        in: query
        name: dateFrom
        required: false
        schema:
          format: date
          type: string
        style: form
      - description: "Inclusive upper bound, widened to 23:59:59 UTC of the named\
          \ day."
        explode: true
        in: query
        name: dateTo
        required: false
        schema:
          format: date
          type: string
        style: form
      - description: Only captures of this tracked query. Must belong to the project
          in the path.
        explode: true
        in: query
        name: trackedQueryId
        required: false
        schema:
          format: uuid
          type: string
        style: form
      - description: "Page size. A larger value is rejected, never silently reduced."
        explode: true
        in: query
        name: limit
        required: false
        schema:
          default: 20
          maximum: 100
          minimum: 1
          type: integer
        style: form
      - description: Number of captures to skip before the page starts.
        explode: true
        in: query
        name: offset
        required: false
        schema:
          default: 0
          minimum: 0
          type: integer
        style: form
      - description: "Direction of the capture-time ordering. An unknown value is\
          \ rejected, not replaced by the default."
        explode: true
        in: query
        name: sortOrder
        required: false
        schema:
          default: desc
          enum:
          - asc
          - desc
          type: string
        style: form
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/listSearchSnapshots_200_response"
          description: A page of captured search-results pages and the total the filter
            matches
        "400":
          description: "A filter, pagination or ordering parameter was rejected; the\
            \ details name the field"
        "401":
          description: Missing or invalid API key
        "403":
          description: The key lacks the read capability
        "404":
          description: No organization or project the caller can access under these
            ids
      summary: List captured search-results pages
      tags:
      - Captures
      x-accepts:
      - application/json
  /api/v1/organizations/{organizationId}/projects/{projectId}/shopping-snapshots:
    get:
      description: "Minimum role: viewer. Every shopping page captured for a project,\
        \ newest first, one page at a time: the ranked offers as they stood at `capturedAt`.\
        \ `price` and `currency` are what the seller showed at that moment — no tax\
        \ normalisation, no shipping, no conversion — and two offers in one snapshot\
        \ may carry different currencies, so convert before comparing. `productId`\
        \ is the marketplace's own identifier for the listing, stable enough to follow\
        \ one offer across snapshots, and it is not a Mencoro id. Offers turn over\
        \ far faster than organic results, so two snapshots days apart routinely share\
        \ none; that is the marketplace behaving normally, not a gap in the capture.\
        \ Unlike organic results, a shopping offer carries no `unresolved` flag: the\
        \ capture pipeline does not record one for this surface. Shopping captures\
        \ are trimmed by the same raw-data purge as AI answers and search pages, so\
        \ the same retention floor applies: a `dateFrom` before it is refused rather\
        \ than answered with an empty page. `total` counts every capture the filter\
        \ matches, not the size of this page."
      operationId: listShoppingSnapshots
      parameters:
      - explode: false
        in: path
        name: organizationId
        required: true
        schema:
          format: uuid
          type: string
        style: simple
      - explode: false
        in: path
        name: projectId
        required: true
        schema:
          format: uuid
          type: string
        style: simple
      - description: "Inclusive lower bound, widened to 00:00:00 UTC of the named\
          \ day. A date before the retention floor is refused rather than answered\
          \ with an empty page: shopping captures are aged out by the raw-data purge\
          \ like every other capture."
        explode: true
        in: query
        name: dateFrom
        required: false
        schema:
          format: date
          type: string
        style: form
      - description: "Inclusive upper bound, widened to 23:59:59 UTC of the named\
          \ day."
        explode: true
        in: query
        name: dateTo
        required: false
        schema:
          format: date
          type: string
        style: form
      - description: Only captures of this tracked query. Must belong to the project
          in the path.
        explode: true
        in: query
        name: trackedQueryId
        required: false
        schema:
          format: uuid
          type: string
        style: form
      - description: "Page size. A larger value is rejected, never silently reduced."
        explode: true
        in: query
        name: limit
        required: false
        schema:
          default: 20
          maximum: 100
          minimum: 1
          type: integer
        style: form
      - description: Number of captures to skip before the page starts.
        explode: true
        in: query
        name: offset
        required: false
        schema:
          default: 0
          minimum: 0
          type: integer
        style: form
      - description: "Direction of the capture-time ordering. An unknown value is\
          \ rejected, not replaced by the default."
        explode: true
        in: query
        name: sortOrder
        required: false
        schema:
          default: desc
          enum:
          - asc
          - desc
          type: string
        style: form
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/listShoppingSnapshots_200_response"
          description: A page of captured shopping-results pages and the total the
            filter matches
        "400":
          description: "A filter, pagination or ordering parameter was rejected; the\
            \ details name the field"
        "401":
          description: Missing or invalid API key
        "403":
          description: The key lacks the read capability
        "404":
          description: No organization or project the caller can access under these
            ids
      summary: List captured shopping-results pages
      tags:
      - Captures
      x-accepts:
      - application/json
  /api/v1/organizations/{organizationId}/projects/{projectId}/clusters/jobs/{jobId}/apply:
    post:
      description: "Minimum role: manager. Writes a completed clustering job onto\
        \ the tracked queries it was computed for: it creates the clusters the job\
        \ proposed that the project does not have yet, then assigns each tracked query\
        \ according to the merge mode the job was started with — fill_gaps leaves\
        \ already grouped queries alone, add_on_top only adds, full_regroup replaces\
        \ a query's clusters with the proposed set and therefore REMOVES clusters\
        \ that are not in it — but only for a tracked query the job actually returned\
        \ an assignment for. A tracked query the job was started over and produced\
        \ no assignment for is listed under `unassigned` and left exactly as it was,\
        \ under every mode; it is not treated as \"belongs to no cluster\" and is\
        \ never stripped. That matters most with restrictToExistingClusters, where\
        \ the model is expected to return nothing for queries that fit no existing\
        \ cluster. The body must be empty: the assignments, the target tracked queries\
        \ and the mode all come from the job, so this call cannot apply something\
        \ the job did not produce. Synchronous and partially successful — always 200,\
        \ with each tracked query under `successful` or `failed`, and nothing rolled\
        \ back because one failed. A job that is not completed, or one that produced\
        \ no assignments, is refused with 409. Requires the write capability and an\
        \ Idempotency-Key header; repeating the key returns the first answer rather\
        \ than applying twice."
      operationId: applyClusteringJob
      parameters:
      - explode: false
        in: path
        name: organizationId
        required: true
        schema:
          format: uuid
          type: string
        style: simple
      - explode: false
        in: path
        name: projectId
        required: true
        schema:
          format: uuid
          type: string
        style: simple
      - description: Must be a clustering job started for this organization and project.
        explode: false
        in: path
        name: jobId
        required: true
        schema:
          format: uuid
          type: string
        style: simple
      - description: Repeat it to retry a lost response without applying the job twice.
        explode: false
        in: header
        name: Idempotency-Key
        required: true
        schema:
          type: string
        style: simple
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApplyClusteringJobOutcome"
          description: What was written; read `failed` rather than inferring success
            from the status
        "400":
          description: "A body was sent, or the Idempotency-Key header is missing;\
            \ the details name the field"
        "401":
          description: Missing or invalid API key
        "403":
          description: The key lacks the write capability
        "404":
          description: "No organization, project or clustering job the caller can\
            \ access under these ids"
        "409":
          description: "The job is not completed, produced nothing that can be applied,\
            \ the organization is archived, the project is archived, or the idempotency\
            \ key was reused for a different request, a concurrent request with the\
            \ same key is still running, or a previous attempt with it ended without\
            \ a known outcome"
      summary: Apply the result of a clustering job
      tags:
      - Clusters
      x-accepts:
      - application/json
  /api/v1/organizations/{organizationId}/projects/{projectId}/clusters/batch:
    post:
      description: "Minimum role: manager. Creates up to 100 empty keyword clusters\
        \ in one call. Names are trimmed and lower-cased and repeated names in the\
        \ body are collapsed before anything is written. Partial success: the answer\
        \ is always 200 and reports every name under `successful` or under `failed`\
        \ — a name already taken in the project fails with query_cluster_name_already_exists\
        \ and is NOT resolved to the existing cluster, while the other names are still\
        \ created. Nothing is rolled back because one name failed. The clusters are\
        \ created empty; no tracked query is assigned to them. The organization must\
        \ not be archived; an archived project IS refused with 409, stricter than\
        \ the Mencoro app, which lets cluster writes into one. Requires the write\
        \ capability and an Idempotency-Key header."
      operationId: batchCreateQueryClusters
      parameters:
      - explode: false
        in: path
        name: organizationId
        required: true
        schema:
          format: uuid
          type: string
        style: simple
      - explode: false
        in: path
        name: projectId
        required: true
        schema:
          format: uuid
          type: string
        style: simple
      - description: Repeat it to retry a lost response without creating the batch
          twice.
        explode: false
        in: header
        name: Idempotency-Key
        required: true
        schema:
          type: string
        style: simple
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/BatchCreateQueryClustersRequestData"
        required: true
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BatchCreateQueryClustersOutcome"
          description: Per-name results; read `failed` rather than inferring success
            from the status
        "400":
          description: "The body was rejected, or the Idempotency-Key header is missing;\
            \ the details name the field"
        "401":
          description: Missing or invalid API key
        "403":
          description: The key lacks the write capability
        "404":
          description: No organization or project the caller can access under these
            ids
        "409":
          description: "The organization is archived, or the idempotency key was reused\
            \ for a different request, a concurrent request with the same key is still\
            \ running, or a previous attempt with it ended without a known outcome"
      summary: Create several keyword clusters at once
      tags:
      - Clusters
      x-content-type: application/json
      x-accepts:
      - application/json
  /api/v1/organizations/{organizationId}/projects/{projectId}/clusters:
    get:
      description: "Minimum role: viewer. The keyword clusters configured on a project,\
        \ one page at a time. Each cluster id is exactly what the analytics operations\
        \ accept in their queryClusterIds filter — pass the id, never the name. Names\
        \ are unique within a project and are stored lower-cased, so the listing is\
        \ ordered by name with no ties and paging over it neither repeats nor skips\
        \ a cluster. `total` counts every cluster in the project, not the size of\
        \ the page returned. A cluster carries no metrics of its own and no membership\
        \ count: for the tracked queries inside a cluster, filter the tracked-query\
        \ operations by its id. An empty list means the project has no clusters configured,\
        \ which is not an error and is not a statement about whether any data has\
        \ been collected.Send `Accept: text/csv` to receive the same page as a CSV\
        \ download instead of JSON: same filters, same authorization, same page window\
        \ and the same maximum of 100 rows — it is this page in another format, not\
        \ a bulk export, so a whole collection is still read by paging. The CSV carries\
        \ no `total`, because a table whose every row is a record has nowhere to put\
        \ one; read it from the JSON representation of the same request. A list-valued\
        \ field is joined into one cell with `; ` as a display projection — parse\
        \ the JSON if you need the structure. Cells beginning with `=`, `+`, `-` or\
        \ `@` are prefixed with an apostrophe so a spreadsheet treats them as text\
        \ rather than running them as formulas."
      operationId: listQueryClusters
      parameters:
      - explode: false
        in: path
        name: organizationId
        required: true
        schema:
          format: uuid
          type: string
        style: simple
      - explode: false
        in: path
        name: projectId
        required: true
        schema:
          format: uuid
          type: string
        style: simple
      - description: "Page size. A larger value is rejected, never silently reduced."
        explode: true
        in: query
        name: limit
        required: false
        schema:
          default: 20
          maximum: 100
          minimum: 1
          type: integer
        style: form
      - description: Number of clusters to skip before the page starts.
        explode: true
        in: query
        name: offset
        required: false
        schema:
          default: 0
          minimum: 0
          type: integer
        style: form
      - description: "Direction of the name ordering. An unknown value is rejected,\
          \ not replaced by the default."
        explode: true
        in: query
        name: sortOrder
        required: false
        schema:
          default: asc
          enum:
          - asc
          - desc
          type: string
        style: form
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/listQueryClusters_200_response"
            text/csv:
              example: |
                id,name
                b3f1...,Acme
              schema:
                type: string
          description: A page of the project's keyword clusters and the total in the
            project
        "400":
          description: A pagination or ordering parameter was rejected; the details
            name the field
        "401":
          description: Missing or invalid API key
        "403":
          description: The key lacks the read capability
        "404":
          description: No organization or project the caller can access under these
            ids
      summary: List the keyword clusters of a project
      tags:
      - Projects
      x-accepts:
      - application/json
      - text/csv
    post:
      description: "Minimum role: manager. Creates one empty keyword cluster in the\
        \ project. The name is trimmed and lower-cased before it is stored, and it\
        \ must be unique within the project: a name that already exists is refused\
        \ with 409 and nothing is merged into the existing cluster. The cluster starts\
        \ with no tracked queries in it — this operation does not assign anything\
        \ to it. The organization must not be archived; an archived project IS refused\
        \ with 409, stricter than the Mencoro app, which lets cluster writes into\
        \ one. Requires the write capability and an Idempotency-Key header."
      operationId: createQueryCluster
      parameters:
      - explode: false
        in: path
        name: organizationId
        required: true
        schema:
          format: uuid
          type: string
        style: simple
      - explode: false
        in: path
        name: projectId
        required: true
        schema:
          format: uuid
          type: string
        style: simple
      - description: "A token you choose per operation, 8 to 255 printable ASCII characters\
          \ with no spaces; a UUID is the obvious choice. Repeat it to retry a lost\
          \ response without creating a second cluster. The key is scoped to your\
          \ API key, and it is bound to the method, the path and the body of the first\
          \ attempt: repeating it with anything else changed is refused with 409 rather\
          \ than replayed."
        example: 3f9d2c18-6b4a-4c77-9f10-6f2d5a7c8e21
        explode: false
        in: header
        name: Idempotency-Key
        required: true
        schema:
          maxLength: 255
          minLength: 8
          type: string
        style: simple
      requestBody:
        content:
          application/json:
            example:
              name: Agencia SEO
            schema:
              $ref: "#/components/schemas/createQueryCluster_request"
        required: true
      responses:
        "201":
          content:
            application/json:
              examples:
                createdOrReplayed:
                  description: "A retry carrying the same Idempotency-Key and the\
                    \ same body does not create a second cluster: the recorded response\
                    \ is replayed, with the same 201 and the same `id`. That is what\
                    \ makes a lost response safe to retry. `name` comes back lower-cased\
                    \ because that is how the domain stores it, so \"Agencia SEO\"\
                    \ is answered as \"agencia seo\"."
                  summary: The created cluster - and byte for byte what a retry with
                    the same Idempotency-Key gets back
                  value:
                    id: 0193c4f4-01c9-7d18-8a52-7b3e9f04a1d7
                    projectId: 0193c4f1-8e2a-7c31-9d40-5b6f1a2c3d4e
                    name: agencia seo
              schema:
                $ref: "#/components/schemas/QueryClusterResource"
          description: The cluster that was created
        "400":
          description: "The body was rejected, or the Idempotency-Key header is missing;\
            \ the details name the field"
        "401":
          description: Missing or invalid API key
        "403":
          description: The key lacks the write capability
        "404":
          description: No organization or project the caller can access under these
            ids
        "409":
          content:
            application/json:
              examples:
                idempotencyKeyReused:
                  description: "The key was already spent on another operation, so\
                    \ replaying it would answer the wrong one and running it would\
                    \ perform a second operation under an authorisation meant for\
                    \ the first. Use a fresh key for a new operation; reuse one only\
                    \ to retry the identical request. The fingerprint covers the method,\
                    \ the path and the body exactly as sent, so the same call written\
                    \ with a differently-cased UUID in the path is a different request\
                    \ and lands here too. Two neighbouring cases carry their own codes:\
                    \ `operation_already_in_progress` while a concurrent request holds\
                    \ the key, and `operation_outcome_uncertain` when an earlier attempt\
                    \ failed without a known outcome - that one is never retried automatically,\
                    \ so check whether it took effect before using a new key."
                  summary: The same Idempotency-Key sent with a different request
                  value:
                    code: idempotency_key_reused
                    message: This idempotency key was already used for a different
                      request. Use a new key for a new operation.
                    requestId: 0193c4f4-3d60-7a0b-9e77-1c5482af6390
              schema:
                $ref: "#/components/schemas/createQueryCluster_409_response"
          description: "A cluster with this name already exists in the project, the\
            \ organization is archived, the project is archived, or the idempotency\
            \ key was reused for a different request, a concurrent request with the\
            \ same key is still running, or a previous attempt with it ended without\
            \ a known outcome"
      summary: Create a keyword cluster
      tags:
      - Clusters
      x-content-type: application/json
      x-accepts:
      - application/json
  /api/v1/organizations/{organizationId}/projects/{projectId}/clusters/{clusterId}:
    delete:
      description: "Minimum role: manager. Deletes one keyword cluster. The tracked\
        \ queries that were in it are NOT deleted: they are unassigned from this cluster\
        \ and keep every other cluster they belong to. Analytics filtered by this\
        \ cluster id return nothing afterwards, including for dates before the deletion,\
        \ because that filter reads current membership. This cannot be undone — recreating\
        \ a cluster with the same name produces a new id and an empty cluster. The\
        \ organization must not be archived, and neither must the project: an archived\
        \ project IS refused with 409, stricter than the Mencoro app, which lets cluster\
        \ writes into one. Requires the write capability and an Idempotency-Key header."
      operationId: deleteQueryCluster
      parameters:
      - explode: false
        in: path
        name: organizationId
        required: true
        schema:
          format: uuid
          type: string
        style: simple
      - explode: false
        in: path
        name: projectId
        required: true
        schema:
          format: uuid
          type: string
        style: simple
      - description: Must be a cluster of the project in the path.
        explode: false
        in: path
        name: clusterId
        required: true
        schema:
          format: uuid
          type: string
        style: simple
      - description: Repeat it to retry a lost response; a fresh key answers 404 once
          the cluster is gone.
        explode: false
        in: header
        name: Idempotency-Key
        required: true
        schema:
          type: string
        style: simple
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/deleteQueryCluster_200_response"
          description: The cluster was deleted
        "400":
          description: "A body was sent, or the Idempotency-Key header is missing;\
            \ the details name the field"
        "401":
          description: Missing or invalid API key
        "403":
          description: The key lacks the write capability
        "404":
          description: "No organization, project or cluster the caller can access\
            \ under these ids"
        "409":
          description: "The organization is archived, or the idempotency key was reused\
            \ for a different request, a concurrent request with the same key is still\
            \ running, or a previous attempt with it ended without a known outcome"
      summary: Delete a keyword cluster
      tags:
      - Clusters
      x-accepts:
      - application/json
    get:
      description: "Minimum role: viewer. Returns a single keyword cluster of the\
        \ project, the same projection the cluster listing returns for each of its\
        \ rows. A cluster belonging to another project answers 404, the same answer\
        \ an unknown id and a malformed one get, so the API never confirms that a\
        \ cluster the caller cannot reach exists."
      operationId: getQueryCluster
      parameters:
      - explode: false
        in: path
        name: organizationId
        required: true
        schema:
          format: uuid
          type: string
        style: simple
      - description: Must belong to the organization in the path.
        explode: false
        in: path
        name: projectId
        required: true
        schema:
          format: uuid
          type: string
        style: simple
      - description: Must be a cluster of the project in the path.
        explode: false
        in: path
        name: clusterId
        required: true
        schema:
          format: uuid
          type: string
        style: simple
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/QueryClusterResource"
          description: The keyword cluster
        "401":
          description: Missing or invalid API key
        "403":
          description: The key lacks the read capability
        "404":
          description: "No organization, project or cluster the caller can access\
            \ under these ids"
      summary: Get one of a project's keyword clusters
      tags:
      - Clusters
      x-accepts:
      - application/json
    patch:
      description: "Minimum role: manager. Changes the name of one keyword cluster\
        \ and nothing else: the tracked queries assigned to it are untouched, and\
        \ its id does not change, so nothing a client stored breaks. The new name\
        \ is trimmed and lower-cased and must be unique within the project; a collision\
        \ is refused with 409. Renaming to the name the cluster already has is accepted\
        \ and is a no-op. The organization must not be archived; an archived project\
        \ IS refused with 409, stricter than the Mencoro app, which lets cluster writes\
        \ into one. Requires the write capability and an Idempotency-Key header."
      operationId: renameQueryCluster
      parameters:
      - explode: false
        in: path
        name: organizationId
        required: true
        schema:
          format: uuid
          type: string
        style: simple
      - explode: false
        in: path
        name: projectId
        required: true
        schema:
          format: uuid
          type: string
        style: simple
      - description: Must be a cluster of the project in the path.
        explode: false
        in: path
        name: clusterId
        required: true
        schema:
          format: uuid
          type: string
        style: simple
      - description: Repeat it to retry a lost response without renaming twice.
        explode: false
        in: header
        name: Idempotency-Key
        required: true
        schema:
          type: string
        style: simple
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/createQueryCluster_request"
        required: true
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/QueryClusterResource"
          description: The cluster in its new state
        "400":
          description: "The body was rejected, or the Idempotency-Key header is missing;\
            \ the details name the field"
        "401":
          description: Missing or invalid API key
        "403":
          description: The key lacks the write capability
        "404":
          description: "No organization, project or cluster the caller can access\
            \ under these ids"
        "409":
          description: "Another cluster in the project already has this name, the\
            \ organization is archived, the project is archived, or the idempotency\
            \ key was reused for a different request, a concurrent request with the\
            \ same key is still running, or a previous attempt with it ended without\
            \ a known outcome"
      summary: Rename a keyword cluster
      tags:
      - Clusters
      x-content-type: application/json
      x-accepts:
      - application/json
  /api/v1/organizations/{organizationId}/projects/{projectId}/clusters/jobs:
    post:
      description: "Minimum role: manager. Asks the clustering service to propose\
        \ keyword clusters for up to 500 tracked queries of the project, and answers\
        \ 202 with the job to poll — the work runs in the background and a 202 says\
        \ it was accepted, never that it succeeded. THE JOB WRITES NOTHING: it produces\
        \ a proposal, and nothing changes until it is applied through the apply operation,\
        \ which is a separate call. Duplicate query texts within the selection are\
        \ sent once. Requires an entitled subscription (402 otherwise) and the organization\
        \ must be active and the project not archived. Rate limited to 10 starts per\
        \ minute per organization, shared with the same operation in the web application.\
        \ Requires the write capability and an Idempotency-Key header; repeating the\
        \ key returns the first job rather than starting a second."
      operationId: startClusteringJob
      parameters:
      - explode: false
        in: path
        name: organizationId
        required: true
        schema:
          format: uuid
          type: string
        style: simple
      - explode: false
        in: path
        name: projectId
        required: true
        schema:
          format: uuid
          type: string
        style: simple
      - description: Repeat it to retry a lost response without starting a second
          job.
        explode: false
        in: header
        name: Idempotency-Key
        required: true
        schema:
          type: string
        style: simple
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/StartClusteringJobRequestData"
        required: true
      responses:
        "202":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/AcceptedJobResource"
          description: The job was accepted; poll trackingUrl until its status is
            terminal
        "400":
          description: "The body was rejected, or the Idempotency-Key header is missing;\
            \ the details name the field"
        "401":
          description: Missing or invalid API key
        "402":
          description: The organization has no entitled subscription
        "403":
          description: The key lacks the write capability
        "404":
          description: "No organization, project or tracked query the caller can access\
            \ under these ids"
        "409":
          description: "The organization is archived, the project is archived, there\
            \ is nothing to cluster, or the idempotency key was reused for a different\
            \ request, a concurrent request with the same key is still running, or\
            \ a previous attempt with it ended without a known outcome"
        "429":
          description: More than 10 clustering starts in a minute for this organization;
            Retry-After says when to try again
      summary: Start a keyword clustering job
      tags:
      - Clusters
      x-content-type: application/json
      x-accepts:
      - application/json
  /api/v1/organizations/{organizationId}/projects/{projectId}/discovery/brands:
    post:
      description: "Minimum role: manager. Starts background discovery of the brands\
        \ and competitors around a project and answers 202 with the job to poll; it\
        \ never returns brands inline. The job PROPOSES names: it creates and updates\
        \ nothing, so neither the brand profile nor the competitor list changes because\
        \ this endpoint was called. The organization must be active. Unlike keyword\
        \ and prompt discovery this operation does not require an entitled subscription,\
        \ matching the application behaviour it mirrors. Ten starts per minute per\
        \ organization, counted across every key of every member and shared with the\
        \ same operation in the Mencoro app; a retry carrying an Idempotency-Key already\
        \ answered is served from the record and does not count. A project whose brand\
        \ monitoring profile has not been created yet is still accepted and still\
        \ billed: the job runs grounded on the project name alone, with no website\
        \ domains, brand names or competitors to work from, and completes normally.\
        \ Configure the brand profile first if you want the discovery grounded."
      operationId: startBrandDiscoveryJob
      parameters:
      - explode: false
        in: path
        name: organizationId
        required: true
        schema:
          format: uuid
          type: string
        style: simple
      - description: Must belong to the organization in the path.
        explode: false
        in: path
        name: projectId
        required: true
        schema:
          format: uuid
          type: string
        style: simple
      - description: Repeat it to retry a start whose response was lost; the same
          key returns the same job instead of starting a second one.
        explode: false
        in: header
        name: Idempotency-Key
        required: true
        schema:
          type: string
        style: simple
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/startBrandDiscoveryJob_request"
        required: false
      responses:
        "202":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/AcceptedJobResource"
          description: The discovery job was accepted
        "400":
          description: "The body is not a JSON object, names an unknown field, or\
            \ fails validation; or the Idempotency-Key header is missing"
        "401":
          description: Missing or invalid API key
        "403":
          description: The key lacks the write capability
        "404":
          description: No organization or project the caller can access under these
            ids
        "409":
          description: "The project is archived, or the organization is archived,\
            \ or the Idempotency-Key was already used for a different request, or\
            \ a previous attempt with it ended without a known outcome"
        "429":
          description: "The organization exceeded ten brand discovery starts per minute,\
            \ or the key exceeded its request budget"
      summary: Start a brand discovery job for a project
      tags:
      - Discovery
      x-content-type: application/json
      x-accepts:
      - application/json
  /api/v1/organizations/{organizationId}/brand-name-suggestions:
    post:
      description: "Minimum role: manager. Starts a background, web-search-grounded\
        \ job that suggests alias spellings for one entity - a brand, a project or\
        \ a competitor - described entirely by the body, and answers 202 with the\
        \ job to poll; it never returns suggestions inline. The entity need not exist:\
        \ nothing is looked up, nothing is attached and nothing is saved, so the suggestions\
        \ are the caller's to apply. Names sent in `enteredBrandNames` are excluded\
        \ from the result. The organization must be active; no subscription is required,\
        \ matching the application behaviour this mirrors. Ten starts per minute per\
        \ organization, counted across every key of every member; a retry carrying\
        \ an Idempotency-Key already answered is served from the record and does not\
        \ count."
      operationId: startBrandNameSuggestionJob
      parameters:
      - explode: false
        in: path
        name: organizationId
        required: true
        schema:
          format: uuid
          type: string
        style: simple
      - description: Repeat it to retry a start whose response was lost; the same
          key returns the same job instead of starting a second one.
        explode: false
        in: header
        name: Idempotency-Key
        required: true
        schema:
          type: string
        style: simple
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/startBrandNameSuggestionJob_request"
        required: true
      responses:
        "202":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/AcceptedJobResource"
          description: The suggestion job was accepted
        "400":
          description: "The body is not a JSON object, names an unknown field, or\
            \ fails validation; or the Idempotency-Key header is missing"
        "401":
          description: Missing or invalid API key
        "403":
          description: The key lacks the write capability
        "404":
          description: No organization the caller can access under this id
        "409":
          description: "The organization is archived, or the Idempotency-Key was already\
            \ used for a different request, or a previous attempt with it ended without\
            \ a known outcome"
        "429":
          description: "The organization exceeded ten suggestion starts per minute,\
            \ or the key exceeded its request budget"
      summary: Start a brand-name alias suggestion job
      tags:
      - Discovery
      x-content-type: application/json
      x-accepts:
      - application/json
  /api/v1/organizations/{organizationId}/projects/{projectId}/discovery/keywords:
    post:
      description: "Minimum role: manager. Starts background keyword discovery from\
        \ free-text seed input and answers 202 with the job to poll; it never returns\
        \ keywords inline. The job SUGGESTS keywords - it creates no tracked queries\
        \ and changes nothing in the project, so a completed job is a list to choose\
        \ from, not work that has been applied. Queries the project already tracks\
        \ are excluded from the suggestions automatically; `excludeQueries` adds more\
        \ on top for this run only. The organization must be active and hold an entitled\
        \ subscription, because the run spends provider budget. Ten starts per minute\
        \ per organization, counted across every key of every member; a retry carrying\
        \ an Idempotency-Key already answered is served from the record and does not\
        \ count. Run tuning is not exposed: the discovery fan-out uses provider defaults."
      operationId: startKeywordDiscoveryJob
      parameters:
      - explode: false
        in: path
        name: organizationId
        required: true
        schema:
          format: uuid
          type: string
        style: simple
      - description: Must belong to the organization in the path.
        explode: false
        in: path
        name: projectId
        required: true
        schema:
          format: uuid
          type: string
        style: simple
      - description: Repeat it to retry a start whose response was lost; the same
          key returns the same job instead of starting a second one.
        explode: false
        in: header
        name: Idempotency-Key
        required: true
        schema:
          type: string
        style: simple
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/startKeywordDiscoveryJob_request"
        required: true
      responses:
        "202":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/AcceptedJobResource"
          description: The discovery job was accepted
        "400":
          description: "The body is not a JSON object, names an unknown field, or\
            \ fails validation; or the Idempotency-Key header is missing"
        "401":
          description: Missing or invalid API key
        "402":
          description: The organization has no entitled subscription
        "403":
          description: The key lacks the write capability
        "404":
          description: No organization or project the caller can access under these
            ids
        "409":
          description: "The project is archived, or the organization is archived,\
            \ or the Idempotency-Key was already used for a different request, or\
            \ a previous attempt with it ended without a known outcome"
        "429":
          description: "The organization exceeded ten discovery starts per minute,\
            \ or the key exceeded its request budget"
      summary: Start a keyword discovery job for a project
      tags:
      - Discovery
      x-content-type: application/json
      x-accepts:
      - application/json
  /api/v1/organizations/{organizationId}/projects/{projectId}/discovery/prompts:
    post:
      description: "Minimum role: manager. Starts background discovery of natural-language\
        \ prompts - the questions an answer engine gets asked - from free-text seed\
        \ input, and answers 202 with the job to poll; it never returns prompts inline.\
        \ The job SUGGESTS prompts: it creates no tracked queries and changes nothing\
        \ in the project. `country` is required here, unlike keyword discovery, because\
        \ a prompt is a question asked from somewhere and the provider request has\
        \ no default for it. Queries the project already tracks are excluded automatically;\
        \ `excludeQueries` adds more for this run only. The organization must be active\
        \ and hold an entitled subscription, because the run spends provider budget.\
        \ Ten starts per minute per organization, counted across every key of every\
        \ member; a retry carrying an Idempotency-Key already answered is served from\
        \ the record and does not count. Run tuning is not exposed: the discovery\
        \ fan-out uses provider defaults."
      operationId: startPromptDiscoveryJob
      parameters:
      - explode: false
        in: path
        name: organizationId
        required: true
        schema:
          format: uuid
          type: string
        style: simple
      - description: Must belong to the organization in the path.
        explode: false
        in: path
        name: projectId
        required: true
        schema:
          format: uuid
          type: string
        style: simple
      - description: Repeat it to retry a start whose response was lost; the same
          key returns the same job instead of starting a second one.
        explode: false
        in: header
        name: Idempotency-Key
        required: true
        schema:
          type: string
        style: simple
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/startPromptDiscoveryJob_request"
        required: true
      responses:
        "202":
          content:
            application/json:
              examples:
                jobAccepted:
                  description: "A 202 means the work was accepted, never that it succeeded\
                    \ and never that any prompt exists yet. Poll `trackingUrl` - GET\
                    \ /api/v1/organizations/{organizationId}/jobs/{jobId} - until\
                    \ `status` is `completed` or `failed`. Use the URL as given rather\
                    \ than assembling one: it is generated from the job route, so\
                    \ a future change to that path arrives here instead of as a 404.\
                    \ `deduplicated` is false when this call started the job and true\
                    \ when it joined one an equivalent earlier request had already\
                    \ started, in which case `jobId` names that earlier job rather\
                    \ than the one this request invented."
                  summary: The 202 body every operation that starts background work
                    returns
                  value:
                    jobId: 0193c4f3-2b5e-7a91-8c44-6e1d0f7b23ac
                    deduplicated: false
                    trackingUrl: https://api.mencoro.com/api/v1/organizations/0193c4f0-11aa-7b22-9d33-4e5f60718293/jobs/0193c4f3-2b5e-7a91-8c44-6e1d0f7b23ac
              schema:
                $ref: "#/components/schemas/AcceptedJobResource"
          description: The discovery job was accepted
        "400":
          description: "The body is not a JSON object, names an unknown field, or\
            \ fails validation; or the Idempotency-Key header is missing"
        "401":
          description: Missing or invalid API key
        "402":
          description: The organization has no entitled subscription
        "403":
          description: The key lacks the write capability
        "404":
          description: No organization or project the caller can access under these
            ids
        "409":
          description: "The project is archived, or the organization is archived,\
            \ or the Idempotency-Key was already used for a different request, or\
            \ a previous attempt with it ended without a known outcome"
        "429":
          description: "The organization exceeded ten discovery starts per minute,\
            \ or the key exceeded its request budget"
      summary: Start a geo prompt discovery job for a project
      tags:
      - Discovery
      x-content-type: application/json
      x-accepts:
      - application/json
  /api/v1/organizations/{organizationId}/invitations/{invitationId}/cancel:
    post:
      description: "Minimum role: owner. The link already emailed to the invitee stops\
        \ working. Only a pending invitation can be cancelled; one that was accepted,\
        \ rejected or has expired is refused."
      operationId: cancelInvitation
      parameters:
      - explode: false
        in: path
        name: organizationId
        required: true
        schema:
          format: uuid
          type: string
        style: simple
      - explode: false
        in: path
        name: invitationId
        required: true
        schema:
          format: uuid
          type: string
        style: simple
      - explode: false
        in: header
        name: X-Mencoro-Confirmation
        required: true
        schema:
          type: string
        style: simple
      - explode: false
        in: header
        name: Idempotency-Key
        required: true
        schema:
          type: string
        style: simple
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/InvitationResource"
          description: The invitation in its new state
        "401":
          description: Missing or invalid API key
        "403":
          description: The key lacks the organization:manage capability
        "404":
          description: No organization or invitation the caller can access under these
            ids
        "409":
          description: "The confirmation is invalid, expired, already used, or its\
            \ effects changed; or the invitation is not pending"
        "428":
          description: No confirmation was sent; preview the operation first
      summary: Cancel a pending invitation
      tags:
      - Invitations
      x-accepts:
      - application/json
  /api/v1/organizations/{organizationId}/invitations:
    get:
      description: "Minimum role: owner. Returns the invitations issued for this organization,\
        \ in every state. The invitation token is never returned. `status` matches\
        \ the stored state, so an invitation that has passed its `expiresAt` is still\
        \ listed as pending until it is transitioned; compare `expiresAt` to decide.\
        \ Rows are ordered by the chosen sort field, and invitations sharing the same\
        \ value keep no guaranteed relative order across pages.Send `Accept: text/csv`\
        \ to receive the same page as a CSV download instead of JSON: same filters,\
        \ same authorization, same page window and the same maximum of 100 rows —\
        \ it is this page in another format, not a bulk export, so a whole collection\
        \ is still read by paging. The CSV carries no `total`, because a table whose\
        \ every row is a record has nowhere to put one; read it from the JSON representation\
        \ of the same request. Cells beginning with `=`, `+`, `-` or `@` are prefixed\
        \ with an apostrophe so a spreadsheet treats them as text rather than running\
        \ them as formulas."
      operationId: listInvitations
      parameters:
      - explode: false
        in: path
        name: organizationId
        required: true
        schema:
          format: uuid
          type: string
        style: simple
      - explode: true
        in: query
        name: limit
        required: false
        schema:
          default: 20
          maximum: 100
          minimum: 1
          type: integer
        style: form
      - explode: true
        in: query
        name: offset
        required: false
        schema:
          default: 0
          minimum: 0
          type: integer
        style: form
      - description: Matches part of the invited email address.
        explode: true
        in: query
        name: search
        required: false
        schema:
          maxLength: 100
          type: string
        style: form
      - description: Absent means every state.
        explode: true
        in: query
        name: status
        required: false
        schema:
          enum:
          - pending
          - accepted
          - rejected
          - expired
          - cancelled
          type: string
        style: form
      - explode: true
        in: query
        name: sortBy
        required: false
        schema:
          default: createdAt
          enum:
          - createdAt
          - expiresAt
          type: string
        style: form
      - explode: true
        in: query
        name: sortOrder
        required: false
        schema:
          default: desc
          enum:
          - asc
          - desc
          type: string
        style: form
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/listInvitations_200_response"
            text/csv:
              example: |
                id,name
                b3f1...,Acme
              schema:
                type: string
          description: The organization's invitations
        "400":
          description: A parameter was rejected; the details name the field
        "401":
          description: Missing or invalid API key
        "403":
          description: The key lacks the read capability
        "404":
          description: No organization the caller owns under this id
      summary: List an organization's invitations
      tags:
      - Invitations
      x-accepts:
      - application/json
      - text/csv
    post:
      description: "Minimum role: owner. Sends an invitation email that expires after\
        \ 14 days. If the address already belongs to an active member the call succeeds\
        \ and creates nothing - answered as 200 with created=false rather than 201,\
        \ so a client can tell the two apart. Preview it first; the preview says which\
        \ of the two will happen."
      operationId: createInvitation
      parameters:
      - explode: false
        in: path
        name: organizationId
        required: true
        schema:
          format: uuid
          type: string
        style: simple
      - explode: false
        in: header
        name: X-Mencoro-Confirmation
        required: true
        schema:
          type: string
        style: simple
      - explode: false
        in: header
        name: Idempotency-Key
        required: true
        schema:
          type: string
        style: simple
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/createInvitation_request"
        required: true
      responses:
        "201":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/createInvitation_201_response"
          description: "The invitation that was created. `created` is true; branch\
            \ on it rather than on the status code, so one code path handles both\
            \ answers."
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/createInvitation_200_response"
          description: "Nothing was created because the address already belongs to\
            \ an active member. Not an error: the end state the caller asked for already\
            \ holds."
        "401":
          description: Missing or invalid API key
        "403":
          description: The key lacks the organization:manage capability
        "404":
          description: No organization the caller can access under this id
        "409":
          description: "The confirmation is invalid, expired, already used, or its\
            \ effects changed; or a pending invitation already exists for this address"
        "428":
          description: No confirmation was sent; preview the operation first
      summary: Invite somebody to an organization
      tags:
      - Invitations
      x-content-type: application/json
      x-accepts:
      - application/json
  /api/v1/organizations/{organizationId}/jobs/{jobId}:
    get:
      description: "Minimum role: manager. The status of one asynchronous job started\
        \ inside this organization — geo prompt discovery, keyword discovery, query\
        \ clustering, brand discovery or brand-name suggestion — and, once it has\
        \ completed, its result. Poll it until `status` is terminal: `completed` or\
        \ `failed`. A null `result` means the result is not known, either because\
        \ the job is still in flight or because it failed; it never means the job\
        \ produced an empty result. A failed job carries no reason: the stored one\
        \ is an internal exception message, not a published field. A job started by\
        \ another organization, a job of a type this API does not publish, and a job\
        \ id that does not exist all answer 404 alike, so the API never confirms that\
        \ an inaccessible job exists."
      operationId: getAsyncJob
      parameters:
      - explode: false
        in: path
        name: organizationId
        required: true
        schema:
          format: uuid
          type: string
        style: simple
      - description: Must be a job started inside the organization in the path.
        explode: false
        in: path
        name: jobId
        required: true
        schema:
          format: uuid
          type: string
        style: simple
      responses:
        "200":
          content:
            application/json:
              examples:
                stillRunning:
                  description: "`result` is null because the result is not known yet.\
                    \ `pending`, `running` and `awaiting_retry` all mean the job is\
                    \ still in flight; keep polling."
                  summary: A poll before the job reached a terminal status
                  value:
                    jobId: 0193c4f3-2b5e-7a91-8c44-6e1d0f7b23ac
                    type: geo_prompts
                    status: running
                    result: null
                completed:
                  description: "The shape of `result` is decided by `type`. A completed\
                    \ `geo_prompts` job carries the suggested prompts, how many were\
                    \ found, the language and country the run resolved, and what the\
                    \ provider inferred from the input. The run-tuning knobs `seedsPerCluster`,\
                    \ `limitPerEndpoint` and `minVolume` are internal and are stripped\
                    \ from `config` before it is published. Nothing was saved: turning\
                    \ a suggestion into a tracked query is a separate call."
                  summary: A completed geo prompt discovery job and its result
                  value:
                    jobId: 0193c4f3-2b5e-7a91-8c44-6e1d0f7b23ac
                    type: geo_prompts
                    status: completed
                    result:
                      prompts:
                      - query: best seo agency madrid
                        volume: 480
                        hasAiOverview: true
                        serpFeatures:
                        - ai_overview
                        wordCount: 4
                        score: 0.92
                        source: ahrefs
                        clusterNames:
                        - agencia seo
                        justification: Consulta comparativa de alta intención en Madrid.
                      totalPrompts: 1
                      config:
                        language: es
                        country: ES
                      inference:
                        detectedLanguage: es
                        targetCountry: ES
                        targetLanguage: es
                failed:
                  description: "A failed job is terminal: stop polling. `result` is\
                    \ null here too, and that is the whole point of the field meaning\
                    \ \"not known\" rather than \"empty\" - a null result never says\
                    \ the job produced nothing. No reason is published: the stored\
                    \ one is an internal exception message, not a reviewed public\
                    \ field. Quote the X-Request-Id of this call when reporting it,\
                    \ and start a new job to retry."
                  summary: "A terminal failed job, with a null result and no reason"
                  value:
                    jobId: 0193c4f3-2b5e-7a91-8c44-6e1d0f7b23ac
                    type: geo_prompts
                    status: failed
                    result: null
              schema:
                $ref: "#/components/schemas/AsyncJobResource"
          description: "The job, and its result once it has completed"
        "401":
          description: Missing or invalid API key
        "403":
          description: The key lacks the read capability
        "404":
          description: No organization or job the caller can access under these ids
      summary: Get an asynchronous job
      tags:
      - Jobs
      x-accepts:
      - application/json
  /api/v1/organizations/{organizationId}/projects/{projectId}/keyword-listings:
    get:
      description: "Minimum role: viewer. One row per distinct keyword text of the\
        \ project — every tracked query asking that text, on any engine in any country,\
        \ collapsed into a single row whose variantIds name the tracked queries behind\
        \ it. Each row carries the metrics of the requested window and a signed trend\
        \ against the window of equal length immediately before it, where POSITIVE\
        \ ALWAYS MEANS BETTER whichever direction the metric itself runs. Positions\
        \ are 1-based and lower is better; rates, positivityIndex and shareOfVoice\
        \ are 0-100 and higher is better; mentionPositionStability is a day-to-day\
        \ spread, so lower is steadier. A null metric means nothing was captured for\
        \ that keyword in the window — it is not a zero. `total` counts the KEYWORDS\
        \ matching the filters, not the rows on this page and not tracked queries;\
        \ `totalVariantCount` counts the tracked queries behind the keywords matching\
        \ every filter EXCEPT `search` — with a text search applied it still counts\
        \ the project's variants, so do not size a force-check budget from it on a\
        \ searched page. NEITHER IS THE PROJECT'S TRACKED-QUERY COUNT: the count operation\
        \ reads the write model, while this listing reads projections refreshed in\
        \ the background from it, so the numbers legitimately differ while those projections\
        \ catch up. A project whose cached tracked-query count has not been refreshed\
        \ yet answers an empty page with total 0 — the same answer as a project with\
        \ no tracked queries at all — so treat an unexpected empty page right after\
        \ creating queries as \"not projected yet\", not as \"no data\". dataDirtySince\
        \ is non-null while a recalculation is pending, meaning the metrics predate\
        \ the latest configuration change. Filters narrow which VARIANTS count towards\
        \ a row, so engines, countries, statuses, checkFrequencies and nPassesValues\
        \ each report what the filters selected rather than everything the keyword\
        \ has. A filter or sort key this endpoint cannot honour is rejected by name,\
        \ never ignored, and a limit above the maximum is rejected rather than quietly\
        \ reduced. This operation answers JSON only: a keyword row carries a nested\
        \ mentionTypeCounts object, so it is not part of the CSV family."
      operationId: listKeywordListings
      parameters:
      - explode: false
        in: path
        name: organizationId
        required: true
        schema:
          format: uuid
          type: string
        style: simple
      - explode: false
        in: path
        name: projectId
        required: true
        schema:
          format: uuid
          type: string
        style: simple
      - description: "Inclusive start of the window, Y-m-d. Must fall inside the data\
          \ retention window."
        explode: true
        in: query
        name: dateFrom
        required: true
        schema:
          format: date
          type: string
        style: form
      - description: "Inclusive end of the window, Y-m-d. The trend compares against\
          \ the equally long window ending the day before dateFrom."
        explode: true
        in: query
        name: dateTo
        required: true
        schema:
          format: date
          type: string
        style: form
      - description: "Repeatable, or comma-separated. Narrows which variants count\
          \ towards each row."
        explode: true
        in: query
        name: engines
        required: false
        schema:
          items:
            enum:
            - chatgpt
            - perplexity
            - google_ai_overview
            - google_ai_mode
            - google_serp
            - google_shopping
            type: string
          type: array
        style: form
      - description: ISO-3166 alpha-2 codes or English names. Must be configured on
          the project.
        explode: true
        in: query
        name: countries
        required: false
        schema:
          items:
            type: string
          type: array
        style: form
      - description: Restrict to keywords with a variant in these clusters. Each must
          belong to the project.
        explode: true
        in: query
        name: queryClusterIds
        required: false
        schema:
          items:
            format: uuid
            type: string
          type: array
        style: form
      - description: "Sent alone, restricts the listing to keywords whose variants\
          \ belong to no cluster; sent with queryClusterIds, adds them to that selection."
        explode: true
        in: query
        name: includeUngroupedQueries
        required: false
        schema:
          default: false
          type: boolean
        style: form
      - description: Restrict to variants with this status. A keyword whose variants
          disagree still reports statusSummary "mixed".
        explode: true
        in: query
        name: status
        required: false
        schema:
          enum:
          - active
          - paused
          type: string
        style: form
      - description: "Repeatable, or comma-separated."
        explode: true
        in: query
        name: checkFrequencies
        required: false
        schema:
          items:
            enum:
            - daily
            - weekly
            - monthly
            type: string
          type: array
        style: form
      - description: "Repeatable, or comma-separated. Restrict to variants configured\
          \ with these pass counts."
        explode: true
        in: query
        name: nPasses
        required: false
        schema:
          items:
            maximum: 10
            minimum: 1
            type: integer
          type: array
        style: form
      - description: Case-insensitive substring match on the keyword text.
        explode: true
        in: query
        name: search
        required: false
        schema:
          maxLength: 100
          type: string
        style: form
      - description: "Named after the field it orders by. Rows with no value for the\
          \ chosen key sort last in either direction. An unknown key is rejected,\
          \ not replaced by the default."
        explode: true
        in: query
        name: sortBy
        required: false
        schema:
          default: keyword
          enum:
          - keyword
          - variantCount
          - lastCheckedAt
          - statusSummary
          - positivityIndex
          - shareOfVoice
          - avgMentionPosition
          - avgLinkPosition
          - mentionPositionStability
          - avgSerpPosition
          - avgShoppingPosition
          type: string
        style: form
      - explode: true
        in: query
        name: sortOrder
        required: false
        schema:
          default: asc
          enum:
          - asc
          - desc
          type: string
        style: form
      - description: "Page size. A larger value is rejected, never silently reduced."
        explode: true
        in: query
        name: limit
        required: false
        schema:
          default: 20
          maximum: 100
          minimum: 1
          type: integer
        style: form
      - description: Number of matching keywords to skip before the page starts.
        explode: true
        in: query
        name: offset
        required: false
        schema:
          default: 0
          minimum: 0
          type: integer
        style: form
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/listKeywordListings_200_response"
          description: "A page of keywords, the totals behind it, and whether the\
            \ metrics are pending recalculation"
        "400":
          description: "A filter, sort key or page bound was rejected; the details\
            \ name the field"
        "401":
          description: Missing or invalid API key
        "403":
          description: The key lacks the read capability
        "404":
          description: No organization or project the caller can access under these
            ids
      summary: List a project's keywords with their windowed metrics
      tags:
      - Analytics
      x-accepts:
      - application/json
  /api/v1/me:
    get:
      description: "Returns the user the API key belongs to, plus the key's capabilities\
        \ and scope. Use it to confirm which credential a call runs under."
      operationId: getMe
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/MeResource"
          description: The authenticated identity
        "403":
          description: The key lacks the read capability
        "401":
          description: Missing or invalid API key
      summary: Get the authenticated identity
      tags:
      - Account
      x-accepts:
      - application/json
  /api/v1/me/stats:
    get:
      description: "Aggregate counts over every organization the key's owner is an\
        \ active member of, narrowed to the key's scope — the same set /api/v1/organizations\
        \ pages through. `organizations.total` is that set's size; `projects.total`\
        \ and `projects.active` count the projects inside it, archived ones included\
        \ in the total and excluded from the active figure. Every value is an exact\
        \ count: zero means zero, and no value here is ever null or unknown. Per-organization\
        \ billing and usage figures are not part of this response; read them from\
        \ the subscription and entitlements operations instead."
      operationId: getMeStats
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/getMeStats_200_response"
          description: Counts across the organizations the key can reach
        "401":
          description: Missing or invalid API key
        "403":
          description: "The key lacks the read capability, or the user it belongs\
            \ to is no longer active"
      summary: Counts across everything the key can reach
      tags:
      - Account
      x-accepts:
      - application/json
  /api/v1/organizations/{organizationId}/members/{memberId}:
    get:
      description: "Minimum role: owner — the same floor the members listing enforces,\
        \ because a caller who can page the roster has already seen this record. Returns\
        \ the facts of one membership: its role, whether it is active or suspended,\
        \ and when it was joined. It does NOT describe the person behind it: no name,\
        \ no email address, no phone number and no profile image, so a membership\
        \ id can never be turned into a contact lookup. The membership is read from\
        \ PostgreSQL, the same row at the same freshness the listing publishes. A\
        \ membership belonging to another organization answers 404, exactly as an\
        \ unknown or malformed id does, so the API never confirms that an inaccessible\
        \ membership exists; the one 403 is a key without the read capability. The\
        \ response carries no organizationId — it is the one in the path — and no\
        \ creation timestamp; joinedAt is the membership fact. No query parameters\
        \ are accepted."
      operationId: getMember
      parameters:
      - explode: false
        in: path
        name: organizationId
        required: true
        schema:
          format: uuid
          type: string
        style: simple
      - description: "The membership id, not the user id. Must belong to the organization\
          \ in the path."
        explode: false
        in: path
        name: memberId
        required: true
        schema:
          format: uuid
          type: string
        style: simple
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/MemberResource"
          description: The membership
        "401":
          description: Missing or invalid API key
        "403":
          description: The key lacks the read capability
        "404":
          description: "No organization the caller owns, or no membership of it, under\
            \ these ids"
      summary: Get one organization membership
      tags:
      - Members
      x-accepts:
      - application/json
    patch:
      description: "Minimum role: owner. The role of a suspended member cannot be\
        \ changed, and the last active owner cannot be demoted. Preview it first:\
        \ the confirmation is bound to the number of active owners, so another owner\
        \ being suspended in between invalidates it rather than stranding the organization."
      operationId: changeMemberRole
      parameters:
      - explode: false
        in: path
        name: organizationId
        required: true
        schema:
          format: uuid
          type: string
        style: simple
      - explode: false
        in: path
        name: memberId
        required: true
        schema:
          format: uuid
          type: string
        style: simple
      - explode: false
        in: header
        name: X-Mencoro-Confirmation
        required: true
        schema:
          type: string
        style: simple
      - explode: false
        in: header
        name: Idempotency-Key
        required: true
        schema:
          type: string
        style: simple
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/changeMemberRole_request"
        required: true
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/MemberResource"
          description: The membership in its new state
        "401":
          description: Missing or invalid API key
        "403":
          description: The key lacks the organization:manage capability
        "404":
          description: No organization or membership the caller can access under these
            ids
        "409":
          description: "The confirmation is invalid, expired, already used, or its\
            \ effects changed; the member is suspended; or this is the last active\
            \ owner"
        "428":
          description: No confirmation was sent; preview the operation first
      summary: Change a member role
      tags:
      - Members
      x-content-type: application/json
      x-accepts:
      - application/json
  /api/v1/organizations/{organizationId}/members:
    get:
      description: "Minimum role: owner. The application exposes this roster twice\
        \ and the two disagree — its member screen shows it to any viewer, while its\
        \ non-BFF endpoint requires an owner — so the published API takes the stricter\
        \ of the two and requires an owner. A membership describes the membership,\
        \ not the person: names and email addresses are never returned here, and there\
        \ is no search parameter, because both would turn the roster into a contact\
        \ export. Omitting \"status\" returns active and suspended memberships alike.\
        \ Sorting by \"role\" is alphabetical on the role name, not by seniority.Send\
        \ `Accept: text/csv` to receive the same page as a CSV download instead of\
        \ JSON: same filters, same authorization, same page window and the same maximum\
        \ of 100 rows — it is this page in another format, not a bulk export, so a\
        \ whole collection is still read by paging. The CSV carries no `total`, because\
        \ a table whose every row is a record has nowhere to put one; read it from\
        \ the JSON representation of the same request. Cells beginning with `=`, `+`,\
        \ `-` or `@` are prefixed with an apostrophe so a spreadsheet treats them\
        \ as text rather than running them as formulas."
      operationId: listMembers
      parameters:
      - explode: false
        in: path
        name: organizationId
        required: true
        schema:
          format: uuid
          type: string
        style: simple
      - explode: true
        in: query
        name: limit
        required: false
        schema:
          default: 20
          maximum: 100
          minimum: 1
          type: integer
        style: form
      - explode: true
        in: query
        name: offset
        required: false
        schema:
          default: 0
          minimum: 0
          type: integer
        style: form
      - description: Absent means both states.
        explode: true
        in: query
        name: status
        required: false
        schema:
          enum:
          - active
          - suspended
          type: string
        style: form
      - explode: true
        in: query
        name: sortBy
        required: false
        schema:
          default: joinedAt
          enum:
          - joinedAt
          - role
          type: string
        style: form
      - explode: true
        in: query
        name: sortOrder
        required: false
        schema:
          default: desc
          enum:
          - asc
          - desc
          type: string
        style: form
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/listMembers_200_response"
            text/csv:
              example: |
                id,name
                b3f1...,Acme
              schema:
                type: string
          description: The organization's memberships
        "400":
          description: "A parameter was rejected; the details name the field. A limit\
            \ above 100 is refused, not clamped"
        "401":
          description: Missing or invalid API key
        "403":
          description: The key lacks the read capability
        "404":
          description: No organization the caller owns under this id
      summary: List an organization's members
      tags:
      - Members
      x-accepts:
      - application/json
      - text/csv
  /api/v1/organizations/{organizationId}/members/{memberId}/reactivate:
    post:
      description: "Minimum role: owner. The member keeps the role they had and regains\
        \ access on their very next request. A member who is already active cannot\
        \ be reactivated."
      operationId: reactivateMember
      parameters:
      - explode: false
        in: path
        name: organizationId
        required: true
        schema:
          format: uuid
          type: string
        style: simple
      - explode: false
        in: path
        name: memberId
        required: true
        schema:
          format: uuid
          type: string
        style: simple
      - explode: false
        in: header
        name: X-Mencoro-Confirmation
        required: true
        schema:
          type: string
        style: simple
      - explode: false
        in: header
        name: Idempotency-Key
        required: true
        schema:
          type: string
        style: simple
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/MemberResource"
          description: The membership in its new state
        "401":
          description: Missing or invalid API key
        "403":
          description: The key lacks the organization:manage capability
        "404":
          description: No organization or membership the caller can access under these
            ids
        "409":
          description: "The confirmation is invalid, expired, already used, or its\
            \ effects changed; or the member is already active"
        "428":
          description: No confirmation was sent; preview the operation first
      summary: Reactivate a suspended member
      tags:
      - Members
      x-accepts:
      - application/json
  /api/v1/organizations/{organizationId}/members/{memberId}/suspend:
    post:
      description: "Minimum role: owner. The member loses access on their very next\
        \ request, including through any API key they own that is scoped to this organization.\
        \ The last active owner cannot be suspended."
      operationId: suspendMember
      parameters:
      - explode: false
        in: path
        name: organizationId
        required: true
        schema:
          format: uuid
          type: string
        style: simple
      - explode: false
        in: path
        name: memberId
        required: true
        schema:
          format: uuid
          type: string
        style: simple
      - explode: false
        in: header
        name: X-Mencoro-Confirmation
        required: true
        schema:
          type: string
        style: simple
      - explode: false
        in: header
        name: Idempotency-Key
        required: true
        schema:
          type: string
        style: simple
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/MemberResource"
          description: The membership in its new state
        "401":
          description: Missing or invalid API key
        "403":
          description: The key lacks the organization:manage capability
        "404":
          description: No organization or membership the caller can access under these
            ids
        "409":
          description: "The confirmation is invalid, expired, already used, or its\
            \ effects changed; the member is already suspended; or this is the last\
            \ active owner"
        "428":
          description: No confirmation was sent; preview the operation first
      summary: Suspend a member
      tags:
      - Members
      x-accepts:
      - application/json
  /api/v1/organizations/{organizationId}/archive:
    post:
      description: "Minimum role: owner. Archiving also archives the active projects\
        \ of the organization, cancels its pending invitations and cancels its subscription\
        \ at the end of the current billing period. Those effects are applied by background\
        \ subscribers, so a 200 means the organization was archived, not that every\
        \ effect has finished. Preview it first to see exactly what will be touched."
      operationId: archiveOrganization
      parameters:
      - explode: false
        in: path
        name: organizationId
        required: true
        schema:
          format: uuid
          type: string
        style: simple
      - description: "The `confirmation.token` returned by POST /api/v1/organization-operation-previews\
          \ for this exact action, organization and body. Single use, valid for five\
          \ minutes, and bound to the effects the preview declared: if the organization\
          \ gained a project or lost an invitation in between, it is refused with\
          \ 409 rather than doing more than was agreed. Without it the call answers\
          \ 428."
        example: mencoro_conf_VmajQznkILZjvFf63JexmT1AKX7iYZjOGSrBZYxlUTk
        explode: false
        in: header
        name: X-Mencoro-Confirmation
        required: true
        schema:
          type: string
        style: simple
      - description: "Repeat it to retry a lost response. The replay is answered from\
          \ the record without claiming the confirmation again, so a retry is never\
          \ refused for presenting a confirmation the first attempt already spent."
        example: 5c1b8e42-0d7f-4a93-8c61-9b2e4d0a7f38
        explode: false
        in: header
        name: Idempotency-Key
        required: true
        schema:
          maxLength: 255
          minLength: 8
          type: string
        style: simple
      responses:
        "200":
          content:
            application/json:
              examples:
                archived:
                  description: "Sent as POST with the preview token in X-Mencoro-Confirmation\
                    \ and an Idempotency-Key, and no body. The organization is re-read\
                    \ after the command rather than echoed, so `status` is what was\
                    \ actually stored. `role` is the caller's own role, resolved live\
                    \ for this request. A 200 means the organization was archived,\
                    \ not that every effect has finished: the projects, the invitations\
                    \ and the subscription are handled by background subscribers,\
                    \ exactly as the preview warned."
                  summary: "Call two of two: the confirmed write, answered with the\
                    \ organization re-read after the command"
                  value:
                    id: 0193c4f0-11aa-7b22-9d33-4e5f60718293
                    name: Acme GmbH
                    description: Brand monitoring for the Acme group.
                    status: archived
                    imageUrl: null
                    contactEmail: ops@acme.example
                    role: owner
                    createdAt: 2026-01-08T07:45:11+00:00
              schema:
                $ref: "#/components/schemas/OrganizationResource"
          description: The organization in its new state
        "401":
          description: Missing or invalid API key
        "403":
          description: The key lacks the organization:manage capability
        "404":
          description: No organization the caller can access under this id
        "409":
          description: "The confirmation is invalid, expired, already used, or its\
            \ declared effects changed; or the status transition is not allowed"
        "428":
          description: No confirmation was sent; preview the operation first
      summary: Archive an organization
      tags:
      - Organizations
      x-accepts:
      - application/json
  /api/v1/organizations:
    get:
      description: "Returns the organizations the key's owner is an active member\
        \ of, narrowed to the key's scope. A key scoped to all organizations also\
        \ sees organizations joined after it was created.Send `Accept: text/csv` to\
        \ receive the same page as a CSV download instead of JSON: same filters, same\
        \ authorization, same page window and the same maximum of 100 rows — it is\
        \ this page in another format, not a bulk export, so a whole collection is\
        \ still read by paging. The CSV carries no `total`, because a table whose\
        \ every row is a record has nowhere to put one; read it from the JSON representation\
        \ of the same request. Cells beginning with `=`, `+`, `-` or `@` are prefixed\
        \ with an apostrophe so a spreadsheet treats them as text rather than running\
        \ them as formulas."
      operationId: listOrganizations
      parameters:
      - explode: true
        in: query
        name: limit
        required: false
        schema:
          default: 20
          maximum: 100
          minimum: 1
          type: integer
        style: form
      - explode: true
        in: query
        name: offset
        required: false
        schema:
          default: 0
          minimum: 0
          type: integer
        style: form
      - explode: true
        in: query
        name: search
        required: false
        schema:
          maxLength: 100
          type: string
        style: form
      - explode: true
        in: query
        name: status
        required: false
        schema:
          enum:
          - active
          - archived
          type: string
        style: form
      - explode: true
        in: query
        name: sortBy
        required: false
        schema:
          default: createdAt
          enum:
          - createdAt
          - name
          type: string
        style: form
      - explode: true
        in: query
        name: sortOrder
        required: false
        schema:
          default: desc
          enum:
          - asc
          - desc
          type: string
        style: form
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/listOrganizations_200_response"
            text/csv:
              example: |
                id,name
                b3f1...,Acme
              schema:
                type: string
          description: Accessible organizations
        "401":
          description: Missing or invalid API key
        "403":
          description: The key lacks the read capability
      summary: List accessible organizations
      tags:
      - Organizations
      x-accepts:
      - application/json
      - text/csv
    post:
      description: "Requires a key scoped to all organizations and the \"organization:manage\"\
        \ capability: a key limited to named organizations cannot widen its own reach\
        \ by creating one. Preview it first and send the confirmation in X-Mencoro-Confirmation\
        \ together with an Idempotency-Key."
      operationId: createOrganization
      parameters:
      - explode: false
        in: header
        name: X-Mencoro-Confirmation
        required: true
        schema:
          type: string
        style: simple
      - explode: false
        in: header
        name: Idempotency-Key
        required: true
        schema:
          type: string
        style: simple
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/createOrganization_request"
        required: true
      responses:
        "201":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/createOrganization_201_response"
          description: "Organization created. Your owner membership follows asynchronously,\
            \ so a read immediately afterwards can answer 404 until it lands."
        "401":
          description: Missing or invalid API key
        "403":
          description: "The key is not scoped to all organizations, or lacks \"organization:manage\""
        "409":
          description: "The confirmation is invalid, expired, already used, or its\
            \ effects changed"
        "428":
          description: No confirmation was sent; preview the operation first
      summary: Create an organization
      tags:
      - Organizations
      x-content-type: application/json
      x-accepts:
      - application/json
  /api/v1/organization-operation-previews:
    post:
      description: "Organization, member and invitation changes need two calls. This\
        \ one reports the concrete changes, the side effects, the warnings and the\
        \ conditions, and returns a confirmation valid for five minutes. Send it back\
        \ in the X-Mencoro-Confirmation header, together with an Idempotency-Key,\
        \ on the call that performs the operation. Previewing has no effects of any\
        \ kind."
      operationId: previewOrganizationOperation
      requestBody:
        content:
          application/json:
            examples:
              previewAnArchive:
                description: "Send the body you intend to send to the operation itself\
                  \ as `payload`, so the confirmation is bound to it; an update would\
                  \ carry the fields it means to change. It is omitted here because\
                  \ archiving accepts no body - sending `{}` is equivalent. `resourceId`\
                  \ is omitted too, because archiving acts on no member or invitation,\
                  \ and sending one for an action that names none is a validation\
                  \ error rather than an ignored field."
                summary: "Call one: ask what archiving this organization would do"
                value:
                  action: archiveOrganization
                  organizationId: 0193c4f0-11aa-7b22-9d33-4e5f60718293
            schema:
              $ref: "#/components/schemas/previewOrganizationOperation_request"
        required: true
      responses:
        "200":
          content:
            application/json:
              examples:
                archivePreview:
                  description: "Read from live state, so it enumerates what the background\
                    \ subscribers will actually touch rather than what the command\
                    \ name suggests. `changes` is the operation itself, `sideEffects`\
                    \ what else it causes with the records it will touch in `targets`,\
                    \ `warnings` how the system behaves, and `conditions` what must\
                    \ hold for the call to be allowed. Send `confirmation.token` in\
                    \ the `confirmation.header` header of the operation, together\
                    \ with an Idempotency-Key, within `confirmation.expiresInSeconds`.\
                    \ The confirmation is single use and is bound to this key, this\
                    \ user, this organization, this action, this payload AND these\
                    \ effects: if a project is added or an invitation accepted in\
                    \ between, the effects no longer match and the operation is refused\
                    \ rather than doing more than was agreed. Previewing has no effects\
                    \ of any kind."
                  summary: "Call one of two: the declared effects, and the confirmation\
                    \ that authorises exactly them"
                  value:
                    action: archiveOrganization
                    organization:
                      id: 0193c4f0-11aa-7b22-9d33-4e5f60718293
                      name: Acme GmbH
                    resourceId: null
                    changes:
                    - code: organization_will_be_archived
                      summary: '"Acme GmbH" will be archived.'
                      targets: []
                    sideEffects:
                    - code: projects_will_be_archived
                      summary: 2 active project(s) will be archived.
                      targets:
                      - 0193c4f1-8e2a-7c31-9d40-5b6f1a2c3d4e
                      - 0193c4f1-9a77-7f08-8e12-c30d9b5a7e61
                    - code: pending_invitations_will_be_cancelled
                      summary: 1 pending invitation(s) will be cancelled.
                      targets:
                      - 0193c4f2-2d15-7b60-9f81-a4c7e0d39215
                    - code: subscription_will_be_cancelled_at_period_end
                      summary: The subscription will be cancelled at the end of the
                        current billing period.
                      targets: []
                    warnings:
                    - code: effects_are_asynchronous
                      summary: "The project, invitation and subscription effects are\
                        \ applied by background subscribers. A successful response\
                        \ means the organization was archived, not that every effect\
                        \ has finished."
                      targets: []
                    conditions:
                    - code: requires_owner
                      summary: You must be an owner of this organization.
                      targets: []
                    - code: requires_active_organization
                      summary: The organization must currently be active.
                      targets: []
                    actor:
                      userId: 0193c4ef-6a01-7c8d-8b44-19e2f3a5c607
                      email: ops@acme.example
                      apiKeyId: 0193c4ef-8f3b-7d22-9a15-63b0c8d41e57
                    confirmation:
                      token: mencoro_conf_VmajQznkILZjvFf63JexmT1AKX7iYZjOGSrBZYxlUTk
                      expiresAt: 2026-03-04T11:27:33+00:00
                      expiresInSeconds: 300
                      header: X-Mencoro-Confirmation
              schema:
                $ref: "#/components/schemas/previewOrganizationOperation_200_response"
          description: "What the operation would do, plus a confirmation"
        "401":
          description: Missing or invalid API key
        "400":
          description: "Unknown action, or a payload the operation does not accept"
        "403":
          description: "The key lacks a required capability, or its scope is too narrow"
        "404":
          description: No organization the caller can access under this id
      summary: Preview an organization operation and obtain a confirmation
      tags:
      - Organization operations
      x-content-type: application/json
      x-accepts:
      - application/json
  /api/v1/organizations/{organizationId}/restore:
    post:
      description: "Minimum role: owner. Restoring reinstates the projects that were\
        \ archived as part of archiving this organization - projects archived on their\
        \ own stay archived - and aborts a pending subscription cancellation. Invitations\
        \ cancelled by the archive are not reinstated. Preview it first to see which\
        \ effects are reversible."
      operationId: restoreOrganization
      parameters:
      - explode: false
        in: path
        name: organizationId
        required: true
        schema:
          format: uuid
          type: string
        style: simple
      - explode: false
        in: header
        name: X-Mencoro-Confirmation
        required: true
        schema:
          type: string
        style: simple
      - explode: false
        in: header
        name: Idempotency-Key
        required: true
        schema:
          type: string
        style: simple
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/OrganizationResource"
          description: The organization in its new state
        "401":
          description: Missing or invalid API key
        "403":
          description: The key lacks the organization:manage capability
        "404":
          description: No organization the caller can access under this id
        "409":
          description: "The confirmation is invalid, expired, already used, or its\
            \ declared effects changed; or the status transition is not allowed"
        "428":
          description: No confirmation was sent; preview the operation first
      summary: Restore an archived organization
      tags:
      - Organizations
      x-accepts:
      - application/json
  /api/v1/organizations/{organizationId}:
    get:
      description: "Minimum role: viewer. An organization outside the key's scope,\
        \ or one the caller is not an active member of, answers 404 - the API never\
        \ confirms that an inaccessible organization exists."
      operationId: getOrganization
      parameters:
      - explode: false
        in: path
        name: organizationId
        required: true
        schema:
          format: uuid
          type: string
        style: simple
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/OrganizationResource"
          description: The organization
        "401":
          description: Missing or invalid API key
        "403":
          description: The key lacks the read capability
        "404":
          description: No organization the caller can access under this id
      summary: Get an organization
      tags:
      - Organizations
      x-accepts:
      - application/json
    patch:
      description: "Minimum role: owner. A partial update: omit a field to leave it\
        \ alone, send it as null to clear it. Preview it first; the confirmation is\
        \ bound to the current values, so an edit made by somebody else in between\
        \ invalidates it rather than being silently overwritten. The organization\
        \ image is deliberately NOT writable here, although the Mencoro app accepts\
        \ one in the equivalent call: a published JSON API is the wrong place to carry\
        \ a 10MB base64 blob in a body that also has to be fingerprinted for idempotency\
        \ and digested for the confirmation. The image stays readable as `imageUrl`;\
        \ changing it is done in the app."
      operationId: updateOrganization
      parameters:
      - explode: false
        in: path
        name: organizationId
        required: true
        schema:
          format: uuid
          type: string
        style: simple
      - explode: false
        in: header
        name: X-Mencoro-Confirmation
        required: true
        schema:
          type: string
        style: simple
      - explode: false
        in: header
        name: Idempotency-Key
        required: true
        schema:
          type: string
        style: simple
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/updateOrganization_request"
        required: true
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/OrganizationResource"
          description: The updated organization
        "401":
          description: Missing or invalid API key
        "403":
          description: The key lacks the organization:manage capability
        "404":
          description: No organization the caller can access under this id
        "409":
          description: "The confirmation is invalid, expired, already used, or the\
            \ organization changed since the preview"
        "428":
          description: No confirmation was sent; preview the operation first
      summary: Update an organization profile
      tags:
      - Organizations
      x-content-type: application/json
      x-accepts:
      - application/json
  /api/v1/organizations/{organizationId}/membership-stats:
    get:
      description: "Minimum role: owner. Pre-computed counts of the organization's\
        \ members, projects and outstanding invitations, read from a materialized\
        \ view that is refreshed periodically — `computedAt` says when the snapshot\
        \ was taken, so a member added since the last refresh is not counted yet.\
        \ Every count is always an integer and a zero means zero; an organization\
        \ whose row has not been computed yet answers 404 with code `organization_membership_stats_not_found`,\
        \ never a body of zeros, so \"none\" and \"not known yet\" are never confused.\
        \ Counts cover the whole organization, active and inactive alike: `totalMembersCount`\
        \ includes suspended members, and `totalProjectsCount` includes archived projects.\
        \ `pendingInvitationsCount` counts only invitations that are still pending\
        \ and not yet expired."
      operationId: getMembershipStats
      parameters:
      - explode: false
        in: path
        name: organizationId
        required: true
        schema:
          format: uuid
          type: string
        style: simple
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/getMembershipStats_200_response"
          description: The organization's membership snapshot
        "401":
          description: Missing or invalid API key
        "403":
          description: The key lacks the read capability
        "404":
          description: "No organization the caller owns under this id (code `organization_not_found`),\
            \ or the snapshot has not been computed for it yet (code `organization_membership_stats_not_found`)"
      summary: "Membership, project and invitation counts for an organization"
      tags:
      - Organizations
      x-accepts:
      - application/json
  /api/v1/organizations/{organizationId}/stats:
    get:
      description: "Minimum role: viewer. Active members, projects (total and active)\
        \ and pending invitations, in one call. Every value is a count and is always\
        \ known: 0 means the organization really has none of that thing, and no field\
        \ is ever null. `members.total` counts ACTIVE memberships only, so a suspended\
        \ member is not included; `projects.total` counts every project including\
        \ archived ones, and `projects.active` the non-archived subset. This is a\
        \ current-state snapshot and takes no date window."
      operationId: getOrganizationStats
      parameters:
      - explode: false
        in: path
        name: organizationId
        required: true
        schema:
          format: uuid
          type: string
        style: simple
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/getOrganizationStats_200_response"
          description: The organization counts
        "401":
          description: Missing or invalid API key
        "403":
          description: The key lacks the read capability
        "404":
          description: No organization the caller can access under this id
      summary: Headline counts for an organization
      tags:
      - Organizations
      x-accepts:
      - application/json
  /api/v1/organizations/{organizationId}/projects/{projectId}/archive:
    post:
      description: "Minimum role: manager, in an active organization. Requires the\
        \ \"write\" capability. Archiving stops a project from being modified: its\
        \ name, brand profile and competitors are refused with 409 until it is restored.\
        \ What is NOT done: nothing is deleted. Tracked queries, captured responses,\
        \ mentions and every metric already collected stay exactly as they are, and\
        \ restoreProject brings the project back with all of it. Archiving is recorded\
        \ as done by the caller, not by an organization cascade, so restoring the\
        \ organization later will not restore this project — restore it explicitly.\
        \ This endpoint takes no body, and one carrying fields is refused. Archiving\
        \ an already archived project answers 409, unless the call is a retry carrying\
        \ the key that archived it. Send an Idempotency-Key; a retry with the same\
        \ key returns the recorded answer."
      operationId: archiveProject
      parameters:
      - explode: false
        in: path
        name: organizationId
        required: true
        schema:
          format: uuid
          type: string
        style: simple
      - explode: false
        in: path
        name: projectId
        required: true
        schema:
          format: uuid
          type: string
        style: simple
      - explode: false
        in: header
        name: Idempotency-Key
        required: true
        schema:
          maxLength: 255
          minLength: 8
          type: string
        style: simple
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ProjectDetailResource"
          description: The project in its new state
        "400":
          description: "A body was sent, or the Idempotency-Key was rejected"
        "401":
          description: Missing or invalid API key
        "403":
          description: The key lacks the write capability
        "404":
          description: No organization or project the caller can access under these
            ids
        "409":
          description: "The project is already archived, the organization is archived,\
            \ the idempotency key was already used for a different request, or a previous\
            \ attempt with it ended without a known outcome"
      summary: Archive a project
      tags:
      - Projects
      x-accepts:
      - application/json
  /api/v1/organizations/{organizationId}/projects/{projectId}/competitors:
    get:
      description: "Minimum role: viewer. The competitors configured on the project,\
        \ one page at a time, with the website domains and brand names each one is\
        \ matched against. `total` counts every competitor of the project, not the\
        \ size of this page, so a project with more than `limit` competitors needs\
        \ `offset` to read them all. Ordering is by id, which for a UUID v7 is roughly\
        \ creation order, and `sortOrder` chooses its direction; there is no other\
        \ sort key and no text search, and sending `sortBy` or `search` is rejected\
        \ rather than ignored. A project whose brand monitoring profile has not been\
        \ created yet answers 200 with an empty collection, which means \"nothing\
        \ configured yet\" rather than \"no competitors found\". Internal fields the\
        \ pipeline writes — the auto-generated brand description used by the mention\
        \ classifier, and the internal brand monitoring profile id — are not part\
        \ of this contract.Send `Accept: text/csv` to receive the same page as a CSV\
        \ download instead of JSON: same filters, same authorization, same page window\
        \ and the same maximum of 100 rows — it is this page in another format, not\
        \ a bulk export, so a whole collection is still read by paging. The CSV carries\
        \ no `total`, because a table whose every row is a record has nowhere to put\
        \ one; read it from the JSON representation of the same request. A list-valued\
        \ field is joined into one cell with `; ` as a display projection — parse\
        \ the JSON if you need the structure. Cells beginning with `=`, `+`, `-` or\
        \ `@` are prefixed with an apostrophe so a spreadsheet treats them as text\
        \ rather than running them as formulas."
      operationId: listCompetitors
      parameters:
      - explode: false
        in: path
        name: organizationId
        required: true
        schema:
          format: uuid
          type: string
        style: simple
      - explode: false
        in: path
        name: projectId
        required: true
        schema:
          format: uuid
          type: string
        style: simple
      - description: "Page size. A larger value is rejected, never silently reduced."
        explode: true
        in: query
        name: limit
        required: false
        schema:
          default: 20
          maximum: 100
          minimum: 1
          type: integer
        style: form
      - description: Number of competitors to skip before the page starts.
        explode: true
        in: query
        name: offset
        required: false
        schema:
          default: 0
          minimum: 0
          type: integer
        style: form
      - description: "Direction of the id ordering. An unknown value is rejected,\
          \ not replaced by the default."
        explode: true
        in: query
        name: sortOrder
        required: false
        schema:
          default: asc
          enum:
          - asc
          - desc
          type: string
        style: form
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/listCompetitors_200_response"
            text/csv:
              example: |
                id,name
                b3f1...,Acme
              schema:
                type: string
          description: The project's competitors
        "401":
          description: Missing or invalid API key
        "403":
          description: The key lacks the read capability
        "404":
          description: No organization or project the caller can access under these
            ids
      summary: List the competitors tracked by a project
      tags:
      - Projects
      x-accepts:
      - application/json
      - text/csv
    post:
      description: "Minimum role: manager, on an active organization and a project\
        \ that is not archived. Creates one competitor with the domains and brand\
        \ names its mentions are matched against. Both lists are required and neither\
        \ may be empty: a competitor that matches on nothing would never be found\
        \ in an answer. A domain may be sent as a full URL or as a bare host; a URL\
        \ is reduced to its host and a leading \"www.\" is dropped. Duplicates are\
        \ collapsed. The id is assigned by the server and cannot be chosen, and neither\
        \ the auto-generated brand description nor the internal brand monitoring profile\
        \ id can be set — sending either is refused as an unknown field. Adding a\
        \ competitor does NOT re-match the answers already captured: it applies to\
        \ checks from here on. A project whose brand monitoring profile has not been\
        \ created yet answers 404. An Idempotency-Key header is required."
      operationId: createCompetitor
      parameters:
      - explode: false
        in: path
        name: organizationId
        required: true
        schema:
          format: uuid
          type: string
        style: simple
      - description: Must belong to the organization in the path.
        explode: false
        in: path
        name: projectId
        required: true
        schema:
          format: uuid
          type: string
        style: simple
      - description: Unique per attempt. Without it a lost response cannot be retried
          without risking a second competitor.
        explode: false
        in: header
        name: Idempotency-Key
        required: true
        schema:
          type: string
        style: simple
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/createCompetitor_request"
        required: true
      responses:
        "201":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/CompetitorResource"
          description: The competitor as stored
        "400":
          description: "The body failed validation, or the Idempotency-Key header\
            \ is missing"
        "401":
          description: Missing or invalid API key
        "403":
          description: The key lacks the write capability
        "404":
          description: "No organization or project the caller can access under these\
            \ ids, or the project has no brand monitoring profile"
        "409":
          description: "The organization is archived, the project is archived, or\
            \ the idempotency key was reused for a different body, a concurrent request\
            \ with the same key is still running, or a previous attempt with it ended\
            \ without a known outcome"
      summary: Add a competitor to a project
      tags:
      - Projects
      x-content-type: application/json
      x-accepts:
      - application/json
  /api/v1/organizations/{organizationId}/projects:
    get:
      description: "Minimum role: viewer. Metrics come from the same read model the\
        \ application uses, so the figures match what the product shows. A null metric\
        \ means \"not known yet\", never zero.Send `Accept: text/csv` to receive the\
        \ same page as a CSV download instead of JSON: same filters, same authorization,\
        \ same page window and the same maximum of 100 rows — it is this page in another\
        \ format, not a bulk export, so a whole collection is still read by paging.\
        \ The CSV carries no `total`, because a table whose every row is a record\
        \ has nowhere to put one; read it from the JSON representation of the same\
        \ request. A list-valued field is joined into one cell with `; ` as a display\
        \ projection — parse the JSON if you need the structure. Cells beginning with\
        \ `=`, `+`, `-` or `@` are prefixed with an apostrophe so a spreadsheet treats\
        \ them as text rather than running them as formulas."
      operationId: listProjects
      parameters:
      - explode: false
        in: path
        name: organizationId
        required: true
        schema:
          format: uuid
          type: string
        style: simple
      - description: "Page size. A larger value is rejected, never silently reduced."
        example: 2
        explode: true
        in: query
        name: limit
        required: false
        schema:
          default: 20
          maximum: 100
          minimum: 1
          type: integer
        style: form
      - description: Rows to skip before the page starts. Page by advancing it in
          steps of `limit` until it reaches `total`.
        example: 4
        explode: true
        in: query
        name: offset
        required: false
        schema:
          default: 0
          minimum: 0
          type: integer
        style: form
      - explode: true
        in: query
        name: search
        required: false
        schema:
          maxLength: 100
          type: string
        style: form
      - explode: true
        in: query
        name: status
        required: false
        schema:
          enum:
          - active
          - archived
          type: string
        style: form
      - explode: true
        in: query
        name: sortBy
        required: false
        schema:
          default: createdAt
          enum:
          - createdAt
          - name
          - trackedQueryCount
          - avgSerpPosition
          - avgShoppingPosition
          - avgMentionPosition
          - avgLinkPosition
          - mentionRate
          - serpRate
          - shoppingRate
          - positivityIndex
          - shareOfVoice
          - serpPositionStability
          - shoppingPositionStability
          - lastRankDetectedAt
          type: string
        style: form
      - explode: true
        in: query
        name: sortOrder
        required: false
        schema:
          default: desc
          enum:
          - asc
          - desc
          type: string
        style: form
      responses:
        "200":
          content:
            application/json:
              examples:
                pageOfProjects:
                  description: "The canonical pagination shape of this API: `items`\
                    \ carries at most `limit` rows and `total` counts every row matching\
                    \ the filters, not the page. Here 2 of 57. Advance `offset` by\
                    \ `limit` until it reaches `total` - the next page is ?limit=2&offset=6.\
                    \ `limit` is capped at 100 and a larger value is rejected rather\
                    \ than reduced, so a whole collection is read by paging and never\
                    \ in one call. The second project has completed no checks: every\
                    \ metric is null, which means \"not known yet\" and never zero."
                  summary: "One page of a larger collection, returned by ?limit=2&offset=4"
                  value:
                    items:
                    - id: 0193c4f1-8e2a-7c31-9d40-5b6f1a2c3d4e
                      name: Acme Cloud
                      status: active
                      createdAt: 2026-02-11T09:14:22+00:00
                      trackedQueryCount: 128
                      avgSerpPosition: 4.7
                      avgShoppingPosition: 11.2
                      avgMentionPosition: 2.4
                      avgLinkPosition: 3.1
                      mentionRate: 62
                      serpRate: 48
                      shoppingRate: 17
                      positivityIndex: 34
                      shareOfVoice: 21.8
                      serpPositionStability: 0.82
                      shoppingPositionStability: 0.61
                      lastRankDetectedAt: 2026-03-04T02:11:07+00:00
                    - id: 0193c4f1-9a77-7f08-8e12-c30d9b5a7e61
                      name: Acme Retail
                      status: active
                      createdAt: 2026-02-19T16:02:58+00:00
                      trackedQueryCount: 0
                      avgSerpPosition: null
                      avgShoppingPosition: null
                      avgMentionPosition: null
                      avgLinkPosition: null
                      mentionRate: null
                      serpRate: null
                      shoppingRate: null
                      positivityIndex: null
                      shareOfVoice: null
                      serpPositionStability: null
                      shoppingPositionStability: null
                      lastRankDetectedAt: null
                    total: 57
              schema:
                $ref: "#/components/schemas/listProjects_200_response"
            text/csv:
              example: |
                id,name
                b3f1...,Acme
              schema:
                type: string
          description: The organization's projects
        "401":
          description: Missing or invalid API key
        "403":
          description: The key lacks the read capability
        "404":
          description: No organization the caller can access under this id
      summary: List an organization's projects
      tags:
      - Projects
      x-accepts:
      - application/json
      - text/csv
    post:
      description: "Minimum role: manager, in an active organization. Requires the\
        \ \"write\" capability. Creates the project, the brand monitoring profile\
        \ holding the domains and brand names to watch, and one competitor per entry\
        \ of `competitors`, in a single call. The project id is minted by the server;\
        \ a caller-supplied id is rejected as an unknown field. What is NOT done:\
        \ no tracked queries are created, no check is run and no scraping is scheduled\
        \ — a new project has nothing collected against it until tracked queries are\
        \ added. Each website entry may be a full URL or a bare domain: a URL is reduced\
        \ to its host with any leading \"www.\" removed, so \"https://www.acme.com/pricing\"\
        \ is stored as \"acme.com\". Duplicate domains and duplicate brand names are\
        \ collapsed, exactly as the stored value objects do. Send an Idempotency-Key:\
        \ a retry with the same key and the same body returns this same project instead\
        \ of creating a second one."
      operationId: createProject
      parameters:
      - explode: false
        in: path
        name: organizationId
        required: true
        schema:
          format: uuid
          type: string
        style: simple
      - explode: false
        in: header
        name: Idempotency-Key
        required: true
        schema:
          maxLength: 255
          minLength: 8
          type: string
        style: simple
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/createProject_request"
        required: true
      responses:
        "201":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ProjectDetailResource"
          description: The created project and its brand monitoring configuration
        "400":
          description: The body or the Idempotency-Key was rejected; `details` names
            every field at fault
        "401":
          description: Missing or invalid API key
        "403":
          description: The key lacks the write capability
        "404":
          description: No organization the caller can access under this id
        "409":
          description: "The organization is archived, the idempotency key was already\
            \ used for a different request, or a previous attempt with it ended without\
            \ a known outcome"
      summary: Create a project and the brand monitoring profile its checks run against
      tags:
      - Projects
      x-content-type: application/json
      x-accepts:
      - application/json
  /api/v1/organizations/{organizationId}/projects/{projectId}/competitors/{competitorId}:
    delete:
      description: "Minimum role: manager, on an active organization and a project\
        \ that is not archived. Removes the competitor and, asynchronously, every\
        \ stored mention, search result and shopping result attributed to it, in AI\
        \ answers and search captures already taken. This is permanent and it changes\
        \ historical analytics: share of voice and competitor co-occurrence recomputed\
        \ after the cascade will not include it. A 200 means the competitor is gone;\
        \ the cascade runs on the event bus and finishes shortly afterwards. The response\
        \ body is the competitor as it was immediately before removal, because it\
        \ can no longer be read back. The request takes no body, and sending one is\
        \ refused. A competitor belonging to another project answers 404, the same\
        \ answer an unknown id gets. An Idempotency-Key header is required."
      operationId: deleteCompetitor
      parameters:
      - explode: false
        in: path
        name: organizationId
        required: true
        schema:
          format: uuid
          type: string
        style: simple
      - description: Must belong to the organization in the path.
        explode: false
        in: path
        name: projectId
        required: true
        schema:
          format: uuid
          type: string
        style: simple
      - description: Must belong to the project in the path.
        explode: false
        in: path
        name: competitorId
        required: true
        schema:
          format: uuid
          type: string
        style: simple
      - description: Unique per attempt. A retry carrying the same key is answered
          from the record instead of running again.
        explode: false
        in: header
        name: Idempotency-Key
        required: true
        schema:
          type: string
        style: simple
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/CompetitorResource"
          description: The competitor as it was immediately before removal
        "400":
          description: "A body was sent, or the Idempotency-Key header is missing"
        "401":
          description: Missing or invalid API key
        "403":
          description: The key lacks the write capability
        "404":
          description: "No organization, project or competitor the caller can access\
            \ under these ids"
        "409":
          description: "The organization is archived, the project is archived, or\
            \ the idempotency key was reused for a different request, a concurrent\
            \ request with the same key is still running, or a previous attempt with\
            \ it ended without a known outcome"
      summary: Remove a competitor from a project
      tags:
      - Projects
      x-accepts:
      - application/json
    get:
      description: "Minimum role: viewer. Returns a single competitor of the project,\
        \ the same projection the competitor listing returns for each of its rows.\
        \ A competitor belonging to another project answers 404, the same answer an\
        \ unknown id and a malformed one get, so the API never confirms that a competitor\
        \ the caller cannot reach exists. A project whose brand monitoring profile\
        \ has not been created yet has no competitors at all and answers 404 for any\
        \ competitor id."
      operationId: getCompetitor
      parameters:
      - explode: false
        in: path
        name: organizationId
        required: true
        schema:
          format: uuid
          type: string
        style: simple
      - description: Must belong to the organization in the path.
        explode: false
        in: path
        name: projectId
        required: true
        schema:
          format: uuid
          type: string
        style: simple
      - description: Must belong to the project in the path.
        explode: false
        in: path
        name: competitorId
        required: true
        schema:
          format: uuid
          type: string
        style: simple
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/CompetitorResource"
          description: The competitor
        "401":
          description: Missing or invalid API key
        "403":
          description: The key lacks the read capability
        "404":
          description: "No organization, project or competitor the caller can access\
            \ under these ids"
      summary: Get one of a project's competitors
      tags:
      - Projects
      x-accepts:
      - application/json
    put:
      description: "Minimum role: manager, on an active organization and a project\
        \ that is not archived. Replaces the competitor's name, domains and brand\
        \ names: the lists sent become the lists stored, so a term left out is removed.\
        \ Both lists are required and neither may be empty — a competitor that matches\
        \ on nothing would never be found in an answer. A domain may be sent as a\
        \ full URL or as a bare host; a URL is reduced to its host and a leading \"\
        www.\" is dropped. Duplicates are collapsed. The auto-generated brand description\
        \ cannot be set, and sending it is refused as an unknown field. Changing the\
        \ matching rules does NOT re-match the answers already captured: it applies\
        \ to checks from here on. A competitor belonging to another project answers\
        \ 404, the same answer an unknown id gets. An Idempotency-Key header is required."
      operationId: updateCompetitor
      parameters:
      - explode: false
        in: path
        name: organizationId
        required: true
        schema:
          format: uuid
          type: string
        style: simple
      - description: Must belong to the organization in the path.
        explode: false
        in: path
        name: projectId
        required: true
        schema:
          format: uuid
          type: string
        style: simple
      - description: Must belong to the project in the path.
        explode: false
        in: path
        name: competitorId
        required: true
        schema:
          format: uuid
          type: string
        style: simple
      - description: Unique per attempt. A retry carrying the same key and the same
          body is answered from the record instead of running again.
        explode: false
        in: header
        name: Idempotency-Key
        required: true
        schema:
          type: string
        style: simple
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/createCompetitor_request"
        required: true
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/CompetitorResource"
          description: The competitor as stored
        "400":
          description: "The body failed validation, or the Idempotency-Key header\
            \ is missing"
        "401":
          description: Missing or invalid API key
        "403":
          description: The key lacks the write capability
        "404":
          description: "No organization, project or competitor the caller can access\
            \ under these ids"
        "409":
          description: "The organization is archived, the project is archived, or\
            \ the idempotency key was reused for a different body, a concurrent request\
            \ with the same key is still running, or a previous attempt with it ended\
            \ without a known outcome"
      summary: Replace a competitor
      tags:
      - Projects
      x-content-type: application/json
      x-accepts:
      - application/json
  /api/v1/organizations/{organizationId}/projects/{projectId}/brand-profile:
    get:
      description: "Minimum role: viewer. The brand identity every check of this project\
        \ is matched against: the tracked brand terms, the website domains, and the\
        \ generated description of what the brand does. A null description means it\
        \ has not been generated yet — the generator runs asynchronously after the\
        \ names or domains change — which is not the same as an empty one. An empty\
        \ brandNames or websiteDomains array means the profile exists and names nothing;\
        \ a project whose profile has not been created at all answers 404."
      operationId: getBrandProfile
      parameters:
      - explode: false
        in: path
        name: organizationId
        required: true
        schema:
          format: uuid
          type: string
        style: simple
      - description: Must belong to the organization in the path.
        explode: false
        in: path
        name: projectId
        required: true
        schema:
          format: uuid
          type: string
        style: simple
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrandProfileResource"
          description: The project's brand monitoring profile
        "401":
          description: Missing or invalid API key
        "403":
          description: The key lacks the read capability
        "404":
          description: "No organization or project the caller can access under these\
            \ ids, or the project has no brand profile"
      summary: Get a project's brand monitoring profile
      tags:
      - Projects
      x-accepts:
      - application/json
    put:
      description: "Minimum role: manager, on an active organization and a project\
        \ that is not archived. Replaces the brand terms and website domains every\
        \ check of this project is matched against: the lists sent become the lists\
        \ stored, so a term left out is removed. Both lists are required and neither\
        \ may be empty — a project must keep at least one brand name and one domain\
        \ to match anything. A domain may be sent as a full URL or as a bare host;\
        \ a URL is reduced to its host and a leading \"www.\" is dropped, which is\
        \ the form the profile is read back in. Duplicates, including two URLs that\
        \ reduce to the same host, are collapsed. This operation does NOT touch the\
        \ project name or its competitors, which are separate resources, and it does\
        \ not regenerate the brand description: that runs asynchronously afterwards,\
        \ so the description in the response is the one stored at the time of the\
        \ write. An Idempotency-Key header is required."
      operationId: updateProjectBrandProfile
      parameters:
      - explode: false
        in: path
        name: organizationId
        required: true
        schema:
          format: uuid
          type: string
        style: simple
      - description: Must belong to the organization in the path.
        explode: false
        in: path
        name: projectId
        required: true
        schema:
          format: uuid
          type: string
        style: simple
      - description: Unique per attempt. A retry carrying the same key and the same
          body is answered from the record instead of running again.
        explode: false
        in: header
        name: Idempotency-Key
        required: true
        schema:
          type: string
        style: simple
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/updateProjectBrandProfile_request"
        required: true
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrandProfileResource"
          description: The profile as stored
        "400":
          description: "The body failed validation, or the Idempotency-Key header\
            \ is missing"
        "401":
          description: Missing or invalid API key
        "403":
          description: The key lacks the write capability
        "404":
          description: "No organization or project the caller can access under these\
            \ ids, or the project has no brand profile"
        "409":
          description: "The organization is archived, the project is archived, or\
            \ the idempotency key was reused for a different body, a concurrent request\
            \ with the same key is still running, or a previous attempt with it ended\
            \ without a known outcome"
      summary: Replace a project's brand monitoring profile
      tags:
      - Projects
      x-content-type: application/json
      x-accepts:
      - application/json
  /api/v1/organizations/{organizationId}/projects/{projectId}:
    get:
      description: "Minimum role: viewer. Returns the project together with the domains\
        \ and brand names it is monitored for and the competitors it is measured against.\
        \ A project that exists but belongs to another organization answers 404, never\
        \ 403. A project that has been created but not yet configured for brand monitoring\
        \ reports empty `websiteDomains`, `brandNames` and `competitors`. Headline\
        \ metrics are not part of this response: use `listProjects` for the per-project\
        \ figures, or `getProjectMetrics` for a window."
      operationId: getProject
      parameters:
      - explode: false
        in: path
        name: organizationId
        required: true
        schema:
          format: uuid
          type: string
        style: simple
      - explode: false
        in: path
        name: projectId
        required: true
        schema:
          format: uuid
          type: string
        style: simple
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ProjectDetailResource"
          description: The project and its brand monitoring configuration
        "401":
          description: Missing or invalid API key
        "403":
          description: The key lacks the read capability
        "404":
          description: "No organization the caller can access under this id, or no\
            \ project with this id inside it"
      summary: Get a project and its brand monitoring configuration
      tags:
      - Projects
      x-accepts:
      - application/json
    patch:
      description: "Minimum role: manager, in an active organization. Requires the\
        \ \"write\" capability. `name` is the only writable field and it is required.\
        \ What is NOT done: the monitored domains and brand names are not touched\
        \ (use updateProjectBrandProfile) and competitors are not touched, added or\
        \ removed (use the competitor endpoints). Sending `websiteDomains`, `brandNames`\
        \ or `competitors` here is refused with the field named, never applied in\
        \ part and never ignored. Renaming a project changes nothing about the data\
        \ already collected against it. A project that belongs to another organization\
        \ answers 404, never 403. An archived project answers 409: restore it first\
        \ — unless the call is a retry carrying the key of a rename that already succeeded,\
        \ which is answered from the record whatever the project's state is now. Send\
        \ an Idempotency-Key; a retry with the same key and body returns the recorded\
        \ answer."
      operationId: updateProject
      parameters:
      - explode: false
        in: path
        name: organizationId
        required: true
        schema:
          format: uuid
          type: string
        style: simple
      - explode: false
        in: path
        name: projectId
        required: true
        schema:
          format: uuid
          type: string
        style: simple
      - explode: false
        in: header
        name: Idempotency-Key
        required: true
        schema:
          maxLength: 255
          minLength: 8
          type: string
        style: simple
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/updateProject_request"
        required: true
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ProjectDetailResource"
          description: The project in its new state
        "400":
          description: The body or the Idempotency-Key was rejected; `details` names
            every field at fault
        "401":
          description: Missing or invalid API key
        "403":
          description: The key lacks the write capability
        "404":
          description: No organization or project the caller can access under these
            ids
        "409":
          description: "The project is archived, the organization is archived, the\
            \ idempotency key was already used for a different request, or a previous\
            \ attempt with it ended without a known outcome"
      summary: Rename a project
      tags:
      - Projects
      x-content-type: application/json
      x-accepts:
      - application/json
  /api/v1/organizations/{organizationId}/projects/{projectId}/restore:
    post:
      description: "Minimum role: manager, in an active organization. Requires the\
        \ \"write\" capability. Brings an archived project back to active, with every\
        \ tracked query, capture and metric it had when it was archived. What is NOT\
        \ done: no check is run and no scraping is scheduled as a result — collection\
        \ resumes on the project's own schedule. Restoring clears the record of who\
        \ archived the project, so a project restored here is treated as an ordinary\
        \ active project by any later organization archive. A project archived because\
        \ its organization was archived cannot be restored on its own: restore the\
        \ organization, which restores them all. Restoring a project that is already\
        \ active answers 409, unless the call is a retry carrying the key that restored\
        \ it. This endpoint takes no body, and one carrying fields is refused. Send\
        \ an Idempotency-Key; a retry with the same key returns the recorded answer."
      operationId: restoreProject
      parameters:
      - explode: false
        in: path
        name: organizationId
        required: true
        schema:
          format: uuid
          type: string
        style: simple
      - explode: false
        in: path
        name: projectId
        required: true
        schema:
          format: uuid
          type: string
        style: simple
      - explode: false
        in: header
        name: Idempotency-Key
        required: true
        schema:
          maxLength: 255
          minLength: 8
          type: string
        style: simple
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ProjectDetailResource"
          description: The project in its new state
        "400":
          description: "A body was sent, or the Idempotency-Key was rejected"
        "401":
          description: Missing or invalid API key
        "403":
          description: The key lacks the write capability
        "404":
          description: No organization or project the caller can access under these
            ids
        "409":
          description: "The project is not archived, the organization is archived,\
            \ the idempotency key was already used for a different request, or a previous\
            \ attempt with it ended without a known outcome"
      summary: Restore an archived project
      tags:
      - Projects
      x-accepts:
      - application/json
  /api/v1/organizations/{organizationId}/projects/{projectId}/tracked-queries/{trackedQueryId}/clusters:
    delete:
      description: "Minimum role: manager. Removes the tracked query from every cluster\
        \ named in \"queryClusterIds\" and answers with the query in its new state.\
        \ A cluster the query does not belong to is skipped, not reported as an error.\
        \ Neither the clusters nor the tracked query are deleted: only the membership\
        \ between them. Every cluster must belong to the project in the path. NOTE:\
        \ this DELETE requires a request body — some HTTP client libraries and proxies\
        \ strip bodies from DELETE, and a stripped body is refused with a validation\
        \ error rather than interpreted as \"remove all clusters\". The \"Idempotency-Key\"\
        \ header is required, and a repeat of the same key and body returns the recorded\
        \ answer without removing anything again."
      operationId: removeClustersFromTrackedQuery
      parameters:
      - explode: false
        in: path
        name: organizationId
        required: true
        schema:
          format: uuid
          type: string
        style: simple
      - explode: false
        in: path
        name: projectId
        required: true
        schema:
          format: uuid
          type: string
        style: simple
      - description: Must belong to the project in the path.
        explode: false
        in: path
        name: trackedQueryId
        required: true
        schema:
          format: uuid
          type: string
        style: simple
      - description: "A client-chosen key, unique per operation, so a lost response\
          \ can be retried without repeating the write."
        explode: false
        in: header
        name: Idempotency-Key
        required: true
        schema:
          type: string
        style: simple
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/ClusterMembershipRequestData"
        description: Required. A DELETE with no body is rejected.
        required: true
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/TrackedQueryDetailResource"
          description: "The tracked query, including the clusters it still belongs\
            \ to"
        "400":
          description: "The body was rejected or missing: an unknown field, a malformed\
            \ cluster id, a cluster outside this project, a stripped DELETE body,\
            \ or a missing Idempotency-Key"
        "401":
          description: Missing or invalid API key
        "403":
          description: The key lacks the write capability
        "404":
          description: "No organization, project or tracked query the caller can access\
            \ under these ids"
        "409":
          description: "The organization is archived, the project is archived, the\
            \ idempotency key was reused for a different body, or a cluster was deleted\
            \ between validation and the write, a concurrent request with the same\
            \ key is still running, or a previous attempt with it ended without a\
            \ known outcome"
      summary: Remove a tracked query from clusters
      tags:
      - Tracked Queries
      x-content-type: application/json
      x-accepts:
      - application/json
    put:
      description: "Minimum role: manager. Adds the tracked query to every cluster\
        \ named in \"queryClusterIds\" and answers with the query in its new state.\
        \ Membership is a set: a cluster the query already belongs to is skipped,\
        \ not reported as an error, and the response still lists it. Clusters the\
        \ query belongs to and this call does not name are left alone — this adds,\
        \ it does not replace the membership. Every cluster must belong to the project\
        \ in the path; one that does not is rejected with its field named, and nothing\
        \ is written. The \"Idempotency-Key\" header is required, and a repeat of\
        \ the same key and body returns the recorded answer without adding anything\
        \ again."
      operationId: addClustersToTrackedQuery
      parameters:
      - explode: false
        in: path
        name: organizationId
        required: true
        schema:
          format: uuid
          type: string
        style: simple
      - explode: false
        in: path
        name: projectId
        required: true
        schema:
          format: uuid
          type: string
        style: simple
      - description: Must belong to the project in the path.
        explode: false
        in: path
        name: trackedQueryId
        required: true
        schema:
          format: uuid
          type: string
        style: simple
      - description: "A client-chosen key, unique per operation, so a lost response\
          \ can be retried without repeating the write."
        explode: false
        in: header
        name: Idempotency-Key
        required: true
        schema:
          type: string
        style: simple
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/ClusterMembershipRequestData"
        required: true
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/TrackedQueryDetailResource"
          description: "The tracked query, including the clusters it now belongs to"
        "400":
          description: "The body was rejected: an unknown field, a malformed cluster\
            \ id, a cluster outside this project, or a missing Idempotency-Key"
        "401":
          description: Missing or invalid API key
        "403":
          description: The key lacks the write capability
        "404":
          description: "No organization, project or tracked query the caller can access\
            \ under these ids"
        "409":
          description: "The organization is archived, the project is archived, the\
            \ idempotency key was reused for a different body, or a cluster was deleted\
            \ between validation and the write, a concurrent request with the same\
            \ key is still running, or a previous attempt with it ended without a\
            \ known outcome"
      summary: Add a tracked query to clusters
      tags:
      - Tracked Queries
      x-content-type: application/json
      x-accepts:
      - application/json
  /api/v1/organizations/{organizationId}/projects/{projectId}/tracked-queries:
    get:
      description: "Minimum role: viewer. One row per tracked query — a single keyword\
        \ on a single engine in a single country — carrying the metrics of its most\
        \ recent completed check. Filter by status, engine, country and a free-text\
        \ search over the keyword, and sort by any of the returned metrics. Positions\
        \ (lastSerpPosition, lastMentionPosition, lastLinkPosition, lastShoppingPosition)\
        \ are 1-based ranks, so LOWER is better; lastShareOfVoice and lastPositivityIndex\
        \ are percentages from 0 to 100, where HIGHER is better. Every nullable field\
        \ means \"not known yet\" rather than zero: a null position is a query with\
        \ no data for that surface, a null lastPositivityIndex is a check with no\
        \ mentions to score, and a null lastCheckedAt is a query that has never been\
        \ checked — none of them is a score of zero. This listing reads a projection\
        \ refreshed by background subscribers, not the write model, so a tracked query\
        \ created or changed moments ago may not appear here yet or may still show\
        \ its previous settings. It catches up on its own; nothing is lost. If you\
        \ need to read back what you just wrote, the creation response carries the\
        \ new ids and the single tracked-query operation reads the write model directly.\
        \ total counts the tracked queries the filters match, not the rows on this\
        \ page. A limit above the maximum is rejected, never clamped, and a filter\
        \ this endpoint does not support is rejected rather than ignored.Send `Accept:\
        \ text/csv` to receive the same page as a CSV download instead of JSON: same\
        \ filters, same authorization, same page window and the same maximum of 100\
        \ rows — it is this page in another format, not a bulk export, so a whole\
        \ collection is still read by paging. The CSV carries no `total`, because\
        \ a table whose every row is a record has nowhere to put one; read it from\
        \ the JSON representation of the same request. A list-valued field is joined\
        \ into one cell with `; ` as a display projection — parse the JSON if you\
        \ need the structure. Cells beginning with `=`, `+`, `-` or `@` are prefixed\
        \ with an apostrophe so a spreadsheet treats them as text rather than running\
        \ them as formulas."
      operationId: searchTrackedQueries
      parameters:
      - explode: false
        in: path
        name: organizationId
        required: true
        schema:
          format: uuid
          type: string
        style: simple
      - explode: false
        in: path
        name: projectId
        required: true
        schema:
          format: uuid
          type: string
        style: simple
      - description: "Page size. A value above the maximum is rejected, never clamped."
        explode: true
        in: query
        name: limit
        required: false
        schema:
          default: 20
          maximum: 100
          minimum: 1
          type: integer
        style: form
      - explode: true
        in: query
        name: offset
        required: false
        schema:
          default: 0
          minimum: 0
          type: integer
        style: form
      - description: Free-text search over the keyword.
        explode: true
        in: query
        name: search
        required: false
        schema:
          maxLength: 100
          type: string
        style: form
      - explode: true
        in: query
        name: status
        required: false
        schema:
          enum:
          - active
          - paused
          type: string
        style: form
      - description: "Repeatable, or comma-separated."
        explode: true
        in: query
        name: engines
        required: false
        schema:
          items:
            enum:
            - chatgpt
            - perplexity
            - google_ai_overview
            - google_ai_mode
            - google_serp
            - google_shopping
            type: string
          type: array
        style: form
      - description: ISO-3166 alpha-2 codes or English names. Must be configured on
          the project.
        explode: true
        in: query
        name: countries
        required: false
        schema:
          items:
            type: string
          type: array
        style: form
      - explode: true
        in: query
        name: sortBy
        required: false
        schema:
          default: queryText
          enum:
          - queryText
          - lastSerpPosition
          - lastMentionPosition
          - lastShoppingPosition
          - lastShareOfVoice
          - lastPositivityIndex
          - lastMentionCount
          - lastCheckedAt
          type: string
        style: form
      - explode: true
        in: query
        name: sortOrder
        required: false
        schema:
          default: desc
          enum:
          - asc
          - desc
          type: string
        style: form
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/searchTrackedQueries_200_response"
            text/csv:
              example: |
                id,name
                b3f1...,Acme
              schema:
                type: string
          description: The project's tracked queries
        "400":
          description: A filter or page bound was rejected; the details name the field
        "401":
          description: Missing or invalid API key
        "403":
          description: The key lacks the read capability
        "404":
          description: No organization or project the caller can access under these
            ids
      summary: Search a project's tracked queries
      tags:
      - Tracked Queries
      x-accepts:
      - application/json
      - text/csv
    post:
      description: "Minimum role: manager. Creates the cross product of `queryTexts`\
        \ x `engines` x `countries`: three texts, two engines and two countries create\
        \ twelve tracked queries, not three. At most 100 combinations per call. The\
        \ organization must be active and the project must not be archived. Partial\
        \ success: the response lists, per combination, either the id it was created\
        \ under or why nothing was created for it, and the status code never reports\
        \ item-level outcomes. A combination the project already tracks is NOT created\
        \ again and NOT re-identified — it appears under `failed` with `tracked_query_already_exists`,\
        \ keeps the id it already had, and has any `queryClusterIds` in this request\
        \ merged into it. Query text is normalised before it is compared and stored\
        \ (lower-cased, whitespace collapsed, leading list markers stripped), so two\
        \ texts differing only in those respects are one tracked query: the first\
        \ of them owns the outcome and every later one appears under `failed` with\
        \ `duplicate_combination_in_request`, naming the entry it repeats. `nPasses`\
        \ applies to AI engines only: `google_serp` and `google_shopping` rows are\
        \ always created with one pass, whatever is sent. Google AI Mode is unavailable\
        \ in a few countries and those combinations are reported under `failed` rather\
        \ than created. Creating a tracked query does not run a check: the first check\
        \ happens on the normal schedule for the `checkFrequency` chosen. The Idempotency-Key\
        \ header is required; a retry with the same key and the same body returns\
        \ this same answer without creating anything again."
      operationId: batchCreateTrackedQueries
      parameters:
      - explode: false
        in: path
        name: organizationId
        required: true
        schema:
          format: uuid
          type: string
        style: simple
      - explode: false
        in: path
        name: projectId
        required: true
        schema:
          format: uuid
          type: string
        style: simple
      - description: "A client-chosen key, unique per operation. Replaying it returns\
          \ the first answer instead of creating anything again."
        explode: false
        in: header
        name: Idempotency-Key
        required: true
        schema:
          type: string
        style: simple
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/BatchCreateTrackedQueriesRequestData"
        required: true
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BatchCreateTrackedQueriesResultResource"
          description: "Per-combination results. Read `failed`: a non-empty `failed`\
            \ with a 200 is the normal partial-success answer."
        "400":
          description: "The body was rejected, or the Idempotency-Key header is missing;\
            \ `details` names the field"
        "401":
          description: Missing or invalid API key
        "403":
          description: The key lacks the write capability
        "404":
          description: "No organization, project or query cluster the caller can access\
            \ under these ids"
        "409":
          description: "The organization is archived, the project is archived, or\
            \ the idempotency key was already used for a different request, a concurrent\
            \ request with the same key is still running, or a previous attempt with\
            \ it ended without a known outcome"
        "422":
          description: A query cluster exists but belongs to another project
      summary: Create tracked queries
      tags:
      - Tracked Queries
      x-content-type: application/json
      x-accepts:
      - application/json
  /api/v1/organizations/{organizationId}/projects/{projectId}/tracked-queries/bulk-delete:
    post:
      description: "Minimum role: manager. Permanently deletes the tracked queries\
        \ named in `ids`, at most 100 distinct ids per call. The organization must\
        \ be active and the project must not be archived. Deletion is hard and cannot\
        \ be undone: the tracked query is removed along with its captured answers,\
        \ matches, search pages and rank history, and any check already in flight\
        \ for it is cancelled. Those cascades run in the background, so a 200 means\
        \ the tracked queries were deleted, not that every derived record has finished\
        \ being purged. Partial success: an id that is not a tracked query of this\
        \ project — unknown, malformed, already deleted, or belonging to somewhere\
        \ else — is reported under `failed` with `tracked_query_not_found` while the\
        \ rest are deleted. It never deletes more than the ids it is given: there\
        \ is no \"delete everything matching a filter\" mode. The Idempotency-Key\
        \ header is required; a retry with the same key and the same body returns\
        \ this same answer without deleting anything again."
      operationId: bulkDeleteTrackedQueries
      parameters:
      - explode: false
        in: path
        name: organizationId
        required: true
        schema:
          format: uuid
          type: string
        style: simple
      - explode: false
        in: path
        name: projectId
        required: true
        schema:
          format: uuid
          type: string
        style: simple
      - description: "A client-chosen key, unique per operation. Replaying it returns\
          \ the first answer instead of deleting anything again."
        explode: false
        in: header
        name: Idempotency-Key
        required: true
        schema:
          type: string
        style: simple
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/BatchTargetsRequestData"
        required: true
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BatchWriteOutcome"
          description: "Per-id results. Read `failed`: a non-empty `failed` with a\
            \ 200 is the normal partial-success answer."
        "400":
          description: "The body was rejected, or the Idempotency-Key header is missing;\
            \ `details` names the field"
        "401":
          description: Missing or invalid API key
        "403":
          description: The key lacks the write capability
        "404":
          description: No organization or project the caller can access under these
            ids
        "409":
          description: "The organization is archived, the project is archived, or\
            \ the idempotency key was already used for a different request, a concurrent\
            \ request with the same key is still running, or a previous attempt with\
            \ it ended without a known outcome"
      summary: Delete tracked queries
      tags:
      - Tracked Queries
      x-content-type: application/json
      x-accepts:
      - application/json
  /api/v1/organizations/{organizationId}/projects/{projectId}/tracked-queries/{trackedQueryId}/check-frequency:
    put:
      description: "Minimum role: manager. Sets how often one tracked query is checked\
        \ while it is active. The organization must be active and the project must\
        \ not be archived. Setting the frequency it already has is accepted and changes\
        \ nothing. This does NOT run a check, does not backfill history, and does\
        \ not reschedule a check already in flight: the new cadence applies from the\
        \ next time the query is considered. A paused query keeps the setting but\
        \ is not checked until it is resumed. A tracked query belonging to another\
        \ project answers 404, the same answer an unknown id gets. The Idempotency-Key\
        \ header is required; a retry with the same key and the same body returns\
        \ this same answer without applying anything again."
      operationId: changeTrackedQueryCheckFrequency
      parameters:
      - explode: false
        in: path
        name: organizationId
        required: true
        schema:
          format: uuid
          type: string
        style: simple
      - explode: false
        in: path
        name: projectId
        required: true
        schema:
          format: uuid
          type: string
        style: simple
      - description: Must belong to the project in the path.
        explode: false
        in: path
        name: trackedQueryId
        required: true
        schema:
          format: uuid
          type: string
        style: simple
      - description: "A client-chosen key, unique per operation. Replaying it returns\
          \ the first answer instead of applying anything again."
        explode: false
        in: header
        name: Idempotency-Key
        required: true
        schema:
          type: string
        style: simple
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/ChangeTrackedQueryCheckFrequencyRequestData"
        required: true
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/TrackedQueryDetailResource"
          description: The tracked query in its new state
        "400":
          description: "The body was rejected, or the Idempotency-Key header is missing;\
            \ `details` names the field"
        "401":
          description: Missing or invalid API key
        "403":
          description: The key lacks the write capability
        "404":
          description: "No organization, project or tracked query the caller can access\
            \ under these ids"
        "409":
          description: "The organization is archived, the project is archived, or\
            \ the idempotency key was already used for a different request, a concurrent\
            \ request with the same key is still running, or a previous attempt with\
            \ it ended without a known outcome"
      summary: Change how often a tracked query is checked
      tags:
      - Tracked Queries
      x-content-type: application/json
      x-accepts:
      - application/json
  /api/v1/organizations/{organizationId}/projects/{projectId}/tracked-queries/{trackedQueryId}/passes:
    put:
      description: "Minimum role: manager. Sets how many times one tracked query is\
        \ asked per check. AI engines are not deterministic, so several passes are\
        \ averaged; `google_serp` and `google_shopping` run a single pass and refuse\
        \ any value above one with 409 `n_passes_not_supported_for_engine`. Setting\
        \ the value back to 1 is always allowed. The organization must be active and\
        \ the project must not be archived. Setting the value it already has is accepted\
        \ and changes nothing. This does NOT run a check and does not change history\
        \ already captured: it applies from the next check. More passes cost proportionally\
        \ more of the plan's check budget. A tracked query belonging to another project\
        \ answers 404, the same answer an unknown id gets. The Idempotency-Key header\
        \ is required; a retry with the same key and the same body returns this same\
        \ answer without applying anything again."
      operationId: changeTrackedQueryNPasses
      parameters:
      - explode: false
        in: path
        name: organizationId
        required: true
        schema:
          format: uuid
          type: string
        style: simple
      - explode: false
        in: path
        name: projectId
        required: true
        schema:
          format: uuid
          type: string
        style: simple
      - description: Must belong to the project in the path.
        explode: false
        in: path
        name: trackedQueryId
        required: true
        schema:
          format: uuid
          type: string
        style: simple
      - description: "A client-chosen key, unique per operation. Replaying it returns\
          \ the first answer instead of applying anything again."
        explode: false
        in: header
        name: Idempotency-Key
        required: true
        schema:
          type: string
        style: simple
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/ChangeTrackedQueryPassesRequestData"
        required: true
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/TrackedQueryDetailResource"
          description: The tracked query in its new state
        "400":
          description: "The body was rejected, or the Idempotency-Key header is missing;\
            \ `details` names the field"
        "401":
          description: Missing or invalid API key
        "403":
          description: The key lacks the write capability
        "404":
          description: "No organization, project or tracked query the caller can access\
            \ under these ids"
        "409":
          description: "The engine runs a single pass and cannot take more; or the\
            \ organization or project is archived; or the idempotency key was already\
            \ used for a different request, a concurrent request with the same key\
            \ is still running, or a previous attempt with it ended without a known\
            \ outcome"
      summary: Change how many passes a tracked query runs per check
      tags:
      - Tracked Queries
      x-content-type: application/json
      x-accepts:
      - application/json
  /api/v1/organizations/{organizationId}/projects/{projectId}/tracked-queries/{trackedQueryId}:
    get:
      description: "Minimum role: viewer. The configuration of one tracked query:\
        \ the text sent to the engine, the engine, locale and country it is asked\
        \ in, the clusters it belongs to, and how often it is checked. A lastCheckedAt\
        \ of null means no check has completed yet — it is not a check that found\
        \ nothing. A tracked query belonging to another project answers 404, the same\
        \ answer an unknown id gets, so the API never confirms that an inaccessible\
        \ tracked query exists. Rank positions, share of voice and sentiment are not\
        \ part of this response: they belong to a date window and are served by the\
        \ analytics endpoints."
      operationId: getTrackedQuery
      parameters:
      - explode: false
        in: path
        name: organizationId
        required: true
        schema:
          format: uuid
          type: string
        style: simple
      - explode: false
        in: path
        name: projectId
        required: true
        schema:
          format: uuid
          type: string
        style: simple
      - description: Must belong to the project in the path.
        explode: false
        in: path
        name: trackedQueryId
        required: true
        schema:
          format: uuid
          type: string
        style: simple
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/TrackedQueryDetailResource"
          description: The tracked query
        "401":
          description: Missing or invalid API key
        "403":
          description: The key lacks the read capability
        "404":
          description: "No organization, project or tracked query the caller can access\
            \ under these ids"
      summary: Get a tracked query
      tags:
      - Tracked Queries
      x-accepts:
      - application/json
  /api/v1/organizations/{organizationId}/projects/{projectId}/tracked-queries/{trackedQueryId}/pause:
    put:
      description: "Minimum role: manager. Stops the scheduler from checking this\
        \ tracked query; it keeps its configuration, its clusters and every result\
        \ already collected, and nothing is deleted. Pausing a query that is already\
        \ paused succeeds and answers the same body — this is a PUT asserting a state,\
        \ not a transition, so it is safe to repeat. It does NOT cancel a check that\
        \ is already running: a check in flight when the pause lands still completes\
        \ and still consumes the budget unit it reserved. The request takes no body,\
        \ and any field sent is rejected. Requires an Idempotency-Key header."
      operationId: pauseTrackedQuery
      parameters:
      - explode: false
        in: path
        name: organizationId
        required: true
        schema:
          format: uuid
          type: string
        style: simple
      - explode: false
        in: path
        name: projectId
        required: true
        schema:
          format: uuid
          type: string
        style: simple
      - description: Must belong to the project in the path.
        explode: false
        in: path
        name: trackedQueryId
        required: true
        schema:
          format: uuid
          type: string
        style: simple
      - description: Repeating a request with the same key answers with the first
          attempt's result instead of pausing again.
        explode: false
        in: header
        name: Idempotency-Key
        required: true
        schema:
          type: string
        style: simple
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/TrackedQueryDetailResource"
          description: "The tracked query, paused"
        "400":
          description: "The Idempotency-Key header is missing or malformed, or the\
            \ request carried a body"
        "401":
          description: Missing or invalid API key
        "403":
          description: The key lacks the write capability
        "404":
          description: "No organization, project or tracked query the caller can access\
            \ under these ids"
        "409":
          description: "The organization or the project is archived, or the idempotency\
            \ key was already used for a different request, a concurrent request with\
            \ the same key is still running, or a previous attempt with it ended without\
            \ a known outcome"
      summary: Pause a tracked query
      tags:
      - Tracked Queries
      x-accepts:
      - application/json
  /api/v1/organizations/{organizationId}/projects/{projectId}/tracked-queries/{trackedQueryId}/responses/{aiResponseId}/report:
    post:
      description: "Minimum role: viewer — deliberately lower than the other tracked-query\
        \ writes, because a report changes nothing a viewer cannot already read. The\
        \ key still needs the \"write\" capability. The report is forwarded to the\
        \ team that reviews the scrape run behind the capture; it does not change\
        \ the capture, the tracked query, or any metric derived from them, and nothing\
        \ in this API will show the report afterwards. A 200 means the report was\
        \ accepted for review, not that anything was corrected, and there is no identifier\
        \ to poll. A capture that belongs to another tracked query or project answers\
        \ 404, the same answer an unknown id gets. A capture that cannot be routed\
        \ back to its scrape run answers 409 \"ai_check_session_unavailable\", and\
        \ it cannot be reported. That code covers two moments, which behave differently\
        \ for your key: when the capture carries no scrape run at all the refusal\
        \ is decided BEFORE anything is sent, so the idempotency key is left unused\
        \ and the same key answers the same way however often it is presented; when\
        \ the review system itself rejects the run — its own retention having elapsed\
        \ — the refusal comes after the forward was attempted, so the key is spent\
        \ like any outcome we cannot confirm. You can tell them apart without guessing:\
        \ retry the same key, and a reply of \"operation_outcome_uncertain\" means\
        \ the refusal came from the review system. Delivery is at-most-once: the \"\
        Idempotency-Key\" header is required and a repeat of the same key and body\
        \ returns the recorded answer, but a delivery whose outcome is unknown refuses\
        \ replay on that key rather than risk filing the report twice."
      operationId: reportAiResponse
      parameters:
      - explode: false
        in: path
        name: organizationId
        required: true
        schema:
          format: uuid
          type: string
        style: simple
      - explode: false
        in: path
        name: projectId
        required: true
        schema:
          format: uuid
          type: string
        style: simple
      - description: Must belong to the project in the path.
        explode: false
        in: path
        name: trackedQueryId
        required: true
        schema:
          format: uuid
          type: string
        style: simple
      - description: A capture id from the AI responses listing. Must belong to the
          tracked query in the path.
        explode: false
        in: path
        name: aiResponseId
        required: true
        schema:
          format: uuid
          type: string
        style: simple
      - description: "A client-chosen key, unique per report, so a lost response can\
          \ be retried without filing the report twice."
        explode: false
        in: header
        name: Idempotency-Key
        required: true
        schema:
          type: string
        style: simple
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/reportAiResponse_request"
        required: true
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/AiResponseReportResource"
          description: "The report as accepted, in its normalised form"
        "400":
          description: "The body was rejected: an unknown field, a missing or unsupported\
            \ \"type\", an over-long comment or brand name, or a missing Idempotency-Key"
        "401":
          description: Missing or invalid API key
        "403":
          description: The key lacks the write capability
        "404":
          description: "No organization, project, tracked query or capture the caller\
            \ can access under these ids"
        "409":
          description: "The organization is archived, the capture can no longer be\
            \ routed back to its scrape run, the idempotency key was reused for a\
            \ different body, or a previous attempt with this key ended with an unknown\
            \ outcome"
        "502":
          description: "The review system could not be reached or refused the forward.\
            \ Whether the report was filed is UNKNOWN: delivery is at-most-once and\
            \ the failure can happen after the report was recorded upstream. This\
            \ idempotency key will not replay; retrying with a new key may file the\
            \ report twice."
      summary: Report a problem with a captured AI answer
      tags:
      - Tracked Queries
      x-content-type: application/json
      x-accepts:
      - application/json
  /api/v1/organizations/{organizationId}/projects/{projectId}/tracked-queries/{trackedQueryId}/resume:
    put:
      description: "Minimum role: manager. Puts a paused tracked query back under\
        \ the scheduler. Resuming a query that is already active succeeds and answers\
        \ the same body — this is a PUT asserting a state, not a transition. It does\
        \ NOT run a check: the query is checked when it next falls due under its own\
        \ checkFrequency, and results collected while it was paused are unaffected.\
        \ Nothing is back-filled for the time it spent paused. The request takes no\
        \ body, and any field sent is rejected. Requires an Idempotency-Key header."
      operationId: resumeTrackedQuery
      parameters:
      - explode: false
        in: path
        name: organizationId
        required: true
        schema:
          format: uuid
          type: string
        style: simple
      - explode: false
        in: path
        name: projectId
        required: true
        schema:
          format: uuid
          type: string
        style: simple
      - description: Must belong to the project in the path.
        explode: false
        in: path
        name: trackedQueryId
        required: true
        schema:
          format: uuid
          type: string
        style: simple
      - description: Repeating a request with the same key answers with the first
          attempt's result instead of resuming again.
        explode: false
        in: header
        name: Idempotency-Key
        required: true
        schema:
          type: string
        style: simple
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/TrackedQueryDetailResource"
          description: "The tracked query, active"
        "400":
          description: "The Idempotency-Key header is missing or malformed, or the\
            \ request carried a body"
        "401":
          description: Missing or invalid API key
        "403":
          description: The key lacks the write capability
        "404":
          description: "No organization, project or tracked query the caller can access\
            \ under these ids"
        "409":
          description: "The organization or the project is archived, or the idempotency\
            \ key was already used for a different request, a concurrent request with\
            \ the same key is still running, or a previous attempt with it ended without\
            \ a known outcome"
      summary: Resume a tracked query
      tags:
      - Tracked Queries
      x-accepts:
      - application/json
  /api/v1/organizations/{organizationId}/usage/tracked-queries:
    get:
      description: "Minimum role: viewer. How many tracked queries the organization\
        \ has configured, counted live in PostgreSQL — the write model — over EVERY\
        \ project it owns, archived projects included, and over every tracked query\
        \ in them, active and paused alike. It is deliberately a DIFFERENT number\
        \ from `aggregate.totalTrackedQueries` in getOrganizationOverview: that one\
        \ sums a per-project copy held in the Elasticsearch read model and covers\
        \ ACTIVE projects only, so it excludes archived projects and can lag behind\
        \ a change that already shows here. Expect the two to disagree and do not\
        \ treat either as wrong. This figure does reconcile exactly with countTrackedQueries,\
        \ which counts one project through the same counter in the same store: sum\
        \ its unfiltered `count` over every project, archived included, and you get\
        \ this number. For the ACTIVE subset and what it will consume, call getOrganizationProjectedMonthlyChecks,\
        \ which ranges over the same projects. Do NOT derive the paused count by subtracting\
        \ one from the other: this number is read live from Postgres while that one\
        \ is served from a cache invalidated by background subscribers, so the two\
        \ can disagree while that invalidation catches up and the difference is then\
        \ a count of nothing. For how many projects this ranges over, read `projects.total`\
        \ and `projects.active` from getOrganizationStats. The value is never null:\
        \ 0 means none are configured. This operation takes no query parameters —\
        \ the population is fixed, and a parameter it cannot honour is rejected with\
        \ 400 rather than silently ignored."
      operationId: countOrganizationTrackedQueries
      parameters:
      - explode: false
        in: path
        name: organizationId
        required: true
        schema:
          format: uuid
          type: string
        style: simple
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/TrackedQueryUsageResource"
          description: The organization's tracked-query count
        "400":
          description: A query parameter was sent to an operation that accepts none;
            the details name it
        "401":
          description: Missing or invalid API key
        "403":
          description: The key lacks the read capability
        "404":
          description: No organization the caller can access under this id
      summary: Count the tracked queries an organization has configured
      tags:
      - Organizations
      x-accepts:
      - application/json
  /api/v1/organizations/{organizationId}/usage/projected-monthly-checks:
    get:
      description: "Minimum role: viewer. What the organization's current configuration\
        \ would consume in a month, in check budget units — the same unit `checkBudget`\
        \ and `checksAvailable` are counted in by the entitlements operation — so\
        \ the two are directly comparable when sizing a plan. The arithmetic is published\
        \ so it can be reproduced rather than trusted: for each ACTIVE tracked query,\
        \ runs per month (daily 30, weekly 4, monthly 1) multiplied by its nPasses,\
        \ summed. A weekly query bills 4, not 4.345: a month is modelled as 30 days\
        \ and 4 weeks, a planning convention rather than a calendar. Take `checkFrequency`\
        \ and `nPasses` from the tracked-query listing and the arithmetic will match\
        \ — though the two sides are read from different places and settle at different\
        \ times: this figure is computed in the Postgres write model and served from\
        \ a cache that background subscribers invalidate, while that listing reads\
        \ an Elasticsearch projection. Immediately after a write the two can disagree;\
        \ neither is wrong, they are catching up. PAUSED queries are excluded from\
        \ both figures; archived PROJECTS are not — archiving a project does not pause\
        \ its tracked queries, so they still count here, exactly as they do in countOrganizationTrackedQueries.\
        \ `activeTrackedQueryCount` describes the same population as that count, minus\
        \ the paused queries — but do not compute the difference to learn how many\
        \ are paused: THIS OPERATION IS CACHED and that one is read live, so the two\
        \ can disagree while the cache is invalidated in the background. This is a\
        \ projection of the configuration, not a forecast of what will actually be\
        \ spent: it does not look at the remaining plan budget, does not know which\
        \ checks will be skipped or retried, and reserves and debits nothing. It is\
        \ also not the `checkCost` of countTrackedQueries, which prices one round\
        \ over one project rather than a month over the organization. Both values\
        \ are never null: 0 means nothing is scheduled. This operation takes no query\
        \ parameters — the population is fixed, and a parameter it cannot honour is\
        \ rejected with 400 rather than silently ignored."
      operationId: getOrganizationProjectedMonthlyChecks
      parameters:
      - explode: false
        in: path
        name: organizationId
        required: true
        schema:
          format: uuid
          type: string
        style: simple
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ProjectedMonthlyChecksResource"
          description: The projected monthly checks and the active tracked queries
            behind them
        "400":
          description: A query parameter was sent to an operation that accepts none;
            the details name it
        "401":
          description: Missing or invalid API key
        "403":
          description: The key lacks the read capability
        "404":
          description: No organization the caller can access under this id
      summary: Project a month of check consumption from the current tracking configuration
      tags:
      - Organizations
      x-accepts:
      - application/json
components:
  schemas:
    BatchChangeTrackedQueryCheckFrequencyRequestData:
      description: "Tracked queries to retune, and the check frequency to set on them"
      example:
        ids:
        - 046b6c7f-0b8a-43b9-b35d-6489e6daee91
        - 046b6c7f-0b8a-43b9-b35d-6489e6daee91
        - 046b6c7f-0b8a-43b9-b35d-6489e6daee91
        - 046b6c7f-0b8a-43b9-b35d-6489e6daee91
        - 046b6c7f-0b8a-43b9-b35d-6489e6daee91
        checkFrequency: daily
      properties:
        ids:
          description: Ids of the tracked queries to change. Duplicates are collapsed.
          items:
            format: uuid
            type: string
          maxItems: 100
          minItems: 1
          type: array
        checkFrequency:
          description: How often each query is checked while it is active.
          enum:
          - daily
          - weekly
          - monthly
          type: string
      required:
      - checkFrequency
      - ids
      type: object
    BatchWriteOutcome:
      description: Per-item results of a batch write
      example:
        successful:
        - id: 046b6c7f-0b8a-43b9-b35d-6489e6daee91
        - id: 046b6c7f-0b8a-43b9-b35d-6489e6daee91
        failed:
        - id: 046b6c7f-0b8a-43b9-b35d-6489e6daee91
          errorCode: errorCode
          errorMessage: errorMessage
        - id: 046b6c7f-0b8a-43b9-b35d-6489e6daee91
          errorCode: errorCode
          errorMessage: errorMessage
      properties:
        successful:
          description: The resources the operation was applied to.
          items:
            $ref: "#/components/schemas/batchPauseTrackedQueries_200_response_successful_inner"
          type: array
        failed:
          description: "Always present, empty list included: read it rather than inferring\
            \ success from the status code."
          items:
            $ref: "#/components/schemas/BatchWriteOutcome_failed_inner"
          type: array
      type: object
    BatchChangeTrackedQueryPassesRequestData:
      description: "Tracked queries to retune, and the passes per check to set on\
        \ them"
      example:
        ids:
        - 046b6c7f-0b8a-43b9-b35d-6489e6daee91
        - 046b6c7f-0b8a-43b9-b35d-6489e6daee91
        - 046b6c7f-0b8a-43b9-b35d-6489e6daee91
        - 046b6c7f-0b8a-43b9-b35d-6489e6daee91
        - 046b6c7f-0b8a-43b9-b35d-6489e6daee91
        nPasses: 1
      properties:
        ids:
          description: Ids of the tracked queries to change. Duplicates are collapsed.
          items:
            format: uuid
            type: string
          maxItems: 100
          minItems: 1
          type: array
        nPasses:
          description: Passes run per check. Only an AI engine accepts more than one;
            a non-AI target is reported under "failed".
          maximum: 10
          minimum: 1
          type: integer
      required:
      - ids
      - nPasses
      type: object
    BatchTargetsRequestData:
      description: The resources a batch operation acts on
      example:
        ids:
        - 046b6c7f-0b8a-43b9-b35d-6489e6daee91
        - 046b6c7f-0b8a-43b9-b35d-6489e6daee91
        - 046b6c7f-0b8a-43b9-b35d-6489e6daee91
        - 046b6c7f-0b8a-43b9-b35d-6489e6daee91
        - 046b6c7f-0b8a-43b9-b35d-6489e6daee91
      properties:
        ids:
          description: Ids of the resources to act on. Duplicates are collapsed.
          items:
            format: uuid
            type: string
          maxItems: 100
          minItems: 1
          type: array
      required:
      - ids
      type: object
    TrackedQueryCountResource:
      description: "How many tracked queries a project has, and what checking them\
        \ would cost in budget units"
      example:
        count: 42
        checkCost: 96
      properties:
        count:
          description: Tracked queries in the project matching the status filter.
            Every status when none was sent.
          example: 42
          type: integer
        checkCost:
          description: "Budget cost of force-checking exactly those tracked queries,\
            \ in check budget units: the sum of each one's configured passes. One\
            \ unit per pass, the same unit the plan allowance is counted in. Reserves\
            \ nothing and debits nothing."
          example: 96
          type: integer
      required:
      - checkCost
      - count
      type: object
    SubmittedChecksResource:
      description: The tracked queries submitted for an immediate check
      example:
        submitted: 0
        trackedQueryIds:
        - 046b6c7f-0b8a-43b9-b35d-6489e6daee91
        - 046b6c7f-0b8a-43b9-b35d-6489e6daee91
      properties:
        submitted:
          description: "How many tracked queries were submitted. Zero is a valid answer:\
            \ it means nothing in the project was eligible."
          type: integer
        trackedQueryIds:
          description: "The tracked queries submitted, in the order they were submitted\
            \ (least recently checked first). Poll these to follow progress."
          items:
            format: uuid
            type: string
          type: array
      required:
      - submitted
      - trackedQueryIds
      type: object
    EntitlementsResource:
      description: What an organization's plan allows and what it has consumed
      example:
        status: active
        isEntitled: true
        checkBudget: 0
        checksConsumed: 6
        checksAvailable: 1
        billingCycleType: monthly
        billingCycleAnchor: 2000-01-23T04:56:07.000+00:00
        nextResetAt: 2000-01-23T04:56:07.000+00:00
        cancelledAt: 2000-01-23T04:56:07.000+00:00
        scheduledToCancelAt: 2000-01-23T04:56:07.000+00:00
        gracePeriodEndsAt: 2000-01-23T04:56:07.000+00:00
      properties:
        status:
          description: '"none" when the organization has never had a subscription
            contract.'
          enum:
          - active
          - inactive
          - none
          type: string
        isEntitled:
          description: "Whether the organization may use paid features right now:\
            \ an active contract, or a cancelled one still inside its paid grace window."
          type: boolean
        checkBudget:
          description: "Checks the current cycle allows. Null when there is no plan\
            \ on file, which is not the same as a budget of zero."
          nullable: true
          type: integer
        checksConsumed:
          description: Checks consumed so far in the current cycle. Null when there
            is no plan on file.
          nullable: true
          type: integer
        checksAvailable:
          description: Checks left in the current cycle. Null when there is no plan
            on file.
          nullable: true
          type: integer
        billingCycleType:
          description: How often the allowance renews. Null when there is no plan
            on file.
          enum:
          - monthly
          - yearly
          nullable: true
          type: string
        billingCycleAnchor:
          description: Start of the current cycle. Null when there is no plan on file.
          format: date-time
          nullable: true
          type: string
        nextResetAt:
          description: When the check budget next resets. Null when there is no plan
            on file.
          format: date-time
          nullable: true
          type: string
        cancelledAt:
          description: When the subscription was terminally cancelled. Null while
            it has not been.
          format: date-time
          nullable: true
          type: string
        scheduledToCancelAt:
          description: "When a scheduled cancellation takes effect, while the subscription\
            \ is still running. Null when none is scheduled."
          format: date-time
          nullable: true
          type: string
        gracePeriodEndsAt:
          description: When paid access ends after a cancellation. Null when the subscription
            has not been cancelled.
          format: date-time
          nullable: true
          type: string
      required:
      - isEntitled
      - status
      type: object
    SubscriptionResource:
      description: The current subscription contract of an organization
      example:
        status: active
        tierCode: tier_2000
        billingCycleType: monthly
        billingCycleAnchor: 2000-01-23T04:56:07.000+00:00
        checkBudget: 0
        checksConsumed: 6
        checksAvailable: 1
        nextResetAt: 2000-01-23T04:56:07.000+00:00
        cancelledAt: 2000-01-23T04:56:07.000+00:00
        scheduledToCancelAt: 2000-01-23T04:56:07.000+00:00
        gracePeriodEndsAt: 2000-01-23T04:56:07.000+00:00
        deactivationReason: payment_failed
        isEntitled: true
      properties:
        status:
          description: Contract state. "none" when the organization has never held
            a subscription.
          enum:
          - active
          - inactive
          - none
          type: string
        tierCode:
          description: Public tier identifier derived from the check budget.
          example: tier_2000
          nullable: true
          type: string
        billingCycleType:
          description: Billing interval of the contract.
          enum:
          - monthly
          - yearly
          nullable: true
          type: string
        billingCycleAnchor:
          description: Instant the billing cycle is anchored to.
          format: date-time
          nullable: true
          type: string
        checkBudget:
          description: "Checks the current cycle grants. Null when there is no subscription,\
            \ never a stand-in for zero."
          nullable: true
          type: integer
        checksConsumed:
          description: Checks consumed so far in the current cycle. Null when there
            is no subscription; zero means none consumed yet.
          nullable: true
          type: integer
        checksAvailable:
          description: Checks still available in the current cycle. Null when there
            is no subscription; zero means the budget is exhausted.
          nullable: true
          type: integer
        nextResetAt:
          description: When the check budget resets next.
          format: date-time
          nullable: true
          type: string
        cancelledAt:
          description: When the subscription was terminally cancelled. Null while
            it has not been.
          format: date-time
          nullable: true
          type: string
        scheduledToCancelAt:
          description: "When a scheduled cancellation takes effect, while the subscription\
            \ is still running. Null when none is scheduled."
          format: date-time
          nullable: true
          type: string
        gracePeriodEndsAt:
          description: When paid access ends after a cancellation. Null when the subscription
            is not cancelled.
          format: date-time
          nullable: true
          type: string
        deactivationReason:
          description: "Why the subscription is currently inactive. Null for an active\
            \ subscription, and meaningless once \"cancelledAt\" is set - the cancellation\
            \ supersedes it."
          enum:
          - payment_failed
          - paused
          - incomplete
          - other
          nullable: true
          type: string
        isEntitled:
          description: "Whether the organization may use paid features right now:\
            \ active, or cancelled but still inside its paid grace window."
          type: boolean
      required:
      - isEntitled
      - status
      type: object
    CitationResource:
      description: A source cited inside one AI answer
      example:
        url: url
        position: 1
        anchorText: anchorText
        title: title
        snippet: snippet
        publicationDate: publicationDate
        thumbnailUrl: thumbnailUrl
        domain: domain
        sourceName: sourceName
        unresolved: true
      properties:
        url:
          description: "The URL exactly as the engine cited it, redirector included\
            \ when unresolved is true"
          type: string
        position:
          description: "Place within this answer, starting at 1"
          minimum: 1
          type: integer
        anchorText:
          description: "The visible link text, when the engine provided one"
          nullable: true
          type: string
        title:
          description: "Title of the cited page, when the engine provided one"
          nullable: true
          type: string
        snippet:
          description: "Excerpt the engine showed for this source, when it provided\
            \ one"
          nullable: true
          type: string
        publicationDate:
          description: "Publication date the engine reported, when it provided one"
          nullable: true
          type: string
        thumbnailUrl:
          description: "Preview image the engine showed for this source, when it provided\
            \ one"
          nullable: true
          type: string
        domain:
          description: "Host of url. Belongs to the redirector, not the publisher,\
            \ when unresolved is true"
          nullable: true
          type: string
        sourceName:
          description: "Publisher name the engine reported, when it provided one"
          nullable: true
          type: string
        unresolved:
          description: "True when url still points at a redirector: count the citation,\
            \ do not attribute the domain"
          type: boolean
      required:
      - position
      - unresolved
      - url
      type: object
    AiResponseResource:
      description: One captured AI answer with its citations
      example:
        id: 046b6c7f-0b8a-43b9-b35d-6489e6daee91
        projectId: 046b6c7f-0b8a-43b9-b35d-6489e6daee91
        trackedQueryId: 046b6c7f-0b8a-43b9-b35d-6489e6daee91
        engine: engine
        responseText: responseText
        citations:
        - url: url
          position: 1
          anchorText: anchorText
          title: title
          snippet: snippet
          publicationDate: publicationDate
          thumbnailUrl: thumbnailUrl
          domain: domain
          sourceName: sourceName
          unresolved: true
        - url: url
          position: 1
          anchorText: anchorText
          title: title
          snippet: snippet
          publicationDate: publicationDate
          thumbnailUrl: thumbnailUrl
          domain: domain
          sourceName: sourceName
          unresolved: true
        capturedAt: 2000-01-23T04:56:07.000+00:00
        modelName: modelName
        passIndex: 0
        passCount: 1
      properties:
        id:
          format: uuid
          type: string
        projectId:
          description: The project this capture belongs to
          format: uuid
          type: string
        trackedQueryId:
          description: The tracked query that was asked
          format: uuid
          type: string
        engine:
          description: The AI engine that answered
          type: string
        responseText:
          description: The full answer text as the engine produced it
          type: string
        citations:
          description: "Sources the engine cited, in the order it cited them"
          items:
            $ref: "#/components/schemas/CitationResource"
          type: array
        capturedAt:
          description: "When the answer was captured, UTC"
          format: date-time
          type: string
        modelName:
          description: "Engine-reported model, when it reported one. Absent on captures\
            \ taken before engines exposed it"
          nullable: true
          type: string
        passIndex:
          description: "Which sampling pass of the run this answer is, starting at\
            \ 0"
          minimum: 0
          type: integer
        passCount:
          description: How many passes that run took. Rows of one run share trackedQueryId
            and capturedAt
          minimum: 1
          type: integer
      required:
      - capturedAt
      - citations
      - engine
      - id
      - passCount
      - passIndex
      - projectId
      - responseText
      - trackedQueryId
      type: object
    SearchResultResource:
      description: One organic result of a captured search page
      example:
        position: 1
        url: url
        title: title
        snippet: snippet
        domain: domain
        rating: 6.0274563
        ratingVotes: 1
        unresolved: true
      properties:
        position:
          description: "Rank on the captured page, starting at 1"
          minimum: 1
          type: integer
        url:
          description: "The result URL as captured, redirector included when unresolved\
            \ is true"
          type: string
        title:
          description: "Result title, when the page showed one"
          nullable: true
          type: string
        snippet:
          description: "Result snippet, when the page showed one"
          nullable: true
          type: string
        domain:
          description: "Host of url. Belongs to the redirector, not the publisher,\
            \ when unresolved is true"
          nullable: true
          type: string
        rating:
          description: "Star rating shown in the rich result, when there was one"
          format: float
          nullable: true
          type: number
        ratingVotes:
          description: "Number of votes behind rating, when the rich result reported\
            \ one"
          nullable: true
          type: integer
        unresolved:
          description: "True when url still points at a redirector: count the result,\
            \ do not attribute the domain"
          type: boolean
      required:
      - position
      - unresolved
      - url
      type: object
    SearchSnapshotResource:
      description: One captured search-results page
      example:
        id: 046b6c7f-0b8a-43b9-b35d-6489e6daee91
        projectId: 046b6c7f-0b8a-43b9-b35d-6489e6daee91
        trackedQueryId: 046b6c7f-0b8a-43b9-b35d-6489e6daee91
        engine: engine
        results:
        - position: 1
          url: url
          title: title
          snippet: snippet
          domain: domain
          rating: 6.0274563
          ratingVotes: 1
          unresolved: true
        - position: 1
          url: url
          title: title
          snippet: snippet
          domain: domain
          rating: 6.0274563
          ratingVotes: 1
          unresolved: true
        capturedAt: 2000-01-23T04:56:07.000+00:00
      properties:
        id:
          format: uuid
          type: string
        projectId:
          description: The project this capture belongs to
          format: uuid
          type: string
        trackedQueryId:
          description: The tracked query that was searched
          format: uuid
          type: string
        engine:
          description: The search engine that was captured
          type: string
        results:
          description: Organic results in rank order
          items:
            $ref: "#/components/schemas/SearchResultResource"
          type: array
        capturedAt:
          description: "When the page was captured, UTC"
          format: date-time
          type: string
      required:
      - capturedAt
      - engine
      - id
      - projectId
      - results
      - trackedQueryId
      type: object
    ShoppingOfferResource:
      description: One offer of a captured shopping page
      example:
        rank: 1
        title: title
        productUrl: productUrl
        sellerName: sellerName
        sellerDomain: sellerDomain
        productId: productId
        thumbnailUrl: thumbnailUrl
        price: 6.0274563
        currency: currency
        rating: 1.4658129
        ratingVotes: 5
      properties:
        rank:
          description: "Rank on the captured page, starting at 1"
          minimum: 1
          type: integer
        title:
          description: Product title as the marketplace showed it
          type: string
        productUrl:
          description: Link to the offer
          type: string
        sellerName:
          description: Seller name as the marketplace showed it
          type: string
        sellerDomain:
          description: "Seller host, when the marketplace reported one"
          nullable: true
          type: string
        productId:
          description: "The marketplace's own identifier for this listing, not a Mencoro\
            \ id"
          type: string
        thumbnailUrl:
          description: "Product image the marketplace showed, when it showed one"
          nullable: true
          type: string
        price:
          description: "Price as shown at capture time: no tax normalisation, no shipping,\
            \ no conversion"
          format: float
          type: number
        currency:
          description: ISO-4217 currency of price. May differ between offers in one
            snapshot
          type: string
        rating:
          description: "Star rating shown for the offer, when there was one"
          format: float
          nullable: true
          type: number
        ratingVotes:
          description: "Number of votes behind rating, when the marketplace reported\
            \ one"
          nullable: true
          type: integer
      required:
      - currency
      - price
      - productId
      - productUrl
      - rank
      - sellerName
      - title
      type: object
    ShoppingSnapshotResource:
      description: One captured shopping-results page
      example:
        id: 046b6c7f-0b8a-43b9-b35d-6489e6daee91
        projectId: 046b6c7f-0b8a-43b9-b35d-6489e6daee91
        trackedQueryId: 046b6c7f-0b8a-43b9-b35d-6489e6daee91
        engine: engine
        offers:
        - rank: 1
          title: title
          productUrl: productUrl
          sellerName: sellerName
          sellerDomain: sellerDomain
          productId: productId
          thumbnailUrl: thumbnailUrl
          price: 6.0274563
          currency: currency
          rating: 1.4658129
          ratingVotes: 5
        - rank: 1
          title: title
          productUrl: productUrl
          sellerName: sellerName
          sellerDomain: sellerDomain
          productId: productId
          thumbnailUrl: thumbnailUrl
          price: 6.0274563
          currency: currency
          rating: 1.4658129
          ratingVotes: 5
        capturedAt: 2000-01-23T04:56:07.000+00:00
      properties:
        id:
          format: uuid
          type: string
        projectId:
          description: The project this capture belongs to
          format: uuid
          type: string
        trackedQueryId:
          description: The tracked query that was searched
          format: uuid
          type: string
        engine:
          description: The shopping surface that was captured
          type: string
        offers:
          description: Offers in rank order
          items:
            $ref: "#/components/schemas/ShoppingOfferResource"
          type: array
        capturedAt:
          description: "When the page was captured, UTC"
          format: date-time
          type: string
      required:
      - capturedAt
      - engine
      - id
      - offers
      - projectId
      - trackedQueryId
      type: object
    ApplyClusteringJobOutcome:
      description: Per-tracked-query results of applying a clustering job
      example:
        successful:
        - trackedQueryId: 046b6c7f-0b8a-43b9-b35d-6489e6daee91
          queryClusterIds:
          - 046b6c7f-0b8a-43b9-b35d-6489e6daee91
          - 046b6c7f-0b8a-43b9-b35d-6489e6daee91
        - trackedQueryId: 046b6c7f-0b8a-43b9-b35d-6489e6daee91
          queryClusterIds:
          - 046b6c7f-0b8a-43b9-b35d-6489e6daee91
          - 046b6c7f-0b8a-43b9-b35d-6489e6daee91
        failed:
        - trackedQueryId: 046b6c7f-0b8a-43b9-b35d-6489e6daee91
          errorCode: errorCode
          errorMessage: errorMessage
        - trackedQueryId: 046b6c7f-0b8a-43b9-b35d-6489e6daee91
          errorCode: errorCode
          errorMessage: errorMessage
        clusters:
        - id: 046b6c7f-0b8a-43b9-b35d-6489e6daee91
          name: name
          created: true
        - id: 046b6c7f-0b8a-43b9-b35d-6489e6daee91
          name: name
          created: true
        skippedClusters:
        - name: name
          reason: reason
        - name: name
          reason: reason
        unassigned:
        - 046b6c7f-0b8a-43b9-b35d-6489e6daee91
        - 046b6c7f-0b8a-43b9-b35d-6489e6daee91
      properties:
        successful:
          description: "Tracked queries written, each with the cluster ids it ended\
            \ up with - the resulting state, not the delta."
          items:
            $ref: "#/components/schemas/ApplyClusteringJobOutcome_successful_inner"
          type: array
        failed:
          description: "Always present, empty list included: read it rather than inferring\
            \ success from the status code."
          items:
            $ref: "#/components/schemas/ApplyClusteringJobOutcome_failed_inner"
          type: array
        clusters:
          description: "Every cluster the assignments referred to, and whether this\
            \ call created it."
          items:
            $ref: "#/components/schemas/ApplyClusteringJobOutcome_clusters_inner"
          type: array
        skippedClusters:
          description: Proposed names the store cannot hold. A tracked query whose
            proposed cluster was skipped ends up with fewer clusters than the proposal
            showed.
          items:
            $ref: "#/components/schemas/ApplyClusteringJobOutcome_skippedClusters_inner"
          type: array
        unassigned:
          description: "Tracked queries the job produced no assignment for. Nothing\
            \ was written for them and they keep the clusters they already had, under\
            \ every merge mode."
          items:
            format: uuid
            type: string
          type: array
      type: object
    BatchCreateQueryClustersRequestData:
      description: The cluster names a batch create asks for
      example:
        names:
        - names
        - names
        - names
        - names
        - names
      properties:
        names:
          description: "Cluster names to create, already trimmed and lower-cased on\
            \ the server. Duplicates are collapsed."
          items:
            maxLength: 200
            type: string
          maxItems: 100
          minItems: 1
          type: array
      required:
      - names
      type: object
    BatchCreateQueryClustersOutcome:
      description: Per-name results of creating clusters in bulk
      example:
        successful:
        - name: name
          id: 046b6c7f-0b8a-43b9-b35d-6489e6daee91
        - name: name
          id: 046b6c7f-0b8a-43b9-b35d-6489e6daee91
        failed:
        - name: name
          errorCode: errorCode
          errorMessage: errorMessage
        - name: name
          errorCode: errorCode
          errorMessage: errorMessage
      properties:
        successful:
          description: Clusters created by this call.
          items:
            $ref: "#/components/schemas/BatchCreateQueryClustersOutcome_successful_inner"
          type: array
        failed:
          description: "Always present, empty list included: read it rather than inferring\
            \ success from the status code."
          items:
            $ref: "#/components/schemas/BatchCreateQueryClustersOutcome_failed_inner"
          type: array
      type: object
    QueryClusterResource:
      description: A keyword cluster of a project
      example:
        id: 046b6c7f-0b8a-43b9-b35d-6489e6daee91
        projectId: 046b6c7f-0b8a-43b9-b35d-6489e6daee91
        name: name
      properties:
        id:
          description: Pass this value in the queryClusterIds filter of the analytics
            operations
          format: uuid
          type: string
        projectId:
          description: The project this cluster belongs to
          format: uuid
          type: string
        name:
          description: "Unique within the project, stored lower-cased"
          maxLength: 200
          type: string
      required:
      - id
      - name
      - projectId
      type: object
    StartClusteringJobRequestData:
      description: "What a clustering job should group, and how"
      example:
        trackedQueryIds:
        - 046b6c7f-0b8a-43b9-b35d-6489e6daee91
        - 046b6c7f-0b8a-43b9-b35d-6489e6daee91
        - 046b6c7f-0b8a-43b9-b35d-6489e6daee91
        - 046b6c7f-0b8a-43b9-b35d-6489e6daee91
        - 046b6c7f-0b8a-43b9-b35d-6489e6daee91
        mode: fill_gaps
        restrictToExistingClusters: false
      properties:
        trackedQueryIds:
          description: The tracked queries to cluster. Duplicates are collapsed.
          items:
            format: uuid
            type: string
          maxItems: 500
          minItems: 1
          type: array
        mode:
          description: fill_gaps groups only tracked queries that belong to no cluster;
            add_on_top adds the new clusters to whatever each query already has; full_regroup
            replaces the current clusters with the job's.
          enum:
          - fill_gaps
          - full_regroup
          - add_on_top
          type: string
        restrictToExistingClusters:
          default: false
          description: "When true the job may only use clusters the project already\
            \ has, and leaves a query ungrouped rather than inventing a name for it."
          type: boolean
      required:
      - mode
      - trackedQueryIds
      type: object
    AcceptedJobResource:
      description: Background work accepted for processing
      example:
        jobId: 046b6c7f-0b8a-43b9-b35d-6489e6daee91
        deduplicated: true
        trackingUrl: https://openapi-generator.tech
      properties:
        jobId:
          description: "The job to poll. May name a job started by an earlier, equivalent\
            \ request."
          format: uuid
          type: string
        deduplicated:
          description: "Whether this call started the job, or joined one that was\
            \ already running"
          type: boolean
        trackingUrl:
          description: Poll this until the job reaches a terminal status
          format: uri
          type: string
      required:
      - deduplicated
      - jobId
      - trackingUrl
      type: object
    InvitationResource:
      description: An invitation to join an organization
      example:
        id: 046b6c7f-0b8a-43b9-b35d-6489e6daee91
        email: email
        role: owner
        state: pending
        createdAt: 2000-01-23T04:56:07.000+00:00
        expiresAt: 2000-01-23T04:56:07.000+00:00
        acceptedAt: 2000-01-23T04:56:07.000+00:00
      properties:
        id:
          format: uuid
          type: string
        email:
          format: email
          type: string
        role:
          enum:
          - owner
          - manager
          - viewer
          type: string
        state:
          enum:
          - pending
          - accepted
          - rejected
          - expired
          - cancelled
          type: string
        createdAt:
          format: date-time
          type: string
        expiresAt:
          format: date-time
          type: string
        acceptedAt:
          format: date-time
          nullable: true
          type: string
      required:
      - createdAt
      - email
      - expiresAt
      - id
      - role
      - state
      type: object
    AsyncJobResource:
      description: "An asynchronous job and, once it has completed, its result"
      example:
        jobId: 046b6c7f-0b8a-43b9-b35d-6489e6daee91
        type: geo_prompts
        status: pending
        result: "{}"
      properties:
        jobId:
          format: uuid
          type: string
        type:
          description: "What the job produces, and therefore the shape of `result`"
          enum:
          - geo_prompts
          - keywords
          - query_clustering
          - brand_discovery
          - brand_name_suggestion
          type: string
        status:
          description: "A job in `pending`, `running` or `awaiting_retry` is still\
            \ in flight; `completed` and `failed` are terminal."
          enum:
          - pending
          - running
          - awaiting_retry
          - completed
          - failed
          type: string
        result:
          description: "The job output, shaped by `type`. Null while the job is still\
            \ in flight and for a job that failed: it means the result is not known,\
            \ never that the job produced nothing."
          nullable: true
          type: object
      required:
      - jobId
      - status
      - type
      type: object
    KeywordListingResource:
      description: "One keyword of a project, its variants and its metrics over the\
        \ requested window"
      example:
        keyword: keyword
        keywordNormalized: keywordNormalized
        variantCount: 0
        variantIds:
        - 046b6c7f-0b8a-43b9-b35d-6489e6daee91
        - 046b6c7f-0b8a-43b9-b35d-6489e6daee91
        engines:
        - chatgpt
        - chatgpt
        countries:
        - countries
        - countries
        queryClusterIds:
        - 046b6c7f-0b8a-43b9-b35d-6489e6daee91
        - 046b6c7f-0b8a-43b9-b35d-6489e6daee91
        hasUnclusteredVariant: true
        statusSummary: active
        statuses:
        - active
        - active
        checkFrequencies:
        - daily
        - daily
        nPassesValues:
        - 6
        - 6
        lastCheckedAt: 2000-01-23T04:56:07.000+00:00
        avgSerpPosition: 1.4658129
        trendSerp: 5.962134
        avgShoppingPosition: 5.637377
        trendShopping: 2.302136
        avgMentionPosition: 7.0614014
        trendMention: 9.301444
        avgLinkPosition: 3.6160767
        trendLink: 2.027123
        mentionPositionStability: 4.145608
        trendStability: 7.386282
        positivityIndex: 1
        trendPositivity: 1
        mentionRate: 1
        trendMentionRate: 6
        serpRate: 7
        trendSerpRate: 1
        shareOfVoice: 4.9652185
        trendShareOfVoice: 5.025005
        sentimentPositive: 9
        sentimentNeutral: 9
        sentimentNegative: 6
        avgMentionCount: 8.762042
        mentionTypeCounts:
          recommendation: 9
          comparison: 6
          listing: 3
          example: 6
          reference: 1
      properties:
        keyword:
          description: "The keyword as a human types it, picked from the variants\
            \ for display."
          type: string
        keywordNormalized:
          description: "The grouping key: the keyword lower-cased and unaccented.\
            \ Unique within the project, and the stable way to match a row across\
            \ pages."
          type: string
        variantCount:
          description: Tracked queries behind this row that the current filters selected.
          type: integer
        variantIds:
          description: Ids of those tracked queries. Accepted by the single tracked-query
            operation.
          items:
            format: uuid
            type: string
          type: array
        engines:
          description: "Distinct engines across the variants, not a single engine."
          items:
            enum:
            - chatgpt
            - perplexity
            - google_ai_overview
            - google_ai_mode
            - google_serp
            - google_shopping
            type: string
          type: array
        countries:
          description: Distinct ISO-3166 alpha-2 countries across the variants.
          items:
            type: string
          type: array
        queryClusterIds:
          description: Distinct keyword clusters the variants belong to. Empty when
            none of them is clustered.
          items:
            format: uuid
            type: string
          type: array
        hasUnclusteredVariant:
          description: Whether at least one variant belongs to no cluster.
          type: boolean
        statusSummary:
          description: '"mixed" when the variants disagree; otherwise the one status
            they share.'
          enum:
          - active
          - paused
          - mixed
          type: string
        statuses:
          description: Distinct statuses across the variants.
          items:
            enum:
            - active
            - paused
            type: string
          type: array
        checkFrequencies:
          description: Distinct check frequencies across the variants.
          items:
            enum:
            - daily
            - weekly
            - monthly
            type: string
          type: array
        nPassesValues:
          description: Distinct pass counts across the variants. A pass is one budget
            unit per check.
          items:
            type: integer
          type: array
        lastCheckedAt:
          description: The most recent completed check across the variants. Null when
            none has ever completed — not a check that found nothing.
          format: date-time
          nullable: true
          type: string
        avgSerpPosition:
          description: "Average position in traditional search over the window. 1-based,\
            \ LOWER is better."
          format: float
          nullable: true
          type: number
        trendSerp:
          description: Signed improvement in avgSerpPosition against the previous
            window. Positive is better.
          format: float
          nullable: true
          type: number
        avgShoppingPosition:
          description: "Average position in shopping results over the window. 1-based,\
            \ LOWER is better."
          format: float
          nullable: true
          type: number
        trendShopping:
          description: Signed improvement in avgShoppingPosition. Positive is better.
          format: float
          nullable: true
          type: number
        avgMentionPosition:
          description: "Average position of the brand mention inside the AI answer.\
            \ 1-based, LOWER is better."
          format: float
          nullable: true
          type: number
        trendMention:
          description: Signed improvement in avgMentionPosition. Positive is better.
          format: float
          nullable: true
          type: number
        avgLinkPosition:
          description: "Average position of a cited link to the brand. 1-based, LOWER\
            \ is better."
          format: float
          nullable: true
          type: number
        trendLink:
          description: Signed improvement in avgLinkPosition. Positive is better.
          format: float
          nullable: true
          type: number
        mentionPositionStability:
          description: "Day-to-day spread of the mention position (a standard deviation):\
            \ LOWER means steadier."
          format: float
          nullable: true
          type: number
        trendStability:
          description: Signed improvement in mentionPositionStability. Positive means
            steadier than the previous window.
          format: float
          nullable: true
          type: number
        positivityIndex:
          description: 0-100 weighted sentiment score of the mentions; HIGHER is better.
            Null when there were no mentions to score.
          nullable: true
          type: integer
        trendPositivity:
          description: Signed improvement in positivityIndex. Positive is better.
          nullable: true
          type: integer
        mentionRate:
          description: 0-100 share of AI captures in the window that mentioned the
            brand; HIGHER is better.
          nullable: true
          type: integer
        trendMentionRate:
          description: Signed improvement in mentionRate. Positive is better.
          nullable: true
          type: integer
        serpRate:
          description: 0-100 share of traditional-search captures in the window that
            ranked the brand; HIGHER is better.
          nullable: true
          type: integer
        trendSerpRate:
          description: Signed improvement in serpRate. Positive is better.
          nullable: true
          type: integer
        shareOfVoice:
          description: 0-100 share of the weighted AI mentions against every tracked
            brand; HIGHER is better.
          format: float
          nullable: true
          type: number
        trendShareOfVoice:
          description: Signed improvement in shareOfVoice. Positive is better.
          format: float
          nullable: true
          type: number
        sentimentPositive:
          description: Positive brand mentions counted in the window. Zero here really
            is zero.
          type: integer
        sentimentNeutral:
          description: Neutral brand mentions counted in the window.
          type: integer
        sentimentNegative:
          description: Negative brand mentions counted in the window.
          type: integer
        avgMentionCount:
          description: "Mentions per AI engine per day with a capture, over the window.\
            \ Not a total: the window total is not part of this contract."
          format: float
          nullable: true
          type: number
        mentionTypeCounts:
          $ref: "#/components/schemas/KeywordListingResource_mentionTypeCounts"
      required:
      - checkFrequencies
      - countries
      - engines
      - hasUnclusteredVariant
      - keyword
      - keywordNormalized
      - mentionTypeCounts
      - nPassesValues
      - queryClusterIds
      - sentimentNegative
      - sentimentNeutral
      - sentimentPositive
      - statusSummary
      - statuses
      - variantCount
      - variantIds
      type: object
    MeResource:
      description: The authenticated user and the API key the request was made with
      example:
        userId: 046b6c7f-0b8a-43b9-b35d-6489e6daee91
        fullName: fullName
        email: email
        language: en
        createdAt: 2000-01-23T04:56:07.000+00:00
        apiKeyId: 046b6c7f-0b8a-43b9-b35d-6489e6daee91
        capabilities:
        - read
        - read
        scopeMode: selected
        organizationIds:
        - 046b6c7f-0b8a-43b9-b35d-6489e6daee91
        - 046b6c7f-0b8a-43b9-b35d-6489e6daee91
      properties:
        userId:
          format: uuid
          type: string
        fullName:
          type: string
        email:
          format: email
          type: string
        language:
          description: IETF language tag the user reads the product in
          example: en
          type: string
        createdAt:
          format: date-time
          type: string
        apiKeyId:
          description: The API key this request authenticated with
          format: uuid
          type: string
        capabilities:
          items:
            enum:
            - read
            - write
            - organization:manage
            type: string
          type: array
        scopeMode:
          enum:
          - selected
          - all_organizations
          type: string
        organizationIds:
          description: "Organizations the key names. Empty for a key scoped to all\
            \ organizations, which instead follows the owner's membership."
          items:
            format: uuid
            type: string
          type: array
      required:
      - apiKeyId
      - capabilities
      - createdAt
      - email
      - fullName
      - language
      - organizationIds
      - scopeMode
      - userId
      type: object
    MemberResource:
      description: A membership of an organization
      example:
        id: 046b6c7f-0b8a-43b9-b35d-6489e6daee91
        userId: 046b6c7f-0b8a-43b9-b35d-6489e6daee91
        role: owner
        state: active
        isActive: true
        joinedAt: 2000-01-23T04:56:07.000+00:00
      properties:
        id:
          format: uuid
          type: string
        userId:
          description: The user this membership belongs to
          format: uuid
          type: string
        role:
          enum:
          - owner
          - manager
          - viewer
          type: string
        state:
          enum:
          - active
          - suspended
          type: string
        isActive:
          type: boolean
        joinedAt:
          format: date-time
          type: string
      required:
      - id
      - isActive
      - joinedAt
      - role
      - state
      - userId
      type: object
    OrganizationResource:
      description: An organization the caller is a member of
      example:
        id: 046b6c7f-0b8a-43b9-b35d-6489e6daee91
        name: name
        description: description
        status: active
        imageUrl: https://openapi-generator.tech
        contactEmail: contactEmail
        role: owner
        createdAt: 2000-01-23T04:56:07.000+00:00
      properties:
        id:
          format: uuid
          type: string
        name:
          type: string
        description:
          nullable: true
          type: string
        status:
          enum:
          - active
          - archived
          type: string
        imageUrl:
          format: uri
          nullable: true
          type: string
        contactEmail:
          format: email
          nullable: true
          type: string
        role:
          description: The caller's current role in this organization
          enum:
          - owner
          - manager
          - viewer
          type: string
        createdAt:
          format: date-time
          type: string
      required:
      - createdAt
      - id
      - name
      - role
      - status
      type: object
    OperationEffect:
      description: "A single declared change, side effect, warning or condition"
      example:
        code: projects_will_be_archived
        summary: summary
        targets:
        - targets
        - targets
      properties:
        code:
          description: Stable machine-readable identifier for this effect
          example: projects_will_be_archived
          type: string
        summary:
          description: "Human-readable description, safe to show to whoever must approve\
            \ the operation"
          type: string
        targets:
          default: []
          items:
            type: string
          type: array
      required:
      - code
      - summary
      type: object
    ProjectDetailResource:
      description: A project and the brand monitoring configuration its checks run
        against
      example:
        id: 046b6c7f-0b8a-43b9-b35d-6489e6daee91
        organizationId: 046b6c7f-0b8a-43b9-b35d-6489e6daee91
        name: name
        status: active
        createdAt: 2000-01-23T04:56:07.000+00:00
        websiteDomains:
        - websiteDomains
        - websiteDomains
        brandNames:
        - brandNames
        - brandNames
        competitors:
        - id: 046b6c7f-0b8a-43b9-b35d-6489e6daee91
          name: name
          websiteDomains:
          - websiteDomains
          - websiteDomains
          brandNames:
          - brandNames
          - brandNames
        - id: 046b6c7f-0b8a-43b9-b35d-6489e6daee91
          name: name
          websiteDomains:
          - websiteDomains
          - websiteDomains
          brandNames:
          - brandNames
          - brandNames
      properties:
        id:
          format: uuid
          type: string
        organizationId:
          format: uuid
          type: string
        name:
          type: string
        status:
          enum:
          - active
          - archived
          type: string
        createdAt:
          format: date-time
          type: string
        websiteDomains:
          description: Domains the project is monitored for. Empty when the project
            has no brand monitoring profile yet.
          items:
            type: string
          type: array
        brandNames:
          description: Brand names matched in AI answers and search results. Empty
            when the project has no brand monitoring profile yet.
          items:
            type: string
          type: array
        competitors:
          description: Competitors this project is measured against. Empty when none
            are configured.
          items:
            $ref: "#/components/schemas/ProjectDetailResource_competitors_inner"
          type: array
      required:
      - brandNames
      - competitors
      - createdAt
      - id
      - name
      - organizationId
      - status
      - websiteDomains
      type: object
    CompetitorResource:
      description: A competitor tracked by a project
      example:
        id: 046b6c7f-0b8a-43b9-b35d-6489e6daee91
        name: name
        websiteDomains:
        - websiteDomains
        - websiteDomains
        brandNames:
        - brandNames
        - brandNames
      properties:
        id:
          format: uuid
          type: string
        name:
          description: The competitor's display name
          type: string
        websiteDomains:
          description: Domains a result is matched against for this competitor
          items:
            type: string
          type: array
        brandNames:
          description: Names a mention is matched against for this competitor
          items:
            type: string
          type: array
      required:
      - brandNames
      - id
      - name
      - websiteDomains
      type: object
    ProjectResource:
      description: A project and its headline metrics
      example:
        id: 046b6c7f-0b8a-43b9-b35d-6489e6daee91
        name: name
        status: active
        createdAt: 2000-01-23T04:56:07.000+00:00
        trackedQueryCount: 0
        avgSerpPosition: 6.0274563
        avgShoppingPosition: 1.4658129
        avgMentionPosition: 5.962134
        avgLinkPosition: 5.637377
        mentionRate: 2
        serpRate: 7
        shoppingRate: 9
        positivityIndex: 3
        shareOfVoice: 2.027123
        serpPositionStability: 4.145608
        shoppingPositionStability: 7.386282
        lastRankDetectedAt: 2000-01-23T04:56:07.000+00:00
      properties:
        id:
          format: uuid
          type: string
        name:
          type: string
        status:
          enum:
          - active
          - archived
          type: string
        createdAt:
          format: date-time
          type: string
        trackedQueryCount:
          description: Number of tracked queries in the project
          type: integer
        avgSerpPosition:
          description: Average position in traditional search results. Null when unknown.
          format: float
          nullable: true
          type: number
        avgShoppingPosition:
          description: Average position in shopping results. Null when unknown.
          format: float
          nullable: true
          type: number
        avgMentionPosition:
          description: Average position of the brand mention inside AI answers. Null
            when unknown.
          format: float
          nullable: true
          type: number
        avgLinkPosition:
          description: Average position of a cited link to the brand. Null when unknown.
          format: float
          nullable: true
          type: number
        mentionRate:
          description: Share of checks where the brand was mentioned. Null when unknown.
          nullable: true
          type: integer
        serpRate:
          nullable: true
          type: integer
        shoppingRate:
          nullable: true
          type: integer
        positivityIndex:
          description: Sentiment balance of the brand's mentions. Null when unknown.
          nullable: true
          type: integer
        shareOfVoice:
          description: Share of voice against the tracked competitors. Null when unknown.
          format: float
          nullable: true
          type: number
        serpPositionStability:
          format: float
          nullable: true
          type: number
        shoppingPositionStability:
          format: float
          nullable: true
          type: number
        lastRankDetectedAt:
          description: When a rank was last detected for this project.
          format: date-time
          nullable: true
          type: string
      required:
      - createdAt
      - id
      - name
      - status
      - trackedQueryCount
      type: object
    BrandProfileResource:
      description: The brand identity a project's checks are matched against
      example:
        projectId: 046b6c7f-0b8a-43b9-b35d-6489e6daee91
        brandNames:
        - brandNames
        - brandNames
        websiteDomains:
        - websiteDomains
        - websiteDomains
        description: description
      properties:
        projectId:
          description: The project this profile belongs to
          format: uuid
          type: string
        brandNames:
          description: The brand terms a mention in an AI answer is matched against.
            Empty when none has been configured yet.
          items:
            type: string
          type: array
        websiteDomains:
          description: The domains a cited link or a search result is matched against.
            Empty when none has been configured yet.
          items:
            type: string
          type: array
        description:
          description: "What the brand does, as generated from its names and domains.\
            \ Null means it has not been generated yet, which is not the same as an\
            \ empty description."
          nullable: true
          type: string
      required:
      - brandNames
      - projectId
      - websiteDomains
      type: object
    ClusterMembershipRequestData:
      description: The clusters a tracked query is added to or removed from
      example:
        queryClusterIds:
        - 046b6c7f-0b8a-43b9-b35d-6489e6daee91
        - 046b6c7f-0b8a-43b9-b35d-6489e6daee91
        - 046b6c7f-0b8a-43b9-b35d-6489e6daee91
        - 046b6c7f-0b8a-43b9-b35d-6489e6daee91
        - 046b6c7f-0b8a-43b9-b35d-6489e6daee91
      properties:
        queryClusterIds:
          description: Ids of the query clusters. Duplicates are collapsed. Every
            id must belong to the project in the path.
          items:
            format: uuid
            type: string
          maxItems: 100
          minItems: 1
          type: array
      required:
      - queryClusterIds
      type: object
    TrackedQueryDetailResource:
      description: A tracked query and how it is checked
      example:
        id: 046b6c7f-0b8a-43b9-b35d-6489e6daee91
        projectId: 046b6c7f-0b8a-43b9-b35d-6489e6daee91
        queryText: queryText
        engine: chatgpt
        locale: locale
        country: country
        queryClusterIds:
        - 046b6c7f-0b8a-43b9-b35d-6489e6daee91
        - 046b6c7f-0b8a-43b9-b35d-6489e6daee91
        status: active
        checkFrequency: daily
        nPasses: 1
        lastCheckedAt: 2000-01-23T04:56:07.000+00:00
      properties:
        id:
          format: uuid
          type: string
        projectId:
          description: The project this tracked query belongs to
          format: uuid
          type: string
        queryText:
          description: The prompt or keyword sent to the engine
          type: string
        engine:
          enum:
          - chatgpt
          - perplexity
          - google_ai_overview
          - google_ai_mode
          - google_serp
          - google_shopping
          type: string
        locale:
          description: Language tag the query is asked in. Null when the engine is
            asked without one.
          nullable: true
          type: string
        country:
          description: ISO-3166 alpha-2 country the query is asked from
          type: string
        queryClusterIds:
          description: Clusters (groups) this query belongs to. Empty when it is ungrouped.
          items:
            format: uuid
            type: string
          type: array
        status:
          enum:
          - active
          - paused
          type: string
        checkFrequency:
          description: How often the query is checked while active
          enum:
          - daily
          - weekly
          - monthly
          type: string
        nPasses:
          description: Passes run per check. Greater than 1 only for AI engines.
          minimum: 1
          type: integer
        lastCheckedAt:
          description: "When a check last completed. Null means no check has completed\
            \ yet, which is not the same as a check that found nothing."
          format: date-time
          nullable: true
          type: string
      required:
      - checkFrequency
      - country
      - engine
      - id
      - nPasses
      - projectId
      - queryClusterIds
      - queryText
      - status
      type: object
    TrackedQueryResource:
      description: A tracked query and the metrics of its most recent check
      example:
        id: 046b6c7f-0b8a-43b9-b35d-6489e6daee91
        queryText: queryText
        engine: chatgpt
        country: country
        status: active
        queryClusterIds:
        - 046b6c7f-0b8a-43b9-b35d-6489e6daee91
        - 046b6c7f-0b8a-43b9-b35d-6489e6daee91
        checkFrequency: daily
        nPasses: 0
        lastSerpPosition: 6
        lastMentionPosition: 1
        lastLinkPosition: 5
        lastShoppingPosition: 5
        lastShareOfVoice: 2.302136
        lastPositivityIndex: 7
        lastPositiveMentionCount: 9
        lastNeutralMentionCount: 3
        lastNegativeMentionCount: 2
        lastCheckedAt: 2000-01-23T04:56:07.000+00:00
      properties:
        id:
          format: uuid
          type: string
        queryText:
          description: The keyword or prompt being tracked
          type: string
        engine:
          enum:
          - chatgpt
          - perplexity
          - google_ai_overview
          - google_ai_mode
          - google_serp
          - google_shopping
          type: string
        country:
          description: ISO-3166 alpha-2 country code the query is tracked in
          type: string
        status:
          enum:
          - active
          - paused
          type: string
        queryClusterIds:
          description: Ids of the keyword clusters this query belongs to
          items:
            format: uuid
            type: string
          type: array
        checkFrequency:
          description: How often the query is checked
          enum:
          - daily
          - weekly
          - monthly
          type: string
        nPasses:
          description: How many times the query is asked per check
          type: integer
        lastSerpPosition:
          description: "Position in traditional search results at the last check.\
            \ 1-based, LOWER is better. Null when not known yet."
          nullable: true
          type: integer
        lastMentionPosition:
          description: "Position of the brand mention inside the AI answer at the\
            \ last check. 1-based, LOWER is better. Null when not known yet."
          nullable: true
          type: integer
        lastLinkPosition:
          description: "Position of a cited link to the brand at the last check. 1-based,\
            \ LOWER is better. Null when not known yet."
          nullable: true
          type: integer
        lastShoppingPosition:
          description: "Position in shopping results at the last check. 1-based, LOWER\
            \ is better. Null when not known yet."
          nullable: true
          type: integer
        lastShareOfVoice:
          description: "0-100 share of the weighted AI mentions against every tracked\
            \ brand; HIGHER is better. Null when not known yet, which is not a share\
            \ of zero."
          format: float
          nullable: true
          type: number
        lastPositivityIndex:
          description: "0-100 sentiment score of the brand mentions; HIGHER is better.\
            \ Null when there were no mentions to score, which is not a score of zero."
          nullable: true
          type: integer
        lastPositiveMentionCount:
          description: Positive brand mentions at the last check. Null when not known
            yet.
          nullable: true
          type: integer
        lastNeutralMentionCount:
          description: Neutral brand mentions at the last check. Null when not known
            yet.
          nullable: true
          type: integer
        lastNegativeMentionCount:
          description: Negative brand mentions at the last check. Null when not known
            yet.
          nullable: true
          type: integer
        lastCheckedAt:
          description: When the query was last checked. Null when it never has been.
          format: date-time
          nullable: true
          type: string
      required:
      - checkFrequency
      - country
      - engine
      - id
      - nPasses
      - queryClusterIds
      - queryText
      - status
      type: object
    BatchCreateTrackedQueriesRequestData:
      description: "Tracked queries to create, as a cross product of texts, engines\
        \ and countries"
      example:
        queryTexts:
        - queryTexts
        - queryTexts
        engines:
        - chatgpt
        - chatgpt
        countries:
        - countries
        - countries
        locale: locale
        checkFrequency: daily
        nPasses: 1
        queryClusterIds:
        - 046b6c7f-0b8a-43b9-b35d-6489e6daee91
        - 046b6c7f-0b8a-43b9-b35d-6489e6daee91
      properties:
        queryTexts:
          description: Prompts or keywords to track. Stored lower-cased with whitespace
            collapsed and leading list markers removed.
          items:
            maxLength: 500
            type: string
          minItems: 1
          type: array
        engines:
          description: Engines each text is asked in.
          items:
            enum:
            - chatgpt
            - perplexity
            - google_ai_overview
            - google_ai_mode
            - google_serp
            - google_shopping
            type: string
          minItems: 1
          type: array
        countries:
          description: ISO 3166-1 alpha-2 countries each text is asked from.
          items:
            maxLength: 2
            minLength: 2
            type: string
          minItems: 1
          type: array
        locale:
          description: ISO 639-1 language the queries are asked in. Omit it to ask
            the engine without a language.
          maxLength: 2
          minLength: 2
          nullable: true
          type: string
        checkFrequency:
          description: How often every created query is checked.
          enum:
          - daily
          - weekly
          - monthly
          type: string
        nPasses:
          description: "Passes run per check. Applied to AI-engine combinations only:\
            \ a non-AI engine is always created with 1, whatever is sent here."
          maximum: 10
          minimum: 1
          type: integer
        queryClusterIds:
          description: Clusters every created query joins. Must already exist in the
            project. A combination that is already tracked has these clusters merged
            into it.
          items:
            format: uuid
            type: string
          type: array
      required:
      - checkFrequency
      - countries
      - engines
      - nPasses
      - queryClusterIds
      - queryTexts
      type: object
    BatchCreateTrackedQueriesResultResource:
      description: Per-combination results of creating tracked queries in bulk
      example:
        successful:
        - id: 046b6c7f-0b8a-43b9-b35d-6489e6daee91
          queryText: queryText
          engine: engine
          country: country
        - id: 046b6c7f-0b8a-43b9-b35d-6489e6daee91
          queryText: queryText
          engine: engine
          country: country
        failed:
        - queryText: queryText
          engine: engine
          country: country
          errorCode: errorCode
          errorMessage: errorMessage
        - queryText: queryText
          engine: engine
          country: country
          errorCode: errorCode
          errorMessage: errorMessage
      properties:
        successful:
          description: "Tracked queries created by this call, one entry per query\
            \ text, engine and country combination."
          items:
            $ref: "#/components/schemas/BatchCreateTrackedQueriesResultResource_successful_inner"
          type: array
        failed:
          description: "Always present, empty list included: read it rather than inferring\
            \ success from the status code."
          items:
            $ref: "#/components/schemas/BatchCreateTrackedQueriesResultResource_failed_inner"
          type: array
      type: object
    ChangeTrackedQueryCheckFrequencyRequestData:
      description: How often a tracked query is checked
      example:
        checkFrequency: daily
      properties:
        checkFrequency:
          description: How often the query is checked while it is active.
          enum:
          - daily
          - weekly
          - monthly
          type: string
      required:
      - checkFrequency
      type: object
    ChangeTrackedQueryPassesRequestData:
      description: How many passes a tracked query runs per check
      example:
        nPasses: 1
      properties:
        nPasses:
          description: Passes run per check. Only an AI engine accepts more than one.
          maximum: 10
          minimum: 1
          type: integer
      required:
      - nPasses
      type: object
    AiResponseReportResource:
      description: Acknowledgement of a report about a captured AI answer
      example:
        aiResponseId: 046b6c7f-0b8a-43b9-b35d-6489e6daee91
        trackedQueryId: 046b6c7f-0b8a-43b9-b35d-6489e6daee91
        type: missed_mention
        comment: comment
        missedBrandNames:
        - missedBrandNames
        - missedBrandNames
        accepted: true
      properties:
        aiResponseId:
          description: The captured AI answer the report is about
          format: uuid
          type: string
        trackedQueryId:
          description: The tracked query that answer was captured for
          format: uuid
          type: string
        type:
          description: The kind of problem reported
          enum:
          - missed_mention
          - other
          type: string
        comment:
          description: The note as stored. An empty or whitespace-only comment is
            stored as null.
          nullable: true
          type: string
        missedBrandNames:
          description: "Brand names reported as missed, de-duplicated and trimmed."
          items:
            type: string
          type: array
        accepted:
          description: "True once the report has been accepted for review. It is never\
            \ false: a report that could not be delivered answers with an error instead."
          type: boolean
      required:
      - accepted
      - aiResponseId
      - missedBrandNames
      - trackedQueryId
      - type
      type: object
    TrackedQueryUsageResource:
      description: How many tracked queries an organization has configured
      example:
        trackedQueryCount: 124
      properties:
        trackedQueryCount:
          description: "Tracked queries across every project of the organization,\
            \ archived projects included, counting active and paused queries alike.\
            \ Never null; 0 means none are configured."
          example: 124
          minimum: 0
          type: integer
      required:
      - trackedQueryCount
      type: object
    ProjectedMonthlyChecksResource:
      description: What an organization's current tracking configuration would consume
        in a month
      example:
        projectedMonthlyChecks: 720
        activeTrackedQueryCount: 24
      properties:
        projectedMonthlyChecks:
          description: "Checks a month of the current configuration would consume:\
            \ for each ACTIVE tracked query, its runs per month (daily 30, weekly\
            \ 4, monthly 1) multiplied by its nPasses, summed. Never null."
          example: 720
          minimum: 0
          type: integer
        activeTrackedQueryCount:
          description: "Active tracked queries the projection was summed over, across\
            \ every project of the organization, archived projects included. Paused\
            \ queries are excluded from both figures. Never null."
          example: 24
          minimum: 0
          type: integer
      required:
      - activeTrackedQueryCount
      - projectedMonthlyChecks
      type: object
    batchPauseTrackedQueries_200_response_successful_inner:
      example:
        id: 046b6c7f-0b8a-43b9-b35d-6489e6daee91
      properties:
        id:
          format: uuid
          type: string
      type: object
    batchPauseTrackedQueries_200_response_failed_inner:
      example:
        id: 046b6c7f-0b8a-43b9-b35d-6489e6daee91
        errorCode: tracked_query_not_found
        errorMessage: errorMessage
      properties:
        id:
          format: uuid
          type: string
        errorCode:
          example: tracked_query_not_found
          type: string
        errorMessage:
          type: string
      type: object
    batchPauseTrackedQueries_200_response:
      example:
        successful:
        - id: 046b6c7f-0b8a-43b9-b35d-6489e6daee91
        - id: 046b6c7f-0b8a-43b9-b35d-6489e6daee91
        failed:
        - id: 046b6c7f-0b8a-43b9-b35d-6489e6daee91
          errorCode: tracked_query_not_found
          errorMessage: errorMessage
        - id: 046b6c7f-0b8a-43b9-b35d-6489e6daee91
          errorCode: tracked_query_not_found
          errorMessage: errorMessage
      properties:
        successful:
          description: "Tracked queries that are now paused, including ones that already\
            \ were."
          items:
            $ref: "#/components/schemas/batchPauseTrackedQueries_200_response_successful_inner"
          type: array
        failed:
          description: "Tracked queries left untouched, each with the domain error\
            \ code a single-item call would have returned."
          items:
            $ref: "#/components/schemas/batchPauseTrackedQueries_200_response_failed_inner"
          type: array
      type: object
    batchResumeTrackedQueries_200_response:
      example:
        successful:
        - id: 046b6c7f-0b8a-43b9-b35d-6489e6daee91
        - id: 046b6c7f-0b8a-43b9-b35d-6489e6daee91
        failed:
        - id: 046b6c7f-0b8a-43b9-b35d-6489e6daee91
          errorCode: tracked_query_not_found
          errorMessage: errorMessage
        - id: 046b6c7f-0b8a-43b9-b35d-6489e6daee91
          errorCode: tracked_query_not_found
          errorMessage: errorMessage
      properties:
        successful:
          description: "Tracked queries that are now active, including ones that already\
            \ were."
          items:
            $ref: "#/components/schemas/batchPauseTrackedQueries_200_response_successful_inner"
          type: array
        failed:
          description: "Tracked queries left untouched, each with the domain error\
            \ code a single-item call would have returned."
          items:
            $ref: "#/components/schemas/batchPauseTrackedQueries_200_response_failed_inner"
          type: array
      type: object
    bulkAddClustersToTrackedQueries_request:
      properties:
        ids:
          description: Tracked queries to add. At most 100 distinct ids; duplicates
            are collapsed.
          items:
            format: uuid
            type: string
          maxItems: 100
          minItems: 1
          type: array
        queryClusterIds:
          description: Clusters every named tracked query is added to. All must belong
            to the project in the path.
          items:
            format: uuid
            type: string
          maxItems: 100
          minItems: 1
          type: array
      type: object
    bulkAddClustersToTrackedQueries_200_response:
      example:
        successful:
        - id: 046b6c7f-0b8a-43b9-b35d-6489e6daee91
        - id: 046b6c7f-0b8a-43b9-b35d-6489e6daee91
        failed:
        - id: 046b6c7f-0b8a-43b9-b35d-6489e6daee91
          errorCode: tracked_query_not_found
          errorMessage: errorMessage
        - id: 046b6c7f-0b8a-43b9-b35d-6489e6daee91
          errorCode: tracked_query_not_found
          errorMessage: errorMessage
      properties:
        successful:
          description: Tracked queries that were added to every named cluster.
          items:
            $ref: "#/components/schemas/batchPauseTrackedQueries_200_response_successful_inner"
          type: array
        failed:
          description: "Tracked queries left untouched, each with the domain error\
            \ code a single-item call would have returned."
          items:
            $ref: "#/components/schemas/batchPauseTrackedQueries_200_response_failed_inner"
          type: array
      type: object
    bulkRemoveClustersFromTrackedQueries_request:
      properties:
        ids:
          description: Tracked queries to remove. At most 100 distinct ids; duplicates
            are collapsed.
          items:
            format: uuid
            type: string
          maxItems: 100
          minItems: 1
          type: array
        queryClusterIds:
          description: Clusters every named tracked query is removed from. All must
            belong to the project in the path.
          items:
            format: uuid
            type: string
          maxItems: 100
          minItems: 1
          type: array
      type: object
    bulkRemoveClustersFromTrackedQueries_200_response:
      example:
        successful:
        - id: 046b6c7f-0b8a-43b9-b35d-6489e6daee91
        - id: 046b6c7f-0b8a-43b9-b35d-6489e6daee91
        failed:
        - id: 046b6c7f-0b8a-43b9-b35d-6489e6daee91
          errorCode: tracked_query_not_found
          errorMessage: errorMessage
        - id: 046b6c7f-0b8a-43b9-b35d-6489e6daee91
          errorCode: tracked_query_not_found
          errorMessage: errorMessage
      properties:
        successful:
          description: Tracked queries that were removed from every named cluster.
          items:
            $ref: "#/components/schemas/batchPauseTrackedQueries_200_response_successful_inner"
          type: array
        failed:
          description: "Tracked queries left untouched, each with the domain error\
            \ code a single-item call would have returned."
          items:
            $ref: "#/components/schemas/batchPauseTrackedQueries_200_response_failed_inner"
          type: array
      type: object
    batchForceCheckTrackedQueries_200_response_failed_inner:
      example:
        id: 046b6c7f-0b8a-43b9-b35d-6489e6daee91
        errorCode: check_budget_forecast_exhausted
        errorMessage: errorMessage
      properties:
        id:
          format: uuid
          type: string
        errorCode:
          example: check_budget_forecast_exhausted
          type: string
        errorMessage:
          type: string
      type: object
    batchForceCheckTrackedQueries_200_response:
      example:
        successful:
        - id: 046b6c7f-0b8a-43b9-b35d-6489e6daee91
        - id: 046b6c7f-0b8a-43b9-b35d-6489e6daee91
        failed:
        - id: 046b6c7f-0b8a-43b9-b35d-6489e6daee91
          errorCode: check_budget_forecast_exhausted
          errorMessage: errorMessage
        - id: 046b6c7f-0b8a-43b9-b35d-6489e6daee91
          errorCode: check_budget_forecast_exhausted
          errorMessage: errorMessage
      properties:
        successful:
          description: Tracked queries whose check was accepted for submission. Accepted
            is not completed.
          items:
            $ref: "#/components/schemas/batchPauseTrackedQueries_200_response_successful_inner"
          type: array
        failed:
          description: "Tracked queries no check was submitted for, each with the\
            \ reason: check_budget_forecast_exhausted, subscription_not_found, tracked_query_already_paused\
            \ or tracked_query_not_found."
          items:
            $ref: "#/components/schemas/batchForceCheckTrackedQueries_200_response_failed_inner"
          type: array
      type: object
    getAvailableFilters_200_response_clusters_inner:
      example:
        id: 046b6c7f-0b8a-43b9-b35d-6489e6daee91
        name: name
      properties:
        id:
          description: "Pass this in queryClusterIds, never the name"
          format: uuid
          type: string
        name:
          type: string
      type: object
    getAvailableFilters_200_response:
      example:
        engines:
        - engines
        - engines
        countries:
        - countries
        - countries
        clusters:
        - id: 046b6c7f-0b8a-43b9-b35d-6489e6daee91
          name: name
        - id: 046b6c7f-0b8a-43b9-b35d-6489e6daee91
          name: name
      properties:
        engines:
          description: "Engine codes, ready to pass as the engines filter"
          items:
            type: string
          type: array
        countries:
          description: "ISO-3166 alpha-2 codes, ready to pass as the countries filter"
          items:
            type: string
          type: array
        clusters:
          items:
            $ref: "#/components/schemas/getAvailableFilters_200_response_clusters_inner"
          type: array
      type: object
    getMentionSamples_400_response_details_value_inner:
      example:
        code: code
        message: message
        parameters:
        - parameters
        - parameters
      properties:
        code:
          type: string
        message:
          type: string
        parameters:
          description: "Placeholder values behind the message. Always empty on this\
            \ API: no rule it enforces carries any."
          items:
            type: string
          type: array
      type: object
    getMentionSamples_400_response:
      example:
        code: code
        message: message
        requestId: 046b6c7f-0b8a-43b9-b35d-6489e6daee91
        details:
          key:
          - code: code
            message: message
            parameters:
            - parameters
            - parameters
          - code: code
            message: message
            parameters:
            - parameters
            - parameters
      properties:
        code:
          description: Machine-readable reason. `validation_error` when the request
            was rejected; otherwise the domain error code.
          type: string
        message:
          description: Human-readable reason. A 4xx carries the real message; a 5xx
            carries a fixed sentence and the detail goes to the logs only.
          type: string
        requestId:
          description: "Correlation id, also returned in the X-Request-Id header."
          format: uuid
          type: string
        details:
          additionalProperties:
            items:
              $ref: "#/components/schemas/getMentionSamples_400_response_details_value_inner"
            type: array
          description: "Present only on a validation failure: one entry per rejected\
            \ field, each an array of errors."
          type: object
      required:
      - code
      - message
      - requestId
      type: object
    getMetricGlossary_200_response_metrics_inner:
      example:
        metric: metric
        aliases:
        - aliases
        - aliases
        unit: unit
        range: range
        direction: higher_is_better
        operation: operation
        exampleQuestions:
        - exampleQuestions
        - exampleQuestions
      properties:
        metric:
          type: string
        aliases:
          items:
            type: string
          type: array
        unit:
          type: string
        range:
          type: string
        direction:
          enum:
          - higher_is_better
          - lower_is_better
          - n/a
          type: string
        operation:
          description: The operationId that returns this metric
          type: string
        exampleQuestions:
          items:
            type: string
          type: array
      type: object
    getMetricGlossary_200_response:
      example:
        metrics:
        - metric: metric
          aliases:
          - aliases
          - aliases
          unit: unit
          range: range
          direction: higher_is_better
          operation: operation
          exampleQuestions:
          - exampleQuestions
          - exampleQuestions
        - metric: metric
          aliases:
          - aliases
          - aliases
          unit: unit
          range: range
          direction: higher_is_better
          operation: operation
          exampleQuestions:
          - exampleQuestions
          - exampleQuestions
      properties:
        metrics:
          items:
            $ref: "#/components/schemas/getMetricGlossary_200_response_metrics_inner"
          type: array
      type: object
    getOrganizationOverview_200_response_projects_inner:
      example:
        projectId: 046b6c7f-0b8a-43b9-b35d-6489e6daee91
        name: name
        status: status
        trackedQueryCount: 0
        shareOfVoice: 6.0274563
        mentionRate: 1
        avgMentionPosition: 5.962134
        positivityIndex: 5
      properties:
        projectId:
          format: uuid
          type: string
        name:
          type: string
        status:
          type: string
        trackedQueryCount:
          type: integer
        shareOfVoice:
          description: 0-100 percentage share of weighted AI mentions against all
            tracked brands; higher is better. Null when the project has no rank data
            yet.
          format: float
          nullable: true
          type: number
        mentionRate:
          description: 0-100 percentage of tracked queries with a result; higher is
            better. Null when the project has no rank data yet.
          nullable: true
          type: integer
        avgMentionPosition:
          description: 1-based average rank of the brand within the answer; LOWER
            is better. Null when the project has no rank data yet.
          format: float
          nullable: true
          type: number
        positivityIndex:
          description: 0-100 sentiment score; higher is better. Null when the project
            has no rank data yet.
          nullable: true
          type: integer
      type: object
    getOrganizationOverview_200_response_aggregate:
      example:
        projectCount: 2
        projectsWithData: 7
        totalTrackedQueries: 9
        avgShareOfVoice: 3.6160767
        avgMentionRate: 2
        avgMentionPosition: 4.145608
        avgPositivityIndex: 7
      properties:
        projectCount:
          description: "Active projects in the organization, the length of `projects`."
          type: integer
        projectsWithData:
          description: "How many of those have a share of voice; the averages below\
            \ are means over the projects that report each metric, never over the\
            \ ones still null."
          type: integer
        totalTrackedQueries:
          type: integer
        avgShareOfVoice:
          description: Null when no project reports a share of voice.
          format: float
          nullable: true
          type: number
        avgMentionRate:
          nullable: true
          type: integer
        avgMentionPosition:
          description: 1-based rank; LOWER is better.
          format: float
          nullable: true
          type: number
        avgPositivityIndex:
          nullable: true
          type: integer
      type: object
    getOrganizationOverview_200_response:
      example:
        projects:
        - projectId: 046b6c7f-0b8a-43b9-b35d-6489e6daee91
          name: name
          status: status
          trackedQueryCount: 0
          shareOfVoice: 6.0274563
          mentionRate: 1
          avgMentionPosition: 5.962134
          positivityIndex: 5
        - projectId: 046b6c7f-0b8a-43b9-b35d-6489e6daee91
          name: name
          status: status
          trackedQueryCount: 0
          shareOfVoice: 6.0274563
          mentionRate: 1
          avgMentionPosition: 5.962134
          positivityIndex: 5
        aggregate:
          projectCount: 2
          projectsWithData: 7
          totalTrackedQueries: 9
          avgShareOfVoice: 3.6160767
          avgMentionRate: 2
          avgMentionPosition: 4.145608
          avgPositivityIndex: 7
      properties:
        projects:
          items:
            $ref: "#/components/schemas/getOrganizationOverview_200_response_projects_inner"
          type: array
        aggregate:
          $ref: "#/components/schemas/getOrganizationOverview_200_response_aggregate"
      type: object
    getShareOfVoiceFormula_200_response:
      example:
        mentionTypeWeights:
          key: 0.8008282
        sentimentMultipliers:
          key: 6.0274563
        directMultiplier: 1.4658129
        conditionalMultiplier: 5.962134
      properties:
        mentionTypeWeights:
          additionalProperties:
            format: float
            type: number
          description: Base weight per mention type; higher means a structurally stronger
            brand association.
          type: object
        sentimentMultipliers:
          additionalProperties:
            format: float
            type: number
          description: "Multiplier per tone, applied on top of the base weight."
          type: object
        directMultiplier:
          description: Applied when the mention carries no condition.
          format: float
          type: number
        conditionalMultiplier:
          description: Applied instead when the answer hedged the mention with a condition.
          format: float
          type: number
      type: object
    getTrackingCoverage_200_response_sample_inner:
      example:
        trackedQueryId: 046b6c7f-0b8a-43b9-b35d-6489e6daee91
        queryText: queryText
        engine: engine
        country: country
        checkFrequency: daily
        lastCheckedAt: 2000-01-23T04:56:07.000+00:00
      properties:
        trackedQueryId:
          format: uuid
          type: string
        queryText:
          type: string
        engine:
          type: string
        country:
          description: ISO-3166 alpha-2 code
          type: string
        checkFrequency:
          enum:
          - daily
          - weekly
          - monthly
          type: string
        lastCheckedAt:
          description: "RFC 3339 timestamp of the last check. Null means never checked,\
            \ which this sample never contains."
          format: date-time
          nullable: true
          type: string
      type: object
    getTrackingCoverage_200_response:
      example:
        projectId: 046b6c7f-0b8a-43b9-b35d-6489e6daee91
        total: 0
        active: 6
        paused: 1
        neverChecked: 5
        overdue: 5
        sample:
        - trackedQueryId: 046b6c7f-0b8a-43b9-b35d-6489e6daee91
          queryText: queryText
          engine: engine
          country: country
          checkFrequency: daily
          lastCheckedAt: 2000-01-23T04:56:07.000+00:00
        - trackedQueryId: 046b6c7f-0b8a-43b9-b35d-6489e6daee91
          queryText: queryText
          engine: engine
          country: country
          checkFrequency: daily
          lastCheckedAt: 2000-01-23T04:56:07.000+00:00
      properties:
        projectId:
          format: uuid
          type: string
        total:
          description: "Every tracked query on the project, whatever its status"
          type: integer
        active:
          description: Tracked queries currently being checked
          type: integer
        paused:
          description: Tracked queries whose checks are suspended
          type: integer
        neverChecked:
          description: Active queries that have never run yet
          type: integer
        overdue:
          description: "Active queries past their check-frequency interval, never-checked\
            \ ones excluded"
          type: integer
        sample:
          description: "Up to 20 overdue queries, oldest check first"
          items:
            $ref: "#/components/schemas/getTrackingCoverage_200_response_sample_inner"
          type: array
      type: object
    listAiResponses_200_response:
      example:
        items:
        - id: 046b6c7f-0b8a-43b9-b35d-6489e6daee91
          projectId: 046b6c7f-0b8a-43b9-b35d-6489e6daee91
          trackedQueryId: 046b6c7f-0b8a-43b9-b35d-6489e6daee91
          engine: engine
          responseText: responseText
          citations:
          - url: url
            position: 1
            anchorText: anchorText
            title: title
            snippet: snippet
            publicationDate: publicationDate
            thumbnailUrl: thumbnailUrl
            domain: domain
            sourceName: sourceName
            unresolved: true
          - url: url
            position: 1
            anchorText: anchorText
            title: title
            snippet: snippet
            publicationDate: publicationDate
            thumbnailUrl: thumbnailUrl
            domain: domain
            sourceName: sourceName
            unresolved: true
          capturedAt: 2000-01-23T04:56:07.000+00:00
          modelName: modelName
          passIndex: 0
          passCount: 1
        - id: 046b6c7f-0b8a-43b9-b35d-6489e6daee91
          projectId: 046b6c7f-0b8a-43b9-b35d-6489e6daee91
          trackedQueryId: 046b6c7f-0b8a-43b9-b35d-6489e6daee91
          engine: engine
          responseText: responseText
          citations:
          - url: url
            position: 1
            anchorText: anchorText
            title: title
            snippet: snippet
            publicationDate: publicationDate
            thumbnailUrl: thumbnailUrl
            domain: domain
            sourceName: sourceName
            unresolved: true
          - url: url
            position: 1
            anchorText: anchorText
            title: title
            snippet: snippet
            publicationDate: publicationDate
            thumbnailUrl: thumbnailUrl
            domain: domain
            sourceName: sourceName
            unresolved: true
          capturedAt: 2000-01-23T04:56:07.000+00:00
          modelName: modelName
          passIndex: 0
          passCount: 1
        total: 5
      properties:
        items:
          items:
            $ref: "#/components/schemas/AiResponseResource"
          type: array
        total:
          description: "Captures matching the filter, not the size of this page"
          type: integer
      type: object
    listSearchSnapshots_200_response:
      example:
        items:
        - id: 046b6c7f-0b8a-43b9-b35d-6489e6daee91
          projectId: 046b6c7f-0b8a-43b9-b35d-6489e6daee91
          trackedQueryId: 046b6c7f-0b8a-43b9-b35d-6489e6daee91
          engine: engine
          results:
          - position: 1
            url: url
            title: title
            snippet: snippet
            domain: domain
            rating: 6.0274563
            ratingVotes: 1
            unresolved: true
          - position: 1
            url: url
            title: title
            snippet: snippet
            domain: domain
            rating: 6.0274563
            ratingVotes: 1
            unresolved: true
          capturedAt: 2000-01-23T04:56:07.000+00:00
        - id: 046b6c7f-0b8a-43b9-b35d-6489e6daee91
          projectId: 046b6c7f-0b8a-43b9-b35d-6489e6daee91
          trackedQueryId: 046b6c7f-0b8a-43b9-b35d-6489e6daee91
          engine: engine
          results:
          - position: 1
            url: url
            title: title
            snippet: snippet
            domain: domain
            rating: 6.0274563
            ratingVotes: 1
            unresolved: true
          - position: 1
            url: url
            title: title
            snippet: snippet
            domain: domain
            rating: 6.0274563
            ratingVotes: 1
            unresolved: true
          capturedAt: 2000-01-23T04:56:07.000+00:00
        total: 5
      properties:
        items:
          items:
            $ref: "#/components/schemas/SearchSnapshotResource"
          type: array
        total:
          description: "Captures matching the filter, not the size of this page"
          type: integer
      type: object
    listShoppingSnapshots_200_response:
      example:
        items:
        - id: 046b6c7f-0b8a-43b9-b35d-6489e6daee91
          projectId: 046b6c7f-0b8a-43b9-b35d-6489e6daee91
          trackedQueryId: 046b6c7f-0b8a-43b9-b35d-6489e6daee91
          engine: engine
          offers:
          - rank: 1
            title: title
            productUrl: productUrl
            sellerName: sellerName
            sellerDomain: sellerDomain
            productId: productId
            thumbnailUrl: thumbnailUrl
            price: 6.0274563
            currency: currency
            rating: 1.4658129
            ratingVotes: 5
          - rank: 1
            title: title
            productUrl: productUrl
            sellerName: sellerName
            sellerDomain: sellerDomain
            productId: productId
            thumbnailUrl: thumbnailUrl
            price: 6.0274563
            currency: currency
            rating: 1.4658129
            ratingVotes: 5
          capturedAt: 2000-01-23T04:56:07.000+00:00
        - id: 046b6c7f-0b8a-43b9-b35d-6489e6daee91
          projectId: 046b6c7f-0b8a-43b9-b35d-6489e6daee91
          trackedQueryId: 046b6c7f-0b8a-43b9-b35d-6489e6daee91
          engine: engine
          offers:
          - rank: 1
            title: title
            productUrl: productUrl
            sellerName: sellerName
            sellerDomain: sellerDomain
            productId: productId
            thumbnailUrl: thumbnailUrl
            price: 6.0274563
            currency: currency
            rating: 1.4658129
            ratingVotes: 5
          - rank: 1
            title: title
            productUrl: productUrl
            sellerName: sellerName
            sellerDomain: sellerDomain
            productId: productId
            thumbnailUrl: thumbnailUrl
            price: 6.0274563
            currency: currency
            rating: 1.4658129
            ratingVotes: 5
          capturedAt: 2000-01-23T04:56:07.000+00:00
        total: 5
      properties:
        items:
          items:
            $ref: "#/components/schemas/ShoppingSnapshotResource"
          type: array
        total:
          description: "Captures matching the filter, not the size of this page"
          type: integer
      type: object
    listQueryClusters_200_response:
      example:
        items:
        - id: 046b6c7f-0b8a-43b9-b35d-6489e6daee91
          projectId: 046b6c7f-0b8a-43b9-b35d-6489e6daee91
          name: name
        - id: 046b6c7f-0b8a-43b9-b35d-6489e6daee91
          projectId: 046b6c7f-0b8a-43b9-b35d-6489e6daee91
          name: name
        total: 0
      properties:
        items:
          items:
            $ref: "#/components/schemas/QueryClusterResource"
          type: array
        total:
          description: "Clusters in the project, not the size of this page"
          type: integer
      type: object
    createQueryCluster_request:
      properties:
        name:
          description: Stored trimmed and lower-cased. Unique within the project.
          maxLength: 200
          type: string
      type: object
    createQueryCluster_409_response:
      example:
        code: code
        message: message
        requestId: 046b6c7f-0b8a-43b9-b35d-6489e6daee91
      properties:
        code:
          type: string
        message:
          type: string
        requestId:
          format: uuid
          type: string
      type: object
    deleteQueryCluster_200_response:
      example:
        id: 046b6c7f-0b8a-43b9-b35d-6489e6daee91
        name: name
        deleted: true
      properties:
        id:
          description: The cluster that was deleted
          format: uuid
          type: string
        name:
          description: Its name at the moment it was deleted
          type: string
        deleted:
          description: Always true; present so the body is self-describing
          type: boolean
      type: object
    startBrandDiscoveryJob_request:
      properties:
        shoppingEnabled:
          default: false
          description: "Adds a shopping discovery pass. Set it only for a project\
            \ that tracks a shopping engine: on any other project the pass spends\
            \ provider budget on results nothing will use."
          type: boolean
        country:
          description: ISO 3166-1 alpha-2 country used to localize the discovery.
          example: ES
          maxLength: 2
          minLength: 2
          nullable: true
          type: string
      type: object
    startBrandNameSuggestionJob_request:
      properties:
        name:
          description: The display name of the entity to find aliases for.
          example: Acme
          maxLength: 255
          type: string
        websiteDomains:
          description: "The entity website domains. At least one is required: the\
            \ web search is grounded on them, and without one the name alone is ambiguous.\
            \ Duplicates are collapsed, after the entry count has been checked against\
            \ maxItems."
          example:
          - https://acme.com
          items:
            maxLength: 2048
            type: string
          maxItems: 25
          minItems: 1
          type: array
        enteredBrandNames:
          description: "Names already known, excluded from the suggestions. Duplicates\
            \ are collapsed, after the entry count has been checked against maxItems."
          example:
          - Acme
          - Acme Inc
          items:
            maxLength: 255
            type: string
          maxItems: 100
          type: array
        country:
          description: ISO 3166-1 alpha-2 country used to localize the web search.
          example: ES
          maxLength: 2
          minLength: 2
          nullable: true
          type: string
      required:
      - name
      - websiteDomains
      type: object
    startKeywordDiscoveryJob_request:
      properties:
        input:
          description: Free text describing the keywords or topics to expand.
          example: "crm for plumbers, field service software"
          maxLength: 10000
          type: string
        language:
          description: Language code. Omit to let the provider detect it from the
            input.
          example: es
          maxLength: 5
          nullable: true
          type: string
        country:
          description: ISO 3166-1 alpha-2 country. Omit to let the provider infer
            it.
          example: ES
          maxLength: 2
          minLength: 2
          nullable: true
          type: string
        excludeQueries:
          description: "Extra queries to keep out of the suggestions for this run.\
            \ Duplicates are collapsed, after the entry count has been checked against\
            \ maxItems. The project tracked queries are excluded whether or not this\
            \ is sent."
          items:
            maxLength: 500
            type: string
          maxItems: 5000
          type: array
      required:
      - input
      type: object
    startPromptDiscoveryJob_request:
      properties:
        input:
          description: Free text describing the topics to turn into prompts.
          example: "crm for plumbers, field service software"
          maxLength: 10000
          type: string
        country:
          description: ISO 3166-1 alpha-2 country the prompts are asked from.
          example: ES
          maxLength: 2
          minLength: 2
          type: string
        language:
          description: Language code. Omit to let the provider detect it from the
            input.
          example: es
          maxLength: 5
          nullable: true
          type: string
        excludeQueries:
          description: "Extra queries to keep out of the suggestions for this run.\
            \ Duplicates are collapsed, after the entry count has been checked against\
            \ maxItems. The project tracked queries are excluded whether or not this\
            \ is sent."
          items:
            maxLength: 500
            type: string
          maxItems: 5000
          type: array
      required:
      - country
      - input
      type: object
    listInvitations_200_response:
      example:
        items:
        - id: 046b6c7f-0b8a-43b9-b35d-6489e6daee91
          email: email
          role: owner
          state: pending
          createdAt: 2000-01-23T04:56:07.000+00:00
          expiresAt: 2000-01-23T04:56:07.000+00:00
          acceptedAt: 2000-01-23T04:56:07.000+00:00
        - id: 046b6c7f-0b8a-43b9-b35d-6489e6daee91
          email: email
          role: owner
          state: pending
          createdAt: 2000-01-23T04:56:07.000+00:00
          expiresAt: 2000-01-23T04:56:07.000+00:00
          acceptedAt: 2000-01-23T04:56:07.000+00:00
        total: 0
      properties:
        items:
          items:
            $ref: "#/components/schemas/InvitationResource"
          type: array
        total:
          type: integer
      type: object
    createInvitation_request:
      properties:
        email:
          format: email
          type: string
        role:
          default: viewer
          enum:
          - owner
          - manager
          - viewer
          type: string
      type: object
    createInvitation_201_response:
      allOf:
      - properties:
          created:
            description: Always true on a 201.
            type: boolean
        type: object
      - $ref: "#/components/schemas/InvitationResource"
      example:
        created: true
        id: 046b6c7f-0b8a-43b9-b35d-6489e6daee91
        email: email
        role: owner
        state: pending
        createdAt: 2000-01-23T04:56:07.000+00:00
        expiresAt: 2000-01-23T04:56:07.000+00:00
        acceptedAt: 2000-01-23T04:56:07.000+00:00
    createInvitation_200_response:
      example:
        created: true
        reason: address_is_already_a_member
        email: email
      properties:
        created:
          description: Always false on a 200.
          type: boolean
        reason:
          description: Why nothing was created.
          enum:
          - address_is_already_a_member
          type: string
        email:
          description: The address as it was normalised.
          format: email
          type: string
      type: object
    listKeywordListings_200_response:
      example:
        items:
        - keyword: keyword
          keywordNormalized: keywordNormalized
          variantCount: 0
          variantIds:
          - 046b6c7f-0b8a-43b9-b35d-6489e6daee91
          - 046b6c7f-0b8a-43b9-b35d-6489e6daee91
          engines:
          - chatgpt
          - chatgpt
          countries:
          - countries
          - countries
          queryClusterIds:
          - 046b6c7f-0b8a-43b9-b35d-6489e6daee91
          - 046b6c7f-0b8a-43b9-b35d-6489e6daee91
          hasUnclusteredVariant: true
          statusSummary: active
          statuses:
          - active
          - active
          checkFrequencies:
          - daily
          - daily
          nPassesValues:
          - 6
          - 6
          lastCheckedAt: 2000-01-23T04:56:07.000+00:00
          avgSerpPosition: 1.4658129
          trendSerp: 5.962134
          avgShoppingPosition: 5.637377
          trendShopping: 2.302136
          avgMentionPosition: 7.0614014
          trendMention: 9.301444
          avgLinkPosition: 3.6160767
          trendLink: 2.027123
          mentionPositionStability: 4.145608
          trendStability: 7.386282
          positivityIndex: 1
          trendPositivity: 1
          mentionRate: 1
          trendMentionRate: 6
          serpRate: 7
          trendSerpRate: 1
          shareOfVoice: 4.9652185
          trendShareOfVoice: 5.025005
          sentimentPositive: 9
          sentimentNeutral: 9
          sentimentNegative: 6
          avgMentionCount: 8.762042
          mentionTypeCounts:
            recommendation: 9
            comparison: 6
            listing: 3
            example: 6
            reference: 1
        - keyword: keyword
          keywordNormalized: keywordNormalized
          variantCount: 0
          variantIds:
          - 046b6c7f-0b8a-43b9-b35d-6489e6daee91
          - 046b6c7f-0b8a-43b9-b35d-6489e6daee91
          engines:
          - chatgpt
          - chatgpt
          countries:
          - countries
          - countries
          queryClusterIds:
          - 046b6c7f-0b8a-43b9-b35d-6489e6daee91
          - 046b6c7f-0b8a-43b9-b35d-6489e6daee91
          hasUnclusteredVariant: true
          statusSummary: active
          statuses:
          - active
          - active
          checkFrequencies:
          - daily
          - daily
          nPassesValues:
          - 6
          - 6
          lastCheckedAt: 2000-01-23T04:56:07.000+00:00
          avgSerpPosition: 1.4658129
          trendSerp: 5.962134
          avgShoppingPosition: 5.637377
          trendShopping: 2.302136
          avgMentionPosition: 7.0614014
          trendMention: 9.301444
          avgLinkPosition: 3.6160767
          trendLink: 2.027123
          mentionPositionStability: 4.145608
          trendStability: 7.386282
          positivityIndex: 1
          trendPositivity: 1
          mentionRate: 1
          trendMentionRate: 6
          serpRate: 7
          trendSerpRate: 1
          shareOfVoice: 4.9652185
          trendShareOfVoice: 5.025005
          sentimentPositive: 9
          sentimentNeutral: 9
          sentimentNegative: 6
          avgMentionCount: 8.762042
          mentionTypeCounts:
            recommendation: 9
            comparison: 6
            listing: 3
            example: 6
            reference: 1
        total: 2
        totalVariantCount: 6
        dataDirtySince: 2000-01-23T04:56:07.000+00:00
      properties:
        items:
          items:
            $ref: "#/components/schemas/KeywordListingResource"
          type: array
        total:
          description: "Keywords matching the filters, not the size of this page."
          type: integer
        totalVariantCount:
          description: "Tracked queries behind the keywords matching every filter\
            \ EXCEPT `search`. With a text search applied this still counts the whole\
            \ project, so it is not a budget figure for a searched page."
          type: integer
        dataDirtySince:
          description: When a pending recalculation was triggered. Null when the metrics
            are up to date.
          format: date-time
          nullable: true
          type: string
      type: object
    getMeStats_200_response_organizations:
      example:
        total: 3
      properties:
        total:
          description: "Organizations the caller is an active member of, within the\
            \ key's scope."
          example: 3
          minimum: 0
          type: integer
      type: object
    getMeStats_200_response_projects:
      example:
        total: 25
        active: 18
      properties:
        total:
          description: "Projects across those organizations, archived ones included."
          example: 25
          minimum: 0
          type: integer
        active:
          description: How many of those are active; the remainder are archived.
          example: 18
          minimum: 0
          type: integer
      type: object
    getMeStats_200_response:
      example:
        organizations:
          total: 3
        projects:
          total: 25
          active: 18
      properties:
        organizations:
          $ref: "#/components/schemas/getMeStats_200_response_organizations"
        projects:
          $ref: "#/components/schemas/getMeStats_200_response_projects"
      type: object
    changeMemberRole_request:
      properties:
        role:
          enum:
          - owner
          - manager
          - viewer
          type: string
      type: object
    listMembers_200_response:
      example:
        items:
        - id: 046b6c7f-0b8a-43b9-b35d-6489e6daee91
          userId: 046b6c7f-0b8a-43b9-b35d-6489e6daee91
          role: owner
          state: active
          isActive: true
          joinedAt: 2000-01-23T04:56:07.000+00:00
        - id: 046b6c7f-0b8a-43b9-b35d-6489e6daee91
          userId: 046b6c7f-0b8a-43b9-b35d-6489e6daee91
          role: owner
          state: active
          isActive: true
          joinedAt: 2000-01-23T04:56:07.000+00:00
        total: 0
      properties:
        items:
          items:
            $ref: "#/components/schemas/MemberResource"
          type: array
        total:
          type: integer
      type: object
    listOrganizations_200_response:
      example:
        items:
        - id: 046b6c7f-0b8a-43b9-b35d-6489e6daee91
          name: name
          description: description
          status: active
          imageUrl: https://openapi-generator.tech
          contactEmail: contactEmail
          role: owner
          createdAt: 2000-01-23T04:56:07.000+00:00
        - id: 046b6c7f-0b8a-43b9-b35d-6489e6daee91
          name: name
          description: description
          status: active
          imageUrl: https://openapi-generator.tech
          contactEmail: contactEmail
          role: owner
          createdAt: 2000-01-23T04:56:07.000+00:00
        total: 0
      properties:
        items:
          items:
            $ref: "#/components/schemas/OrganizationResource"
          type: array
        total:
          type: integer
      type: object
    createOrganization_request:
      properties:
        name:
          maxLength: 100
          minLength: 3
          type: string
      type: object
    createOrganization_201_response:
      example:
        id: 046b6c7f-0b8a-43b9-b35d-6489e6daee91
        name: name
        status: status
      properties:
        id:
          format: uuid
          type: string
        name:
          type: string
        status:
          description: Always active on creation.
          type: string
      type: object
    previewOrganizationOperation_request:
      properties:
        action:
          enum:
          - createOrganization
          - updateOrganization
          - archiveOrganization
          - restoreOrganization
          - changeMemberRole
          - suspendMember
          - reactivateMember
          - createInvitation
          - cancelInvitation
          type: string
        organizationId:
          description: Required for every action except createOrganization.
          format: uuid
          nullable: true
          type: string
        resourceId:
          description: "The member or invitation the action acts on, for the actions\
            \ that name one."
          format: uuid
          nullable: true
          type: string
        payload:
          description: The body you intend to send to the operation itself.
          type: object
      type: object
    previewOrganizationOperation_200_response_organization:
      description: "The organization the operation acts on. For createOrganization\
        \ there is none yet, so `id` is null and `name` carries the name you proposed\
        \ — the preview shows what you are about to create rather than hiding it.\
        \ Branch on `organization.id`, never on `organization` itself."
      example:
        id: 046b6c7f-0b8a-43b9-b35d-6489e6daee91
        name: name
      nullable: true
      properties:
        id:
          format: uuid
          nullable: true
          type: string
        name:
          type: string
      type: object
    previewOrganizationOperation_200_response:
      example:
        action: action
        organization:
          id: 046b6c7f-0b8a-43b9-b35d-6489e6daee91
          name: name
        resourceId: 046b6c7f-0b8a-43b9-b35d-6489e6daee91
        changes:
        - code: projects_will_be_archived
          summary: summary
          targets:
          - targets
          - targets
        - code: projects_will_be_archived
          summary: summary
          targets:
          - targets
          - targets
        sideEffects:
        - code: projects_will_be_archived
          summary: summary
          targets:
          - targets
          - targets
        - code: projects_will_be_archived
          summary: summary
          targets:
          - targets
          - targets
        warnings:
        - code: projects_will_be_archived
          summary: summary
          targets:
          - targets
          - targets
        - code: projects_will_be_archived
          summary: summary
          targets:
          - targets
          - targets
        conditions:
        - code: projects_will_be_archived
          summary: summary
          targets:
          - targets
          - targets
        - code: projects_will_be_archived
          summary: summary
          targets:
          - targets
          - targets
        actor: "{}"
        confirmation: "{}"
      properties:
        action:
          type: string
        organization:
          $ref: "#/components/schemas/previewOrganizationOperation_200_response_organization"
        resourceId:
          description: The member or invitation the action acts on; null for the actions
            that name none.
          format: uuid
          nullable: true
          type: string
        changes:
          description: What the operation itself will do.
          items:
            $ref: "#/components/schemas/OperationEffect"
          type: array
        sideEffects:
          description: "What else it will cause, with the records it will touch in\
            \ `targets`."
          items:
            $ref: "#/components/schemas/OperationEffect"
          type: array
        warnings:
          description: "How the system behaves around it. Not part of the effects\
            \ digest: rewording one does not invalidate a confirmation."
          items:
            $ref: "#/components/schemas/OperationEffect"
          type: array
        conditions:
          description: What must hold for the operation to be allowed.
          items:
            $ref: "#/components/schemas/OperationEffect"
          type: array
        actor:
          description: "Who the confirmation is issued to: the user and the API key.\
            \ It is valid for that pair only."
          type: object
        confirmation:
          description: "The token to send back, the header to send it in, and how\
            \ long it lives."
          type: object
      type: object
    updateOrganization_request:
      properties:
        name:
          maxLength: 100
          minLength: 3
          type: string
        description:
          nullable: true
          type: string
        contactEmail:
          format: email
          nullable: true
          type: string
      type: object
    getMembershipStats_200_response:
      example:
        activeMembersCount: 85
        suspendedMembersCount: 3
        totalMembersCount: 88
        activeProjectsCount: 18
        totalProjectsCount: 25
        pendingInvitationsCount: 5
        computedAt: 2026-02-12 10:30:00
      properties:
        activeMembersCount:
          description: Members in the active state.
          example: 85
          minimum: 0
          type: integer
        suspendedMembersCount:
          description: Members in the suspended state.
          example: 3
          minimum: 0
          type: integer
        totalMembersCount:
          description: "Every membership record, suspended ones included."
          example: 88
          minimum: 0
          type: integer
        activeProjectsCount:
          description: Projects in the active state.
          example: 18
          minimum: 0
          type: integer
        totalProjectsCount:
          description: "Every project, archived ones included."
          example: 25
          minimum: 0
          type: integer
        pendingInvitationsCount:
          description: Invitations still pending and not yet expired.
          example: 5
          minimum: 0
          type: integer
        computedAt:
          description: "When the snapshot was computed. A database timestamp, not\
            \ an ISO-8601 instant."
          example: 2026-02-12 10:30:00
          type: string
      type: object
    getOrganizationStats_200_response_members:
      example:
        total: 12
      properties:
        total:
          description: Active memberships. Suspended members are excluded.
          example: 12
          minimum: 0
          type: integer
      type: object
    getOrganizationStats_200_response_projects:
      example:
        total: 25
        active: 18
      properties:
        total:
          description: "Every project in the organization, archived ones included."
          example: 25
          minimum: 0
          type: integer
        active:
          description: Projects that are not archived; never greater than `total`.
          example: 18
          minimum: 0
          type: integer
      type: object
    getOrganizationStats_200_response_invitations:
      example:
        pending: 3
      properties:
        pending:
          description: "Invitations still awaiting an answer. Accepted, cancelled\
            \ and expired ones are excluded."
          example: 3
          minimum: 0
          type: integer
      type: object
    getOrganizationStats_200_response:
      example:
        members:
          total: 12
        projects:
          total: 25
          active: 18
        invitations:
          pending: 3
      properties:
        members:
          $ref: "#/components/schemas/getOrganizationStats_200_response_members"
        projects:
          $ref: "#/components/schemas/getOrganizationStats_200_response_projects"
        invitations:
          $ref: "#/components/schemas/getOrganizationStats_200_response_invitations"
      type: object
    listCompetitors_200_response:
      example:
        items:
        - id: 046b6c7f-0b8a-43b9-b35d-6489e6daee91
          name: name
          websiteDomains:
          - websiteDomains
          - websiteDomains
          brandNames:
          - brandNames
          - brandNames
        - id: 046b6c7f-0b8a-43b9-b35d-6489e6daee91
          name: name
          websiteDomains:
          - websiteDomains
          - websiteDomains
          brandNames:
          - brandNames
          - brandNames
        total: 0
      properties:
        items:
          items:
            $ref: "#/components/schemas/CompetitorResource"
          type: array
        total:
          description: Number of competitors configured on the project
          type: integer
      type: object
    createCompetitor_request:
      properties:
        name:
          description: The competitor's display name
          maxLength: 200
          type: string
        websiteDomains:
          description: Domains a result is matched against for this competitor. A
            full URL or a bare host.
          items:
            type: string
          maxItems: 100
          minItems: 1
          type: array
        brandNames:
          description: Names a mention is matched against for this competitor
          items:
            type: string
          maxItems: 100
          minItems: 1
          type: array
      type: object
    listProjects_200_response:
      example:
        items:
        - id: 046b6c7f-0b8a-43b9-b35d-6489e6daee91
          name: name
          status: active
          createdAt: 2000-01-23T04:56:07.000+00:00
          trackedQueryCount: 0
          avgSerpPosition: 6.0274563
          avgShoppingPosition: 1.4658129
          avgMentionPosition: 5.962134
          avgLinkPosition: 5.637377
          mentionRate: 2
          serpRate: 7
          shoppingRate: 9
          positivityIndex: 3
          shareOfVoice: 2.027123
          serpPositionStability: 4.145608
          shoppingPositionStability: 7.386282
          lastRankDetectedAt: 2000-01-23T04:56:07.000+00:00
        - id: 046b6c7f-0b8a-43b9-b35d-6489e6daee91
          name: name
          status: active
          createdAt: 2000-01-23T04:56:07.000+00:00
          trackedQueryCount: 0
          avgSerpPosition: 6.0274563
          avgShoppingPosition: 1.4658129
          avgMentionPosition: 5.962134
          avgLinkPosition: 5.637377
          mentionRate: 2
          serpRate: 7
          shoppingRate: 9
          positivityIndex: 3
          shareOfVoice: 2.027123
          serpPositionStability: 4.145608
          shoppingPositionStability: 7.386282
          lastRankDetectedAt: 2000-01-23T04:56:07.000+00:00
        total: 1
      properties:
        items:
          items:
            $ref: "#/components/schemas/ProjectResource"
          type: array
        total:
          description: "Every project matching the filters, not the size of this page."
          type: integer
      type: object
    createProject_request_competitors_inner:
      properties:
        name:
          type: string
        websiteDomains:
          items:
            type: string
          maxItems: 100
          minItems: 1
          type: array
        brandNames:
          items:
            type: string
          maxItems: 100
          minItems: 1
          type: array
      required:
      - brandNames
      - name
      - websiteDomains
      type: object
    createProject_request:
      properties:
        name:
          maxLength: 255
          minLength: 1
          type: string
        websiteDomains:
          description: At least one website URL or domain to monitor.
          items:
            type: string
          maxItems: 100
          minItems: 1
          type: array
        brandNames:
          description: At least one brand name to match in AI answers and search results.
          items:
            type: string
          maxItems: 100
          minItems: 1
          type: array
        competitors:
          description: Competitors to create with the project. Optional; they can
            also be added later through the competitor endpoints.
          items:
            $ref: "#/components/schemas/createProject_request_competitors_inner"
          maxItems: 100
          type: array
      required:
      - brandNames
      - name
      - websiteDomains
      type: object
    updateProjectBrandProfile_request:
      properties:
        websiteDomains:
          description: Domains a cited link or a search result is matched against.
            A full URL or a bare host.
          items:
            type: string
          maxItems: 100
          minItems: 1
          type: array
        brandNames:
          description: Terms a mention in an AI answer is matched against.
          items:
            type: string
          maxItems: 100
          minItems: 1
          type: array
      type: object
    updateProject_request:
      properties:
        name:
          maxLength: 255
          minLength: 1
          type: string
      required:
      - name
      type: object
    searchTrackedQueries_200_response:
      example:
        items:
        - id: 046b6c7f-0b8a-43b9-b35d-6489e6daee91
          queryText: queryText
          engine: chatgpt
          country: country
          status: active
          queryClusterIds:
          - 046b6c7f-0b8a-43b9-b35d-6489e6daee91
          - 046b6c7f-0b8a-43b9-b35d-6489e6daee91
          checkFrequency: daily
          nPasses: 0
          lastSerpPosition: 6
          lastMentionPosition: 1
          lastLinkPosition: 5
          lastShoppingPosition: 5
          lastShareOfVoice: 2.302136
          lastPositivityIndex: 7
          lastPositiveMentionCount: 9
          lastNeutralMentionCount: 3
          lastNegativeMentionCount: 2
          lastCheckedAt: 2000-01-23T04:56:07.000+00:00
        - id: 046b6c7f-0b8a-43b9-b35d-6489e6daee91
          queryText: queryText
          engine: chatgpt
          country: country
          status: active
          queryClusterIds:
          - 046b6c7f-0b8a-43b9-b35d-6489e6daee91
          - 046b6c7f-0b8a-43b9-b35d-6489e6daee91
          checkFrequency: daily
          nPasses: 0
          lastSerpPosition: 6
          lastMentionPosition: 1
          lastLinkPosition: 5
          lastShoppingPosition: 5
          lastShareOfVoice: 2.302136
          lastPositivityIndex: 7
          lastPositiveMentionCount: 9
          lastNeutralMentionCount: 3
          lastNegativeMentionCount: 2
          lastCheckedAt: 2000-01-23T04:56:07.000+00:00
        total: 4
      properties:
        items:
          items:
            $ref: "#/components/schemas/TrackedQueryResource"
          type: array
        total:
          type: integer
      type: object
    reportAiResponse_request:
      properties:
        type:
          description: What is wrong with the capture.
          enum:
          - missed_mention
          - other
          type: string
        comment:
          description: "Optional note for the reviewer, at most 1000 characters. An\
            \ empty string is stored as no comment."
          maxLength: 1000
          nullable: true
          type: string
        missedBrandNames:
          description: Brands the engine mentioned that the pipeline did not record.
            Most useful with type "missed_mention"; accepted with either. At most
            50 distinct names of 255 characters each.
          items:
            maxLength: 255
            type: string
          type: array
      required:
      - type
      type: object
    BatchWriteOutcome_failed_inner:
      example:
        id: 046b6c7f-0b8a-43b9-b35d-6489e6daee91
        errorCode: errorCode
        errorMessage: errorMessage
      properties:
        id:
          format: uuid
          type: string
        errorCode:
          description: The same code a single-item call returns for this problem.
          type: string
        errorMessage:
          type: string
      type: object
    ApplyClusteringJobOutcome_successful_inner:
      example:
        trackedQueryId: 046b6c7f-0b8a-43b9-b35d-6489e6daee91
        queryClusterIds:
        - 046b6c7f-0b8a-43b9-b35d-6489e6daee91
        - 046b6c7f-0b8a-43b9-b35d-6489e6daee91
      properties:
        trackedQueryId:
          format: uuid
          type: string
        queryClusterIds:
          items:
            format: uuid
            type: string
          type: array
      type: object
    ApplyClusteringJobOutcome_failed_inner:
      example:
        trackedQueryId: 046b6c7f-0b8a-43b9-b35d-6489e6daee91
        errorCode: errorCode
        errorMessage: errorMessage
      properties:
        trackedQueryId:
          format: uuid
          type: string
        errorCode:
          type: string
        errorMessage:
          type: string
      type: object
    ApplyClusteringJobOutcome_clusters_inner:
      example:
        id: 046b6c7f-0b8a-43b9-b35d-6489e6daee91
        name: name
        created: true
      properties:
        id:
          format: uuid
          type: string
        name:
          type: string
        created:
          type: boolean
      type: object
    ApplyClusteringJobOutcome_skippedClusters_inner:
      example:
        name: name
        reason: reason
      properties:
        name:
          type: string
        reason:
          type: string
      type: object
    BatchCreateQueryClustersOutcome_successful_inner:
      example:
        name: name
        id: 046b6c7f-0b8a-43b9-b35d-6489e6daee91
      properties:
        name:
          type: string
        id:
          format: uuid
          type: string
      type: object
    BatchCreateQueryClustersOutcome_failed_inner:
      example:
        name: name
        errorCode: errorCode
        errorMessage: errorMessage
      properties:
        name:
          type: string
        errorCode:
          type: string
        errorMessage:
          type: string
      type: object
    KeywordListingResource_mentionTypeCounts:
      description: "Mentions counted in the window by the role the mention plays in\
        \ the answer. Totals, not averages."
      example:
        recommendation: 9
        comparison: 6
        listing: 3
        example: 6
        reference: 1
      properties:
        recommendation:
          type: integer
        comparison:
          type: integer
        listing:
          type: integer
        example:
          type: integer
        reference:
          type: integer
      type: object
    ProjectDetailResource_competitors_inner:
      example:
        id: 046b6c7f-0b8a-43b9-b35d-6489e6daee91
        name: name
        websiteDomains:
        - websiteDomains
        - websiteDomains
        brandNames:
        - brandNames
        - brandNames
      properties:
        id:
          format: uuid
          type: string
        name:
          type: string
        websiteDomains:
          items:
            type: string
          type: array
        brandNames:
          items:
            type: string
          type: array
      type: object
    BatchCreateTrackedQueriesResultResource_successful_inner:
      example:
        id: 046b6c7f-0b8a-43b9-b35d-6489e6daee91
        queryText: queryText
        engine: engine
        country: country
      properties:
        id:
          format: uuid
          type: string
        queryText:
          type: string
        engine:
          type: string
        country:
          type: string
      type: object
    BatchCreateTrackedQueriesResultResource_failed_inner:
      example:
        queryText: queryText
        engine: engine
        country: country
        errorCode: errorCode
        errorMessage: errorMessage
      properties:
        queryText:
          type: string
        engine:
          type: string
        country:
          type: string
        errorCode:
          type: string
        errorMessage:
          type: string
      type: object
  securitySchemes:
    ApiKey:
      description: "A Mencoro API key, created from the app under API keys in the\
        \ user menu. The plaintext key is shown once, at creation, and never again.\
        \ A key only ever RESTRICTS: it never grants more than its owner's live membership\
        \ and role already allow, and every request re-checks those."
      scheme: bearer
      type: http

